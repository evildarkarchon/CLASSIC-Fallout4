"""Main Screen for CLASSIC TUI.

The main screen hosts the tabbed interface - but we use CLASSICApp directly
with TabbedContent, so this is a placeholder for potential future use.
"""

from textual.screen import Screen


class MainScreen(Screen[None]):
    """Main tabbed container screen.

    Note: This is a placeholder. The actual tabbed interface is
    implemented directly in CLASSICApp.compose() for simplicity.
    """
