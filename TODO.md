# TODO List
1. ~~Prevent people from setting the F4SE directory as the custom scan directory.~~
2. ~~Modularize the ClassicScanLogs class, it's way too bloated at the moment.~~
3. ~~Abstract the messaging so that messages can be sent on the command line or in the GUI using the same functions, with the ability to restrict the message to the GUI or CLI.~~
4. Add button to open the main log directory in the GUI.
5. Add a tab to the GUI for managing logs, including deleting logs and viewing them.
6. Add functionality to select multiple custom scan directories.