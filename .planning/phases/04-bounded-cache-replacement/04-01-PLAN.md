---
phase: 04-bounded-cache-replacement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs
  - ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs
  - docs/api/classic-yaml-core.md
autonomous: true
requirements: [CACHE-01, CONS-03]
must_haves:
  truths:
    - "YAML file caching is bounded to 128 entries while keeping the current mtime-aware freshness check"
    - "YAML cache stats expose the canonical five-field contract: hits, misses, hit_rate, size, capacity"
    - "YAML cache tests prove clear/reset isolation without relying on DashMap internals"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs"
      provides: "Bounded YAML cache plus canonical CacheStats"
      contains: "Cache::new(128)"
    - path: "ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs"
      provides: "Public-behavior regression coverage for bounded YAML caching"
      contains: "cache_stats"
    - path: "docs/api/classic-yaml-core.md"
      provides: "Contributor-facing YAML cache contract documentation"
      contains: "capacity"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs"
      to: "quick_cache::sync::Cache"
      via: "global YAML_CACHE initialization"
      pattern: "Cache::new\(128\)"
    - from: "ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs"
      to: "CachedYaml.modified"
      via: "mtime-aware stale entry invalidation"
      pattern: "YAML_CACHE\.(get|remove|insert)"
---

<objective>
Replace the YAML global DashMap with a bounded quick_cache implementation first so the most visible cache surface adopts the locked Phase 4 contract without changing YAML freshness behavior.

Purpose: CACHE-01 and the Phase 4 observability contract both depend on the YAML cache preserving its current mtime-aware semantics while switching to a bounded store per D-01, D-02, D-06, D-07, and D-08. This plan also resolves the roadmap/research wording mismatch explicitly: use `quick_cache` per locked decision D-01, document its bounded eviction semantics, and do not treat exact LRU victim order as the acceptance target.
Output: A 128-entry bounded YAML cache, canonical `CacheStats`, rewritten public-behavior tests, and synced API docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bounded-cache-replacement/04-CONTEXT.md
@.planning/phases/04-bounded-cache-replacement/04-RESEARCH.md
@.planning/phases/04-bounded-cache-replacement/04-VALIDATION.md
@docs/api/README.md
@docs/api/classic-yaml-core.md

<interfaces>
From `ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs`:
```rust
static YAML_CACHE: Lazy<DashMap<PathBuf, CachedYaml>> = Lazy::new(DashMap::new);

#[derive(Debug, Clone, Serialize)]
pub struct CacheStats {
    pub hits: u64,
    pub misses: u64,
    pub hit_rate: f64,
    pub size: usize,
    pub total_bytes: usize,
}

pub fn cache_stats() -> CacheStats { ... }
pub fn reset_cache_stats() { ... }
```

From `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs`:
```rust
use quick_cache::sync::Cache;

let read_cache = Cache::new(cache_size);
if let Some(cached) = self.read_cache.get(path) {
    return Ok(cached);
}
self.read_cache.insert(path.to_path_buf(), content.clone());
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert YAML_CACHE to bounded quick_cache with canonical stats</name>
  <files>ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs
    - ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs
    - docs/api/classic-yaml-core.md
    - .planning/phases/04-bounded-cache-replacement/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: loading the same unchanged YAML file twice records one miss then one hit.
    - Test 2: touching the file mtime forces a semantic miss and refreshes the cached value.
    - Test 3: cache statistics always report hits, misses, hit_rate, size, and capacity only.
    - Test 4: bounded-cache coverage proves capacity is enforced without asserting a specific evicted key, because `quick_cache` is the locked implementation per D-01 even though research documents S3-FIFO rather than strict LRU.
  </behavior>
  <action>Write or update the failing YAML cache tests first, then replace `static YAML_CACHE: Lazy<DashMap<PathBuf, CachedYaml>>` with `static YAML_CACHE: LazyLock<Cache<PathBuf, CachedYaml>>` initialized as `Cache::new(128)` per D-01. Keep `CachedYaml` and the current mtime check exactly as the freshness rule per D-06: if `current_modified <= cached.modified`, count a hit and return the cached value; otherwise remove the stale entry, count a miss, and reload from disk. Preserve the global atomic counters instead of using quick_cache built-in stats so stale YAML entries still count as misses per D-06 and research guidance. Replace the canonical `CacheStats` fields with exactly `hits`, `misses`, `hit_rate`, `size`, and `capacity` per D-02, remove `total_bytes` from that struct per D-03, and keep legacy helpers only as adapters derived from the canonical stats per D-05. Explicitly treat roadmap `LRU eviction` wording as the phase shorthand for bounded eviction with `quick_cache`; do not add tests that assert exact victim identity, because research shows the locked crate documents S3-FIFO semantics.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs` contains `use quick_cache::sync::Cache`
    - `ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs` contains `static YAML_CACHE: LazyLock<Cache<PathBuf, CachedYaml>>`
    - `ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs` contains `Cache::new(128)`
    - `ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs` contains `pub capacity: usize`
    - `ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs` does not contain `pub total_bytes: usize`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-yaml-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>`classic-yaml-core` uses a bounded 128-entry quick_cache, still invalidates stale entries by mtime, and exposes only the canonical five-field CacheStats contract.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite YAML cache tests around public behavior and sync the API guide</name>
  <files>ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs, docs/api/classic-yaml-core.md</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs
    - ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs
    - docs/api/classic-yaml-core.md
    - .planning/phases/04-bounded-cache-replacement/04-VALIDATION.md
  </read_first>
  <action>Replace any YAML cache assertions that depend on `DashMap` internals such as direct iteration or `contains_key()` with public-behavior checks per D-08: call `clear_global_yaml_cache()`, `reset_cache_stats()`, `load_yaml_file()`, and `cache_stats()` to prove bounded size, hit/miss accounting, and stale-entry reload behavior. Keep `clear_global_yaml_cache()` and `reset_cache_stats()` as the supported test-isolation surface per D-07. Update `docs/api/classic-yaml-core.md` so it documents the new canonical `CacheStats` fields, the fixed capacity `128`, and the fact that any legacy `get_cache_stats()` helper is now adapter-only rather than the source of truth per D-05. Add one explicit note resolving the roadmap wording mismatch: Phase 4 uses `quick_cache` per D-01, so acceptance is bounded eviction plus observable stats, not proof of strict LRU victim order.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs` contains `cache_stats()`
    - `ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs` contains `clear_global_yaml_cache()`
    - `ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs` does not contain `contains_key(`
    - `docs/api/classic-yaml-core.md` contains `capacity`
    - `docs/api/classic-yaml-core.md` contains `128`
    - `docs/api/classic-yaml-core.md` does not contain `total_bytes` in the canonical `CacheStats` field list
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-yaml-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>YAML cache coverage validates only public behavior, and the contributor API guide matches the new bounded-cache contract.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-yaml-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo clippy -p classic-yaml-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` passes.
- `docs/api/classic-yaml-core.md` describes `hits`, `misses`, `hit_rate`, `size`, and `capacity` as the YAML cache stats contract.
- YAML docs/tests explicitly state that Phase 4 validates bounded `quick_cache` eviction semantics rather than exact LRU victim order.
</verification>

<success_criteria>
- CACHE-01: `YAML_CACHE` is bounded at 128 entries with quick_cache while preserving mtime-aware reload behavior, and the roadmap `LRU` wording is satisfied for this phase as bounded eviction rather than exact victim-order proof.
- CONS-03: YAML exposes the canonical five-field `CacheStats` contract without `total_bytes` in the shared struct.
- Existing clear/reset helpers still provide deterministic test isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bounded-cache-replacement/04-01-SUMMARY.md`
</output>
