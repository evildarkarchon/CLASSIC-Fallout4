---
phase: 06-repo-root-workspace-cutover
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/planning/test_phase06_validation.py
  - tests/planning/phase06_clean_run.ps1
autonomous: true
requirements:
  - ROOT-01
  - ROOT-02
must_haves:
  truths:
    - "Wave 0 creates the Phase 6 validation scaffold before any Wave 1 work depends on it."
    - "A reusable clean-state proof helper exists before repo-root cargo-cutover execution starts."
  artifacts:
    - path: "tests/planning/test_phase06_validation.py"
      provides: "Phase 6 validation scaffold with named audit hooks"
      contains: "validation_bootstrap"
    - path: "tests/planning/phase06_clean_run.ps1"
      provides: "Reusable clean repo-root Cargo proof helper"
      contains: "cargo locate-project --workspace"
  key_links:
    - from: "tests/planning/test_phase06_validation.py"
      to: "tests/planning/phase06_clean_run.ps1"
      via: "clean_target_guard audit"
      pattern: "phase06_clean_run.ps1"
---

<objective>
Bootstrap the Phase 6 validation assets before any workspace-cutover task depends on them.

Purpose: satisfy Nyquist Wave 0 by creating the planning audit scaffold and clean-run helper before Wave 1 begins.
Output: `tests/planning/test_phase06_validation.py` scaffold and `tests/planning/phase06_clean_run.ps1` clean-state helper.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
@.planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
@.planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
@tests/planning/test_phase05_validation.py

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 6 audit scaffold and clean-state helper</name>
  <read_first>
    - .planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
    - .planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
    - .planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
    - tests/planning/test_phase05_validation.py
  </read_first>
  <files>tests/planning/test_phase06_validation.py, tests/planning/phase06_clean_run.ps1</files>
  <action>Create `tests/planning/test_phase06_validation.py` as a `unittest`-style scaffold patterned after existing planning audit files. Include a passing bootstrap test named `validation_bootstrap`, repo-root path helpers, and placeholder tests or skipped stubs named `workspace_root_manifest`, `core_root_files`, `stub_validator`, `rebuild_script`, `cargo_aliases`, `benchmark_support_set`, `repo_root_workflows`, `benchmark_workflow_paths`, `cargo_root_detection`, `old_manifest_audit`, `clean_target_guard`, `readme_sync`, and `agents_sync` so later plans can fill them in without renaming the validation contract. Create `tests/planning/phase06_clean_run.ps1` with the clean-state command sequence for Phase 6: guard or rename `ClassicLib-rs/target`, then run `cargo locate-project --workspace`, `cargo metadata --format-version 1 --no-deps`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace --release -- --nocapture`, `cargo build -p classic-scanlog-core`, and `python -m pytest tests/planning/test_phase06_validation.py -q`, then restore any renamed target directory.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "validation_bootstrap or clean_target_guard"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/planning/test_phase06_validation.py` exists
    - `tests/planning/test_phase06_validation.py` contains `validation_bootstrap`
    - `tests/planning/test_phase06_validation.py` contains `workspace_root_manifest`, `benchmark_support_set`, and `agents_sync`
    - `tests/planning/phase06_clean_run.ps1` exists
    - `tests/planning/phase06_clean_run.ps1` contains `cargo locate-project --workspace`
    - `tests/planning/phase06_clean_run.ps1` contains `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `tests/planning/phase06_clean_run.ps1` contains `cargo build -p classic-scanlog-core`
  </acceptance_criteria>
  <done>Wave 0 validation assets exist before any Phase 6 cutover task depends on them.</done>
</task>

</tasks>

<verification>
The Phase 6 validation scaffold and reusable clean-state helper exist before Wave 1 work starts.
</verification>

<success_criteria>
  - `tests/planning/test_phase06_validation.py` exists with the named audit hooks used by later plans.
  - `tests/planning/phase06_clean_run.ps1` exists with the clean repo-root Cargo proof sequence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-root-workspace-cutover/06-00-SUMMARY.md`
</output>
