---
phase: 06-repo-root-workspace-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .github/workflows/ci-rust.yml
  - .github/workflows/benchmarks.yml
  - tests/planning/test_phase06_validation.py
autonomous: true
requirements:
  - ROOT-01
  - ROOT-02
must_haves:
  truths:
    - "Contributor can run `cargo fmt --all`, `cargo clippy --workspace`, `cargo test --workspace`, and `cargo build -p classic-scanlog-core` from repo root."
    - "Rust CI runs against the repo-root workspace and no longer names ClassicLib-rs/Cargo.toml as an active manifest."
    - "Phase proof includes Cargo-native root detection and at least one clean run that does not rely on ClassicLib-rs/target."
  artifacts:
    - path: ".github/workflows/ci-rust.yml"
      provides: "Repo-root Rust CI workflow"
      contains: "cargo clippy --workspace --all-targets --all-features -- -D warnings"
    - path: ".github/workflows/benchmarks.yml"
      provides: "Benchmark workflow with minimum viable repo-root path fixes"
      contains: "target/criterion"
    - path: "tests/planning/test_phase06_validation.py"
      provides: "Audit coverage for CI/workflow/root-detection/clean-run invariants"
      contains: "cargo locate-project --workspace"
  key_links:
    - from: ".github/workflows/ci-rust.yml"
      to: "Cargo.toml"
      via: "plain repo-root cargo commands"
      pattern: "cargo test --workspace --release -- --nocapture"
    - from: ".github/workflows/ci-rust.yml"
      to: "target/"
      via: "actions/cache path"
      pattern: "path: target"
    - from: "tests/planning/test_phase06_validation.py"
      to: "cargo metadata --format-version 1 --no-deps"
      via: "subprocess root-detection assertion"
      pattern: "cargo metadata --format-version 1 --no-deps"
---

<objective>
Rewire cargo-based automation to the repo-root workspace and prove the cutover with Cargo-native root detection plus a clean validation pass.

Purpose: satisfy the Phase 6 closure rule that repo-root Cargo workflows and Rust CI both operate without `ClassicLib-rs/Cargo.toml`.
Output: repo-root `ci-rust.yml`, minimum benchmark workflow path fixes if needed, and a passing clean proof suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
@.planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
@.planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
@.planning/phases/06-repo-root-workspace-cutover/06-01-PLAN.md
@.github/workflows/ci-rust.yml
@.github/workflows/benchmarks.yml
@tests/planning/test_phase06_validation.py

<interfaces>
From `.github/workflows/ci-rust.yml`:
```yaml
- name: Run rustfmt check
  run: cargo fmt --all --manifest-path ClassicLib-rs/Cargo.toml -- --check

- name: Cache cargo build
  with:
    path: ClassicLib-rs/target

- name: Run Rust tests
  run: cargo test --workspace --release --manifest-path ClassicLib-rs/Cargo.toml -- --nocapture
```

From `.github/workflows/benchmarks.yml`:
```yaml
- name: Cache cargo build
  with:
    path: ClassicLib-rs/target

- name: Check baseline exists
  working-directory: ClassicLib-rs

- name: Run benchmarks
  working-directory: ClassicLib-rs
  run: cargo bench -- --save-baseline current
```

From `06-VALIDATION.md`:
```text
cargo locate-project --workspace && cargo metadata --format-version 1 --no-deps && cargo build -p classic-scanlog-core
cargo fmt --all -- --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo test --workspace --release -- --nocapture
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Rust CI and minimum benchmark paths to repo-root Cargo</name>
  <read_first>
    - .planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
    - .planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
    - .github/workflows/ci-rust.yml
    - .github/workflows/benchmarks.yml
    - tests/planning/test_phase06_validation.py
    - Cargo.toml
  </read_first>
  <files>.github/workflows/ci-rust.yml, .github/workflows/benchmarks.yml, tests/planning/test_phase06_validation.py</files>
  <action>Per D-07, D-09, D-10, D-13, and D-16, rewrite `.github/workflows/ci-rust.yml` to use plain repo-root Cargo commands with no `--manifest-path` references, and change cargo cache/output paths from `ClassicLib-rs/target` to `target`. Keep the same jobs (`format`, `lint`, `build`, `test`) and the same crate-specific test intent, but run them from repo root: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo build --workspace --release`, `cargo test -p classic-message-core -- --nocapture`, `cargo test --workspace --release -- --nocapture`, and `cargo test --workspace --release --all-features -- --nocapture`. Audit `.github/workflows/benchmarks.yml` after the support-file move; if it still points at now-dead `ClassicLib-rs` working directories, `ClassicLib-rs/target`, or `ClassicLib-rs/Cargo.toml` assumptions, apply only the minimum path fixes needed to keep the workflow viable with repo-root `criterion.toml`, `benchmark-config.yaml`, `benches/`, and `target/criterion/**`. Extend `tests/planning/test_phase06_validation.py` so it asserts `ci-rust.yml` has no `ClassicLib-rs/Cargo.toml` usage and `benchmarks.yml` uses repo-root benchmark/target paths when touched.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "repo_root_workflows or benchmark_workflow_paths"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci-rust.yml` contains `cargo fmt --all -- --check`
    - `.github/workflows/ci-rust.yml` contains `cargo clippy --workspace --all-targets --all-features -- -D warnings`
    - `.github/workflows/ci-rust.yml` contains `cargo build --workspace --release`
    - `.github/workflows/ci-rust.yml` contains `cargo test --workspace --release -- --nocapture`
    - `.github/workflows/ci-rust.yml` does not contain `ClassicLib-rs/Cargo.toml`
    - `.github/workflows/ci-rust.yml` contains `path: target`
    - `.github/workflows/benchmarks.yml` does not contain `ClassicLib-rs/Cargo.toml`
    - `.github/workflows/benchmarks.yml` contains `target/criterion/baseline` if benchmark paths are still managed there
    - `tests/planning/test_phase06_validation.py` contains the strings `repo_root_workflows` and `benchmark_workflow_paths`
  </acceptance_criteria>
  <done>Active cargo-based CI surfaces target the repo-root workspace and no longer depend on the old manifest path.</done>
</task>

<task type="auto">
  <name>Task 2: Add Cargo-native proof and execute the clean root-workflow validation suite</name>
  <read_first>
    - .planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
    - .planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
    - .planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
    - tests/planning/test_phase06_validation.py
    - .github/workflows/ci-rust.yml
    - validate_stubs.py
    - rebuild_rust.ps1
  </read_first>
  <files>tests/planning/test_phase06_validation.py</files>
  <action>Per D-14, D-15, and D-16, finish `tests/planning/test_phase06_validation.py` so it shells out to `cargo locate-project --workspace` and `cargo metadata --format-version 1 --no-deps`, parses the JSON response, and asserts `workspace_root == REPO_ROOT` and `target_directory == REPO_ROOT / "target"`. Add a grep-style audit over active cargo-first surfaces (`.github/workflows/ci-rust.yml`, `rebuild_rust.ps1`, `validate_stubs.py`) so the old `ClassicLib-rs/Cargo.toml` path fails the phase audit if it survives in active workflow logic. Then perform one explicit clean pass that does not reuse stale `ClassicLib-rs/target` outputs: remove or rename `ClassicLib-rs/target` if present, run `cargo locate-project --workspace`, `cargo metadata --format-version 1 --no-deps`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace --release -- --nocapture`, `cargo build -p classic-scanlog-core`, and finally `python -m pytest tests/planning/test_phase06_validation.py -q`. If the clean pass exposes stale-path breakage, fix it in the already-in-scope active cargo-first files only; do not expand into later-phase wrapper/CMake rewires.</action>
  <verify>
    <automated>cargo locate-project --workspace && cargo metadata --format-version 1 --no-deps && cargo fmt --all -- --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo test --workspace --release -- --nocapture && cargo build -p classic-scanlog-core && python -m pytest tests/planning/test_phase06_validation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/planning/test_phase06_validation.py` contains `cargo locate-project --workspace`
    - `tests/planning/test_phase06_validation.py` contains `cargo metadata --format-version 1 --no-deps`
    - `tests/planning/test_phase06_validation.py` asserts `workspace_root`
    - `tests/planning/test_phase06_validation.py` asserts `target_directory`
    - `tests/planning/test_phase06_validation.py` contains `ClassicLib-rs/Cargo.toml`
    - The automated verify command completes successfully from repo root
    - The verify command succeeds after removing or bypassing stale `ClassicLib-rs/target`
  </acceptance_criteria>
  <done>The repo-root Cargo contract, Rust CI rewiring, root detection, and clean-state proof are all green.</done>
</task>

</tasks>

<verification>
Root Cargo commands, Rust CI, benchmark-path viability, and clean-state proof all operate from repo root with Cargo-native root detection.
</verification>

<success_criteria>
- `ci-rust.yml` no longer uses `ClassicLib-rs/Cargo.toml` or `ClassicLib-rs/target`.
- Cargo reports the repository root as the only workspace root and uses `target/` at repo root.
- `cargo fmt --all`, `cargo clippy --workspace`, `cargo test --workspace`, and `cargo build -p classic-scanlog-core` all pass from repo root.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-root-workspace-cutover/06-02-SUMMARY.md`
</output>
