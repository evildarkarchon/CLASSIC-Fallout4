---
phase: 06-repo-root-workspace-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - criterion.toml
  - benchmark-config.yaml
  - benches/common/mod.rs
  - benches/common/config.rs
  - benches/common/db_fixtures.rs
  - benches/common/fixtures.rs
  - ClassicLib-rs/criterion.toml
  - ClassicLib-rs/benchmark-config.yaml
  - ClassicLib-rs/benches/common/mod.rs
  - ClassicLib-rs/benches/common/config.rs
  - ClassicLib-rs/benches/common/db_fixtures.rs
  - ClassicLib-rs/benches/common/fixtures.rs
  - ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
  - ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
  - ClassicLib-rs/business-logic/classic-database-core/benches/database_benchmarks.rs
  - ClassicLib-rs/python-bindings/classic-scanlog-py/benches/gil_benchmarks.rs
  - ClassicLib-rs/python-bindings/classic-file-io-py/benches/gil_benchmarks.rs
  - tests/planning/test_phase06_validation.py
autonomous: true
requirements:
  - ROOT-01
  - ROOT-02
must_haves:
  truths:
    - "The repo root owns the benchmark support files that D-02 includes in the Phase 6 workspace-owned file set."
    - "The old benchmark support copies under ClassicLib-rs are removed so root-scoped benchmark discovery does not stay ambiguous."
    - "All crate-level benchmark helper includes still resolve after `benches/` moves to repo root."
  artifacts:
    - path: "criterion.toml"
      provides: "Repo-root Criterion configuration"
      contains: "criterion_home = \"./target/criterion\""
    - path: "benchmark-config.yaml"
      provides: "Repo-root benchmark threshold configuration"
      contains: "warning_threshold: 5"
    - path: "benches/common/mod.rs"
      provides: "Repo-root shared benchmark helper module"
      contains: "pub mod config;"
    - path: "tests/planning/test_phase06_validation.py"
      provides: "Audit coverage for benchmark support-file relocation and include rewrites"
      contains: "benchmark_support_set"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs"
      to: "benches/common/mod.rs"
      via: "updated #[path] include after repo-root move"
      pattern: "benches/common/mod.rs"
    - from: "ClassicLib-rs/business-logic/classic-database-core/benches/database_benchmarks.rs"
      to: "benches/common/db_fixtures.rs"
      via: "updated #[path] include after repo-root move"
      pattern: "benches/common/db_fixtures.rs"
---

<objective>
Move the benchmark-owned support files in the Phase 6 workspace-owned file set to the repo root and keep crate-level benchmark helper wiring valid.

Purpose: satisfy D-02 completely without promoting benchmark CI green status into the Phase 6 closure gate.
Output: repo-root `criterion.toml`, `benchmark-config.yaml`, `benches/common/*`, repaired benchmark include paths, and audit coverage for benchmark support-file relocation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
@.planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
@.planning/phases/06-repo-root-workspace-cutover/06-REVIEWS.md
@.planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
@.planning/phases/06-repo-root-workspace-cutover/06-01-PLAN.md
@ClassicLib-rs/criterion.toml
@ClassicLib-rs/benchmark-config.yaml
@ClassicLib-rs/benches/common/mod.rs
@ClassicLib-rs/benches/common/config.rs
@ClassicLib-rs/benches/common/db_fixtures.rs
@ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
@ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
@ClassicLib-rs/business-logic/classic-database-core/benches/database_benchmarks.rs
@ClassicLib-rs/python-bindings/classic-scanlog-py/benches/gil_benchmarks.rs
@ClassicLib-rs/python-bindings/classic-file-io-py/benches/gil_benchmarks.rs

<interfaces>
From `ClassicLib-rs/criterion.toml`:
```toml
criterion_home = "./target/criterion"

[output]
verbose = true
```

From `ClassicLib-rs/benchmark-config.yaml`:
```yaml
defaults:
  warning_threshold: 5
  failure_threshold: 10
```

From current benchmark crates:
```rust
#[path = "../../../benches/common/mod.rs"]
mod common;

#[path = "../../../benches/common/db_fixtures.rs"]
mod db_fixtures;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate the benchmark support set to repo root using the real file contents</name>
  <files>criterion.toml, benchmark-config.yaml, benches/common/mod.rs, benches/common/config.rs, benches/common/db_fixtures.rs, benches/common/fixtures.rs, ClassicLib-rs/criterion.toml, ClassicLib-rs/benchmark-config.yaml, ClassicLib-rs/benches/common/mod.rs, ClassicLib-rs/benches/common/config.rs, ClassicLib-rs/benches/common/db_fixtures.rs, ClassicLib-rs/benches/common/fixtures.rs, tests/planning/test_phase06_validation.py</files>
  <action>Per D-01 and D-02, move `ClassicLib-rs/criterion.toml` to `./criterion.toml`, `ClassicLib-rs/benchmark-config.yaml` to `./benchmark-config.yaml`, and `ClassicLib-rs/benches/common/*` to `./benches/common/*`. Use the real current file contents as the source of truth for all assertions and comments; do not invent placeholder strings such as `output_format` or `thresholds:` if they are not present. Delete the old `ClassicLib-rs` copies after the move so benchmark support-file discovery is rooted only at the repository root. Extend `tests/planning/test_phase06_validation.py` with a `benchmark_support_set` audit that asserts repo-root copies exist, the old `ClassicLib-rs` copies are absent, `criterion.toml` still points at `./target/criterion`, and `benchmark-config.yaml` still exposes the current warning/failure threshold keys. Addresses review concern: align artifact assertions with the real benchmark config files before execution.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "benchmark_support_set"</automated>
  </verify>
  <done>The benchmark-owned support files now live only at repo root, and the phase audit proves the moved files match the real source content.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep and repair benchmark helper include paths after the move</name>
  <files>ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs, ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs, ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs, ClassicLib-rs/business-logic/classic-database-core/benches/database_benchmarks.rs, ClassicLib-rs/python-bindings/classic-scanlog-py/benches/gil_benchmarks.rs, ClassicLib-rs/python-bindings/classic-file-io-py/benches/gil_benchmarks.rs, tests/planning/test_phase06_validation.py</files>
  <action>Addresses review concern: benchmark-path fallout. Sweep every crate-level benchmark file that currently imports shared helpers from `../../../benches/common/...` and update the relative `#[path = "..."]` attributes so they still resolve to the new repo-root `benches/common/*` location after Task 1. Cover both `mod.rs` and `db_fixtures.rs` imports where present, and keep the files otherwise unchanged. Extend `tests/planning/test_phase06_validation.py` so `benchmark_support_set` or a sibling benchmark include audit fails if any known benchmark file still points at the pre-move relative paths or if the new include target does not exist. Do not turn benchmark job green status into a closure gate here; only preserve local source-level viability for Phase 6.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "benchmark_support_set"</automated>
  </verify>
  <done>Every known benchmark helper include still resolves after `benches/common/*` moves to repo root.</done>
</task>

</tasks>

<verification>
Repo-root benchmark support files exist, their old `ClassicLib-rs` copies are gone, and crate-level benchmark helper includes still resolve after the move.
</verification>

<success_criteria>
  - `criterion.toml`, `benchmark-config.yaml`, and `benches/common/*` exist at repo root.
  - Old `ClassicLib-rs` benchmark support copies are absent.
  - Every known benchmark file that imported shared helpers under `ClassicLib-rs/benches/common/*` is rewired to the new repo-root location.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-root-workspace-cutover/06-02-SUMMARY.md`
</output>
