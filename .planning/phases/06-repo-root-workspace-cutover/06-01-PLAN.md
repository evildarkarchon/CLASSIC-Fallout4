---
phase: 06-repo-root-workspace-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - .cargo/config.toml
  - criterion.toml
  - benchmark-config.yaml
  - benches/
  - validate_stubs.py
  - rebuild_rust.ps1
  - tests/planning/test_phase06_validation.py
autonomous: true
requirements:
  - ROOT-01
  - ROOT-02
must_haves:
  truths:
    - "Contributor can run Cargo from the repository root and Cargo resolves the repo root as the workspace root."
    - "The repo root owns the full workspace support set instead of leaving root-scoped files under ClassicLib-rs/."
    - "ClassicLib-rs/Cargo.toml is no longer a live workspace manifest."
  artifacts:
    - path: "Cargo.toml"
      provides: "Authoritative repo-root virtual workspace manifest"
      contains: "[workspace]"
    - path: ".cargo/config.toml"
      provides: "Repo-root Cargo alias/profile discovery"
      contains: "[alias]"
    - path: "validate_stubs.py"
      provides: "Repo-root stub validation entrypoint"
      contains: "default=Path(__file__).parent"
    - path: "tests/planning/test_phase06_validation.py"
      provides: "Planning audit for Phase 6 root-cutover invariants"
      contains: "workspace_root_manifest"
  key_links:
    - from: "Cargo.toml"
      to: "ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml"
      via: "[workspace].members entry"
      pattern: "ClassicLib-rs/business-logic/classic-scanlog-core"
    - from: ".cargo/config.toml"
      to: "Cargo.toml"
      via: "repo-root Cargo config discovery"
      pattern: "profile-build = \"build --profile release-with-debug\""
    - from: "validate_stubs.py"
      to: "ClassicLib-rs/python-bindings/"
      via: "--rust-dir repo-root default"
      pattern: "bindings_dir = rust_dir / \"python-bindings\""
---

<objective>
Establish the repository root as the only live Cargo workspace shell and move the full Phase-6 workspace-owned file set there.

Purpose: satisfy the one-root cutover first so later CI and integration rewires have a real canonical workspace to target.
Output: repo-root workspace/support files, root-aware helper scripts, and a Phase 6 planning audit file.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
@.planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
@.planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
@ClassicLib-rs/Cargo.toml
@ClassicLib-rs/.cargo/config.toml
@ClassicLib-rs/validate_stubs.py
@rebuild_rust.ps1

<interfaces>
From `ClassicLib-rs/Cargo.toml`:
```toml
[workspace]
members = [
    "foundation/classic-shared-core",
    "business-logic/classic-scanlog-core",
    "python-bindings/classic-config-py",
    "node-bindings/classic-node",
    "ui-applications/classic-tui",
    "cpp-bindings/classic-cpp-bridge",
]
resolver = "2"

[workspace.lints.rust]
deprecated = "deny"
unused = "deny"

[profile.release-with-debug]
inherits = "release"
debug = true
```

From `ClassicLib-rs/.cargo/config.toml`:
```toml
[alias]
flame = "flamegraph"
flame-bench = "flamegraph --bench"
profile-build = "build --profile release-with-debug"
```

From `ClassicLib-rs/validate_stubs.py`:
```python
parser.add_argument(
    "--rust-dir",
    type=Path,
    default=Path(__file__).parent,
    help="Path to rust directory (default: script directory)",
)
bindings_dir = rust_dir / "python-bindings"
```

From `rebuild_rust.ps1`:
```powershell
$ProjectRoot = $PSScriptRoot
$WorkspaceManifest = Join-Path $ProjectRoot "ClassicLib-rs/Cargo.toml"
$PythonBindingsRoot = Join-Path $ProjectRoot "ClassicLib-rs/python-bindings"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the workspace shell and support-file set to repo root</name>
  <read_first>
    - .planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
    - .planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
    - .planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
    - ClassicLib-rs/Cargo.toml
    - ClassicLib-rs/.cargo/config.toml
    - tests/planning/test_phase05_validation.py
  </read_first>
  <files>Cargo.toml, Cargo.lock, .cargo/config.toml, criterion.toml, benchmark-config.yaml, benches/, tests/planning/test_phase06_validation.py</files>
  <action>Per D-01, D-02, D-04, D-05, D-06, D-09, D-10, and D-11, create `./Cargo.toml` as the only authoritative virtual workspace by copying the current workspace tables from `ClassicLib-rs/Cargo.toml`, prefixing every `members` entry with `ClassicLib-rs/`, preserving `[workspace.dependencies]`, `[workspace.lints]`, `[profile.release]`, and `[profile.release-with-debug]` exactly, and keeping `resolver = "2"`. Do not add `workspace.default-members`. Move `ClassicLib-rs/Cargo.lock` to `./Cargo.lock`, `ClassicLib-rs/.cargo/config.toml` to `./.cargo/config.toml`, `ClassicLib-rs/criterion.toml` to `./criterion.toml`, `ClassicLib-rs/benchmark-config.yaml` to `./benchmark-config.yaml`, and `ClassicLib-rs/benches/` to `./benches/`. Delete `ClassicLib-rs/Cargo.toml` entirely after the new root manifest exists. Create `tests/planning/test_phase06_validation.py` as a `unittest` audit patterned after existing phase audit files; include tests named for `workspace_root_manifest` and `workspace_owned_file_set` that assert repo-root manifest existence, old-manifest absence, preserved `resolver = "2"`, absence of `default-members`, and existence of the moved support files/directories.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "workspace_root_manifest or workspace_owned_file_set"</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` exists at repo root and contains `[workspace]`
    - `Cargo.toml` contains `resolver = "2"`
    - `Cargo.toml` contains `ClassicLib-rs/business-logic/classic-scanlog-core`
    - `Cargo.toml` does not contain `default-members`
    - `ClassicLib-rs/Cargo.toml` is absent from disk
    - `Cargo.lock`, `.cargo/config.toml`, `criterion.toml`, and `benchmark-config.yaml` exist at repo root
    - `benches/common/mod.rs` exists at repo root
    - `tests/planning/test_phase06_validation.py` contains the string `workspace_root_manifest`
  </acceptance_criteria>
  <done>Repo root owns the workspace shell and support files, and the old live manifest no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget root-aware helper scripts to the new workspace contract</name>
  <read_first>
    - .planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
    - .planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
    - Cargo.toml
    - validate_stubs.py
    - rebuild_rust.ps1
    - tests/planning/test_phase06_validation.py
  </read_first>
  <files>validate_stubs.py, rebuild_rust.ps1, tests/planning/test_phase06_validation.py</files>
  <action>Per D-03, D-11, and D-12, move `ClassicLib-rs/validate_stubs.py` to `./validate_stubs.py`, keep the script semantics the same, but make the repo root its default `--rust-dir` and update any usage/help text or path assumptions so `rust_dir / "ClassicLib-rs/python-bindings"` is not required. Update `rebuild_rust.ps1` so `$WorkspaceManifest` points to `Join-Path $ProjectRoot "Cargo.toml"`, workspace builds use plain repo-root Cargo invocation where possible, and package-filtered builds still map to `cargo build -p <crate>` from repo root; preserve Python bindings discovery under `ClassicLib-rs/python-bindings` and keep existing `.venv` behavior intact. Extend `tests/planning/test_phase06_validation.py` with explicit audits for the moved `validate_stubs.py`, repo-root `rebuild_rust.ps1` manifest usage, and root `.cargo/config.toml` alias preservation.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "stub_validator or rebuild_script or cargo_aliases"</automated>
  </verify>
  <acceptance_criteria>
    - `validate_stubs.py` exists at repo root
    - `validate_stubs.py` contains `default=Path(__file__).parent`
    - `validate_stubs.py` contains `bindings_dir = rust_dir / "ClassicLib-rs/python-bindings"` or equivalent repo-root-aware logic that still reaches `ClassicLib-rs/python-bindings`
    - `rebuild_rust.ps1` contains `Join-Path $ProjectRoot "Cargo.toml"`
    - `rebuild_rust.ps1` does not contain `Join-Path $ProjectRoot "ClassicLib-rs/Cargo.toml"`
    - `.cargo/config.toml` at repo root contains `profile-build = "build --profile release-with-debug"`
    - `tests/planning/test_phase06_validation.py` contains the strings `stub_validator` and `rebuild_script`
  </acceptance_criteria>
  <done>Root helper scripts default to the repo-root workspace without changing alias, profile, or Python-binding behavior.</done>
</task>

</tasks>

<verification>
Repo-root workspace shell exists, old manifest is gone, helper scripts default to repo root, and the Phase 6 planning audit can prove those invariants.
</verification>

<success_criteria>
- Repo-root `Cargo.toml` is the only live workspace manifest.
- Repo-root support files (`Cargo.lock`, `.cargo/config.toml`, `criterion.toml`, `benchmark-config.yaml`, `benches/`, `validate_stubs.py`) exist and are no longer stranded under `ClassicLib-rs/`.
- `rebuild_rust.ps1` and the planning audit target repo-root Cargo behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-root-workspace-cutover/06-01-SUMMARY.md`
</output>
