---
phase: 06-repo-root-workspace-cutover
plan: 01
type: execute
wave: 1
depends_on:
  - 06-00
files_modified:
  - Cargo.toml
  - Cargo.lock
  - .cargo/config.toml
  - validate_stubs.py
  - ClassicLib-rs/Cargo.toml
  - ClassicLib-rs/Cargo.lock
  - ClassicLib-rs/.cargo/config.toml
  - ClassicLib-rs/validate_stubs.py
  - rebuild_rust.ps1
  - tests/planning/test_phase06_validation.py
autonomous: true
requirements:
  - ROOT-01
  - ROOT-02
must_haves:
  truths:
    - "Contributor can run Cargo from the repository root and Cargo resolves the repo root as the workspace root."
    - "The repo root owns the canonical workspace shell, shared lockfile, and Cargo config instead of leaving those root-scoped files under ClassicLib-rs."
    - "Repo-root helper scripts use the new workspace contract without leaving `ClassicLib-rs/Cargo.toml` as a live manifest dependency."
  artifacts:
    - path: "Cargo.toml"
      provides: "Authoritative repo-root virtual workspace manifest"
      contains: "[workspace]"
    - path: "Cargo.lock"
      provides: "Repo-root shared dependency lockfile"
      contains: "version = 4"
    - path: ".cargo/config.toml"
      provides: "Repo-root Cargo alias/profile discovery"
      contains: "profile-build = \"build --profile release-with-debug\""
    - path: "validate_stubs.py"
      provides: "Repo-root stub validation entrypoint with explicit rust-dir contract"
      contains: "ClassicLib-rs/python-bindings"
    - path: "rebuild_rust.ps1"
      provides: "Repo-root Rust rebuild entrypoint using plain Cargo invocation"
      contains: "cargo build --workspace"
    - path: "tests/planning/test_phase06_validation.py"
      provides: "Planning audit for Phase 6 root-cutover invariants"
      contains: "stub_validator"
  key_links:
    - from: "Cargo.toml"
      to: "ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml"
      via: "[workspace].members entry"
      pattern: "ClassicLib-rs/business-logic/classic-scanlog-core"
    - from: ".cargo/config.toml"
      to: "Cargo.toml"
      via: "repo-root Cargo config discovery"
      pattern: "profile-build = \"build --profile release-with-debug\""
    - from: "validate_stubs.py"
      to: "ClassicLib-rs/python-bindings/"
      via: "repo-root default plus explicit legacy-path normalization"
      pattern: "ClassicLib-rs/python-bindings"
    - from: "rebuild_rust.ps1"
      to: "Cargo.toml"
      via: "plain repo-root cargo commands with no manifest-path variable"
      pattern: "cargo build --workspace"
---

<objective>
Establish the repository root as the only live Cargo workspace shell and retarget the root-aware helper entrypoints that must follow it immediately.

Purpose: satisfy the one-root cutover first so later benchmark, CI, and doc rewires have a real canonical workspace to target.
Output: repo-root workspace shell files, root-aware helper scripts, and a Phase 6 planning audit file.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
@.planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
@.planning/phases/06-repo-root-workspace-cutover/06-REVIEWS.md
@.planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
@ClassicLib-rs/Cargo.toml
@ClassicLib-rs/.cargo/config.toml
@ClassicLib-rs/validate_stubs.py
@rebuild_rust.ps1

<interfaces>
From `ClassicLib-rs/Cargo.toml`:
```toml
[workspace]
members = [
    "foundation/classic-shared-core",
    "business-logic/classic-scanlog-core",
    "python-bindings/classic-config-py",
    "node-bindings/classic-node",
    "ui-applications/classic-tui",
    "cpp-bindings/classic-cpp-bridge",
]
resolver = "2"
```

From `ClassicLib-rs/.cargo/config.toml`:
```toml
[alias]
flame = "flamegraph"
flame-bench = "flamegraph --bench"
profile-build = "build --profile release-with-debug"
```

From `ClassicLib-rs/validate_stubs.py`:
```python
parser.add_argument(
    "--rust-dir",
    type=Path,
    default=Path(__file__).parent,
    help="Path to rust directory (default: script directory)",
)
bindings_dir = rust_dir / "python-bindings"
```

From `rebuild_rust.ps1`:
```powershell
$ProjectRoot = $PSScriptRoot
$WorkspaceManifest = Join-Path $ProjectRoot "ClassicLib-rs/Cargo.toml"
...
Assert-LastExitCode -CommandLabel "cargo clean --manifest-path ClassicLib-rs/Cargo.toml"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the workspace shell and root-scoped Cargo files to repo root</name>
  <files>Cargo.toml, Cargo.lock, .cargo/config.toml, ClassicLib-rs/Cargo.toml, ClassicLib-rs/Cargo.lock, ClassicLib-rs/.cargo/config.toml, tests/planning/test_phase06_validation.py</files>
  <action>Per D-01, D-02, D-04, D-05, D-06, D-09, D-10, and D-11, create `./Cargo.toml` as the only authoritative virtual workspace by copying the current workspace tables from `ClassicLib-rs/Cargo.toml`, prefixing every `members` entry with `ClassicLib-rs/`, preserving `[workspace.dependencies]`, `[workspace.lints]`, `[profile.release]`, and `[profile.release-with-debug]` exactly, and keeping `resolver = "2"`. Do not add `workspace.default-members`. Preserve helpful grouping/comments from the existing member list while promoting it to root. Move `ClassicLib-rs/Cargo.lock` to `./Cargo.lock` and `ClassicLib-rs/.cargo/config.toml` to `./.cargo/config.toml`. Delete the old `ClassicLib-rs/Cargo.toml`, `ClassicLib-rs/Cargo.lock`, and `ClassicLib-rs/.cargo/config.toml` copies so Phase 6 leaves no dual-root steady state. Fill in the Wave 0 planning audit file rather than recreating it: add `workspace_root_manifest` and `core_root_files` tests that assert repo-root manifest existence, old-manifest absence, preserved `resolver = "2"`, absence of `default-members`, existence of the moved root workspace shell files, and absence of the old `ClassicLib-rs` copies. Addresses review concern: preserve manifest readability while making the cutover explicit.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "workspace_root_manifest or core_root_files"</automated>
  </verify>
  <done>Repo root owns the workspace shell files, and the old live manifest plus old root-scoped lock/config copies no longer exist.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget `validate_stubs.py` and `rebuild_rust.ps1` to the new repo-root contract</name>
  <files>ClassicLib-rs/validate_stubs.py, validate_stubs.py, rebuild_rust.ps1, tests/planning/test_phase06_validation.py</files>
  <action>Per D-03, D-11, and D-12, move `ClassicLib-rs/validate_stubs.py` to `./validate_stubs.py` and make its post-move contract explicit: repo root is the default `--rust-dir`, but the script must still accept an explicit `ClassicLib-rs` path during the transition by normalizing either input to the live bindings location for Phase 6. Update the usage/help text and validation logic so the meaning of `--rust-dir` is unambiguous and repo-root default behavior still reaches `ClassicLib-rs/python-bindings`. Then inventory every `ClassicLib-rs/Cargo.toml` call site in `rebuild_rust.ps1` before editing, including clean/build/test/package-filtered flows, and replace only the old-manifest dependency with plain repo-root Cargo invocation; preserve legitimate `ClassicLib-rs/python-bindings` and crate-directory paths because the physical crate move is Phase 7+. Remove `ClassicLib-rs/validate_stubs.py` after the move so only the repo-root entrypoint remains. Extend `tests/planning/test_phase06_validation.py` with audits for the moved `validate_stubs.py`, old-script absence, explicit rust-dir normalization behavior, all enumerated `rebuild_rust.ps1` old-manifest call sites being removed, and root `.cargo/config.toml` alias preservation. Addresses review concern: document `validate_stubs.py` semantics clearly and do not miss hidden `rebuild_rust.ps1` manifest-path call sites.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "stub_validator or rebuild_script or cargo_aliases"</automated>
  </verify>
  <done>Root helper scripts default to the repo-root workspace, `validate_stubs.py` has an explicit Phase 6 rust-dir contract, and `rebuild_rust.ps1` no longer depends on `ClassicLib-rs/Cargo.toml`.</done>
</task>

</tasks>

<verification>
Repo-root workspace shell exists, old manifest is gone, helper scripts default to repo root, and the Phase 6 planning audit proves those invariants.
</verification>

<success_criteria>
- Repo-root `Cargo.toml` is the only live workspace manifest.
- Repo-root `Cargo.lock`, `.cargo/config.toml`, and `validate_stubs.py` exist and are no longer stranded under `ClassicLib-rs/`.
- `rebuild_rust.ps1` and the planning audit target repo-root Cargo behavior without ambiguous `--rust-dir` semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-root-workspace-cutover/06-01-SUMMARY.md`
</output>
