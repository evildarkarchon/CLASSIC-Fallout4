# 08-04 Summary

- Corrected `node-bindings/classic-node` package script depth to `../../tools/...` with `--repo-root ../..`.
- Rewired Node parity and d.ts freshness defaults to repo-root binding paths.
- Added Node regression tests for script depth and root-only defaults.
