---
phase: 08-wrapper-and-parity-rewire
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/cxx_api_parity/check_parity_gate.py
  - tools/cxx_api_parity/generate_baseline.py
  - tools/cxx_api_parity/tests/test_gate.py
  - tools/cxx_api_parity/tests/test_parser.py
  - docs/implementation/cxx_api_parity/baseline/parity_contract.json
  - docs/implementation/cxx_api_parity/baseline/rust_api_surface.json
  - docs/implementation/cxx_api_parity/baseline/cxx_diff_report.json
  - docs/implementation/cxx_api_parity/baseline/cxx_diff_report.md
  - docs/implementation/cxx_api_parity/baseline/cxx_gate_report.md
autonomous: true
requirements: [INTG-02]
must_haves:
  truths:
    - "The CXX parity gate scans the repo-root bridge tree instead of `ClassicLib-rs/cpp-bindings/...`."
    - "Checked-in CXX path-bearing baseline artifacts no longer record stale `ClassicLib-rs/...` source paths per D-11."
    - "The CXX parity contract stays semantically unchanged while path metadata is refreshed."
  artifacts:
    - path: "tools/cxx_api_parity/generate_baseline.py"
      provides: "Repo-root bridge discovery and baseline generation"
    - path: "tools/cxx_api_parity/check_parity_gate.py"
      provides: "Repo-root CXX parity gate defaults"
    - path: "docs/implementation/cxx_api_parity/baseline/rust_api_surface.json"
      provides: "Refreshed sourceFile metadata under repo-root bridge paths"
  key_links:
    - from: "tools/cxx_api_parity/generate_baseline.py"
      to: "cpp-bindings/classic-cpp-bridge"
      via: "default bridge crate path"
      pattern: "cpp-bindings/classic-cpp-bridge"
    - from: "docs/implementation/cxx_api_parity/baseline/rust_api_surface.json"
      to: "cpp-bindings/classic-cpp-bridge/src"
      via: "sourceFile metadata"
      pattern: "cpp-bindings/classic-cpp-bridge/src"
---

<objective>
Cut the CXX parity gate over to repo-root bridge paths and refresh stale checked-in baseline metadata.

Purpose: Close the CXX half of INTG-02, including the checked-in path-bearing artifacts that still encode the pre-move bridge location.
Output: Root-level CXX parity defaults, updated CXX tests, and refreshed baseline artifacts with unchanged API semantics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wrapper-and-parity-rewire/08-CONTEXT.md
@.planning/phases/08-wrapper-and-parity-rewire/08-RESEARCH.md
@tools/cxx_api_parity/check_parity_gate.py
@tools/cxx_api_parity/generate_baseline.py
@tools/cxx_api_parity/tests/test_gate.py
@tools/cxx_api_parity/tests/test_parser.py
@docs/implementation/cxx_api_parity/baseline/parity_contract.json
@docs/implementation/cxx_api_parity/baseline/rust_api_surface.json

<interfaces>
From `tools/cxx_api_parity/generate_baseline.py`:
```python
bridge_crate_rel: str = "ClassicLib-rs/cpp-bindings/classic-cpp-bridge"
```

From `tools/cxx_api_parity/check_parity_gate.py`:
```python
parser.add_argument("--output-dir", default="ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CXX parity tests for relocated bridge paths and synthetic fixtures</name>
  <files>tools/cxx_api_parity/tests/test_gate.py, tools/cxx_api_parity/tests/test_parser.py</files>
  <action>Update the CXX parity integration and parser tests so both live-repo and synthetic fixture trees derive the bridge location from repo-root `cpp-bindings/classic-cpp-bridge`, not `ClassicLib-rs/...`, per D-09 and D-12. Keep the current semantic assertions intact while adding explicit checks that synthetic test trees, parsed `sourceFile` values, and gate defaults all reject the old bridge root. Addresses the review concern that CXX synthetic test fixtures must be rewired explicitly, not assumed by the live-path fix.</action>
  <verify>
    <automated>python -m pytest tools/cxx_api_parity/tests/test_parser.py tools/cxx_api_parity/tests/test_gate.py -q</automated>
  </verify>
  <done>CXX parity tests fail if the gate or synthetic fixtures assume the old bridge path.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire CXX parity defaults and refresh checked-in baseline artifacts without semantic drift</name>
  <files>tools/cxx_api_parity/check_parity_gate.py, tools/cxx_api_parity/generate_baseline.py, docs/implementation/cxx_api_parity/baseline/parity_contract.json, docs/implementation/cxx_api_parity/baseline/rust_api_surface.json, docs/implementation/cxx_api_parity/baseline/cxx_diff_report.json, docs/implementation/cxx_api_parity/baseline/cxx_diff_report.md, docs/implementation/cxx_api_parity/baseline/cxx_gate_report.md</files>
  <action>Change the CXX parity gate and baseline generator defaults to repo-root `cpp-bindings/classic-cpp-bridge` paths per D-09 and D-10. Then regenerate the checked-in baseline artifacts so every stored `sourceFile` or path-bearing field loses the `ClassicLib-rs/` prefix per D-11 while preserving the same bridge module set, contract IDs, and semantic API rows. Treat semantic no-drift as a first-class invariant: the refresh is allowed to rewrite path metadata, generated timestamps, and reports, but not ownership or exposed surface shape. Addresses the review concern that this plan must not become self-validating.</action>
  <verify>
    <automated>python -m pytest tools/cxx_api_parity/tests/test_parser.py tools/cxx_api_parity/tests/test_gate.py -q</automated>
  </verify>
  <done>The CXX gate uses repo-root bridge defaults, checked-in baseline artifacts reflect repo-root source paths, and semantic parity rows remain unchanged.</done>
</task>

</tasks>

<verification>
During execution, run the real gate after the targeted pytest suite:
- `python tools/cxx_api_parity/check_parity_gate.py --repo-root .`
</verification>

<success_criteria>
- No CXX parity tool default or checked-in baseline artifact depends on `ClassicLib-rs/cpp-bindings/...`.
- The CXX baseline still covers the same bridge-module/API surface after the path refresh.
- Regression tests cover both live-repo and synthetic-repo gate paths under the new bridge location.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wrapper-and-parity-rewire/08-05-SUMMARY.md`
</output>
