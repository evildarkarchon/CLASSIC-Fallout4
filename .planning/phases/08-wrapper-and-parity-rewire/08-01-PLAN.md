---
phase: 08-wrapper-and-parity-rewire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rebuild_rust.ps1
  - rebuild_node.ps1
  - tests/powershell/rebuild_rust.general_target.test.ps1
  - tests/powershell/rebuild_node.wrapper_contract.test.ps1
autonomous: true
requirements: [INTG-01]
must_haves:
  truths:
    - "Contributor can run the repo-root rebuild wrappers without any live ClassicLib-rs path dependency."
    - "rebuild_node.ps1 stays available per D-03, but delegates to one canonical Node rebuild implementation."
    - "Wrapper failures teach the repo-root replacement path per D-06 and D-07."
  artifacts:
    - path: "rebuild_rust.ps1"
      provides: "Canonical repo-root rebuild orchestration for workspace, Python, and Node targets"
    - path: "rebuild_node.ps1"
      provides: "Thin alias wrapper over the canonical Node rebuild flow"
    - path: "tests/powershell/rebuild_node.wrapper_contract.test.ps1"
      provides: "Regression coverage for aliasing, normal alias flow, and legacy-path messaging"
  key_links:
    - from: "rebuild_node.ps1"
      to: "rebuild_rust.ps1"
      via: "delegated PowerShell invocation"
      pattern: "rebuild_rust\.ps1.*-Target node"
    - from: "rebuild_rust.ps1"
      to: "python-bindings and node-bindings/classic-node"
      via: "repo-root Join-Path logic"
      pattern: "python-bindings|node-bindings[\\/]classic-node"
---

<objective>
Keep the public rebuild wrappers working after relocation while making repo-root paths canonical.

Purpose: Satisfy the wrapper half of INTG-01 without preserving a hidden dual-layout contract.
Output: Rewired `rebuild_rust.ps1`/`rebuild_node.ps1` plus wrapper-focused regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wrapper-and-parity-rewire/08-CONTEXT.md
@.planning/phases/08-wrapper-and-parity-rewire/08-RESEARCH.md
@rebuild_rust.ps1
@rebuild_node.ps1
@tests/powershell/rebuild_rust.general_target.test.ps1

<interfaces>
From `rebuild_rust.ps1`:
```powershell
param (
    [string[]]$Crates,
    [ValidateSet("python", "workspace", "node")]
    [string]$Target = "workspace",
    [switch]$Clean,
    [switch]$BuildOnly,
    [switch]$DebugBuild
)
```

From `rebuild_node.ps1`:
```powershell
param(
    [switch]$Clean,
    [switch]$Debug
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the wrapper contract with PowerShell regression tests</name>
  <files>tests/powershell/rebuild_rust.general_target.test.ps1, tests/powershell/rebuild_node.wrapper_contract.test.ps1</files>
  <action>Extend the existing rebuild-script contract test and add a focused `rebuild_node` wrapper test so Phase 8 has executable coverage for D-01, D-03, D-05, D-06, and D-07. Add explicit assertions that `rebuild_rust.ps1` contains only repo-root `python-bindings/` and `node-bindings/classic-node/` internal defaults, that stale `ClassicLib-rs/...` body text/help text is rejected, and that `rebuild_node.ps1` exercises one canonical execution path for both normal and debug alias invocations. Addresses review concerns: stronger Python wrapper proof, normal alias proof, and stale help-text detection.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File tests/powershell/rebuild_rust.general_target.test.ps1; pwsh -ExecutionPolicy Bypass -File tests/powershell/rebuild_node.wrapper_contract.test.ps1</automated>
  </verify>
  <done>Wrapper contract tests fail if a live `ClassicLib-rs/...` default, stale migration hint, or second maintained Node implementation returns.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire rebuild wrappers to the repo-root contract</name>
  <files>rebuild_rust.ps1, rebuild_node.ps1</files>
  <action>Update `rebuild_rust.ps1` so its Python and Node path joins, internal messages, and failure guidance use repo-root `python-bindings/` and `node-bindings/classic-node/` locations only per D-01, D-02, D-05, and D-07. Replace the bespoke `rebuild_node.ps1` implementation with a thin alias that invokes the canonical Node flow through `rebuild_rust.ps1 -Target node`, preserving supported clean/debug behavior per D-03 while avoiding any exact-text overfitting in the alias contract. Do not add compatibility normalization that silently accepts old layout paths.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File tests/powershell/rebuild_rust.general_target.test.ps1; pwsh -ExecutionPolicy Bypass -File tests/powershell/rebuild_node.wrapper_contract.test.ps1</automated>
  </verify>
  <done>`rebuild_rust.ps1` and `rebuild_node.ps1` both point at root-level bindings paths, `rebuild_node.ps1` is only a delegating wrapper, and stale `ClassicLib-rs/...` guidance is gone.</done>
</task>

</tasks>

<verification>
After the task-level tests pass, run real wrapper smoke checks during execution with:
- `pwsh -ExecutionPolicy Bypass -File rebuild_rust.ps1 -Target python -BuildOnly`
- `pwsh -ExecutionPolicy Bypass -File rebuild_rust.ps1 -Target node -BuildOnly`
- `pwsh -ExecutionPolicy Bypass -File rebuild_node.ps1`
- `pwsh -ExecutionPolicy Bypass -File rebuild_node.ps1 -Debug`
</verification>

<success_criteria>
- `rebuild_rust.ps1` uses repo-root Python/Node binding paths only.
- `rebuild_node.ps1` remains supported but only delegates to the canonical Node rebuild flow.
- Wrapper-focused regression tests catch reintroduced live `ClassicLib-rs/...` paths, stale help text, or duplicate Node rebuild logic.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wrapper-and-parity-rewire/08-01-SUMMARY.md`
</output>
