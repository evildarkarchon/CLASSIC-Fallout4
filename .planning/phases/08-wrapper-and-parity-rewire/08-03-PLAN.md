---
phase: 08-wrapper-and-parity-rewire
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - validate_stubs.py
  - tools/python_api_parity/check_parity_gate.py
  - tools/python_api_parity/generate_baseline.py
  - tools/python_api_parity/tests/test_check_parity_gate.py
  - tools/python_api_parity/tests/test_generate_baseline_targets.py
autonomous: true
requirements: [INTG-02]
must_haves:
  truths:
    - "Python parity and stub validation default to repo-root binding paths per D-09."
    - "Legacy ClassicLib-rs Python-path inputs hard-fail with migration guidance per D-06 and D-12."
    - "Python parity tooling still targets the same contract semantics after the path cutover."
  artifacts:
    - path: "validate_stubs.py"
      provides: "Root-only stub validation entrypoint with explicit legacy rejection"
    - path: "tools/python_api_parity/check_parity_gate.py"
      provides: "Repo-root parity gate defaults for Python bindings"
    - path: "tools/python_api_parity/generate_baseline.py"
      provides: "Repo-root Python baseline target inventory"
  key_links:
    - from: "validate_stubs.py"
      to: "python-bindings"
      via: "--rust-dir normalization/remediation logic"
      pattern: "python-bindings"
    - from: "tools/python_api_parity/check_parity_gate.py"
      to: "python-bindings/parity-artifacts"
      via: "--output-dir default"
      pattern: "python-bindings/parity-artifacts"
---

<objective>
Cut Python stub-validation and parity tooling over to the relocated repo-root layout.

Purpose: Satisfy the Python half of INTG-02 without keeping legacy `ClassicLib-rs/...` path normalization alive.
Output: Root-level Python parity/stub defaults plus regression tests for legacy-path rejection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wrapper-and-parity-rewire/08-CONTEXT.md
@.planning/phases/08-wrapper-and-parity-rewire/08-RESEARCH.md
@validate_stubs.py
@tools/python_api_parity/check_parity_gate.py
@tools/python_api_parity/generate_baseline.py
@tools/python_api_parity/tests/test_check_parity_gate.py
@tools/python_api_parity/tests/test_generate_baseline_targets.py

<interfaces>
From `validate_stubs.py`:
```python
def normalize_rust_dir(rust_dir: Path) -> Path:
    if resolved == LEGACY_WORKSPACE_DIR or resolved.name == "ClassicLib-rs":
        candidates.append(resolved.parent)
```

From `tools/python_api_parity/check_parity_gate.py`:
```python
parser.add_argument("--output-dir", default="ClassicLib-rs/python-bindings/parity-artifacts")
parser.add_argument("--runtime-registry", default="ClassicLib-rs/python-bindings/tests/fixtures/runtime_coverage_registry.json")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Python parity/stub tripwires for root-only defaults</name>
  <files>tools/python_api_parity/tests/test_check_parity_gate.py, tools/python_api_parity/tests/test_generate_baseline_targets.py</files>
  <action>Add assertions that Python parity tooling targets repo-root `python-bindings/...` paths and fails on live `ClassicLib-rs/...` defaults per D-09 and D-12. Extend the tests so they verify output-dir/runtime-registry defaults, baseline target inventory, and legacy rejection/remediation messaging instead of allowing Phase 6/7 transitional normalization to linger.</action>
  <verify>
    <automated>python -m pytest tools/python_api_parity/tests/test_check_parity_gate.py tools/python_api_parity/tests/test_generate_baseline_targets.py -q</automated>
  </verify>
  <done>Python parity tests fail if any default still points at `ClassicLib-rs/...` or if legacy-path rejection is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire Python parity and stub tooling to repo-root paths</name>
  <files>validate_stubs.py, tools/python_api_parity/check_parity_gate.py, tools/python_api_parity/generate_baseline.py</files>
  <action>Replace the remaining transitional `ClassicLib-rs/...` defaults with repo-root `python-bindings/...` targets per D-09 and D-10. In `validate_stubs.py`, stop silently normalizing legacy `ClassicLib-rs` inputs; fail fast and print the repo-root replacement per D-06 and D-07. Update Python parity gate and baseline-generator defaults so runtime registry and artifact directories live under repo-root `python-bindings/`, with no hidden compatibility fallback.</action>
  <verify>
    <automated>python -m pytest tools/python_api_parity/tests/test_check_parity_gate.py tools/python_api_parity/tests/test_generate_baseline_targets.py -q</automated>
  </verify>
  <done>Python stub/parity tools use root-level defaults only, and legacy-path attempts fail with explicit repo-root guidance.</done>
</task>

</tasks>

<verification>
During execution, confirm the real toolchain still runs with:
- `python validate_stubs.py --rust-dir . --parity-contract docs/implementation/python_api_parity/baseline/parity_contract.json --json-out python-bindings/parity-artifacts/stub_validation_report.json --fail-on-warnings`
- `python tools/python_api_parity/check_parity_gate.py --repo-root .`
</verification>

<success_criteria>
- `validate_stubs.py` no longer treats `ClassicLib-rs` as a supported live workspace root.
- Python parity defaults and tests point at repo-root `python-bindings/` paths only.
- Legacy-path rejection is automated and regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wrapper-and-parity-rewire/08-03-SUMMARY.md`
</output>
