---
phase: 08-wrapper-and-parity-rewire
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - node-bindings/classic-node/package.json
  - tools/node_api_parity/check_parity_gate.py
  - tools/node_api_parity/check_dts_freshness.py
  - tools/node_api_parity/generate_baseline.py
  - tools/node_api_parity/tests/test_check_parity_gate.py
  - tools/node_api_parity/tests/test_generate_baseline_targets.py
autonomous: true
requirements: [INTG-02]
must_haves:
  truths:
    - "Package-local Node scripts still work from `node-bindings/classic-node` after relocation per D-01 and D-02."
    - "Node parity and d.ts freshness tooling default to repo-root binding paths per D-09."
    - "Node parity/freshness workflows do not silently accept legacy `ClassicLib-rs/...` path assumptions per D-12."
  artifacts:
    - path: "node-bindings/classic-node/package.json"
      provides: "Corrected package-local script depth and repo-root arguments"
    - path: "tools/node_api_parity/check_parity_gate.py"
      provides: "Repo-root Node parity defaults"
    - path: "tools/node_api_parity/check_dts_freshness.py"
      provides: "Repo-root Node d.ts freshness defaults"
  key_links:
    - from: "node-bindings/classic-node/package.json"
      to: "tools/node_api_parity/check_parity_gate.py"
      via: "package-local parity scripts"
      pattern: "\.\./\.\./tools/node_api_parity/check_parity_gate\.py"
    - from: "tools/node_api_parity/generate_baseline.py"
      to: "RUST_TARGET_CRATES"
      via: "hardcoded target inventory paths"
      pattern: "RUST_TARGET_CRATES"
---

<objective>
Rewire Node package scripts and parity/freshness tooling to the relocated repo-root layout.

Purpose: Satisfy the Node half of INTG-02 while keeping the package-local workflow alive as a secondary entrypoint per D-02.
Output: Corrected `classic-node` package scripts, root-level Node parity defaults, and Node-specific regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wrapper-and-parity-rewire/08-CONTEXT.md
@.planning/phases/08-wrapper-and-parity-rewire/08-RESEARCH.md
@node-bindings/classic-node/package.json
@tools/node_api_parity/check_parity_gate.py
@tools/node_api_parity/check_dts_freshness.py
@tools/node_api_parity/generate_baseline.py
@tools/node_api_parity/tests/test_check_parity_gate.py
@tools/node_api_parity/tests/test_generate_baseline_targets.py

<interfaces>
From `node-bindings/classic-node/package.json`:
```json
{
  "scripts": {
    "build": "napi build --release --platform --manifest-path ./Cargo.toml && bun run build:cli",
    "parity:gate": "python ../../../tools/node_api_parity/check_parity_gate.py --repo-root ../../..",
    "dts:freshness:check": "python ../../../tools/node_api_parity/check_dts_freshness.py --repo-root ../../.. --check-only",
    "parity:gate:local:vsdev": "pwsh -ExecutionPolicy Bypass -File ../../../tools/enter_vs_dev_shell.ps1 -WorkingDirectory . -Command \"bun run parity:gate:local\""
  }
}
```

From `tools/node_api_parity/check_dts_freshness.py`:
```python
parser.add_argument("--package-dir", default="ClassicLib-rs/node-bindings/classic-node")
parser.add_argument("--output-dir", default="ClassicLib-rs/node-bindings/classic-node/parity-artifacts")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Node tool-depth and default-path regression coverage</name>
  <files>tools/node_api_parity/tests/test_check_parity_gate.py, tools/node_api_parity/tests/test_generate_baseline_targets.py</files>
  <action>Extend the existing Node parity tests so they assert the relocated repo-root target inventory, corrected package-local script depth, and root-level default output/index/package paths per D-09 and D-10. Make the test text explicit about the hardcoded generator inventory (`RUST_TARGET_CRATES`) and package scripts that commonly drift, including `parity:gate`, `dts:freshness:check`, `parity:gate:local:vsdev`, and `shell:vsdev`. Addresses the agreed review concern that 08-04 must name internal inventories and stale package-script depth, not only CLI defaults.</action>
  <verify>
    <automated>python -m pytest tools/node_api_parity/tests/test_check_parity_gate.py tools/node_api_parity/tests/test_generate_baseline_targets.py -q</automated>
  </verify>
  <done>Node tooling tests catch stale package depth, stale `ClassicLib-rs/...` defaults, and stale VS-dev helper paths automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire package-local Node scripts and parity/freshness defaults</name>
  <files>node-bindings/classic-node/package.json, tools/node_api_parity/check_parity_gate.py, tools/node_api_parity/check_dts_freshness.py, tools/node_api_parity/generate_baseline.py</files>
  <action>Correct `node-bindings/classic-node/package.json` so package-local scripts call `../../tools/...` with `--repo-root ../..` from the new package depth, and ensure the standard `bun run build` path still works per D-01 and D-02. Update Node parity and d.ts freshness defaults to repo-root `node-bindings/classic-node/...` paths per D-09 and D-10, including package-dir, output-dir, runtime-registry, and any hardcoded generator inventories such as `RUST_TARGET_CRATES`. Do not keep any silent compatibility shim for `ClassicLib-rs/node-bindings/...`; stale defaults should fail fast as regressions per D-12.</action>
  <verify>
    <automated>python -m pytest tools/node_api_parity/tests/test_check_parity_gate.py tools/node_api_parity/tests/test_generate_baseline_targets.py -q</automated>
  </verify>
  <done>Package-local Node commands use the right post-move depth, `bun run build` remains valid from `node-bindings/classic-node`, and all Node parity/freshness defaults point at repo-root bindings paths only.</done>
</task>

</tasks>

<verification>
During execution, finish with the real package-local commands from `node-bindings/classic-node`:
- `bun run build`
- `bun run dts:freshness:check`
- `bun run parity:gate`
</verification>

<success_criteria>
- `classic-node` package scripts use the corrected `../../tools/...` depth.
- `bun run build` still works from `node-bindings/classic-node`.
- Node parity and d.ts freshness defaults reference repo-root `node-bindings/classic-node` paths only.
- Node tool tests fail immediately if legacy defaults or stale script depth return.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wrapper-and-parity-rewire/08-04-SUMMARY.md`
</output>
