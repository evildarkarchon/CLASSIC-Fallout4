---
phase: 04-gate-validation-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - .planning/phases/04-gate-validation-documentation/04-VERIFICATION.md
autonomous: true
requirements:
  - GATE-01
  - GATE-02
  - GATE-03
  - GATE-04
  - GATE-05
  - GATE-06
must_haves:
  truths:
    - `cargo test --workspace` passes after the three consolidation phases.
    - CLI and GUI wrapper validations pass after the cheap audits are already green.
    - Phase 4 ends with one explicit verification artifact that records doc audit evidence, gate evidence, and final suite results.
  artifacts:
    - path: .planning/phases/04-gate-validation-documentation/04-VERIFICATION.md
      provides: Milestone-closure checklist and command/result evidence.
  key_links:
    - from: .planning/phases/04-gate-validation-documentation/04-VERIFICATION.md
      to: classic-cli/build_cli.ps1
      via: recorded CLI wrapper result
      pattern: build_cli.ps1 -Test
    - from: .planning/phases/04-gate-validation-documentation/04-VERIFICATION.md
      to: classic-gui/build_gui.ps1
      via: recorded GUI wrapper result
      pattern: build_gui.ps1 -Test
---

<objective>
Produce final closure proof after docs and parity are already green.

Purpose: Finish GATE-01 and capture the Phase 4 success evidence required by D-05 through D-07.
Output: One checklist-style `04-VERIFICATION.md` file proving docs, parity, workspace Rust tests, and native wrapper validation all ended green.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-gate-validation-documentation/04-CONTEXT.md
@.planning/phases/04-gate-validation-documentation/04-RESEARCH.md
@.planning/phases/04-gate-validation-documentation/04-VALIDATION.md
@.planning/phases/03-constants-version-registry-merge/03-VALIDATION.md
@ClassicLib-rs/Cargo.toml
@classic-cli/build_cli.ps1
@classic-gui/build_gui.ps1

<interfaces>
Canonical heavy validation commands from repo policy:

```powershell
cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml
pwsh -ExecutionPolicy Bypass -File classic-cli/build_cli.ps1 -Test
pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test
```

Verification artifact requirements from D-05 through D-07:

```text
- one checklist-style Phase 4 verification file
- includes docs audit evidence
- includes final plain parity reruns
- includes workspace/native validation results
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full post-cleanup closure suite in fast-fail order</name>
  <files>.planning/phases/04-gate-validation-documentation/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-gate-validation-documentation/04-CONTEXT.md
    - .planning/phases/04-gate-validation-documentation/04-RESEARCH.md
    - .planning/phases/04-gate-validation-documentation/04-VALIDATION.md
    - classic-cli/build_cli.ps1
    - classic-gui/build_gui.ps1
  </read_first>
  <acceptance_criteria>
    - `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0 per D-15.
    - `pwsh -ExecutionPolicy Bypass -File classic-cli/build_cli.ps1 -Test` exits 0.
    - `pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test` exits 0, or if the environment blocks it, execution stops and records the blocker instead of falsely closing the phase.
    - Final plain parity reruns still exit 0 after native validation, satisfying D-04.
  </acceptance_criteria>
  <action>After plans 04-01 and 04-02 are green, run the heavy closure suite in the locked order from D-13 through D-15. First run `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml`. Then run the native wrapper validations exactly through `pwsh -ExecutionPolicy Bypass -File classic-cli/build_cli.ps1 -Test` and `pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test`; do not call raw `ctest` or test binaries. After the native wrappers pass, rerun the plain CXX/Python/Node parity gates one more time so the final recorded state matches D-04. If the GUI wrapper fails because the machine lacks required environment setup, stop and document it as an environment blocker rather than pretending GATE-01 passed.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -ExecutionPolicy Bypass -File classic-cli/build_cli.ps1 -Test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python tools/cxx_api_parity/check_parity_gate.py --repo-root .; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; python tools/python_api_parity/check_parity_gate.py --repo-root .; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Set-Location 'ClassicLib-rs/node-bindings/classic-node'; bun run parity:gate"</automated>
  </verify>
  <done>The full milestone suite is green in fast-fail order, and the final state still has plain zero-drift parity across all three bindings.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 4 milestone-closure checklist artifact</name>
  <files>.planning/phases/04-gate-validation-documentation/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-gate-validation-documentation/04-CONTEXT.md
    - .planning/phases/04-gate-validation-documentation/04-RESEARCH.md
    - .planning/phases/04-gate-validation-documentation/04-VALIDATION.md
    - .planning/phases/03-constants-version-registry-merge/03-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/04-gate-validation-documentation/04-VERIFICATION.md` exists.
    - The file contains checklist evidence for docs audit, CXX/Python/Node plain gate results, `cargo test --workspace`, CLI wrapper, and GUI wrapper.
    - The file explicitly states that current green one-tier Python/Node parity semantics are the operational successor to legacy `deferred_total == 0` wording.
  </acceptance_criteria>
  <action>Create `.planning/phases/04-gate-validation-documentation/04-VERIFICATION.md` as the dedicated closure artifact required by D-05, D-06, and D-07. Use a checklist structure with one section for active-doc audit evidence, one section for parity-gate evidence, one section for workspace/native validation, and a final closure verdict. Record the exact commands and exit outcomes from Task 1. Include an explicit note that GATE-03's historical `deferred_total == 0` wording is now evidenced by current one-tier parity semantics: plain gate exit 0, zero coverage gaps, zero registry mismatch, and no stale tracked artifacts. Keep the artifact concise and auditable; do not replace it with pasted terminal transcripts.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$path = '.planning/phases/04-gate-validation-documentation/04-VERIFICATION.md'; if (-not (Test-Path $path)) { exit 1 }; $patterns = @('docs audit','CXX','Python','Node','cargo test --workspace','build_cli.ps1 -Test','build_gui.ps1 -Test','deferred_total == 0'); foreach ($pattern in $patterns) { if (-not (Select-String -Path $path -Pattern $pattern -SimpleMatch -Quiet)) { exit 1 } }"</automated>
  </verify>
  <done>`04-VERIFICATION.md` gives a single, auditable closure checklist proving all Phase 4 requirements and recording the current one-tier parity interpretation.</done>
</task>

</tasks>

<verification>
- Heavy validation stays blocked until the cheaper docs/parity cleanup plans are complete.
- The final closure artifact records the exact evidence required to close the milestone.
</verification>

<success_criteria>
- GATE-01 is met via green workspace and native wrapper validation.
- GATE-02 through GATE-06 are re-confirmed in the final verification file.
- Phase 4 ends with a single closure artifact rather than scattered command output.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gate-validation-documentation/04-03-SUMMARY.md`
</output>
