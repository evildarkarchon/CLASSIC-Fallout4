---
phase: 04-gate-validation-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - docs/api/README.md
  - docs/api/binding-parity-overview.md
  - docs/api/binding-contract-refresh-note.md
  - docs/api/QUICK_START.md
  - docs/api/classic-config-core.md
autonomous: true
requirements:
  - GATE-05
  - GATE-06
must_haves:
  truths:
    - Active contributor docs describe the surviving 16-crate topology consistently.
    - Phase 4 docs describe verify-first parity auditing and do not present `parity:gate:local` as the canonical closure command.
    - Historical crate names remain only as short, clearly marked migration context.
  artifacts:
    - path: CLAUDE.md
      provides: Updated contributor stack + closure guidance for the 16-crate workspace.
    - path: .planning/PROJECT.md
      provides: Milestone state and active constraints aligned with one-tier parity wording.
    - path: docs/api/README.md
      provides: Current API-doc index that points to surviving crate owners only.
    - path: docs/api/binding-contract-refresh-note.md
      provides: Canonical Node/Python refresh workflow wording for Phase 4.
  key_links:
    - from: CLAUDE.md
      to: .planning/codebase/STACK.md
      via: GSD stack sync block
      pattern: 16 (pure Rust|business-logic) crates
    - from: docs/api/QUICK_START.md
      to: ClassicLib-rs/node-bindings/classic-node/package.json
      via: documented Node audit commands
      pattern: parity:gate:update-baseline
---

<objective>
Make active contributor docs truthful for milestone closure.

Purpose: Close GATE-05 and GATE-06 before expensive validation by removing stale topology/gate wording per D-09 through D-12.
Output: Updated active docs that describe the 16-crate workspace, one-tier parity semantics, and the Phase 4 verify-first Node audit flow.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-gate-validation-documentation/04-CONTEXT.md
@.planning/phases/04-gate-validation-documentation/04-RESEARCH.md
@.planning/phases/01-yaml-settings-merge/01-03-SUMMARY.md
@.planning/phases/02-crashgen-config-merge/02-02-SUMMARY.md
@.planning/phases/03-constants-version-registry-merge/03-04-SUMMARY.md
@CLAUDE.md
@docs/api/README.md
@docs/api/binding-parity-overview.md
@ClassicLib-rs/node-bindings/classic-node/package.json

<interfaces>
From `ClassicLib-rs/node-bindings/classic-node/package.json`:

```json
{
  "parity:gate": "python ../../../tools/node_api_parity/check_parity_gate.py --repo-root ../../..",
  "parity:gate:update-baseline": "python ../../../tools/node_api_parity/check_parity_gate.py --repo-root ../../.. --update-baseline",
  "parity:gate:local": "bun run dts:freshness:local && bun run parity:gate:update-baseline"
}
```

Use the first two as the canonical audit/refresh sequence per D-02. Do not document `parity:gate:local` as the milestone-closure command.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align topology docs with the 16-crate closure state</name>
  <files>CLAUDE.md, .planning/PROJECT.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md</files>
  <read_first>
    - CLAUDE.md
    - .planning/PROJECT.md
    - .planning/codebase/ARCHITECTURE.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/STACK.md
    - .planning/phases/04-gate-validation-documentation/04-CONTEXT.md
    - .planning/phases/04-gate-validation-documentation/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `CLAUDE.md`, `.planning/PROJECT.md`, `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/STRUCTURE.md`, and `.planning/codebase/STACK.md` agree on the post-Phase-3 16-crate topology.
    - Touched topology docs do not use `deferred_total` as an active Phase 4 success metric; if the historical term remains, it is explicitly framed as past milestone history per D-10/D-12.
    - `CLAUDE.md` states the current topology explicitly per D-11 and keeps only a short Phase 1-3 history summary.
  </acceptance_criteria>
  <action>Update the active topology docs per D-09, D-11, and D-12. In `CLAUDE.md`, rewrite the Node parity guidance so the canonical Phase 4 audit path is `bun run parity:gate`, with `bun run parity:gate:update-baseline` documented only as the intentional-refresh step; do not present `parity:gate:local` as the closure command. In `CLAUDE.md`, `.planning/PROJECT.md`, and the `.planning/codebase/*.md` files, describe the workspace as the surviving 16 business-logic/pure Rust crates after the yaml/settings merge, crashgen/config merge, and constants redistribution. Replace active one-tier parity claims that still talk about `deferred_total == 0` with present-tense zero-drift wording, while preserving brief historical notes only when they are explicitly labeled as v9.1.0-bindings history.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$files = @('CLAUDE.md','.planning/PROJECT.md','.planning/codebase/ARCHITECTURE.md','.planning/codebase/STRUCTURE.md','.planning/codebase/STACK.md'); if ((Select-String -Path $files -Pattern 'parity:gate:local|deferred_total' -SimpleMatch -ErrorAction SilentlyContinue)) { exit 1 }; $crateHits = Select-String -Path @('CLAUDE.md','.planning/PROJECT.md','.planning/codebase/ARCHITECTURE.md') -Pattern '16 pure Rust crates|16 business-logic'; if ($crateHits.Count -lt 3) { exit 1 }"</automated>
  </verify>
  <done>All five active topology docs describe the current 16-crate state, and none of them instruct contributors to use `parity:gate:local` or `deferred_total` as the live closure metric.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep active API docs for present-day parity and owner wording</name>
  <files>docs/api/README.md, docs/api/binding-parity-overview.md, docs/api/binding-contract-refresh-note.md, docs/api/QUICK_START.md, docs/api/classic-config-core.md</files>
  <read_first>
    - docs/api/README.md
    - docs/api/binding-parity-overview.md
    - docs/api/binding-contract-refresh-note.md
    - docs/api/QUICK_START.md
    - docs/api/classic-config-core.md
    - ClassicLib-rs/node-bindings/classic-node/package.json
    - .planning/phases/04-gate-validation-documentation/04-CONTEXT.md
    - .planning/phases/04-gate-validation-documentation/04-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `docs/api/binding-contract-refresh-note.md` and `docs/api/QUICK_START.md` both document `bun run parity:gate` and `bun run parity:gate:update-baseline`.
    - The touched API docs do not present `parity:gate:local` as the canonical milestone-audit command.
    - `docs/api/classic-config-core.md` keeps crashgen merge context brief and historical rather than treating `classic-crashgen-settings-core` as a live owner.
  </acceptance_criteria>
  <action>Apply the Phase 4 doc-audit policy to the active API guides per D-09 through D-12. Update `docs/api/binding-contract-refresh-note.md` and `docs/api/QUICK_START.md` so the canonical Node audit flow is verify first with `bun run parity:gate`, then refresh only if needed with `bun run parity:gate:update-baseline`, then rerun the plain gate. Keep `parity:gate:local` only if you explicitly label it as a local convenience helper, not as closure evidence. In `docs/api/README.md`, `docs/api/binding-parity-overview.md`, and `docs/api/classic-config-core.md`, keep absorbed-crate notes short, clearly historical, and focused on the surviving owners. Do not recreate retired crate pages or reintroduce retired-owner language as present-day truth.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$files = @('docs/api/README.md','docs/api/binding-parity-overview.md','docs/api/binding-contract-refresh-note.md','docs/api/QUICK_START.md','docs/api/classic-config-core.md'); $mustHave = Select-String -Path @('docs/api/binding-contract-refresh-note.md','docs/api/QUICK_START.md') -Pattern 'parity:gate|parity:gate:update-baseline'; if ($mustHave.Count -lt 4) { exit 1 }; if ((Select-String -Path @('docs/api/binding-contract-refresh-note.md','docs/api/QUICK_START.md') -Pattern 'parity:gate:local' -SimpleMatch -ErrorAction SilentlyContinue)) { exit 1 }"</automated>
  </verify>
  <done>The active API guides describe the verify-first Node parity workflow, keep retired crate mentions historical-only, and point contributors at surviving owners/docs.</done>
</task>

</tasks>

<verification>
- `Select-String` over touched active docs confirms no live `parity:gate:local` or `deferred_total` guidance remains.
- The active topology docs all expose the same 16-crate wording.
</verification>

<success_criteria>
- GATE-05 doc targets are updated without touching archived milestone/history artifacts.
- GATE-06 stack/topology wording in `CLAUDE.md` matches the active planning docs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gate-validation-documentation/04-01-SUMMARY.md`
</output>
