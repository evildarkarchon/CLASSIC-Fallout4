---
phase: 01-yaml-settings-merge
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs
  - ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs
  - ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs
  - ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
  - docs/api/classic-yaml-core.md
  - docs/api/classic-settings-core.md
  - docs/api/README.md
  - docs/api/binding-parity-overview.md
  - docs/api/classic-config-core.md
  - docs/api/classic-version-registry-core.md
  - docs/api/classic-constants-core.md
  - docs/api/classic-cpp-bridge-data-entrypoints.md
  - docs/api/classic-shared-core.md
  - CLAUDE.md
  - AGENTS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements: [YAML-01, YAML-04]

must_haves:
  truths:
    - "Migrated integration tests and benchmarks compile and run from settings-core"
    - "API documentation reflects the merged crate topology"
    - "All three parity gates pass (CXX, Python, Node)"
    - "No stale references to classic-yaml-core in active docs"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs"
      provides: "Migrated yaml-core integration tests"
      contains: "use classic_settings_core"
    - path: "ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs"
      provides: "Migrated dead code audit test"
      contains: "include_str!"
    - path: "ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs"
      provides: "Migrated yaml benchmarks"
      contains: "use classic_settings_core"
    - path: "docs/api/classic-settings-core.md"
      provides: "Consolidated API doc with yaml ops section"
      contains: "YamlOperations"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml"
      to: "benches/yaml_benchmarks.rs"
      via: "[[bench]] entry"
      pattern: "name = \"yaml_benchmarks\""
    - from: "docs/api/README.md"
      to: "docs/api/classic-settings-core.md"
      via: "index entry"
      pattern: "classic-settings-core"
---

<objective>
Migrate tests/benchmarks, consolidate documentation, and regenerate parity gate baselines.

Purpose: The Rust core and binding layers are merged (Plans 01 + 02), but the original yaml-core test files, benchmarks, and documentation still need migration. This plan moves tests/benchmarks into settings-core, merges API docs, updates cross-references across active docs, and regenerates all three parity gate baselines to confirm the merge is clean.

Output: All tests pass from settings-core, docs reflect 16-crate topology, all parity gates green.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-yaml-settings-merge/01-CONTEXT.md
@.planning/phases/01-yaml-settings-merge/01-RESEARCH.md
@.planning/phases/01-yaml-settings-merge/01-01-SUMMARY.md
@.planning/phases/01-yaml-settings-merge/01-02-SUMMARY.md

@ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
@docs/api/README.md
@docs/api/classic-settings-core.md
@docs/api/classic-yaml-core.md

<interfaces>
<!-- Relevant paths and conventions -->

Test files to migrate:
- ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs -> classic-settings-core/tests/yaml_integration_tests.rs
- ClassicLib-rs/business-logic/classic-yaml-core/tests/phase2_yaml_dead_code_audit.rs -> classic-settings-core/tests/yaml_dead_code_audit.rs
- ClassicLib-rs/business-logic/classic-yaml-core/benches/yaml_benchmarks.rs -> classic-settings-core/benches/yaml_benchmarks.rs

NOTE: If Plan 01 already deleted the classic-yaml-core directory, these files may not exist. In that case, they need to be recreated from git history or reconstructed from the research context. Check first.

Import changes needed in migrated tests:
- `use classic_yaml_core::*` -> `use classic_settings_core::*`
- `cache_stats()` -> `yaml_cache_stats()` per D-03
- `reset_cache_stats()` -> `reset_yaml_cache_stats()` per D-03
- `CacheStats` -> `YamlCacheStats` per D-03

Dead code audit test path fixes:
- `include_str!("../src/lib.rs")` -> `include_str!("../src/yaml_ops.rs")`
- benchmark path reference -> `include_str!("../benches/yaml_benchmarks.rs")`

Benchmark import changes:
- `use classic_yaml_core::YamlOperations` -> `use classic_settings_core::YamlOperations`
- `#[path = "../../../benches/common/mod.rs"]` -- verify this resolves correctly from new location

Parity gate commands:
- CXX: `python tools/cxx_api_parity/generate_baseline.py && python tools/cxx_api_parity/check_parity_gate.py`
- Python: `python tools/python_api_parity/check_parity_gate.py --repo-root .`
- Node: `cd ClassicLib-rs/node-bindings/classic-node && bun run parity:gate:local`

Active docs requiring cross-reference updates (D-14):
- CLAUDE.md, AGENTS.md
- docs/api/*.md (active pages only)
- .planning/codebase/ARCHITECTURE.md, STACK.md, STRUCTURE.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate integration tests and benchmarks into settings-core (D-10)</name>
  <files>
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs,
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs,
    ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs,
    ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
  </files>
  <read_first>
    ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
  </read_first>
  <action>
    NOTE: Plan 01 Task 1 created the tests/ and benches/ directories. Plan 01 Task 2 deleted the entire classic-yaml-core directory. The test/bench files were NOT moved via git mv in Plan 01. They need to be restored from git and placed in settings-core.

    **A. Restore and migrate test files from git history:**

    1. Extract the original test files from the last commit before deletion:
       ```
       git show HEAD~1:ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs > ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs
       git show HEAD~1:ClassicLib-rs/business-logic/classic-yaml-core/tests/phase2_yaml_dead_code_audit.rs > ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs
       git show HEAD~1:ClassicLib-rs/business-logic/classic-yaml-core/benches/yaml_benchmarks.rs > ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs
       ```
       If the above paths don't work, use `git log --all --oneline -- ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs` to find the right commit hash, then `git show <hash>:path`.

    2. Edit `yaml_integration_tests.rs`:
       - Change all `use classic_yaml_core::` to `use classic_settings_core::`
       - Change `cache_stats()` calls to `yaml_cache_stats()`
       - Change `reset_cache_stats()` calls to `reset_yaml_cache_stats()`
       - Change `CacheStats` type references to `YamlCacheStats`
       - Change `clear_global_yaml_cache()` -- this name stays the same, no change needed

    3. Edit `yaml_dead_code_audit.rs`:
       - Change `include_str!("../src/lib.rs")` to `include_str!("../src/yaml_ops.rs")`
       - If there is a reference to the benchmark file path, update it to `include_str!("../benches/yaml_benchmarks.rs")`
       - Change any `classic_yaml_core` references to `classic_settings_core`

    4. Edit `yaml_benchmarks.rs`:
       - Change `use classic_yaml_core::YamlOperations` to `use classic_settings_core::YamlOperations`
       - Verify the `#[path = "../../../benches/common/mod.rs"]` attribute still resolves correctly. From `ClassicLib-rs/business-logic/classic-settings-core/benches/`, three levels up is `ClassicLib-rs/`, so `ClassicLib-rs/benches/common/mod.rs` -- verify this path exists.

    **B. Update settings-core Cargo.toml:**

    The `criterion = { version = "0.6.0", features = ["html_reports"] }` dev-dependency was already added in Plan 01.

    Add the `[[bench]]` section to `Cargo.toml`:
    ```toml
    [[bench]]
    name = "yaml_benchmarks"
    harness = false
    ```

    **C. Commit:**
    Commit with message: `Refactor: Migrate yaml-core tests and benchmarks into settings-core`
  </action>
  <verify>
    <automated>cd J:/CLASSIC-Fallout4 && source tools/use_msvc_from_git_bash.sh && cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs` exists
    - `ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs` exists
    - `ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs` exists
    - `yaml_integration_tests.rs` contains `use classic_settings_core` (not `classic_yaml_core`)
    - `yaml_integration_tests.rs` contains `yaml_cache_stats()` (not bare `cache_stats()`)
    - `yaml_dead_code_audit.rs` contains `include_str!("../src/yaml_ops.rs")` (not `../src/lib.rs`)
    - `yaml_benchmarks.rs` contains `use classic_settings_core::YamlOperations`
    - `ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml` contains `[[bench]]` with `name = "yaml_benchmarks"`
    - `cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml` exits 0
  </acceptance_criteria>
  <done>All yaml-core integration tests, dead code audit tests, and benchmarks are migrated into settings-core with updated imports and paths. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Documentation consolidation and cross-reference updates (D-13, D-14)</name>
  <files>
    docs/api/classic-yaml-core.md,
    docs/api/classic-settings-core.md,
    docs/api/README.md,
    docs/api/binding-parity-overview.md,
    docs/api/classic-config-core.md,
    docs/api/classic-version-registry-core.md,
    docs/api/classic-constants-core.md,
    docs/api/classic-cpp-bridge-data-entrypoints.md,
    docs/api/classic-shared-core.md,
    CLAUDE.md,
    AGENTS.md,
    .planning/codebase/ARCHITECTURE.md,
    .planning/codebase/STACK.md,
    .planning/codebase/STRUCTURE.md
  </files>
  <read_first>
    docs/api/classic-yaml-core.md,
    docs/api/classic-settings-core.md,
    docs/api/README.md,
    docs/api/binding-parity-overview.md,
    docs/api/classic-config-core.md,
    docs/api/classic-version-registry-core.md,
    docs/api/classic-cpp-bridge-data-entrypoints.md,
    CLAUDE.md,
    AGENTS.md
  </read_first>
  <action>
    **A. Merge classic-yaml-core.md into classic-settings-core.md (D-13):**

    1. Read `docs/api/classic-yaml-core.md` fully.
    2. Read `docs/api/classic-settings-core.md` fully.
    3. Add a new section in `classic-settings-core.md` titled "## YAML Operations" (or similar). Copy the relevant content from `classic-yaml-core.md`:
       - API surface descriptions (YamlOperations, YamlError, YamlCacheStats, merge_keys)
       - Usage examples
       - Caching behavior documentation
       - Thread safety notes
    4. Update type names per D-03: `CacheStats` -> `YamlCacheStats`, `cache_stats()` -> `yaml_cache_stats()`, `reset_cache_stats()` -> `reset_yaml_cache_stats()`
    5. Update import paths in examples: `use classic_yaml_core::` -> `use classic_settings_core::`
    6. Delete `docs/api/classic-yaml-core.md`.

    **B. Update docs/api/README.md:**

    1. Remove the entry for `classic-yaml-core.md` (currently item 6 in the list).
    2. Update the `classic-settings-core.md` entry description to mention it now includes YAML operations.
    3. Renumber subsequent entries if needed.
    4. Update the scope notes section at the bottom if it references yaml-core.

    **C. Update cross-references in active docs (D-14):**

    For each file, search for `classic-yaml-core`, `classic_yaml_core`, `yaml-core`, and update:

    - `docs/api/binding-parity-overview.md`: Update any crate references from yaml-core to settings-core.
    - `docs/api/classic-config-core.md`: Update references that mention yaml-core as a dependency or import source.
    - `docs/api/classic-version-registry-core.md`: Same as above.
    - `docs/api/classic-constants-core.md`: Update if it references yaml-core.
    - `docs/api/classic-cpp-bridge-data-entrypoints.md`: Update namespace references from `classic::yaml` to `classic::settings`.
    - `docs/api/classic-shared-core.md`: Update if it references yaml-core.
    - `CLAUDE.md`: Update the Technology Stack section to reflect the crate merge. Reduce the business-logic crate count from 19 to 18 (yaml-core absorbed). Update the Architecture section's layer descriptions. Update the Conventions section if it references yaml-core patterns.
    - `AGENTS.md`: Check for yaml-core references and update.
    - `.planning/codebase/ARCHITECTURE.md`: Update crate inventory and dependency graph.
    - `.planning/codebase/STACK.md`: Update crate list.
    - `.planning/codebase/STRUCTURE.md`: Update directory listing.

    Per D-14: Do NOT update archived milestone plans (.planning/milestones/*), historical docs (docs/plans/*, docs/prd/complete/*), or old phase artifacts.

    **D. Commit:**
    Commit with message: `Docs: Consolidate yaml-core docs into settings-core and update cross-references`
  </action>
  <verify>
    <automated>cd J:/CLASSIC-Fallout4 && grep -rl "classic.yaml.core" docs/api/ CLAUDE.md AGENTS.md .planning/codebase/ 2>/dev/null | grep -v ".git" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `docs/api/classic-yaml-core.md` does NOT exist
    - `docs/api/classic-settings-core.md` contains `YamlOperations` (yaml ops section added)
    - `docs/api/classic-settings-core.md` contains `YamlCacheStats` (D-03 rename applied in docs)
    - `docs/api/README.md` does NOT contain `classic-yaml-core`
    - `docs/api/README.md` entry for classic-settings-core mentions YAML operations
    - `grep -rl "classic.yaml.core" docs/api/ CLAUDE.md AGENTS.md .planning/codebase/ 2>/dev/null | wc -l` returns 0
    - `grep -rl "classic::yaml" docs/api/ 2>/dev/null | wc -l` returns 0
    - `docs/api/classic-cpp-bridge-data-entrypoints.md` references `classic::settings` (not `classic::yaml`)
    - CLAUDE.md crate count reflects the merge
  </acceptance_criteria>
  <done>API docs consolidated, cross-references updated in all active docs. No stale yaml-core references remain in active documentation.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate parity gate baselines and run full validation (D-12)</name>
  <files>
    tools/cxx_api_parity/ (baseline files),
    ClassicLib-rs/node-bindings/classic-node/ (generated artifacts)
  </files>
  <read_first>
    tools/cxx_api_parity/check_parity_gate.py,
    tools/python_api_parity/check_parity_gate.py
  </read_first>
  <action>
    **A. Full workspace validation:**

    1. Run the complete test suite:
       ```
       cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml
       ```

    2. Run clippy:
       ```
       cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings
       ```

    3. Run fmt check:
       ```
       cargo fmt --all --manifest-path ClassicLib-rs/Cargo.toml -- --check
       ```

    **B. CXX parity gate regeneration (D-12):**

    1. Regenerate the CXX baseline:
       ```
       python tools/cxx_api_parity/generate_baseline.py
       ```

    2. Verify the gate passes:
       ```
       python tools/cxx_api_parity/check_parity_gate.py
       ```
       Must exit 0.

    **C. Python parity gate (D-12):**

    1. Rebuild Python bindings:
       ```
       pwsh -ExecutionPolicy Bypass -File rebuild_rust.ps1 -Target python
       ```

    2. Run the parity gate:
       ```
       python tools/python_api_parity/check_parity_gate.py --repo-root .
       ```
       Must exit 0 with `deferred_total == 0`.

    3. Run Python tests:
       ```
       uv run pytest ClassicLib-rs/python-bindings/tests -q
       ```

    **D. Node parity gate (D-12):**

    1. Build Node bindings:
       ```
       cd ClassicLib-rs/node-bindings/classic-node && bun install && bun run build
       ```

    2. Run the parity gate:
       ```
       cd ClassicLib-rs/node-bindings/classic-node && bun run parity:gate:local
       ```
       Must exit 0.

    3. Run Node tests:
       ```
       cd ClassicLib-rs/node-bindings/classic-node && bun run test:bun && bun run test:node
       ```

    **E. C++ build validation:**

    1. Build and test CLI:
       ```
       pwsh -ExecutionPolicy Bypass -File classic-cli/build_cli.ps1 -Clean -Test
       ```

    2. Build GUI (test only if feasible):
       ```
       pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Clean
       ```

    **F. Commit any regenerated baselines:**

    If the CXX baseline file was updated, commit it:
    ```
    Chore: Regenerate parity gate baselines after yaml-core absorption
    ```
  </action>
  <verify>
    <automated>cd J:/CLASSIC-Fallout4 && source tools/use_msvc_from_git_bash.sh && cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml && cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` exits 0
    - `cargo fmt --all --manifest-path ClassicLib-rs/Cargo.toml -- --check` exits 0
    - CXX parity gate (`python tools/cxx_api_parity/check_parity_gate.py`) exits 0
    - Python parity gate (`python tools/python_api_parity/check_parity_gate.py --repo-root .`) exits 0
    - Node parity gate (`bun run parity:gate:local`) exits 0
    - `uv run pytest ClassicLib-rs/python-bindings/tests -q` exits 0
    - C++ CLI builds and tests pass via `build_cli.ps1 -Test`
  </acceptance_criteria>
  <done>All parity gates regenerated and passing. Full workspace test suite green. C++ builds verified. Phase 1 merge is complete and validated.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. `cargo test --workspace` -- all tests pass (including migrated yaml tests)
2. `cargo clippy --workspace` -- clean
3. All three parity gates exit 0
4. C++ CLI and GUI builds succeed
5. Python and Node binding tests pass
6. No stale references to classic-yaml-core in active documentation
</verification>

<success_criteria>
- Migrated integration tests and benchmarks run successfully from settings-core (D-10, YAML-01)
- API docs reflect merged crate topology with no yaml-core references (D-13, D-14)
- All three parity gates pass with regenerated baselines (D-12, YAML-04)
- Full workspace validation green (tests, clippy, fmt)
</success_criteria>

<output>
After completion, create `.planning/phases/01-yaml-settings-merge/01-03-SUMMARY.md`
</output>
