---
phase: 27-test-coverage
plan: 05
type: execute
wave: 3
depends_on: ["27-01"]
files_modified:
  - rust/business-logic/classic-yaml-core/src/**/*.rs
  - rust/business-logic/classic-config-core/src/**/*.rs
  - rust/business-logic/classic-settings-core/src/**/*.rs
autonomous: true

must_haves:
  truths:
    - "classic-yaml-core reaches 60% line coverage"
    - "classic-config-core reaches 60% line coverage"
    - "classic-settings-core reaches 60% line coverage"
    - "All new tests pass with cargo test"
  artifacts:
    - path: "rust/business-logic/classic-yaml-core/src/"
      provides: "Tests for YAML operations"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-config-core/src/"
      provides: "Tests for configuration management"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-settings-core/src/"
      provides: "Tests for settings operations"
      contains: "#[cfg(test)]"
  key_links:
    - from: "rust/business-logic/classic-yaml-core/"
      to: "cargo test -p classic-yaml-core"
      via: "test execution"
      pattern: "#\\[test\\]"
    - from: "rust/business-logic/classic-config-core/"
      to: "cargo test -p classic-config-core"
      via: "test execution"
      pattern: "#\\[test\\]"
    - from: "rust/business-logic/classic-settings-core/"
      to: "cargo test -p classic-settings-core"
      via: "test execution"
      pattern: "#\\[test\\]"
---

<objective>
Fill test coverage gaps in classic-yaml-core (2,982 lines), classic-config-core (2,137 lines), and classic-settings-core (1,967 lines) to reach the 60% line coverage minimum for each.

Purpose: These three crates form the configuration/settings stack. YAML handles document parsing, config handles application configuration, and settings handles user-facing settings. They already have some of the better test coverage in the workspace (yaml-core has 560 test lines, config-core has 674, both have integration tests).

Output: New tests bringing all three crates to 60%+ line coverage.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-RESEARCH.md
@.planning/phases/27-test-coverage/27-BASELINE.md
@.planning/phases/27-test-coverage/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill coverage gaps in classic-yaml-core and classic-config-core</name>
  <files>
    rust/business-logic/classic-yaml-core/src/**/*.rs
    rust/business-logic/classic-yaml-core/tests/**/*.rs
    rust/business-logic/classic-config-core/src/**/*.rs
    rust/business-logic/classic-config-core/tests/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for current coverage of both crates. Skip any crate already at or above 60%.

For each crate below 60%:

1. Run per-crate coverage to identify gaps
2. Read source files with lowest coverage
3. Write tests targeting uncovered code

**classic-yaml-core** already has 560 test lines + 65 inline tests + integration tests in `tests/` directory. Likely close to 60% already. Focus on:
- YAML multi-document handling edge cases
- Anchor/alias resolution
- Error handling for malformed YAML
- Insert-order preservation verification
- Helper methods on Hash nodes (`.get()` behavior per memory: returns BadValue for missing keys)

**classic-config-core** already has 674 test lines + 30 inline tests + integration tests. Focus on:
- Configuration loading from various sources
- Default value handling
- Configuration merging/override logic
- Error paths for missing/corrupt config files
- VR mode migration logic (Phase 24 decision: vr_mode=true + no game_version -> game_version="VR")

Use existing conventions: inline tests for unit, `tests/` directory for integration. Both crates already have `tests/` directories.

Run `cargo test -p classic-yaml-core -p classic-config-core` after writing tests.
  </action>
  <verify>
```
cd rust
cargo test -p classic-yaml-core
cargo test -p classic-config-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-yaml-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-config-core
```
Both crates at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-yaml-core and classic-config-core each reach 60% line coverage with all tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps in classic-settings-core</name>
  <files>
    rust/business-logic/classic-settings-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-settings-core's current coverage. If already at or above 60%, skip this task.

If below 60%:

1. Run per-crate coverage: `cd rust && pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-settings-core`
2. Identify lowest-coverage files and highest-impact functions
3. Read the source code

Write tests for settings operations:
- Settings loading with DashMap cache behavior
- Batch settings loading (`tokio::join!` preserves order per memory)
- Settings value type conversions
- Cache hit/miss behavior
- Error handling for corrupted settings files
- Default value fallback behavior
- Use `serial_test` if tests modify global cache state

Run `cargo test -p classic-settings-core` after writing tests.
  </action>
  <verify>
```
cd rust
cargo test -p classic-settings-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-settings-core
```
classic-settings-core at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-settings-core reaches 60% line coverage with all tests passing.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-yaml-core` passes all tests
- `cargo test -p classic-config-core` passes all tests
- `cargo test -p classic-settings-core` passes all tests
- All three crates at 60%+ line coverage verified by cargo-llvm-cov
</verification>

<success_criteria>
All three configuration stack crates (yaml-core, config-core, settings-core) reach 60% line coverage minimum with all tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-05-SUMMARY.md`
</output>
