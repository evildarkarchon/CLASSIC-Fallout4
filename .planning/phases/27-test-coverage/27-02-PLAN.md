---
phase: 27-test-coverage
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - rust/business-logic/classic-scanlog-core/src/**/*.rs
autonomous: true

must_haves:
  truths:
    - "classic-scanlog-core reaches 60% line coverage"
    - "Error paths and edge cases in scan log parsing are tested"
    - "All new tests pass with cargo test"
  artifacts:
    - path: "rust/business-logic/classic-scanlog-core/src/"
      provides: "Inline #[cfg(test)] modules for gap-filling"
      contains: "#[cfg(test)]"
  key_links:
    - from: "rust/business-logic/classic-scanlog-core/src/"
      to: "cargo test -p classic-scanlog-core"
      via: "test execution"
      pattern: "#\\[test\\]"
---

<objective>
Fill test coverage gaps in classic-scanlog-core (11,172 lines, the largest crate in the workspace) to reach the 60% line coverage minimum.

Purpose: This is the highest-impact crate for coverage improvement due to its size. It handles crash log parsing, the core business logic of CLASSIC.

Output: New inline tests and/or integration tests that bring classic-scanlog-core to 60%+ line coverage.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-RESEARCH.md
@.planning/phases/27-test-coverage/27-BASELINE.md
@.planning/phases/27-test-coverage/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure current coverage and identify gaps</name>
  <files>
    rust/business-logic/classic-scanlog-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-scanlog-core's current coverage. If it is ALREADY at or above 60%, skip this entire plan and report in the summary that no gap-filling was needed.

If below 60%, proceed:

1. Run per-crate coverage to see which source files have the lowest coverage:
   ```
   cd rust && pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-scanlog-core
   ```
2. Open the HTML report to identify the lowest-coverage files/functions
3. Read the source files with the lowest coverage to understand what needs testing
4. Identify the highest-impact test targets: functions with many lines and low coverage

Focus on:
- Public API functions that callers depend on
- Error handling paths (Result::Err branches, unwrap alternatives)
- Edge cases in parsing logic (malformed input, empty input, boundary conditions)
- Functions identified by coverage as having 0% or very low coverage

Do NOT test:
- Trivial getters/setters with 1-2 lines
- Code that is adequately covered already
- Generated code or macro expansions
  </action>
  <verify>
Coverage analysis identifies specific files and functions to target. Gap assessment is clear.
  </verify>
  <done>Know exactly which files/functions in classic-scanlog-core need tests to reach 60%.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests to close coverage gaps</name>
  <files>
    rust/business-logic/classic-scanlog-core/src/**/*.rs
  </files>
  <action>
Write tests to bring classic-scanlog-core to 60%+ line coverage. Follow existing project conventions:

**Test organization:**
- Use inline `#[cfg(test)] mod tests` in source files (dominant pattern in this crate -- 186 existing inline tests)
- For integration tests spanning multiple modules, use `tests/` directory

**Test patterns:**
- Arrange-Act-Assert structure
- Use `tempfile::tempdir()` for tests needing file I/O
- Use `#[tokio::test]` for async functions
- Test both happy paths AND error paths (per user decision: error paths included)
- Test edge cases: empty input, malformed data, boundary values

**Writing strategy:**
- Start with the lowest-coverage files identified in Task 1
- Write tests for the highest-impact functions first (most lines covered per test)
- After each batch of tests, run `cargo test -p classic-scanlog-core` to ensure they pass
- When you estimate you're near 60%, run coverage measurement to verify

**Quality rules:**
- Tests must be meaningful (not just calling a function and ignoring the result)
- Each test should verify a specific behavior
- Use descriptive test names: `test_parse_crash_log_with_missing_header`
- Include comments explaining what behavior each test validates

IMPORTANT: Run `cargo test -p classic-scanlog-core` after writing tests to ensure they all pass. Fix any failures before proceeding.
  </action>
  <verify>
Run from `rust/` directory:
```
cargo test -p classic-scanlog-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-scanlog-core
pwsh -ExecutionPolicy Bypass -File coverage_summary.ps1
```
Verify classic-scanlog-core is at 60%+ line coverage and all tests pass.
  </verify>
  <done>classic-scanlog-core has 60%+ line coverage with all tests passing. Coverage verified by cargo-llvm-cov.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core` passes all tests (including new ones)
- Per-crate coverage for classic-scanlog-core is at or above 60% line coverage
- New tests follow inline `#[cfg(test)]` convention
- Error paths and edge cases are tested, not just happy paths
</verification>

<success_criteria>
classic-scanlog-core reaches 60% line coverage minimum. All tests pass. Coverage verified by measurement tool.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-02-SUMMARY.md`
</output>
