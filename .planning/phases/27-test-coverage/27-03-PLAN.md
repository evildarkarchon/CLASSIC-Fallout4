---
phase: 27-test-coverage
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - rust/business-logic/classic-file-io-core/src/**/*.rs
  - rust/business-logic/classic-path-core/src/**/*.rs
autonomous: true

must_haves:
  truths:
    - "classic-file-io-core reaches 60% line coverage"
    - "classic-path-core reaches 60% line coverage"
    - "All new tests pass with cargo test"
  artifacts:
    - path: "rust/business-logic/classic-file-io-core/src/"
      provides: "Tests for file I/O operations"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-path-core/src/"
      provides: "Tests for path management"
      contains: "#[cfg(test)]"
  key_links:
    - from: "rust/business-logic/classic-file-io-core/"
      to: "cargo test -p classic-file-io-core"
      via: "test execution"
      pattern: "#\\[test\\]"
    - from: "rust/business-logic/classic-path-core/"
      to: "cargo test -p classic-path-core"
      via: "test execution"
      pattern: "#\\[test\\]"
---

<objective>
Fill test coverage gaps in classic-file-io-core (4,411 lines) and classic-path-core (4,377 lines) to reach the 60% line coverage minimum for each.

Purpose: These are the second and third largest business-logic crates. File I/O handles async file reading/writing with Rust acceleration. Path management handles game directory detection and path resolution.

Output: New tests bringing both crates to 60%+ line coverage.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-RESEARCH.md
@.planning/phases/27-test-coverage/27-BASELINE.md
@.planning/phases/27-test-coverage/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill coverage gaps in classic-file-io-core</name>
  <files>
    rust/business-logic/classic-file-io-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-file-io-core's current coverage. If already at or above 60%, skip this task.

If below 60%:

1. Run per-crate coverage: `cd rust && pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-file-io-core`
2. Identify lowest-coverage files and highest-impact functions
3. Read the source code to understand what needs testing

Write tests following project conventions:
- Inline `#[cfg(test)] mod tests` for unit tests
- `tempfile::tempdir()` for file I/O test fixtures (never write to real filesystem paths)
- `#[tokio::test]` for async functions
- Test both success and error paths
- Test edge cases: empty files, missing files, encoding issues, large files

Focus areas for file I/O:
- File reading with different encodings (UTF-8, UTF-16, etc.)
- Async file operations
- Error handling for missing/unreadable files
- Batch file operations
- Memory-mapped file operations if present

Run `cargo test -p classic-file-io-core` after writing tests to verify they pass.
  </action>
  <verify>
```
cd rust
cargo test -p classic-file-io-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-file-io-core
```
classic-file-io-core at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-file-io-core reaches 60% line coverage with all tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps in classic-path-core</name>
  <files>
    rust/business-logic/classic-path-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-path-core's current coverage. If already at or above 60%, skip this task.

If below 60%:

1. Run per-crate coverage: `cd rust && pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-path-core`
2. Identify lowest-coverage files and highest-impact functions
3. Read the source code to understand what needs testing

Write tests following project conventions:
- Inline `#[cfg(test)] mod tests` for unit tests
- `tempfile::tempdir()` for tests needing temp directories
- Test both Windows and cross-platform path handling logic
- Test error paths for invalid/missing paths

Focus areas for path management:
- Game directory detection logic
- Path resolution and normalization
- Registry-based path lookups (mock the registry if needed)
- Edge cases: paths with spaces, Unicode paths, very long paths, relative vs absolute

Run `cargo test -p classic-path-core` after writing tests to verify they pass.
  </action>
  <verify>
```
cd rust
cargo test -p classic-path-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-path-core
```
classic-path-core at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-path-core reaches 60% line coverage with all tests passing.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-file-io-core` passes all tests
- `cargo test -p classic-path-core` passes all tests
- Both crates at 60%+ line coverage verified by cargo-llvm-cov
</verification>

<success_criteria>
Both classic-file-io-core and classic-path-core reach 60% line coverage minimum with all tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-03-SUMMARY.md`
</output>
