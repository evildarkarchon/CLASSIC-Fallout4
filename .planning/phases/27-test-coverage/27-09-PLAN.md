---
phase: 27-test-coverage
plan: 09
type: execute
wave: 5
depends_on: ["27-02", "27-03", "27-04", "27-05", "27-06", "27-07", "27-08"]
files_modified:
  - .planning/phases/27-test-coverage/27-FINAL-COVERAGE.md
autonomous: true

must_haves:
  truths:
    - "Workspace-wide coverage report generated with all new tests"
    - "All non-PyO3 crates verified at 60%+ line coverage (or documented exceptions)"
    - "Final coverage document records the achieved state"
    - "All tests pass workspace-wide"
  artifacts:
    - path: ".planning/phases/27-test-coverage/27-FINAL-COVERAGE.md"
      provides: "Final per-crate coverage results and phase completion record"
      contains: "60%"
    - path: "rust/target/llvm-cov/html/index.html"
      provides: "Final HTML coverage report"
  key_links:
    - from: ".planning/phases/27-test-coverage/27-FINAL-COVERAGE.md"
      to: "rust/target/llvm-cov/coverage.json"
      via: "coverage data extraction"
      pattern: "coverage"
---

<objective>
Run a final workspace-wide coverage measurement to verify all crates meet the 60% minimum, generate the definitive reports, and document the final state.

Purpose: This is the verification gate for the entire phase. All gap-filling plans have completed; now we confirm the 60% target is met everywhere and produce the final artifacts.

Output: Final HTML/lcov/JSON reports and a coverage completion document.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final workspace-wide coverage and verify all targets</name>
  <files>
    .planning/phases/27-test-coverage/27-FINAL-COVERAGE.md
  </files>
  <action>
1. Run `cargo test --workspace` from `rust/` to confirm ALL tests pass workspace-wide. If any tests fail, fix them before proceeding.

2. Run the full workspace coverage suite:
   ```
   cd rust
   pwsh -ExecutionPolicy Bypass -File coverage_report.ps1
   pwsh -ExecutionPolicy Bypass -File coverage_summary.ps1
   ```

3. Review the per-crate summary. For each non-PyO3 crate:
   - If at 60%+: record as PASS
   - If below 60%: this is a gap that needs investigation

4. If any non-PyO3 crate is still below 60%, write targeted tests to close the gap. This might happen if:
   - A crate was borderline and other plan changes shifted coverage
   - Tests from other plans inadvertently affected shared code coverage
   - A crate was harder to test than anticipated

5. Create `.planning/phases/27-test-coverage/27-FINAL-COVERAGE.md` with:

```markdown
# Phase 27: Final Coverage Report

**Measured:** [date]
**Tool:** cargo-llvm-cov 0.8.3, rustc 1.91.1

## Results Summary

- Total Rust crates: 39
- Non-PyO3 crates measured: 22
- Crates at/above 60%: N/22
- Crates below 60%: N/22 (with justification if any)
- Workspace aggregate coverage: X.X%

## Per-Crate Results

### Foundation (2 crates)
| Crate | Baseline | Final | Delta | Status |
|-------|----------|-------|-------|--------|

### Business Logic (19 crates)
| Crate | Baseline | Final | Delta | Status |
|-------|----------|-------|-------|--------|

### UI Applications (1 crate)
| Crate | Baseline | Final | Delta | Status |
|-------|----------|-------|-------|--------|

### Python Bindings (18 crates, informational)
| Crate | Coverage | Notes |
|-------|----------|-------|

## Coverage Improvement Summary

- Total new test functions added: ~N
- Largest improvement: [crate] (X% -> Y%)
- Crates that needed no work: [list]

## Exceptions (if any)

[Document any crate that couldn't reach 60% with justification]

## Reports Location

- HTML: `rust/target/llvm-cov/html/index.html`
- JSON: `rust/target/llvm-cov/coverage.json`
- LCOV: `rust/target/llvm-cov/lcov.info`
```

6. Compare final numbers to baseline (from 27-BASELINE.md) and record the delta for each crate.
  </action>
  <verify>
- `cargo test --workspace` passes in `rust/` directory
- `rust/target/llvm-cov/html/index.html` exists (final HTML report)
- `rust/target/llvm-cov/coverage.json` exists (final JSON)
- `rust/target/llvm-cov/lcov.info` exists (final lcov)
- `.planning/phases/27-test-coverage/27-FINAL-COVERAGE.md` exists with complete per-crate results
- All non-PyO3 crates show 60%+ (or documented exception)
  </verify>
  <done>All non-PyO3 Rust crates verified at 60%+ line coverage (or exceptions documented). Final reports generated in HTML, JSON, and lcov formats. Phase 27 complete.</done>
</task>

</tasks>

<verification>
- `cargo test --workspace` passes with zero failures in `rust/` directory
- Per-crate summary shows all 22 non-PyO3 crates at 60%+ (or documented exceptions)
- Final coverage document compares baseline to final with deltas
- HTML, JSON, and lcov reports all generated
</verification>

<success_criteria>
All 22 non-PyO3 Rust crates meet the 60% line coverage minimum (or have documented justification for exceptions). Final workspace-wide coverage report confirms the target. Phase 27 objectives are fully achieved.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-09-SUMMARY.md`
</output>
