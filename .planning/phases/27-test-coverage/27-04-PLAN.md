---
phase: 27-test-coverage
plan: 04
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - rust/business-logic/classic-scangame-core/src/**/*.rs
  - rust/business-logic/classic-version-registry-core/src/**/*.rs
autonomous: true

must_haves:
  truths:
    - "classic-scangame-core reaches 60% line coverage"
    - "classic-version-registry-core reaches 60% line coverage"
    - "All new tests pass with cargo test"
  artifacts:
    - path: "rust/business-logic/classic-scangame-core/src/"
      provides: "Tests for game scanning operations"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-version-registry-core/src/"
      provides: "Tests for version registry"
      contains: "#[cfg(test)]"
  key_links:
    - from: "rust/business-logic/classic-scangame-core/"
      to: "cargo test -p classic-scangame-core"
      via: "test execution"
      pattern: "#\\[test\\]"
    - from: "rust/business-logic/classic-version-registry-core/"
      to: "cargo test -p classic-version-registry-core"
      via: "test execution"
      pattern: "#\\[test\\]"
---

<objective>
Fill test coverage gaps in classic-scangame-core (4,168 lines) and classic-version-registry-core (3,168 lines) to reach the 60% line coverage minimum for each.

Purpose: Game scanning handles mod detection, INI scanning, and game integrity checks. Version registry handles game version detection and matching patterns.

Output: New tests bringing both crates to 60%+ line coverage.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-RESEARCH.md
@.planning/phases/27-test-coverage/27-BASELINE.md
@.planning/phases/27-test-coverage/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill coverage gaps in classic-scangame-core</name>
  <files>
    rust/business-logic/classic-scangame-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-scangame-core's current coverage. If already at or above 60%, skip this task.

If below 60%:

1. Run per-crate coverage: `cd rust && pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-scangame-core`
2. Identify lowest-coverage files and highest-impact functions
3. Read the source code to understand what needs testing

Write tests following project conventions:
- Inline `#[cfg(test)] mod tests` for unit tests
- `tempfile::tempdir()` for tests needing temp game directories
- Test both success and error paths
- Mock filesystem structures where needed (create temp dirs mimicking game folder layout)

Focus areas for game scanning:
- INI file scanning and duplicate detection
- Mod detection logic
- XSE/ENB integrity checking logic
- Game folder structure validation
- Edge cases: missing files, corrupted INIs, empty mod lists

Run `cargo test -p classic-scangame-core` after writing tests to verify they pass.
  </action>
  <verify>
```
cd rust
cargo test -p classic-scangame-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-scangame-core
```
classic-scangame-core at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-scangame-core reaches 60% line coverage with all tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps in classic-version-registry-core</name>
  <files>
    rust/business-logic/classic-version-registry-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-version-registry-core's current coverage. If already at or above 60%, skip this task.

If below 60%:

1. Run per-crate coverage: `cd rust && pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-version-registry-core`
2. Identify lowest-coverage files and highest-impact functions
3. Read the source code to understand what needs testing

Write tests following project conventions:
- Inline `#[cfg(test)] mod tests` for unit tests
- Test version parsing, matching, and registry lookups
- Test both success and error paths

Focus areas for version registry:
- Version string parsing (semver and non-semver formats)
- Version comparison and matching logic
- Registry lookup operations
- Default/fallback behavior when versions are unknown
- Edge cases: malformed version strings, empty registries, version range matching

Run `cargo test -p classic-version-registry-core` after writing tests to verify they pass.
  </action>
  <verify>
```
cd rust
cargo test -p classic-version-registry-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-version-registry-core
```
classic-version-registry-core at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-version-registry-core reaches 60% line coverage with all tests passing.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scangame-core` passes all tests
- `cargo test -p classic-version-registry-core` passes all tests
- Both crates at 60%+ line coverage verified by cargo-llvm-cov
</verification>

<success_criteria>
Both classic-scangame-core and classic-version-registry-core reach 60% line coverage minimum with all tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-04-SUMMARY.md`
</output>
