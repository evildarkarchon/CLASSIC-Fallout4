---
phase: 27-test-coverage
plan: 07
type: execute
wave: 4
depends_on: ["27-01"]
files_modified:
  - rust/business-logic/classic-update-core/src/**/*.rs
  - rust/business-logic/classic-web-core/src/**/*.rs
  - rust/business-logic/classic-registry-core/src/**/*.rs
  - rust/business-logic/classic-resource-core/src/**/*.rs
  - rust/business-logic/classic-perf-core/src/**/*.rs
  - rust/business-logic/classic-version-core/src/**/*.rs
  - rust/business-logic/classic-xse-core/src/**/*.rs
  - rust/business-logic/classic-pybridge-core/src/**/*.rs
autonomous: true

must_haves:
  truths:
    - "All 8 small business-logic crates reach 60% line coverage"
    - "All new tests pass with cargo test"
  artifacts:
    - path: "rust/business-logic/classic-update-core/src/"
      provides: "Tests for update checking"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-web-core/src/"
      provides: "Tests for web operations"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-registry-core/src/"
      provides: "Tests for global registry"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-resource-core/src/"
      provides: "Tests for resource loading"
      contains: "#[cfg(test)]"
  key_links:
    - from: "rust/business-logic/"
      to: "cargo test --workspace"
      via: "test execution"
      pattern: "#\\[test\\]"
---

<objective>
Fill test coverage gaps in the 8 smaller business-logic crates (each under 1,300 lines) to reach the 60% line coverage minimum for each.

Purpose: These smaller crates provide supporting functionality -- update checking, web operations, Windows registry access, resource loading, performance measurement, version parsing, XSE detection, and Python bridge utilities.

Output: New tests bringing all 8 crates to 60%+ line coverage. Due to their small size, multiple crates are handled in a single plan.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-RESEARCH.md
@.planning/phases/27-test-coverage/27-BASELINE.md
@.planning/phases/27-test-coverage/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill coverage gaps in update-core, web-core, registry-core, and resource-core</name>
  <files>
    rust/business-logic/classic-update-core/src/**/*.rs
    rust/business-logic/classic-web-core/src/**/*.rs
    rust/business-logic/classic-registry-core/src/**/*.rs
    rust/business-logic/classic-resource-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for each crate's current coverage. Skip any already at or above 60%.

For each crate below 60%, write tests. All use inline `#[cfg(test)] mod tests` convention.

**classic-update-core** (1,212 lines, 48 inline tests):
- Version comparison for update detection
- Update URL construction
- Error handling for network failures (mock reqwest if needed, or test the non-network parts)
- Parse update response logic
- Note: Avoid tests that actually make network calls -- focus on parsing and logic

**classic-web-core** (834 lines, 35 inline tests):
- URL construction and validation
- HTML parsing logic (scraper crate usage)
- Error handling for malformed HTML
- Note: Same as above -- avoid real network calls, test parsing logic

**classic-registry-core** (775 lines, 16 inline tests):
- GlobalRegistry singleton behavior (use `serial_test`)
- Registry entry storage and retrieval
- Default values and missing key behavior
- Edge cases: empty registry, duplicate keys

**classic-resource-core** (630 lines, 8 inline tests):
- Resource path resolution
- Data directory detection
- Error handling for missing resources
- Use `tempfile::tempdir()` for test fixtures

Run `cargo test` for each crate after writing tests.
  </action>
  <verify>
```
cd rust
cargo test -p classic-update-core
cargo test -p classic-web-core
cargo test -p classic-registry-core
cargo test -p classic-resource-core
```
All four crates at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-update-core, classic-web-core, classic-registry-core, and classic-resource-core each reach 60% line coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps in perf-core, version-core, xse-core, and pybridge-core</name>
  <files>
    rust/business-logic/classic-perf-core/src/**/*.rs
    rust/business-logic/classic-version-core/src/**/*.rs
    rust/business-logic/classic-xse-core/src/**/*.rs
    rust/business-logic/classic-pybridge-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for each crate's current coverage. Skip any already at or above 60%.

For each crate below 60%, write tests:

**classic-perf-core** (620 lines, 15 inline tests):
- Performance timer/measurement functions
- Statistics calculations (if any)
- Formatting of performance output
- Edge cases: zero-duration operations, very large values

**classic-version-core** (599 lines, 10 inline tests):
- Version string parsing (semver and custom formats)
- Version comparison operators
- Display/formatting of versions
- Edge cases: pre-release versions, invalid version strings, comparison across formats

**classic-xse-core** (561 lines, 8 inline tests):
- XSE (Script Extender) detection logic
- Version checking for F4SE/SKSE
- File existence checking (use tempdir with mock file structure)
- Error handling for missing XSE installations

**classic-pybridge-core** (457 lines, 10 inline tests):
- Python bridge utility functions (ones that don't require Python GIL)
- Type conversion helpers
- Error mapping functions
- Note: Focus on pure Rust helper functions, not functions requiring `Python::with_gil()`

Run `cargo test` for each crate after writing tests.
  </action>
  <verify>
```
cd rust
cargo test -p classic-perf-core
cargo test -p classic-version-core
cargo test -p classic-xse-core
cargo test -p classic-pybridge-core
```
All four crates at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-perf-core, classic-version-core, classic-xse-core, and classic-pybridge-core each reach 60% line coverage.</done>
</task>

</tasks>

<verification>
- All 8 small business-logic crates pass their tests
- All 8 crates at 60%+ line coverage verified by cargo-llvm-cov
</verification>

<success_criteria>
All 8 smaller business-logic crates reach 60% line coverage minimum with all tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-07-SUMMARY.md`
</output>
