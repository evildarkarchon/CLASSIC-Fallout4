---
phase: 27-test-coverage
plan: 08
type: execute
wave: 4
depends_on: ["27-01"]
files_modified:
  - rust/ui-applications/classic-gui/src/**/*.rs
  - rust/foundation/classic-shared-core/src/**/*.rs
  - rust/foundation/classic-shared-py/src/**/*.rs
autonomous: true

must_haves:
  truths:
    - "classic-gui reaches 60% line coverage (excluding Slint-generated code)"
    - "classic-shared-core reaches 60% line coverage"
    - "classic-shared-py reaches 60% line coverage"
    - "GUI tests use MockDispatcher and ScanWindowProperties trait patterns"
    - "All new tests pass with cargo test"
  artifacts:
    - path: "rust/ui-applications/classic-gui/src/"
      provides: "Tests for GUI Rust logic"
      contains: "#[cfg(test)]"
    - path: "rust/foundation/classic-shared-core/src/"
      provides: "Tests for shared runtime and utilities"
      contains: "#[cfg(test)]"
    - path: "rust/foundation/classic-shared-py/src/"
      provides: "Tests for shared PyO3 utilities"
      contains: "#[cfg(test)]"
  key_links:
    - from: "rust/ui-applications/classic-gui/src/"
      to: "cargo test -p classic-gui"
      via: "test execution"
      pattern: "#\\[test\\]"
    - from: "rust/foundation/classic-shared-core/src/"
      to: "cargo test -p classic-shared-core --features gui-bridge"
      via: "test execution with feature flag"
      pattern: "#\\[test\\]"
---

<objective>
Fill test coverage gaps in classic-gui (2,745 lines), classic-shared-core (2,214 lines), and classic-shared-py (1,611 lines) to reach the 60% line coverage minimum for each.

Purpose: The GUI crate contains Rust logic behind the Slint UI. Shared-core provides the foundation runtime, async bridge, and error types. Shared-py provides common PyO3 utilities. These crates require special testing approaches: GUI uses trait-based mocks (MockDispatcher, ScanWindowProperties), shared-core needs `--features gui-bridge`, and shared-py has limited testability without Python.

Output: New tests bringing all three crates to 60%+ line coverage, using established mock patterns from Phase 26.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-RESEARCH.md
@.planning/phases/27-test-coverage/27-BASELINE.md
@.planning/phases/27-test-coverage/27-01-SUMMARY.md
@.planning/phases/26-async-bridge-audit/26-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill coverage gaps in classic-gui</name>
  <files>
    rust/ui-applications/classic-gui/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-gui's current coverage. If already at or above 60%, skip this task.

If below 60%:

1. Run per-crate coverage (generated code MUST be excluded):
   ```
   cd rust && pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-gui
   ```
2. Review the HTML report -- only hand-written source files should appear (not target/debug/build/classic-gui-slint-*/out/)
3. Read source files with lowest coverage

The GUI crate has testable pure-Rust modules per research:
- `markdown.rs` - `parse_markdown()` is a pure function, no Slint dependency
- `results.rs` - `extract_timestamp()`, `prepare_report_entries()` are pure functions
- `settings.rs` - `settings_file_path()`, `game_version_*()` are pure functions
- `state.rs` - `WindowState`, `TabGeometry` are data structures with methods
- `worker.rs` - `ScanWindowProperties` trait enables MockScanWindow testing

**Test strategy per CONTEXT.md (locked decision):** Both direct logic tests AND trait-based mock tests.

For worker.rs, use MockScanWindow pattern (already exists per Phase 26):
```rust
struct MockScanWindow {
    progress: std::cell::RefCell<f32>,
    status: std::cell::RefCell<String>,
    in_progress: std::cell::RefCell<bool>,
}
impl ScanWindowProperties for MockScanWindow { ... }
```

Focus on:
- Markdown parsing: various block types (headers, lists, code, bold, italic)
- Results: timestamp extraction from filenames, report entry preparation
- Settings: path construction, game version string mapping
- State: serialization/deserialization, default values, validation
- Worker: scan flow with mock window (progress updates, status messages, completion)

Run `cargo test -p classic-gui` after writing tests.
  </action>
  <verify>
```
cd rust
cargo test -p classic-gui
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-gui
```
classic-gui at 60%+ line coverage (excluding Slint-generated code), all tests pass.
  </verify>
  <done>classic-gui reaches 60% line coverage with all tests passing. Tests use MockScanWindow for worker testing.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps in classic-shared-core and classic-shared-py</name>
  <files>
    rust/foundation/classic-shared-core/src/**/*.rs
    rust/foundation/classic-shared-py/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for both crates' current coverage. Skip any already at or above 60%.

**classic-shared-core** (2,214 lines, 504 test lines, 17 test files -- likely close to 60% already):
- IMPORTANT: Run with `--features gui-bridge` to include AsyncBridge tests (15 existing tests from Phase 26)
- This crate already has extensive tests. Check if it's already at 60% before writing more.
- If below, focus on:
  - Runtime initialization (`get_runtime()`)
  - Error types and conversions
  - Utility functions
  - Any untested public API surface
  - Use `serial_test` for tests that modify global state (OnceLock dispatcher)

**classic-shared-py** (1,611 lines, 6 inline tests):
- This is a foundation PyO3 crate. Testing is limited since most code needs Python GIL.
- Focus on testable parts without Python:
  - Error type definitions and conversions
  - Pure Rust helper functions within the crate
  - Any non-PyO3 utility code
- If the testable surface is too small to reach 60%, document this in the summary. Per research recommendation, PyO3 crates are thin adapters -- if classic-shared-py is predominantly PyO3 adapter code, note the achievable coverage and move on.

Run tests:
```
cargo test -p classic-shared-core --features gui-bridge
cargo test -p classic-shared-py
```
  </action>
  <verify>
```
cd rust
cargo test -p classic-shared-core --features gui-bridge
cargo test -p classic-shared-py
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-shared-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-shared-py
```
Both crates at 60%+ (or documented as limited by PyO3), all tests pass.
  </verify>
  <done>classic-shared-core reaches 60% line coverage. classic-shared-py reaches 60% or coverage limitation is documented. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-gui` passes all tests
- `cargo test -p classic-shared-core --features gui-bridge` passes all tests
- `cargo test -p classic-shared-py` passes all tests
- All three crates at 60%+ line coverage (or limitation documented for shared-py)
- GUI tests use MockDispatcher/ScanWindowProperties patterns per locked decision
</verification>

<success_criteria>
classic-gui and classic-shared-core reach 60% line coverage. classic-shared-py reaches 60% or its limitation is documented. All tests pass. GUI testing leverages trait-based mock patterns from Phase 26.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-08-SUMMARY.md`
</output>
