---
phase: 27-test-coverage
plan: 06
type: execute
wave: 3
depends_on: ["27-01"]
files_modified:
  - rust/business-logic/classic-database-core/src/**/*.rs
  - rust/business-logic/classic-database-core/tests/**/*.rs
  - rust/business-logic/classic-message-core/src/**/*.rs
  - rust/business-logic/classic-constants-core/src/**/*.rs
autonomous: true

must_haves:
  truths:
    - "classic-database-core reaches 60% line coverage"
    - "classic-message-core reaches 60% line coverage"
    - "classic-constants-core reaches 60% line coverage"
    - "All new tests pass with cargo test"
  artifacts:
    - path: "rust/business-logic/classic-database-core/src/"
      provides: "Tests for database operations"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-message-core/src/"
      provides: "Tests for message handler"
      contains: "#[cfg(test)]"
    - path: "rust/business-logic/classic-constants-core/src/"
      provides: "Tests for constants"
      contains: "#[cfg(test)]"
  key_links:
    - from: "rust/business-logic/classic-database-core/"
      to: "cargo test -p classic-database-core"
      via: "test execution"
      pattern: "#\\[test\\]"
    - from: "rust/business-logic/classic-message-core/"
      to: "cargo test -p classic-message-core"
      via: "test execution"
      pattern: "#\\[test\\]"
    - from: "rust/business-logic/classic-constants-core/"
      to: "cargo test -p classic-constants-core"
      via: "test execution"
      pattern: "#\\[test\\]"
---

<objective>
Fill test coverage gaps in classic-database-core (1,696 lines), classic-message-core (1,336 lines), and classic-constants-core (1,279 lines) to reach the 60% line coverage minimum for each.

Purpose: Database-core handles SQLite operations for FormID lookups. Message-core provides the central MessageHandler for all application output. Constants-core defines game-specific constants and lookup tables.

Output: New tests bringing all three crates to 60%+ line coverage.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-test-coverage/27-CONTEXT.md
@.planning/phases/27-test-coverage/27-RESEARCH.md
@.planning/phases/27-test-coverage/27-BASELINE.md
@.planning/phases/27-test-coverage/27-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill coverage gaps in classic-database-core</name>
  <files>
    rust/business-logic/classic-database-core/src/**/*.rs
    rust/business-logic/classic-database-core/tests/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for classic-database-core's current coverage. If already at or above 60%, skip this task. This crate already has 690 test lines + 17 inline tests + integration tests, so it may already be near the target.

If below 60%:

1. Run per-crate coverage to identify gaps
2. Read source files with lowest coverage

Write tests focusing on:
- SQLite database creation and initialization
- FormID lookup operations (single and batch)
- Database backup operations (rusqlite backup feature)
- Error handling: missing database file, corrupt database, invalid FormIDs
- Edge cases: empty database, very large queries, concurrent access
- Use `tempfile::tempdir()` for test database files (never write to production locations)
- Use `#[tokio::test]` for async database operations

This crate already has a `tests/` directory -- add integration tests there for cross-module workflows. Use inline tests for new unit tests.

Run `cargo test -p classic-database-core` after writing tests.
  </action>
  <verify>
```
cd rust
cargo test -p classic-database-core
pwsh -ExecutionPolicy Bypass -File coverage_report.ps1 -Package classic-database-core
```
classic-database-core at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-database-core reaches 60% line coverage with all tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps in classic-message-core and classic-constants-core</name>
  <files>
    rust/business-logic/classic-message-core/src/**/*.rs
    rust/business-logic/classic-constants-core/src/**/*.rs
  </files>
  <action>
FIRST: Check 27-BASELINE.md for both crates' current coverage. Skip any already at or above 60%.

**classic-message-core** (1,336 lines, 32 inline tests):
If below 60%, write tests for:
- Message creation (msg_info, msg_warning, msg_error patterns)
- Message formatting and output routing
- Message handler singleton behavior (use `serial_test` for global state)
- Error message formatting with context/details
- Edge cases: empty messages, very long messages, special characters

**classic-constants-core** (1,279 lines, 31 inline tests):
If below 60%, write tests for:
- Game-specific constant lookups (Fallout 4 vs Skyrim values)
- Constant table completeness (verify expected keys exist)
- Default/fallback values for unknown inputs
- String matching against constant patterns
- Note: Constants crates are often heavy on data definitions with less logic to test. Focus on the lookup/matching functions rather than data declarations.

Run `cargo test -p classic-message-core -p classic-constants-core` after writing tests.
  </action>
  <verify>
```
cd rust
cargo test -p classic-message-core
cargo test -p classic-constants-core
```
Both crates at 60%+ line coverage, all tests pass.
  </verify>
  <done>classic-message-core and classic-constants-core each reach 60% line coverage with all tests passing.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-database-core` passes all tests
- `cargo test -p classic-message-core` passes all tests
- `cargo test -p classic-constants-core` passes all tests
- All three crates at 60%+ line coverage verified by cargo-llvm-cov
</verification>

<success_criteria>
All three crates (database-core, message-core, constants-core) reach 60% line coverage minimum with all tests passing.
</success_criteria>

<output>
After completion, create `.planning/phases/27-test-coverage/27-06-SUMMARY.md`
</output>
