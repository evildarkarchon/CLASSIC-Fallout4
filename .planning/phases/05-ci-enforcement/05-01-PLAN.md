---
phase: 05-ci-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-cpp.yml
  - tools/test_triple_gate_failure.py
autonomous: true
requirements: [CI-01, CI-02, CI-03, CI-05, CI-06]

must_haves:
  truths:
    - "The CXX parity gate job runs in ci-cpp.yml before CLI and GUI test jobs"
    - "Adding a pub fn to classic-shared-core causes all three parity gates to fail"
    - "CXX baseline freshness is checked by the same gate invocation (no separate step)"
    - "Python and Node parity gates remain green after Phase 3 and Phase 4 changes"
  artifacts:
    - path: ".github/workflows/ci-cpp.yml"
      provides: "CXX parity gate CI job with downstream dependencies"
      contains: "cxx-parity-gate"
    - path: "tools/test_triple_gate_failure.py"
      provides: "Triple-gate canary injection assertion script"
      contains: "_ci05_canary"
  key_links:
    - from: ".github/workflows/ci-cpp.yml::cxx-parity-gate"
      to: "tools/cxx_api_parity/check_parity_gate.py"
      via: "python tools/cxx_api_parity/check_parity_gate.py --repo-root ."
      pattern: "check_parity_gate\\.py --repo-root"
    - from: ".github/workflows/ci-cpp.yml::cli-tests"
      to: ".github/workflows/ci-cpp.yml::cxx-parity-gate"
      via: "needs: [cxx-parity-gate]"
      pattern: "needs:.*cxx-parity-gate"
    - from: ".github/workflows/ci-cpp.yml::gui-tests"
      to: ".github/workflows/ci-cpp.yml::cxx-parity-gate"
      via: "needs: [cxx-parity-gate]"
      pattern: "needs:.*cxx-parity-gate"
    - from: "tools/test_triple_gate_failure.py"
      to: "ClassicLib-rs/foundation/classic-shared-core/src/lib.rs"
      via: "canary injection and revert"
      pattern: "_ci05_canary"
---

<objective>
Add the CXX parity gate as a lightweight CI job in ci-cpp.yml that gates the expensive MSVC build jobs, create the triple-gate assertion script that proves all three gates detect undeclared APIs, and verify CI-01/CI-02/CI-06 coverage.

Purpose: Wire all three parity gates into CI enforcement so no PR can merge with broken parity; prove the triple-gate invariant locally.
Output: Modified ci-cpp.yml with cxx-parity-gate job, new tools/test_triple_gate_failure.py script.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-enforcement/05-CONTEXT.md
@.planning/phases/05-ci-enforcement/05-RESEARCH.md

<interfaces>
<!-- Existing CI workflow job structure to mirror -->

From .github/workflows/ci-python-bindings.yml (template pattern):
```yaml
parity-gates:
  name: Python Parity Gates
  runs-on: windows-latest
  timeout-minutes: 30
  steps:
    - uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: "3.12"
    - name: Run Tier-1 Python parity and runtime coverage gate
      run: python tools/python_api_parity/check_parity_gate.py --repo-root .
    - name: Upload parity and runtime coverage diagnostics
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: python-parity-diagnostics
        path: ClassicLib-rs/python-bindings/parity-artifacts/
        if-no-files-found: warn
        retention-days: 7

build-and-test:
  name: Python Bindings Build + Smoke Tests
  needs: [parity-gates]
```

From .github/workflows/ci-cpp.yml (current job IDs):
```yaml
# Current jobs (no dependencies between them):
cli-tests:
  name: CLI Tests
  runs-on: windows-latest
  timeout-minutes: 90

gui-tests:
  name: GUI Tests
  runs-on: windows-latest
  timeout-minutes: 120
```

Gate script CLI interface (all three share this):
```
python tools/cxx_api_parity/check_parity_gate.py --repo-root .
python tools/python_api_parity/check_parity_gate.py --repo-root .
python tools/node_api_parity/check_parity_gate.py --repo-root .
```

CXX parity-artifacts output path:
```
ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CXX parity gate job to ci-cpp.yml (CI-03, CI-06, CI-01, CI-02)</name>
  <files>.github/workflows/ci-cpp.yml</files>
  <read_first>
    - .github/workflows/ci-cpp.yml
    - .github/workflows/ci-python-bindings.yml
    - .planning/phases/05-ci-enforcement/05-CONTEXT.md
  </read_first>
  <action>
Add a new `cxx-parity-gate` job to `.github/workflows/ci-cpp.yml` that runs BEFORE the existing `cli-tests` and `gui-tests` jobs. The job must be inserted as the FIRST job in the `jobs:` section (before `cli-tests`).

**New job to add (insert before `cli-tests:`):**

```yaml
  cxx-parity-gate:
    name: CXX Parity Gate
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Run CXX parity gate
        run: python tools/cxx_api_parity/check_parity_gate.py --repo-root .

      - name: Upload CXX parity diagnostics
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: cxx-parity-diagnostics
          path: ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/
          if-no-files-found: warn
          retention-days: 7
```

**Modify existing jobs to depend on the new gate:**

Add `needs: [cxx-parity-gate]` to both `cli-tests` and `gui-tests` jobs. Place `needs:` right after the `name:` field in each job. Example for `cli-tests`:

```yaml
  cli-tests:
    name: CLI Tests
    needs: [cxx-parity-gate]
    runs-on: windows-latest
```

Same for `gui-tests`:

```yaml
  gui-tests:
    name: GUI Tests
    needs: [cxx-parity-gate]
    runs-on: windows-latest
```

Per D-01: the gate blocks expensive MSVC builds. Per D-02: lightweight (checkout + Python only, no Rust/MSVC/vcpkg). Per D-03: runs only `check_parity_gate.py --repo-root .`. Per D-04: no modifications to other workflow files. Per D-05/D-07: CI-06 freshness is covered by this same `check_parity_gate.py` invocation (Phase 1 D-14 stale-artifact detection). Per D-06: no committed CXX headers, no separate freshness step.

The diagnostic artifact upload step (Claude's discretion) mirrors the pattern from `ci-python-bindings.yml` and `ci-typescript.yml` for consistency. It costs nothing when the gate passes.

CI-01 and CI-02 (per D-14): Observational verification only -- the Python gate (`ci-python-bindings.yml::parity-gates`) and Node gate (`ci-typescript.yml::parity-gates`) are already running in CI and green after Phase 3 and Phase 4 changes. No code changes needed. Verify by running `gh run list --workflow ci-python-bindings.yml --limit 1 --json conclusion` and `gh run list --workflow ci-typescript.yml --limit 1 --json conclusion` during the verify step.
  </action>
  <verify>
    <automated>python -c "import yaml; data=yaml.safe_load(open('.github/workflows/ci-cpp.yml')); jobs=data['jobs']; assert 'cxx-parity-gate' in jobs, 'missing cxx-parity-gate job'; gate=jobs['cxx-parity-gate']; assert gate['name']=='CXX Parity Gate'; assert gate['timeout-minutes']==10; assert 'cxx-parity-gate' in jobs['cli-tests'].get('needs',[]), 'cli-tests missing needs'; assert 'cxx-parity-gate' in jobs['gui-tests'].get('needs',[]), 'gui-tests missing needs'; print('PASS: ci-cpp.yml structure valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci-cpp.yml` contains a job with key `cxx-parity-gate`
    - The job's `name:` field is exactly `CXX Parity Gate`
    - The job's `timeout-minutes:` is `10`
    - The job's `runs-on:` is `windows-latest`
    - The job uses `actions/checkout@v6` and `actions/setup-python@v6` with `python-version: "3.12"`
    - The job has a step that runs `python tools/cxx_api_parity/check_parity_gate.py --repo-root .`
    - The job has a conditional failure artifact upload step using `actions/upload-artifact@v6` with path `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/`
    - `cli-tests` job contains `needs: [cxx-parity-gate]`
    - `gui-tests` job contains `needs: [cxx-parity-gate]`
    - The job does NOT install Rust, MSVC, or vcpkg
    - No other workflow files are modified
  </acceptance_criteria>
  <done>The CXX parity gate job exists in ci-cpp.yml with correct structure, both downstream jobs depend on it, and no other workflows are changed.</done>
</task>

<task type="auto">
  <name>Task 2: Create triple-gate assertion script (CI-05)</name>
  <files>tools/test_triple_gate_failure.py</files>
  <read_first>
    - ClassicLib-rs/foundation/classic-shared-core/src/lib.rs
    - tools/cxx_api_parity/check_parity_gate.py
    - tools/python_api_parity/check_parity_gate.py
    - tools/node_api_parity/check_parity_gate.py
    - .planning/phases/05-ci-enforcement/05-CONTEXT.md
    - .planning/phases/05-ci-enforcement/05-RESEARCH.md
  </read_first>
  <action>
Create `tools/test_triple_gate_failure.py` -- a local-only Python script that proves the triple-gate invariant (CI-05) per D-08, D-09, D-10.

The script must:

1. Accept `--repo-root` (default `.`) and `--verbose` flags via argparse.
2. Read the original content of `ClassicLib-rs/foundation/classic-shared-core/src/lib.rs`.
3. In a `try/finally` block:
   a. Append `\npub fn _ci05_canary() {}\n` to the end of `lib.rs`.
   b. Run all three gate scripts using `sys.executable` (not bare `python`):
      - `sys.executable tools/cxx_api_parity/check_parity_gate.py --repo-root {repo_root}`
      - `sys.executable tools/python_api_parity/check_parity_gate.py --repo-root {repo_root}`
      - `sys.executable tools/node_api_parity/check_parity_gate.py --repo-root {repo_root}`
   c. Capture each script's return code, stdout, and stderr.
   d. If `--verbose`, print each gate's stdout and stderr.
4. In the `finally` block: restore `lib.rs` to its original content (handles crashes and KeyboardInterrupt).
5. Assert all three gates returned non-zero exit codes.
6. Print a summary table:
   ```
   === Triple-Gate Canary Test (CI-05) ===
     CXX:    rc=1 -> FAIL (expected)
     Python: rc=1 -> FAIL (expected)
     Node:   rc=1 -> FAIL (expected)

   TRIPLE-GATE TEST: PASS -- all three gates detected the canary
   ```
7. Return exit code 0 if all three gates failed (expected behavior), exit code 1 if any gate passed (unexpected -- the invariant is broken).

Include:
- `#!/usr/bin/env python3` shebang line
- `from __future__ import annotations` import
- Module docstring explaining usage: `python tools/test_triple_gate_failure.py --repo-root .`
- Type annotations on all functions

Per D-09: The canary is injected into `classic-shared-core` because it is the foundation crate tracked by all three gates.
Per D-10: This script is local-only, not run in CI. Run once during Phase 5 verification and on-demand by maintainers.

IMPORTANT: Use `sys.executable` (not `"python"`) per Research Pitfall 5 to avoid Windows path issues. Use `Path` objects and convert to `str()` for subprocess arguments.
  </action>
  <verify>
    <automated>python tools/test_triple_gate_failure.py --repo-root . --verbose</automated>
  </verify>
  <acceptance_criteria>
    - `tools/test_triple_gate_failure.py` exists
    - File contains `#!/usr/bin/env python3` on the first line
    - File contains `from __future__ import annotations`
    - File contains `_ci05_canary` string literal for canary injection
    - File contains `sys.executable` (not bare `"python"`)
    - File contains `try:` and `finally:` for safe cleanup
    - File contains `argparse` with `--repo-root` and `--verbose` arguments
    - File contains `subprocess.run` calls for all three gate scripts
    - File references all three gate paths: `cxx_api_parity/check_parity_gate.py`, `python_api_parity/check_parity_gate.py`, `node_api_parity/check_parity_gate.py`
    - Running `python tools/test_triple_gate_failure.py --repo-root .` exits 0 and prints "TRIPLE-GATE TEST: PASS"
    - After script execution, `git diff ClassicLib-rs/foundation/classic-shared-core/src/lib.rs` shows no changes (canary was reverted)
  </acceptance_criteria>
  <done>The triple-gate assertion script exists, runs successfully (all three gates detect the canary), and cleanly reverts the source file.</done>
</task>

</tasks>

<verification>
1. `ci-cpp.yml` YAML is valid and contains the `cxx-parity-gate` job with correct structure
2. Both `cli-tests` and `gui-tests` jobs have `needs: [cxx-parity-gate]`
3. `tools/test_triple_gate_failure.py` runs and reports PASS (all three gates fail on canary injection)
4. `classic-shared-core/src/lib.rs` is clean after triple-gate test (no leftover canary)
5. No other workflow files were modified
</verification>

<success_criteria>
- ci-cpp.yml contains a lightweight CXX parity gate job that uses only checkout + Python 3.12
- CLI and GUI test jobs both depend on the CXX gate via `needs:`
- The triple-gate assertion script proves all three gates detect an undeclared pub fn in classic-shared-core
- The source file is safely restored after triple-gate test execution
- CI-06 is satisfied by the existing check_parity_gate.py stale-artifact detection (no separate step needed)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-enforcement/05-01-SUMMARY.md`
</output>
