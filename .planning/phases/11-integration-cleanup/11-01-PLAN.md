---
phase: 11-integration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib/integration/factory.py
  - ClassicLib/integration/rust/suspect_rust.py
  - ClassicLib/integration/rust/gpu_rust.py
  - ClassicLib/integration/rust/settings_rust.py
  - ClassicLib/integration/rust/fcx_rust.py
autonomous: true

must_haves:
  truths:
    - "Factory functions raise RuntimeError on Rust import failure (no fallbacks)"
    - "Wrapper modules import Rust directly without RUST_AVAILABLE checks"
    - "RUST_AVAILABLE flags removed from all wrapper modules"
  artifacts:
    - path: "ClassicLib/integration/factory.py"
      provides: "Factory functions returning Rust directly"
      contains: "raise RuntimeError"
    - path: "ClassicLib/integration/rust/suspect_rust.py"
      provides: "Rust-only SuspectScanner wrapper"
      min_lines: 50
    - path: "ClassicLib/integration/rust/gpu_rust.py"
      provides: "Rust-only GPU detector"
      min_lines: 20
    - path: "ClassicLib/integration/rust/settings_rust.py"
      provides: "Rust-only SettingsValidator wrapper"
      min_lines: 50
    - path: "ClassicLib/integration/rust/fcx_rust.py"
      provides: "Rust-only FCXModeHandler wrapper"
      min_lines: 50
  key_links:
    - from: "ClassicLib/integration/factory.py"
      to: "classic_scanlog"
      via: "direct import"
      pattern: "from classic_scanlog import"
    - from: "ClassicLib/integration/rust/*_rust.py"
      to: "classic_scanlog"
      via: "direct import (no fallback)"
      pattern: "from classic_scanlog import"
---

<objective>
Remove Python fallback code from factory and wrapper modules, making Rust required.

Purpose: Per CONTEXT.md decisions, Rust is required with hard fail on import errors. This eliminates RUST_AVAILABLE flags, Python fallback imports, and simplifies the codebase by removing conditional branches that are never taken (Rust is always available).

Output: Simplified factory.py and wrapper modules that import Rust directly and raise RuntimeError on failure.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-integration-cleanup/11-CONTEXT.md
@.planning/phases/11-integration-cleanup/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify factory functions to require Rust</name>
  <files>ClassicLib/integration/factory.py</files>
  <action>
Update factory functions to require Rust with hard fail on import errors:

1. `get_suspect_scanner()` - Remove RUST_AVAILABLE check from import line
   - Change from: `from ClassicLib.integration.rust.suspect_rust import RUST_AVAILABLE, SuspectScanner`
   - Change to: `from ClassicLib.integration.rust.suspect_rust import SuspectScanner`
   - Remove the `if RUST_AVAILABLE` / `else` logging branches
   - Keep single log line: `logger.debug("Using Rust-accelerated SuspectScanner")`

2. `get_settings_validator()` - Same pattern as above
   - Remove RUST_AVAILABLE import and conditional logging

3. `get_gpu_detector()` - Same pattern as above
   - Remove `gpu_rust.RUST_AVAILABLE` check and conditional logging

4. `get_fcx_handler()` - Same pattern as above
   - Remove RUST_AVAILABLE import and conditional logging

5. `get_database_pool()` - Require Rust, remove Python fallback
   - Remove try/except fallback to AsyncDatabasePool
   - Add RuntimeError raise if RustAsyncDatabasePool import fails
   - Pattern: wrap import in try/except, raise RuntimeError from ImportError

6. `get_yamldata()` - Require Rust, remove Python fallback
   - Remove try/except fallback to ClassicScanLogsInfo
   - Add RuntimeError raise if Rust YamlData import fails

DO NOT modify:
- `detect_component()`, `is_component_available()`, `get_component()` - These utilities are used elsewhere
- `validate_rust_modules()` - Startup validation still useful
- `get_parser()`, `get_file_io()`, etc. - Already require Rust
- `_COMPONENT_KEY_MAP` and `is_rust_accelerated()` - Backward compatibility, still needed

Factory function pattern after changes:
```python
def get_suspect_scanner(yamldata: ClassicScanLogsInfo) -> SuspectScannerProtocol:
    """Return a SuspectScanner instance (Rust required)."""
    from ClassicLib.integration.rust.suspect_rust import SuspectScanner
    logger.debug("Using Rust-accelerated SuspectScanner")
    return SuspectScanner(yamldata)
```
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.integration.factory import get_suspect_scanner, get_settings_validator, get_gpu_detector, get_fcx_handler, get_database_pool, get_yamldata; print('All factory imports work')"` passes without error
  </verify>
  <done>Factory functions for suspect_scanner, settings_validator, gpu_detector, fcx_handler, database_pool, and yamldata require Rust with no fallback code</done>
</task>

<task type="auto">
  <name>Task 2: Remove RUST_AVAILABLE from wrapper modules</name>
  <files>
ClassicLib/integration/rust/suspect_rust.py
ClassicLib/integration/rust/gpu_rust.py
ClassicLib/integration/rust/settings_rust.py
ClassicLib/integration/rust/fcx_rust.py
  </files>
  <action>
Update each wrapper module to import Rust directly without RUST_AVAILABLE detection:

**suspect_rust.py:**
1. Remove: `RUST_AVAILABLE, RustSuspectScanner = detect_component("classic_scanlog", "SuspectScanner")`
2. Remove: `if not RUST_AVAILABLE: RustSuspectScanner = None`
3. Add: Direct import at top of class methods: `from classic_scanlog import SuspectScanner as RustSuspectScanner`
4. Remove: `self._use_rust = RUST_AVAILABLE` - Always True now
5. Remove: Python fallback imports `from ClassicLib.scanning.logs.analyzers.SuspectScanner import SuspectScanner as PySuspectScannerImpl`
6. Remove: All `if self._use_rust` / `else` branches - keep only Rust path
7. Remove: `RUST_AVAILABLE` from `__all__` export
8. Remove: `from ClassicLib.integration.factory import detect_component` import

**gpu_rust.py:**
1. Remove: `RUST_AVAILABLE, RustGpuDetector = detect_component("classic_scanlog", "GpuDetector")`
2. Add: Direct import inside `get_gpu_info()`: `from classic_scanlog import GpuDetector`
3. Remove: `if RUST_AVAILABLE and RustGpuDetector is not None` check
4. Remove: Python fallback import `from ClassicLib.scanning.logs.analyzers.GPUDetector import get_gpu_info as py_get_gpu_info`
5. Remove: `RUST_AVAILABLE` from `__all__` export
6. Remove: `from ClassicLib.integration.factory import detect_component` import

**settings_rust.py:**
1. Remove: `RUST_AVAILABLE, RustSettingsValidator = detect_component("classic_scanlog", "SettingsValidator")`
2. Add: Direct import in `__init__`: `from classic_scanlog import SettingsValidator as RustSettingsValidator`
3. Remove: `self._use_rust = RUST_AVAILABLE` - Always True now
4. Remove: Python fallback import `from ClassicLib.scanning.logs.analyzers.SettingsScanner import SettingsScannerFragments`
5. Remove: All `if self._use_rust` / `else` branches - keep only Rust path
6. Remove: `RUST_AVAILABLE` from `__all__` export
7. Remove: `from ClassicLib.integration.factory import detect_component` import

**fcx_rust.py:**
1. Remove: `RUST_AVAILABLE, RustFcxModeHandler = detect_component("classic_scanlog", "FcxModeHandler")`
2. Remove: `if not RUST_AVAILABLE: RustFcxModeHandler = None`
3. Add: Direct import in `__init__`: `from classic_scanlog import FcxModeHandler as RustFcxModeHandler`
4. Remove: `self._use_rust = RUST_AVAILABLE` - Always True now
5. Remove: Python fallback import `from ClassicLib.scanning.logs.fcx_mode_handler import FCXModeHandlerFragments`
6. Remove: All `if self._use_rust` / `else` branches - keep only Rust path
7. Remove: `RUST_AVAILABLE` from `__all__` export
8. Remove: `from ClassicLib.integration.factory import detect_component` import
9. Remove: TYPE_CHECKING import of PythonFcxModeHandlerType

Each file should become significantly shorter with all conditional logic removed.
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.integration.rust.suspect_rust import SuspectScanner; from ClassicLib.integration.rust.gpu_rust import get_gpu_info; from ClassicLib.integration.rust.settings_rust import SettingsValidator; from ClassicLib.integration.rust.fcx_rust import FCXModeHandler; print('All wrapper imports work')"` passes without error
  </verify>
  <done>RUST_AVAILABLE flags removed from suspect_rust, gpu_rust, settings_rust, fcx_rust; all wrappers import Rust directly</done>
</task>

<task type="auto">
  <name>Task 3: Verify factory and wrapper integration</name>
  <files>tests/rust_integration/</files>
  <action>
Run the existing Rust integration tests to verify factory and wrapper changes work correctly:

1. Run parity tests to verify wrappers still function:
   ```bash
   uv run pytest tests/rust_integration/parity/ -v --tb=short
   ```

2. Run factory-related tests:
   ```bash
   uv run pytest tests/ -k "factory" -v --tb=short
   ```

3. Run a quick smoke test of the full test suite:
   ```bash
   uv run pytest tests/ -x --ignore=tests/golden --ignore=tests/parity -q
   ```

If any tests fail due to RUST_AVAILABLE references, update those test files:
- Remove imports of RUST_AVAILABLE from test files
- Remove skipif decorators that check RUST_AVAILABLE (Rust is always available now)
- Update any assertions that check RUST_AVAILABLE

DO NOT fix pre-existing test failures unrelated to this plan (there are 46 from Phase 8/9).
  </action>
  <verify>Parity tests in tests/rust_integration/parity/ pass; no new test failures introduced by factory/wrapper changes</verify>
  <done>Factory and wrapper changes verified with existing test suite; RUST_AVAILABLE references in tests updated if needed</done>
</task>

</tasks>

<verification>
1. All factory functions that previously had fallbacks now require Rust (RuntimeError on failure)
2. All wrapper modules in ClassicLib/integration/rust/ import Rust directly (no RUST_AVAILABLE)
3. Existing tests pass (excluding pre-existing failures from Phase 8/9)
4. `grep -r "RUST_AVAILABLE" ClassicLib/integration/rust/` returns no matches in modified files
</verification>

<success_criteria>
- Factory functions get_suspect_scanner, get_settings_validator, get_gpu_detector, get_fcx_handler, get_database_pool, get_yamldata require Rust
- RUST_AVAILABLE removed from suspect_rust.py, gpu_rust.py, settings_rust.py, fcx_rust.py
- Python fallback imports removed from all modified wrapper modules
- All conditional `if RUST_AVAILABLE` / `if self._use_rust` branches removed
- Existing Rust parity tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-cleanup/11-01-SUMMARY.md`
</output>
