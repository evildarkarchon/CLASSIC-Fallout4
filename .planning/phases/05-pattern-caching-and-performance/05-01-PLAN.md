---
phase: 05-pattern-caching-and-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
  - docs/api/classic-scanlog-core.md
autonomous: true
requirements: [PERF-01, CONS-04]
must_haves:
  truths:
    - "detect_mods_single, detect_mods_double, and detect_mods_batch reuse bounded process-wide compiled matcher caches keyed by normalized content hash"
    - "Any input-invariant regex introduced or retained in touched mod_detector code compiles through LazyLock at startup instead of per-call construction"
    - "Tests prove cache reuse and bounded behavior without asserting a specific eviction victim"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml"
      provides: "quick_cache dependency available to classic-scanlog-core"
      contains: "quick_cache = { workspace = true }"
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      provides: "Shared compile helpers, bounded matcher caches, and LazyLock static regex initialization"
      contains: "quick_cache::sync::Cache"
    - path: "docs/api/classic-scanlog-core.md"
      provides: "Contributor-facing note about bounded matcher-cache internals"
      contains: "mod_detector"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      to: "quick_cache::sync::Cache"
      via: "process-wide matcher cache initialization"
      pattern: "Cache::new\(64\)"
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      to: "std::sync::LazyLock"
      via: "static regex initialization"
      pattern: "LazyLock"
---

<objective>
Remove repeated regex compilation from the shared mod-detector hot paths first so the biggest reusable setup cost disappears without changing the important-mod matcher yet.

Purpose: PERF-01 and CONS-04 both live in `mod_detector.rs`, and the research explicitly allows a broader cleanup pass inside that touched file. This plan establishes the Phase 5 cache pattern in the core crate: shared normalization/compile helpers, bounded `LazyLock + quick_cache` caches keyed by normalized content hashes, and startup-time static regex compilation with `LazyLock`.
Output: Cached matcher compilation for `single`/`double`/`batch`, `LazyLock`-backed static regex cleanup, focused regression tests, and a brief API guide note for contributors.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
@.planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
@docs/api/README.md
@docs/api/classic-scanlog-core.md

<interfaces>
From `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs`:
```rust
pub fn detect_mods_single(...) -> bool { ... }
pub fn detect_mods_double(...) -> bool { ... }
pub fn detect_mods_batch(...) -> Vec<bool> { ... }
```

Pattern reference from Phase 4:
```rust
static SETTINGS_CACHE: LazyLock<Cache<u64, Arc<T>>> =
    LazyLock::new(|| Cache::new(64));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared bounded matcher caches for single/double/batch</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml, ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
    - ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs
    - ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs
    - .planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: repeated calls with the same normalized mod-list shape reuse a compiled matcher instead of rebuilding it.
    - Test 2: the shared caches stay bounded and expose reuse behavior without asserting exact eviction order.
    - Test 3: `detect_mods_batch` preserves existing match results while reusing the same compiled matcher across repeated batch invocations with the same normalized input.
  </behavior>
  <action>Per D-06, D-07, and D-08, first add `quick_cache = { workspace = true }` to `ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml`, then write or update focused tests inside the existing inline `#[cfg(test)] mod tests` block in `mod_detector.rs` (not a new `src/tests/mod.rs` file). After the tests fail for the right reason, factor the duplicated normalization and regex compilation setup in `detect_mods_single`, `detect_mods_double`, and `detect_mods_batch` behind shared private helpers. Add one bounded `LazyLock<quick_cache::sync::Cache<...>>` per matcher family, keyed by a normalized content hash per D-08, and store `Arc<Regex>` or the equivalent compiled matcher value so cache hits stay cheap. Keep caches process-wide and bounded, preserve existing outputs, and treat `detect_mods_batch` as a cross-call reuse optimization rather than a within-Rayon-loop hoist.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains `quick_cache::sync::Cache`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains `LazyLock`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains `Cache::new(64)`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains `xxh3`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_</automated>
  </verify>
  <done>`single`/`double`/`batch` matcher compilation is cached process-wide behind bounded content-hash keyed helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Audit touched mod_detector regex setup and use LazyLock only for true constants</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs, docs/api/classic-scanlog-core.md</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
    - docs/api/classic-scanlog-core.md
    - .planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
  </read_first>
  <action>Review the touched `mod_detector` code after Task 1 and only introduce `std::sync::LazyLock` statics for regexes that are actually input-invariant. Do not invent fake statics or claim a conversion from old lazy-init code that does not exist. If every remaining regex in the file is input-derived from YAML/config content, keep those on the new bounded compile-cache path and make the docs note explicit that Phase 5 removed per-call hot-path construction without broadening into a repo-wide Lazy sweep. If any truly constant regex helper remains or is added during the refactor, compile it with `Regex::new(...).unwrap()` behind `LazyLock` per CONS-04.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains `LazyLock` for any retained constant regex helper, or contains no remaining per-call constant regex construction in touched code
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` does not contain `once_cell::sync::Lazy` in touched code
    - `docs/api/classic-scanlog-core.md` explains the bounded matcher-cache pattern now used in `mod_detector`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>The touched static regexes compile once through `LazyLock`, and the contributor docs record the new hot-path setup pattern.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_` passes.
- `cargo clippy -p classic-scanlog-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` passes.
- `docs/api/classic-scanlog-core.md` mentions the bounded matcher-cache/internal LazyLock pattern for Phase 5 contributors.
</verification>

<success_criteria>
- PERF-01: `detect_mods_single`, `detect_mods_double`, and `detect_mods_batch` reuse bounded compiled matcher caches keyed by normalized content hashes.
- CONS-04: no input-invariant regex in touched `mod_detector` code compiles per call; any retained constant regex helper uses `LazyLock` and `Regex::new(...).unwrap()`.
- No new manual invalidation surface is added for these caches.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-caching-and-performance/05-01-SUMMARY.md`
</output>
