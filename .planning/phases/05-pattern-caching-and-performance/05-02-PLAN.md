---
phase: 05-pattern-caching-and-performance
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "detect_mods_important uses one Aho-Corasick matcher over the combined lowercase text surface"
    - "LeftmostLongest semantics are used for overlap resolution"
    - "The legacy regex-per-entry behavior remains available until fixture-backed parity is proven"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      provides: "Aho-Corasick matcher path plus legacy parity helper"
      contains: "MatchKind::LeftmostLongest"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      to: "aho_corasick::AhoCorasick"
      via: "important-mod literal matcher"
      pattern: "MatchKind::LeftmostLongest"
---

<objective>
Replace the highest-cost literal matching path with a compiled Aho-Corasick automaton while explicitly proving behavior parity before the old regex implementation disappears.

Purpose: PERF-02 is the main semantic-risk item of Phase 5. The phase research and state blocker both say parity proof is non-negotiable, so this plan isolates the risky matcher swap and makes parity tests the first-class deliverable.
Output: A new `detect_mods_important` Aho-Corasick path, a retained private legacy helper until parity passes, and fixture-backed parity tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
@.planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md

<interfaces>
From `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs`:
```rust
pub fn detect_mods_important(...) -> Vec<String> { ... }
```

Required matcher semantics:
```rust
use aho_corasick::{AhoCorasick, MatchKind};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixture-backed parity coverage for important-mod detection</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
    - .planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: current fixtures produce identical output from the legacy regex path and the new Aho path.
    - Test 2: overlap-sensitive literals prefer the leftmost-longest result when they start at the same position.
    - Test 3: existing GPU, exclude_when, and "not installed" output quirks remain unchanged.
  </behavior>
  <action>Per D-04 and the STATE.md blocker, add parity-focused tests before swapping implementations, using the existing inline `#[cfg(test)] mod tests` block in `mod_detector.rs` rather than creating a separate test file. Move the current regex-per-entry behavior behind a private helper in the same file so tests can compare legacy and new outputs directly, per research recommendation. Use existing scanlog fixtures where possible, add the smallest synthetic overlap case needed to prove `LeftmostLongest` behavior, and add one defensive assertion that the current `CoreModEntry.detect` values exercised by fixtures behave as literals rather than regex syntax.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains parity coverage for `detect_mods_important` in the inline test module
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains a private legacy helper for parity comparison
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_important</automated>
  </verify>
  <done>Parity coverage blocks semantic drift before the implementation swap lands.</done>
</task>

<task type="auto">
  <name>Task 2: Switch detect_mods_important to Aho-Corasick over the combined lowercase haystack</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
    - .planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
    - .planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
  </read_first>
  <action>Build one lowercased literal dictionary from `entry.detect`, compile an `AhoCorasick` matcher with `MatchKind::LeftmostLongest`, and search the existing combined lowercase plugin/XSE text surface per D-01, D-02, and D-03 without restructuring the rest of the function. Treat Aho matches as detection evidence only; preserve existing source-order gating, `exclude_when`, GPU handling, and output formatting logic. If cross-call reuse is straightforward after 05-01's matcher-cache helpers land, reuse the same bounded hash-keyed cache pattern for the automaton; otherwise keep this plan at one-automaton-per-call and explain that tradeoff in the summary. Keep the legacy helper in place until the parity coverage is green; only remove it if the new tests fully prove equivalence within this plan.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains `AhoCorasick`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` contains `MatchKind::LeftmostLongest`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs` does not compile a new `Regex` inside the important-mod entry loop
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_important</automated>
  </verify>
  <done>`detect_mods_important` uses one compiled literal matcher with parity coverage guarding the change.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_important` passes.
- Existing `detect_mods_important` tests remain green with no output drift.
- The legacy regex path is not removed until fixture-backed parity is proven in the same plan.
</verification>

<success_criteria>
- PERF-02: `detect_mods_important` uses Aho-Corasick with `LeftmostLongest` semantics over one combined lowercase text surface.
- The parity blocker recorded in `.planning/STATE.md` is satisfied by direct legacy-vs-new fixture coverage.
- The surrounding business logic remains unchanged apart from the literal match engine.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-caching-and-performance/05-02-SUMMARY.md`
</output>
