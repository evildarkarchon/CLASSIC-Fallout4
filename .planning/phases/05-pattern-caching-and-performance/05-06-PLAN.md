---
phase: 05-pattern-caching-and-performance
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md
  - performance_baselines/README.md
autonomous: true
requirements: [PERF-04]
gap_closure: true
must_haves:
  truths:
    - "Phase 5 has a committed, reproducible proof artifact showing before/after benchmark results for its locked hotspots"
    - "The repo docs no longer imply that Phase 5 owns mmap throughput benchmarking if that work belongs to Phase 6"
    - "Raw Criterion baselines remain local-only while the committed proof artifact captures the deltas needed for verification"
  artifacts:
    - path: ".planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md"
      provides: "Shareable Phase 5 benchmark comparison report with commands, groups, and measured deltas"
      contains: "phase5_cached_regex_paths"
    - path: "performance_baselines/README.md"
      provides: "Contributor instructions for producing the same proof artifact from local Criterion baselines"
      contains: "phase5-before"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement boundary aligned so Phase 5 PERF-04 and Phase 6 SAFE-05 do not conflict"
      contains: "PERF-04"
  key_links:
    - from: ".planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md"
      to: "ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs"
      via: "named Phase 5 benchmark groups and recorded commands"
      pattern: "phase5_"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 6 mmap TOCTOU Safety"
      via: "requirement wording / ownership clarification"
      pattern: "SAFE-05|mmap"
---

<objective>
Close the remaining PERF-04 proof gap by checking in a reproducible Phase 5 benchmark report and aligning requirement wording so mmap throughput evidence lives with the correct phase.

Purpose: Verification confirmed the harness exists but found no committed proof artifact and flagged a requirement mismatch: `.planning/REQUIREMENTS.md` still assigns mmap throughput measurement to Phase 5 even though the roadmap already scopes mmap safety work to Phase 6. This plan fixes both evidence and traceability.
Output: A committed Phase 5 benchmark-proof report, updated local proof instructions, and aligned requirement wording.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
@.planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
@.planning/phases/05-pattern-caching-and-performance/05-VERIFICATION.md
@.planning/phases/05-pattern-caching-and-performance/05-04-SUMMARY.md
@ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
@performance_baselines/README.md

<interfaces>
Existing Phase 5 benchmark groups from `ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs`:
```rust
fn phase5_hotspot_benchmarks(c: &mut Criterion) {
    // groups: phase5_cached_regex_paths,
    //         phase5_detect_mods_important,
    //         phase5_bridge_crash_pattern_replica
}

criterion_group! {
    targets = /* ... */, phase5_hotspot_benchmarks
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit a shareable Phase 5 benchmark proof report</name>
  <files>.planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md, performance_baselines/README.md</files>
  <action>Use the existing `scanlog_benchmarks` harness per D-09 and D-10 to run a local save/compare workflow for the Phase 5 groups only, then commit a markdown proof artifact at `.planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md`. The report must capture the exact commands used, the benchmark groups compared, the measured deltas for cached regex paths, important-mod matching, and bridge-style crash-pattern parsing, plus whether each hotspot cleared the repo's 5% warning threshold per D-12. Keep raw Criterion output local per D-11; the committed artifact should summarize the results, not vendor `target/criterion` data. Update `performance_baselines/README.md` so contributors can reproduce the same report workflow instead of only seeing generic baseline commands.</action>
  <verify>
    <automated>$env:BENCH_MODE='thorough'; cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks -- --save-baseline phase5-gap-before; cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks -- --baseline phase5-gap-before</automated>
  </verify>
  <done>A committed proof report exists and cites real Phase 5 benchmark deltas without checking in raw Criterion baseline directories.</done>
</task>

<task type="auto">
  <name>Task 2: Align PERF-04 wording with the roadmap's Phase 6 mmap ownership</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md</files>
  <action>Clarify `.planning/REQUIREMENTS.md` so PERF-04 covers the locked Phase 5 hotspots actually implemented in `scanlog_benchmarks`, while mmap throughput benchmarking is explicitly owned by Phase 6 / SAFE-05. Preserve traceability instead of silently deleting context: note the clarification in `05-BENCHMARK-PROOF.md` so future verification can see why mmap evidence is absent from Phase 5. Do not move unrelated requirements or broaden scope into implementing the mmap benchmark here.</action>
  <verify>
    <automated>cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks -- --test</automated>
  </verify>
  <done>Phase 5 proof and requirements no longer conflict about mmap throughput ownership, and PERF-04 is verifiable against committed Phase 5 artifacts.</done>
</task>

</tasks>

<verification>
- The existing scanlog benchmark harness still runs via `cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks -- --test`.
- `.planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md` records real before/after deltas for all Phase 5 hotspot groups.
- `.planning/REQUIREMENTS.md` no longer assigns mmap throughput proof to Phase 5 if the roadmap keeps that work in Phase 6.
</verification>

<success_criteria>
- The verifier's second gap is closed with both evidence and requirement alignment.
- Phase 5 has a committed proof artifact that future reviewers can inspect without needing local raw baselines.
- Phase 6 retains sole ownership of mmap throughput validation, avoiding duplicated or contradictory milestone contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-caching-and-performance/05-06-SUMMARY.md`
</output>
