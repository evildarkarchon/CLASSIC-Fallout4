---
phase: 05-pattern-caching-and-performance
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
autonomous: true
requirements: [PERF-01]
gap_closure: true
must_haves:
  truths:
    - "The focused Phase 5 detector regression command passes reliably under grouped runs"
    - "Repeated `detect_mods_double` matcher lookups for the same conflict set still compile once and reuse the cached regex"
    - "Test proof for matcher reuse does not depend on unrelated global compile-counter state"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      provides: "Deterministic matcher-cache regression helpers and grouped-run-safe reuse tests"
      contains: "test_detect_mods_double_reuses_cached_matcher_for_same_conflict_set"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      to: "DOUBLE_MATCHER_CACHE"
      via: "test-only reset/snapshot helper"
      pattern: "DOUBLE_MATCHER_CACHE"
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      to: "get_double_matcher"
      via: "cache-reuse regression proof"
      pattern: "get_double_matcher"
---

<objective>
Stabilize the Phase 5 matcher-cache regression proof so the focused detector test suite passes under grouped runs, not just in isolation.

Purpose: Verification found that the cache-reuse proof for `detect_mods_double` is flaky because it reads a process-global compile counter as an absolute value. This plan closes that proof gap without weakening the bounded process-wide cache design from Phase 5.
Output: Deterministic matcher-cache tests in `mod_detector.rs` plus passing grouped detector verification commands.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
@.planning/phases/05-pattern-caching-and-performance/05-VERIFICATION.md
@.planning/phases/05-pattern-caching-and-performance/05-01-SUMMARY.md

<interfaces>
From `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs`:
```rust
fn get_double_matcher(entries: &[ModConflictEntry]) -> Result<Arc<Regex>>;

#[cfg(test)]
fn reset_matcher_caches_for_tests();

#[test]
#[serial]
fn test_detect_mods_double_reuses_cached_matcher_for_same_conflict_set();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the double-matcher reuse proof counter-isolated</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs</files>
  <behavior>
    - Test 1: `test_detect_mods_double_reuses_cached_matcher_for_same_conflict_set` passes when run alone and when run inside `detect_mods_double`/`detect_mods_` grouped filters.
    - Test 2: The proof asserts compile-count deltas for the scoped run, not a process-global absolute counter value polluted by other tests.
  </behavior>
  <action>Update the Phase 5 matcher-cache tests in `mod_detector.rs` so the double-matcher reuse assertion measures only the compiles triggered by that test run. Keep the production cache process-wide and bounded per D-06, D-07, and D-08; do not solve this by introducing a production reset hook, per-run cache, or weaker cache contract. Prefer a test-only snapshot/delta helper or similarly explicit scoped assertion so grouped runs remain deterministic while still proving `Arc` reuse and one compile for one normalized conflict set.</action>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml test_detect_mods_double_reuses_cached_matcher_for_same_conflict_set -- --exact</automated>
  </verify>
  <done>The reuse test proves one scoped compile plus cache reuse without depending on unrelated matcher tests.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify grouped detector cache coverage stays green</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs</files>
  <action>Keep the focused cache-regression coverage stable under the same grouped commands the verifier used. If helper placement or shared-state cleanup still causes grouped-run failures, tighten the test-only reset/snapshot flow in `mod_detector.rs` until both grouped filters pass. Do not remove the cache-reuse assertion; the goal is deterministic proof, not reduced coverage.</action>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_double && cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_</automated>
  </verify>
  <done>The Phase 5 detector verification command passes with no matcher-cache regression failures.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml test_detect_mods_double_reuses_cached_matcher_for_same_conflict_set -- --exact` passes.
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_double` passes.
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_` passes.
</verification>

<success_criteria>
- The verifier's first gap is closed: matcher-cache proof is deterministic under grouped runs.
- Phase 5 still proves the original PERF-01 cache-reuse behavior instead of downgrading the test.
- No new production cache lifecycle hooks are introduced just to satisfy test isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-caching-and-performance/05-05-SUMMARY.md`
</output>
