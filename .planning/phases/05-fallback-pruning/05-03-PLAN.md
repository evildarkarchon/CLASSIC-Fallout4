---
phase: 05-fallback-pruning
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - CLASSIC_Interface.py
  - CLASSIC_ScanLogs.py
  - CLASSIC.spec
  - CLASSIC-CLI.spec
  - CLASSIC-GUI-OneFile.spec
  - CLASSIC-QML.spec
  - CLASSIC-QML-Dir.spec
  - CLASSIC-Test.spec
autonomous: false

must_haves:
  truths:
    - "Application refuses to start with clear error message if any required Rust module is missing"
    - "All 6 spec files have no stale hiddenimports referencing deleted modules"
    - "PyInstaller build succeeds without errors"
    - "Built executable launches GUI without ModuleNotFoundError"
  artifacts:
    - path: "CLASSIC_Interface.py"
      provides: "Startup Rust module validation"
      contains: "validate_rust_modules"
    - path: "CLASSIC_ScanLogs.py"
      provides: "Startup Rust module validation"
      contains: "validate_rust_modules"
    - path: "CLASSIC.spec"
      provides: "Clean hiddenimports (no stale references)"
  key_links:
    - from: "CLASSIC_Interface.py"
      to: "ClassicLib/integration/factory.py"
      via: "detect_component calls for startup validation"
      pattern: "detect_component"
---

<objective>
Add startup Rust module validation to both entry points, clean all 6 PyInstaller spec files of stale hiddenimports, build with PyInstaller, and verify the executable launches.

Purpose: Ensure users get a clear error message if Rust modules are missing, and the PyInstaller build is clean.
Output: Both entry points validate Rust modules at startup. Spec files cleaned. Build verified.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fallback-pruning/05-RESEARCH.md
@.planning/phases/05-fallback-pruning/05-02-SUMMARY.md
@CLASSIC_Interface.py
@CLASSIC_ScanLogs.py
@CLASSIC.spec
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add startup validation and clean spec files</name>
  <files>
    CLASSIC_Interface.py
    CLASSIC_ScanLogs.py
    CLASSIC.spec
    CLASSIC-CLI.spec
    CLASSIC-GUI-OneFile.spec
    CLASSIC-QML.spec
    CLASSIC-QML-Dir.spec
    CLASSIC-Test.spec
  </files>
  <action>
    **1. Create startup validation function:**

    Add a `validate_rust_modules()` function that checks all required Rust modules are importable. Place it in `ClassicLib/integration/factory.py` (near the top, after imports) since `detect_component` already lives there. The function should:
    - Check each required Rust component using `detect_component(module_name, class_name)`
    - On first failure, raise `RuntimeError` with the module name, the import error, and a message suggesting reinstallation
    - Required modules to check (verify these by reading factory.py's factory functions):
      - `classic_scanlog.LogParser`
      - `classic_scanlog.FormIDAnalyzerCore` (or whatever the formid Rust export is named)
      - `classic_scanlog.PluginAnalyzer`
      - `classic_scanlog.RecordScanner`
      - `classic_scanlog.ReportGenerator` (or whatever the report Rust export is named)
      - `classic_scanlog.detect_mods_single` (or the mod detector export)
      - `classic_file_io.FileIOCore`
      - `classic_yaml.YamlOperations` (or `RustYamlOperations`)

    IMPORTANT: Read the actual factory functions to determine the exact module and class names. Do not assume -- the research provides approximate names.

    **2. EDIT entry points to wire in validation:**

    EDIT `CLASSIC_Interface.py`: Add near the top (before QApplication creation) an import and call:
    ```python
    from ClassicLib.integration.factory import validate_rust_modules
    validate_rust_modules()
    ```
    Read the file first to find the right insertion point (after existing imports, before QApplication or main window creation).

    EDIT `CLASSIC_ScanLogs.py`: Add near the top (before main logic) an import and call:
    ```python
    from ClassicLib.integration.factory import validate_rust_modules
    validate_rust_modules()
    ```
    Read the file first to find the right insertion point (after existing imports, before the main scanning logic begins).

    **3. Clean all 6 spec files:**

    Read each spec file and remove stale `hiddenimports` entries. Per research, these references are stale from prior phases:
    - `ClassicLib.integration.config` (deleted in Phase 2)
    - `ClassicLib.integration.status` (deleted in Phase 2)
    - `ClassicLib.integration.detector` (deleted in Phase 2)
    - Any `ClassicLib.integration.python.*` references (deleted in Plans 01-02)

    Do NOT remove valid hiddenimports. Read each spec file to understand what's there before editing.

    Spec files to clean: `CLASSIC.spec`, `CLASSIC-CLI.spec`, `CLASSIC-GUI-OneFile.spec`, `CLASSIC-QML.spec`, `CLASSIC-QML-Dir.spec`, `CLASSIC-Test.spec`

    **4. Run tests:**
    `uv run pytest -n auto` to verify nothing broke.
  </action>
  <verify>
    `uv run pytest -n auto` passes.
    `grep -c "def validate_rust_modules" ClassicLib/integration/factory.py` returns 1 (function exists in factory.py).
    `grep -r "validate_rust_modules" CLASSIC_Interface.py CLASSIC_ScanLogs.py` shows both entry points call it.
    `grep "ClassicLib.integration.config\|ClassicLib.integration.status\|ClassicLib.integration.detector\|ClassicLib.integration.python" *.spec` returns no results.
  </verify>
  <done>validate_rust_modules() function created in factory.py and called by both entry points. All 6 spec files cleaned of stale references. Test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PyInstaller build and smoke test</name>
  <what-built>
    Complete fallback pruning: all 8 Python fallback files removed, factory.py converted to hard-error pattern, startup validation added, spec files cleaned.
  </what-built>
  <how-to-verify>
    1. Run PyInstaller build: `uv run pyinstaller --clean CLASSIC.spec`
       (Adjust UPX path if needed: `uv run pyinstaller --clean --upx-dir 'C:\Path\to\UPX' CLASSIC.spec`)
    2. Check build output for errors -- especially missing module warnings
    3. Launch the built executable
    4. Verify: GUI opens without `ModuleNotFoundError` or startup crash
    5. Optional: Run a crash log scan to verify end-to-end functionality
  </how-to-verify>
  <resume-signal>Type "approved" if GUI launches successfully, or describe any errors encountered.</resume-signal>
</task>

</tasks>

<verification>
1. `uv run pytest -n auto` passes
2. Both entry points call `validate_rust_modules()` at startup
3. All 6 spec files have no stale hiddenimports
4. PyInstaller build completes without errors
5. Built executable launches GUI without ModuleNotFoundError
</verification>

<success_criteria>
- validate_rust_modules() function exists in factory.py and is called by both entry points
- All 6 spec files cleaned of stale references from prior phases
- PyInstaller build succeeds
- User confirms executable launches without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-fallback-pruning/05-03-SUMMARY.md`
</output>
