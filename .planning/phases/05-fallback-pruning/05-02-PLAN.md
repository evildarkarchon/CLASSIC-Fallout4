---
phase: 05-fallback-pruning
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - ClassicLib/integration/python/parser_py.py
  - ClassicLib/integration/python/mod_detector_py.py
  - ClassicLib/integration/python/plugin_py.py
  - ClassicLib/integration/python/__init__.py
  - ClassicLib/integration/factory.py
  - ClassicLib/integration/rust/parser_rust.py
  - ClassicLib/integration/rust/mod_detector_rust.py
  - ClassicLib/integration/rust/plugin_rust.py
  - tests/python_fallback/test_parser_py_fallback_unit.py
  - tests/python_fallback/test_mod_detector_py_unit.py
  - tests/python_fallback/test_plugin_py_unit.py
autonomous: true

must_haves:
  truths:
    - "ClassicLib/integration/python/ directory no longer exists"
    - "parser_rust.py, mod_detector_rust.py, plugin_rust.py contain no imports from integration.python"
    - "factory.py contains no PythonParserWrapper class"
    - "All tests/python_fallback/ test files are deleted and the directory is removed"
    - "uv run pytest -n auto passes with no regressions"
  artifacts:
    - path: "ClassicLib/integration/factory.py"
      provides: "All factory functions raise RuntimeError on missing Rust (zero fallbacks)"
      contains: "raise RuntimeError"
    - path: "ClassicLib/integration/rust/parser_rust.py"
      provides: "Parser wrapper with no fallback imports"
    - path: "ClassicLib/integration/rust/mod_detector_rust.py"
      provides: "Mod detector wrapper with no fallback imports"
    - path: "ClassicLib/integration/rust/plugin_rust.py"
      provides: "Plugin wrapper with no fallback imports"
  key_links:
    - from: "ClassicLib/integration/rust/plugin_rust.py"
      to: "classic_scanlog"
      via: "Direct Rust import only (no Python fallback)"
      pattern: "import classic_scanlog"
---

<objective>
Remove the 3 "hard" Python fallback files (parser_py.py, mod_detector_py.py, plugin_py.py) that have rust-wrapper inline fallback references, update the 3 rust wrapper modules to remove their fallback import paths, remove PythonParserWrapper from factory.py, and delete the entire integration/python/ directory.

Purpose: Complete the fallback removal -- zero Python fallbacks remain, integration/python/ directory eliminated.
Output: integration/python/ directory deleted, all 3 rust wrappers cleaned of fallback imports, factory.py fully converted to hard-error pattern.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fallback-pruning/05-RESEARCH.md
@.planning/phases/05-fallback-pruning/05-01-SUMMARY.md
@ClassicLib/integration/factory.py
@ClassicLib/integration/rust/parser_rust.py
@ClassicLib/integration/rust/mod_detector_rust.py
@ClassicLib/integration/rust/plugin_rust.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove parser_py.py and mod_detector_py.py</name>
  <files>
    ClassicLib/integration/python/parser_py.py
    ClassicLib/integration/python/mod_detector_py.py
    ClassicLib/integration/python/__init__.py
    ClassicLib/integration/factory.py
    ClassicLib/integration/rust/parser_rust.py
    ClassicLib/integration/rust/mod_detector_rust.py
    tests/python_fallback/test_parser_py_fallback_unit.py
    tests/python_fallback/test_mod_detector_py_unit.py
  </files>
  <action>
    Remove parser_py.py and mod_detector_py.py one-by-one, running tests after each.

    **1. parser_py.py** (factory + 1 wrapper ref + PythonParserWrapper):

    Read `factory.py` and find:
    - The `PythonParserWrapper` class (research says it lazily imports from parser_py.py at line 189). Delete the entire class.
    - The `get_parser()` factory function's fallback path that returns `PythonParserWrapper`. Replace with `raise RuntimeError(...)`.
    - Remove `PythonParserWrapper` from `__all__` if listed.

    Read `parser_rust.py` and find:
    - Line 84 (per research): a fallback import from `ClassicLib.integration.python.parser_py`. Remove the entire fallback code path. If the method cannot proceed without the fallback, raise RuntimeError instead.

    Then:
    - Delete `ClassicLib/integration/python/parser_py.py`
    - Remove exports from `__init__.py`
    - Delete `tests/python_fallback/test_parser_py_fallback_unit.py`
    - Run `uv run pytest -n auto`

    **2. mod_detector_py.py** (factory + 3 wrapper refs):

    Read `mod_detector_rust.py` and find the 3 fallback import sites (research says lines 105, 143, 193). For each site:
    - Remove the `from ClassicLib.integration.python.mod_detector_py import ...` lazy import
    - Remove the code path that uses the Python fallback
    - If the fallback was in an except clause after a Rust call failure, replace with `raise RuntimeError(...)` or re-raise the original error

    In `factory.py`:
    - Remove `get_mod_detector()` fallback import path, replace with RuntimeError

    Then:
    - Delete `ClassicLib/integration/python/mod_detector_py.py`
    - Remove exports from `__init__.py`
    - Delete `tests/python_fallback/test_mod_detector_py_unit.py`
    - Run `uv run pytest -n auto`

    IMPORTANT: Read each rust wrapper file BEFORE editing. The research line numbers may have shifted after Plan 01 edits to factory.py. Verify current state.
  </action>
  <verify>
    `uv run pytest -n auto` passes after both removals.
    `grep -r "parser_py\|mod_detector_py" ClassicLib/` returns no results.
    `grep -r "PythonParserWrapper" ClassicLib/` returns no results.
  </verify>
  <done>parser_py.py and mod_detector_py.py deleted. parser_rust.py and mod_detector_rust.py have zero fallback imports. PythonParserWrapper removed from factory.py. Test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove plugin_py.py and delete integration/python/ directory</name>
  <files>
    ClassicLib/integration/python/plugin_py.py
    ClassicLib/integration/python/__init__.py
    ClassicLib/integration/rust/plugin_rust.py
    ClassicLib/integration/factory.py
    tests/python_fallback/test_plugin_py_unit.py
  </files>
  <action>
    **1. plugin_py.py** (factory + 5 wrapper refs -- most complex):

    Read `plugin_rust.py` carefully. Research identifies 5 fallback import sites at lines 116, 166, 207, 241, 270. For each site:
    - Identify the lazy import: `from ClassicLib.integration.python.plugin_py import ...`
    - Identify the surrounding fallback logic (usually in except clauses after Rust call failures)
    - Remove the fallback code path entirely
    - Replace with `raise RuntimeError(f"Rust plugin analysis failed: {e}. Reinstall CLASSIC.") from e` or re-raise the original error
    - Ensure the method signature and return type remain consistent

    In `factory.py`:
    - Remove `get_plugin_analyzer()` fallback import path, replace with RuntimeError

    Then:
    - Delete `ClassicLib/integration/python/plugin_py.py`
    - Delete `tests/python_fallback/test_plugin_py_unit.py`
    - Run `uv run pytest -n auto`

    **2. Delete integration/python/ directory:**

    After plugin_py.py is removed, only `__init__.py` remains in `ClassicLib/integration/python/`.
    - Delete `ClassicLib/integration/python/__init__.py`
    - Delete the `ClassicLib/integration/python/` directory itself (use `git rm -r`)
    - Delete the `tests/python_fallback/` directory (should be empty, use `git rm -r`)
    - Run `uv run pytest -n auto` to confirm no imports reference the deleted directory

    IMPORTANT: After deleting the directory, grep the entire project for `from ClassicLib.integration.python` and `import ClassicLib.integration.python` to ensure zero stale references remain. Fix any found.
  </action>
  <verify>
    `uv run pytest -n auto` passes.
    `ls ClassicLib/integration/python/ 2>/dev/null` returns error (directory does not exist).
    `ls tests/python_fallback/ 2>/dev/null` returns error (directory does not exist).
    `grep -r "integration.python\|integration\.python" ClassicLib/ tests/` returns no results (excluding .planning/).
    `grep -r "plugin_py" ClassicLib/` returns no results.
  </verify>
  <done>All 8 fallback files deleted. integration/python/ directory eliminated. tests/python_fallback/ directory eliminated. plugin_rust.py has zero fallback imports. Full test suite passes. Zero Python fallbacks remain.</done>
</task>

</tasks>

<verification>
1. `uv run pytest -n auto` passes with no regressions
2. `ClassicLib/integration/python/` directory does not exist
3. `tests/python_fallback/` directory does not exist
4. `grep -r "from ClassicLib.integration.python" ClassicLib/ tests/` returns no results
5. `grep -r "PythonParserWrapper" ClassicLib/` returns no results
6. All 3 rust wrapper files (parser_rust.py, mod_detector_rust.py, plugin_rust.py) contain zero fallback imports
</verification>

<success_criteria>
- All 8 Python fallback files deleted (5 from Plan 01 + 3 from this plan)
- integration/python/ directory completely removed
- tests/python_fallback/ directory completely removed
- 3 rust wrapper modules cleaned of all fallback import paths
- PythonParserWrapper class removed from factory.py
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-fallback-pruning/05-02-SUMMARY.md`
</output>
