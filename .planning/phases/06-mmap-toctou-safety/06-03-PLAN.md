---
phase: 06-mmap-toctou-safety
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
autonomous: true
requirements: [SAFE-05]
gap_closure: true
must_haves:
  truths:
    - "Declared Phase 6 crate validation commands pass cleanly"
    - "The Phase 6 benchmark still compares `map()`, `map_copy()`, and `map_copy_read_only()` over the locked size set"
    - "Unsafe mmap constructors are isolated to narrowly allowed benchmark helpers instead of inline match-arm calls"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs"
      provides: "Scoped mmap benchmark helpers that preserve Phase 6 coverage while satisfying the crate lint gate"
      contains: "map_file_copy_read_only"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs"
      to: "memmap2::MmapOptions"
      via: "narrow helper functions for the three mmap variants"
      pattern: "map_file_shared|map_file_copy|map_file_copy_read_only"
    - from: "ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs"
      to: "read_file_with_variant"
      via: "variant match dispatch through helpers instead of inline unsafe blocks"
      pattern: "read_file_with_variant|MmapVariant"
---

<objective>
Close the Phase 6 verification gap by scoping the benchmark-only unsafe mmap constructors so the declared crate lint command passes without weakening the throughput proof.

Purpose: 06-VERIFICATION.md found SAFE-05 blocked only because `file_io_benchmarks.rs` inlines three `unsafe` mmap constructor calls that trip the workspace `unsafe_code` deny lint. This plan fixes that exact gap while preserving D-01 through D-06 and the existing `phase6_mmap_variants` benchmark contract.
Output: A lint-clean Phase 6 benchmark helper layout in `file_io_benchmarks.rs` plus green Phase 6 validation commands.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-mmap-toctou-safety/06-CONTEXT.md
@.planning/phases/06-mmap-toctou-safety/06-RESEARCH.md
@.planning/phases/06-mmap-toctou-safety/06-VALIDATION.md
@.planning/phases/06-mmap-toctou-safety/06-VERIFICATION.md
@.planning/phases/06-mmap-toctou-safety/06-02-SUMMARY.md
@ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
@ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs

<interfaces>
From `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs`:
```rust
enum MmapVariant {
    Map,
    MapCopy,
    MapCopyReadOnly,
}

fn decode_mmap_bytes(
    detector: &EncodingDetector,
    default_errors: &str,
    path: &Path,
    bytes: &[u8],
) -> Result<String, String>;

fn read_file_with_variant(
    detector: &EncodingDetector,
    default_errors: &str,
    path: &Path,
    variant: MmapVariant,
) -> Result<String, String>;
```

From `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs`:
```rust
#[allow(unsafe_code)]
pub async fn read_file_mmap(&self, path: &Path) -> Result<String, FileIOError>;
```

Phase 6 benchmark sizes already locked in the bench file:
```rust
const PHASE6_MMAP_SIZES: &[(usize, &str)] = &[
    (1_048_576 + 4_096, "1mb_plus_4kb"),
    (4 * 1_048_576, "4mb"),
    (16 * 1_048_576, "16mb"),
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the Phase 6 benchmark mmap constructors into narrowly allowed helpers</name>
  <files>ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
    - ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs
    - .planning/phases/06-mmap-toctou-safety/06-VERIFICATION.md
    - .planning/phases/06-mmap-toctou-safety/06-RESEARCH.md
    - .planning/phases/06-mmap-toctou-safety/06-02-SUMMARY.md
  </read_first>
  <action>Address the exact blocker from `06-VERIFICATION.md`: keep the existing `phase6_mmap_variants` group, `PHASE6_MMAP_SIZES`, `Throughput::Bytes`, and `MmapVariant::{Map, MapCopy, MapCopyReadOnly}` coverage unchanged, but remove the three inline `unsafe` constructor blocks from `read_file_with_variant()`. Introduce three dedicated helper functions in `file_io_benchmarks.rs` named `map_file_shared`, `map_file_copy`, and `map_file_copy_read_only` that wrap `MmapOptions::new().map(&file)`, `.map_copy(&file)`, and `.map_copy_read_only(&file)` respectively. Put `#[allow(unsafe_code)]` on each helper (or on one helper wrapper that contains only those constructor calls), add a short safety comment explaining the file handle lifetime and benchmark-only read/decode usage, and have `read_file_with_variant()` call those helpers before passing the mapped bytes to `decode_mmap_bytes()`. Do not change the benchmark group name, size labels, decode logic, or proof-facing variant names; this plan is only to make `cargo clippy -p classic-file-io-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` pass while preserving the Phase 6 benchmark contract per D-03, D-04, and D-05.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `fn map_file_shared(`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `fn map_file_copy(`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `fn map_file_copy_read_only(`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `#[allow(unsafe_code)]`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `phase6_mmap_variants`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `1_048_576 + 4_096`
  </acceptance_criteria>
  <verify>
    <automated>cargo bench -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml --bench file_io_benchmarks phase6_mmap_variants -- --test && cargo clippy -p classic-file-io-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>`file_io_benchmarks.rs` preserves the Phase 6 three-variant throughput group, but the unsafe mmap constructors are scoped behind explicit allow-listed helpers and the crate's declared clippy gate passes cleanly.</done>
</task>

</tasks>

<verification>
- `cargo bench -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml --bench file_io_benchmarks phase6_mmap_variants -- --test` passes.
- `cargo clippy -p classic-file-io-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` passes.
- `06-VERIFICATION.md`'s only failed truth is now satisfiable without changing the locked Phase 6 benchmark coverage.
</verification>

<success_criteria>
- SAFE-05 is no longer blocked by the benchmark harness failing the crate lint gate.
- The three mmap variants and locked benchmark sizes remain intact after the refactor.
- The remaining Phase 6 work is executable as a single focused gap-closure plan with no new docs or parity churn.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mmap-toctou-safety/06-03-SUMMARY.md`
</output>
