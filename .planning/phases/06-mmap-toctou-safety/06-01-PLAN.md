---
phase: 06-mmap-toctou-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs
  - docs/api/classic-file-io-core.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SAFE-05]
must_haves:
  truths:
    - "Large-file reads use `map_copy_read_only()` instead of shared `Mmap::map()` access"
    - "Large-file and small-file reads still return the same decoded text contract as before"
    - "Contributor-facing docs and milestone docs all describe the locked `map_copy_read_only()` contract"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs"
      provides: "Phase 6 mmap strategy and regression tests"
      contains: "map_copy_read_only"
    - path: "docs/api/classic-file-io-core.md"
      provides: "Updated file-io mmap behavior contract"
      contains: "map_copy_read_only"
    - path: ".planning/PROJECT.md"
      provides: "Milestone wording aligned to Phase 6 mmap contract"
      contains: "map_copy_read_only"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SAFE-05 wording aligned to Phase 6 mmap contract"
      contains: "map_copy_read_only"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs"
      to: "memmap2::MmapOptions"
      via: "large-file branch in `read_file_mmap()`"
      pattern: "map_copy_read_only"
    - from: "ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs"
      to: "tokio::fs::metadata"
      via: "1 MB threshold branch before mapping"
      pattern: "MMAP_THRESHOLD"
    - from: "docs/api/classic-file-io-core.md"
      to: "ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs"
      via: "documented `read_file()`/`read_file_mmap()` behavior"
      pattern: "map_copy_read_only"
---

<objective>
Switch the production mmap read path to the locked safer mapping and align the crate plus milestone docs to the same contract.

Purpose: SAFE-05 is only complete if `read_file_mmap()` actually stops using `Mmap::map()` and the stale `map_copy()` wording in active docs is removed per D-01 and D-02. This plan keeps the rollout inside Rust core, preserves the 1 MB threshold, and leaves bindings thin per repo rules.
Output: Updated `read_file_mmap()` implementation/tests plus synchronized API and milestone documentation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-mmap-toctou-safety/06-CONTEXT.md
@.planning/phases/06-mmap-toctou-safety/06-RESEARCH.md
@.planning/phases/06-mmap-toctou-safety/06-VALIDATION.md
@docs/api/README.md
@docs/api/classic-file-io-core.md
@ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs

<interfaces>
From `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs`:
```rust
pub async fn read_file(&self, path: &Path) -> Result<String, FileIOError>;
pub async fn read_file_with_encoding(&self, path: &Path) -> Result<String, FileIOError>;
pub async fn read_file_mmap(&self, path: &Path) -> Result<String, FileIOError>;
```

Current Phase 6-relevant tests already in `core.rs`:
```rust
#[tokio::test]
async fn test_read_file_mmap_small_file();

#[tokio::test]
async fn test_read_file_mmap_large_file();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap `read_file_mmap()` to `map_copy_read_only()` without changing the decode contract</name>
  <files>ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs
    - docs/api/classic-file-io-core.md
    - .planning/phases/06-mmap-toctou-safety/06-CONTEXT.md
    - .planning/phases/06-mmap-toctou-safety/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a file below the 1 MB threshold still returns its exact text via the non-mmap path.
    - Test 2: a file above the 1 MB threshold still returns the full decoded text after the mapping constructor changes.
    - Test 3: a large non-UTF-8 file still follows the existing decode/error behavior instead of introducing a new encoding branch.
  </behavior>
  <action>Per D-01 and D-06, keep the existing `MMAP_THRESHOLD` split and all current decode logic, but replace the large-file constructor in `FileIOCore::read_file_mmap()` from `Mmap::map(&file)` to `MmapOptions::new().map_copy_read_only(&file)`. Do not introduce a Windows-only branch, do not switch to `map_copy()`, and do not widen the public API. Write or extend the inline `read_file_mmap` tests first so large-file UTF-8 content and at least one large non-UTF-8 path still round-trip through the same public method after the swap. Update the doc comment in `core.rs` so it describes the conservative snapshot-style mitigation instead of claiming external mutation is undefined in the old shared-map way.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs` contains `use memmap2::MmapOptions`
    - `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs` contains `map_copy_read_only`
    - `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs` contains `MMAP_THRESHOLD`
    - `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs` contains `test_read_file_mmap_large_file`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml read_file_mmap -- --nocapture</automated>
  </verify>
  <done>`read_file_mmap()` uses `MmapOptions::map_copy_read_only()` for files at or above 1 MB and the existing public decode behavior remains covered by inline tests.</done>
</task>

<task type="auto">
  <name>Task 2: Align API and milestone docs to the locked Phase 6 mmap contract</name>
  <files>docs/api/classic-file-io-core.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/api/classic-file-io-core.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/06-mmap-toctou-safety/06-CONTEXT.md
  </read_first>
  <action>Per D-02, replace the stale `map_copy()` wording in `.planning/PROJECT.md` and `.planning/REQUIREMENTS.md` with `map_copy_read_only()` so the living milestone docs match the locked Phase 6 contract. Update `docs/api/classic-file-io-core.md` so the behavior note for `read_file()` / `read_file_mmap()` says the 1 MB+ path uses `MmapOptions::map_copy_read_only()` and describes it conservatively as the repo's safer snapshot-style mitigation validated in Phase 6, not as an upstream removal of all `unsafe` caveats. Do not broaden into unrelated API pages or historical research notes in this task.</action>
  <acceptance_criteria>
    - `docs/api/classic-file-io-core.md` contains `map_copy_read_only()`
    - `.planning/PROJECT.md` contains `map_copy_read_only()`
    - `.planning/REQUIREMENTS.md` contains `map_copy_read_only()`
  </acceptance_criteria>
  <verify>
    <automated>rg "map_copy_read_only" docs/api/classic-file-io-core.md .planning/PROJECT.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The file-io API guide and active milestone docs all describe the same `map_copy_read_only()` contract with no remaining live `map_copy()` wording for SAFE-05.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml read_file_mmap -- --nocapture` passes.
- `cargo clippy -p classic-file-io-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` passes.
- `rg "map_copy_read_only" docs/api/classic-file-io-core.md .planning/PROJECT.md .planning/REQUIREMENTS.md` finds the aligned contract in all active docs.
</verification>

<success_criteria>
- SAFE-05's production code path no longer uses `Mmap::map()`.
- Small-file and large-file read behavior stays stable at the public API boundary.
- Active docs and milestone traceability no longer contradict the locked `map_copy_read_only()` decision.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mmap-toctou-safety/06-01-SUMMARY.md`
</output>
