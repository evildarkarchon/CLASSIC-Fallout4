---
phase: 02-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/version.rs
autonomous: true
requirements: [DEBT-01, DEBT-08]

must_haves:
  truths:
    - "No #[allow(deprecated)] annotations remain in parser.rs test module"
    - "parse_segments, parse_segments_parallel, and named_sections_to_positional are deleted from parser.rs"
    - "is_outdated method is deleted from version.rs"
    - "SEGMENT_BOUNDARIES static is deleted from parser.rs"
    - "fast_contains method is deleted from parser.rs"
    - "Segment boundary, patch ordering, and XSE module slot behaviors are tested via parse_all_sections_arc"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs"
      provides: "Log parser with deprecated methods and dead code removed"
      contains: "parse_all_sections_arc"
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/version.rs"
      provides: "CrashgenVersion with is_outdated method removed"
      contains: "check_version_status"
  key_links:
    - from: "parser.rs tests"
      to: "parse_all_sections_arc"
      via: "direct method call with segment_key constants"
      pattern: "sections\\[segment_key::"
---

<objective>
Migrate 3 deprecated shim tests to parse_all_sections_arc, then delete all deprecated methods and dead code from parser.rs and version.rs.

Purpose: Per D-02, tests must be migrated BEFORE deprecated methods are deleted (deprecated = "deny" lint). Per D-01, also remove fast_contains and SEGMENT_BOUNDARIES alongside the required dead code items.
Output: parser.rs and version.rs with no deprecated methods, no dead code, and migrated test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dead-code-removal/02-CONTEXT.md
@.planning/phases/02-dead-code-removal/02-RESEARCH.md
@.planning/phases/01-deprecated-api-migration/01-01-SUMMARY.md

<interfaces>
<!-- segment_key constants used for test migration -->
From ClassicLib-rs/business-logic/classic-scanlog-core/src/segment_key.rs:
```rust
pub const SETTINGS: &str = "settings";
pub const SYSTEM: &str = "system";
pub const CALLSTACK: &str = "callstack";
pub const MODULES: &str = "modules";
pub const XSE_MODULES: &str = "xse_modules";
pub const PLUGINS: &str = "plugins";
pub const REGISTERS: &str = "registers";
pub const STACK_DUMP: &str = "stack_dump";
```

From ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs:
```rust
pub fn parse_all_sections_arc(&self, lines: &[Arc<str>]) -> HashMap<String, Vec<Arc<str>>>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate deprecated shim tests to parse_all_sections_arc</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs (lines 1557-1680 for test module, lines 400-486 for deprecated methods being replaced)
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/segment_key.rs (for constant names)
  </read_first>
  <action>
Replace the 3 deprecated shim tests in parser.rs test module (lines 1614-1657) with equivalent tests using parse_all_sections_arc. Per D-02, each migrated test must cover the same behavior under the current API.

1. Replace `test_segment_parsing_deprecated_shim` (line 1616) with `test_parse_all_sections_arc_basic_segmentation`:
   - Call `parser.parse_all_sections_arc(&log_lines)` using `create_sample_log()`
   - Assert sections map is not empty
   - Assert `sections[segment_key::SETTINGS]` is not empty (was segment index 0)
   - Remove `#[allow(deprecated)]` annotation

2. Replace `test_segment_parsing_with_patches_first_boundary` (line 1626) with `test_parse_all_sections_arc_patches_in_settings`:
   - Call `parser.parse_all_sections_arc(&log_lines)` using `create_sample_log_patches_only()`
   - Assert `sections[segment_key::SETTINGS]` contains a line with `"[Patches]"` or `"bThreads"`
   - Remove `#[allow(deprecated)]` annotation

3. Replace `test_deprecated_parse_segments_preserves_xse_modules_slot` (line 1642) with `test_parse_all_sections_arc_preserves_xse_modules`:
   - Call `parser.parse_all_sections_arc(&log_lines)` using `make_log_with_known_header()`
   - Assert `sections[segment_key::MODULES]` has a line containing `"module.dll"`
   - Assert `sections[segment_key::XSE_MODULES]` has a line containing `"f4se_plugin.dll"`
   - Assert `sections[segment_key::PLUGINS]` has a line containing `"Fallout4.esm"`
   - Remove `#[allow(deprecated)]` annotation

Add `use crate::segment_key;` to the test module if not already present. The test helper functions (`create_sample_log`, `create_sample_log_patches_only`, `make_log_with_known_header`) remain unchanged.

After migration, run `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml` to confirm all migrated tests pass.
  </action>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - parser.rs contains `fn test_parse_all_sections_arc_basic_segmentation()`
    - parser.rs contains `fn test_parse_all_sections_arc_patches_in_settings()`
    - parser.rs contains `fn test_parse_all_sections_arc_preserves_xse_modules()`
    - parser.rs does NOT contain `test_segment_parsing_deprecated_shim`
    - parser.rs does NOT contain `test_segment_parsing_with_patches_first_boundary`
    - parser.rs does NOT contain `test_deprecated_parse_segments_preserves_xse_modules_slot`
    - parser.rs does NOT contain `#[allow(deprecated)]`
    - parser.rs test module contains `use crate::segment_key`
    - `cargo test -p classic-scanlog-core` exits 0
  </acceptance_criteria>
  <done>Three deprecated tests replaced with parse_all_sections_arc equivalents, all using segment_key constants, all passing</done>
</task>

<task type="auto">
  <name>Task 2: Delete deprecated methods, dead code, and is_outdated from parser.rs and version.rs</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/version.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs (lines 69-80 for SEGMENT_BOUNDARIES, lines 411-486 for named_sections_to_positional + parse_segments + parse_segments_parallel, lines 1205-1225 for fast_contains)
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/version.rs (lines 188-222 for is_outdated)
  </read_first>
  <action>
Delete the following items from parser.rs:

1. **SEGMENT_BOUNDARIES static** (lines ~69-80): Delete the entire `static SEGMENT_BOUNDARIES: Lazy<Vec<...>>` block including the `#[allow(dead_code)]` annotation above it. Keep the `once_cell::sync::Lazy` import on line 14 -- it is still used by `COMMON_PATTERNS` (line 36) and `CRASHGEN_HEADER_PATTERN` (line 64).

2. **named_sections_to_positional method** (lines ~411-450): Delete the entire `fn named_sections_to_positional(...)` method. It is only called by the deprecated methods being deleted in step 3.

3. **parse_segments method** (lines ~452-466): Delete the entire deprecated `pub fn parse_segments(...)` method including its doc comment and `#[deprecated]` attribute.

4. **parse_segments_parallel method** (lines ~468-486): Delete the entire deprecated `pub fn parse_segments_parallel(...)` method including its doc comment and `#[deprecated]` attribute.

5. **fast_contains method** (lines ~1205-1225): Delete the entire `fn fast_contains(...)` method including its `#[allow(dead_code)]` annotation and doc comment. Per D-01, this is known dead code in the same file.

Delete from version.rs:

6. **is_outdated method** (lines ~188-222): Delete the entire deprecated `pub fn is_outdated(...)` method including its doc comment, doc test examples, and `#[deprecated]` attribute.

After all deletions, run `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` to confirm no compile errors, then `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` to confirm all tests pass, then `cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` to confirm no warnings.
  </action>
  <verify>
    <automated>cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml 2>&1 | tail -5 && cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - parser.rs does NOT contain `SEGMENT_BOUNDARIES`
    - parser.rs does NOT contain `fn named_sections_to_positional`
    - parser.rs does NOT contain `fn parse_segments(`
    - parser.rs does NOT contain `fn parse_segments_parallel(`
    - parser.rs does NOT contain `fn fast_contains(`
    - parser.rs does NOT contain `#[allow(dead_code)]`
    - parser.rs still contains `use once_cell::sync::Lazy` (used by COMMON_PATTERNS and CRASHGEN_HEADER_PATTERN)
    - version.rs does NOT contain `fn is_outdated(`
    - version.rs does NOT contain `#[deprecated`
    - `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` exits 0
  </acceptance_criteria>
  <done>All deprecated methods (parse_segments, parse_segments_parallel, is_outdated) and dead code (SEGMENT_BOUNDARIES, fast_contains, named_sections_to_positional) deleted. Workspace builds and tests pass cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` passes (no compile errors)
- `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` passes (all tests green including migrated tests)
- `cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` passes (no dead code, no deprecated usage)
- No `#[allow(dead_code)]` or `#[allow(deprecated)]` annotations remain in parser.rs
- No `#[deprecated` annotations remain in version.rs
</verification>

<success_criteria>
- DEBT-01: SEGMENT_BOUNDARIES deleted from parser.rs
- DEBT-08 (partial): parse_segments, parse_segments_parallel deleted from parser.rs; is_outdated deleted from version.rs
- D-01 (partial): fast_contains deleted from parser.rs
- D-02: 3 deprecated shim tests migrated to parse_all_sections_arc before deletion
- Workspace compiles and tests pass cleanly with deprecated = "deny" lint
</success_criteria>

<output>
After completion, create `.planning/phases/02-dead-code-removal/02-01-SUMMARY.md`
</output>
