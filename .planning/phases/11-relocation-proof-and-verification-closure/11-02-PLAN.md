---
phase: 11-relocation-proof-and-verification-closure
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md
  - tests/planning/test_phase07_validation.py
autonomous: true
requirements: [MOVE-01, MOVE-02]
must_haves:
  truths:
    - Contributor can rerun the checked-in Phase 7 relocation proof without false failures from stale `.cargo/` residue expectations.
    - The checked-in relocation audit matches the current `ClassicLib-rs` residue inventory on disk.
    - The Phase 7 planning audit still proves repo-root workspace membership and legacy-boundary cleanup.
  artifacts:
    - path: .planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md
      provides: Current relocation proof with accurate residue inventory.
    - path: tests/planning/test_phase07_validation.py
      provides: Phase 7 audit aligned to the current residue inventory.
  key_links:
    - from: tests/planning/test_phase07_validation.py
      to: .planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md
      via: exact residue and mapping assertions
      pattern: EXPECTED_RESIDUE|Legacy ClassicLib-rs Residue
---

<objective>
Refresh the stale Phase 7 relocation proof so it matches the current repo state.

Purpose: Remove the false failure identified in the milestone audit while preserving the original moved-crate proof for `MOVE-01` and `MOVE-02`.
Output: Updated Phase 7 audit/test surfaces that pass against the current `ClassicLib-rs` residue set.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v9.1.0-MILESTONE-AUDIT.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-03-SUMMARY.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md
@tests/planning/test_phase07_validation.py
@tests/planning/test_phase11_validation.py

<interfaces>
Current stale contract in `tests/planning/test_phase07_validation.py`:

```python
EXPECTED_RESIDUE = [
    ".cargo/",
    ".gitignore",
    ".idea/",
    "CLASSIC_Settings.yaml",
    "clippy_full_report.txt",
    "clippy_report.txt",
    "coverage_report.ps1",
    "coverage_summary.ps1",
    "Crash Logs/",
    "target/",
]
```

Current stale audit row in `07-RELOCATION-AUDIT.md`:

```markdown
| `.cargo/` | Empty legacy directory; no `config.toml`, manifest, or Rust source remains. |
```

Both must be replaced with the live residue inventory on disk.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the checked-in relocation audit to the live residue inventory</name>
  <files>.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md</files>
  <action>Update `07-RELOCATION-AUDIT.md` so the `Legacy ClassicLib-rs Residue` table and any surrounding prose match the current disk state, per the Phase 11 milestone gap. Remove the stale `.cargo/` row because that directory no longer exists. Reconfirm the rest of the residue inventory and keep each row explicitly non-authoritative. Preserve the existing 37-row old→new mapping and repo-root Cargo proof sections; this is a refresh, not a redesign.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase07_validation.py -q -k "relocation_audit_complete or mapping_matches_workspace_exactly or legacy_residue_inventory_matches_disk"</automated>
  </verify>
  <done>The checked-in relocation audit reflects the live residue set and no longer claims `ClassicLib-rs/.cargo/` exists.</done>
</task>

<task type="auto">
  <name>Task 2: Align the Phase 7 planning audit with the refreshed relocation proof</name>
  <files>tests/planning/test_phase07_validation.py</files>
  <action>Update `tests/planning/test_phase07_validation.py` so its residue expectations, relocation-audit fragment checks, and any line-sensitive assertions match the refreshed `07-RELOCATION-AUDIT.md`. Keep the exact-mapping, workspace-membership, representative-manifest, cargo-root, and legacy-boundary assertions intact for `MOVE-01`/`MOVE-02`; only change what the stale `.cargo/` assumption invalidated. Verify with the repo-root Cargo metadata commands named in `07-03-SUMMARY.md`, not with wrapper or parity flows.</action>
  <verify>
    <automated>cargo locate-project --workspace --message-format plain && cargo metadata --format-version 1 --no-deps && python -m pytest tests/planning/test_phase07_validation.py -q</automated>
  </verify>
  <done>The Phase 7 planning audit passes against the current repo and still proves the relocated workspace/member contract.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/planning/test_phase07_validation.py -q`
- `cargo locate-project --workspace --message-format plain && cargo metadata --format-version 1 --no-deps`
</verification>

<success_criteria>
- The stale `.cargo/` residue expectation is gone from both the audit artifact and the Phase 7 planning test.
- The refreshed Phase 7 proof remains rerunnable and deterministic.
- `MOVE-01` and `MOVE-02` still have current cargo-root and legacy-boundary evidence to cite in verification.
</success_criteria>

<output>
After completion, create `.planning/phases/11-relocation-proof-and-verification-closure/11-02-SUMMARY.md`
</output>
