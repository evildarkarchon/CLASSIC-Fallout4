---
phase: 06-documentation-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/python_api_parity/check_parity_gate.py
  - tools/node_api_parity/check_parity_gate.py
  - tools/binding_parity_runtime_coverage.py
  - .planning/milestones/v9.1.0-bindings-promotion-audit.md
autonomous: true
requirements: [DOC-01, DOC-04]

must_haves:
  truths:
    - "Python parity gate runs without --deferred-registry and exits zero"
    - "Node parity gate runs without --deferred-registry and exits zero"
    - "No deferred_registry variable, deferred_total field, or --deferred-registry arg exists in either gate script or the shared helper"
    - "Dead Tier-2 scripts are deleted from both parity tool directories"
    - "Promotion audit trail exists with full content of all 8 governance files archived before any deletion"
  artifacts:
    - path: "tools/python_api_parity/check_parity_gate.py"
      provides: "Python gate without deferred logic"
      contains: "def main"
    - path: "tools/node_api_parity/check_parity_gate.py"
      provides: "Node gate without deferred logic"
      contains: "def main"
    - path: "tools/binding_parity_runtime_coverage.py"
      provides: "Shared coverage helper without deferred_registry parameter"
      contains: "def build_coverage_summary"
    - path: ".planning/milestones/v9.1.0-bindings-promotion-audit.md"
      provides: "Raw governance file archive for audit trail"
      min_lines: 100
  key_links:
    - from: "tools/python_api_parity/check_parity_gate.py"
      to: "tools/binding_parity_runtime_coverage.py"
      via: "import build_coverage_summary without deferred_registry"
      pattern: "build_coverage_summary"
    - from: "tools/node_api_parity/check_parity_gate.py"
      to: "tools/binding_parity_runtime_coverage.py"
      via: "import build_coverage_summary without deferred_registry"
      pattern: "build_coverage_summary"
---

<objective>
Clean up deferred-registry logic from both parity gate scripts and the shared helper, delete dead Tier-2 scripts, and create the promotion audit trail that archives governance file content before Phase 6 Plan 02 deletes them.

Purpose: DOC-01 requires gate scripts to be safe before governance files are deleted. DOC-04 requires the audit trail to exist BEFORE any governance file is deleted (per D-19 ordering constraint). This plan satisfies both prerequisites.
Output: Clean gate scripts, deleted dead scripts, committed audit trail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-reset/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deferred-registry logic from gate scripts and shared helper</name>
  <files>
    tools/python_api_parity/check_parity_gate.py
    tools/node_api_parity/check_parity_gate.py
    tools/binding_parity_runtime_coverage.py
  </files>
  <read_first>
    tools/python_api_parity/check_parity_gate.py
    tools/node_api_parity/check_parity_gate.py
    tools/binding_parity_runtime_coverage.py
  </read_first>
  <action>
Per D-06, D-07: Remove all deferred-registry logic from both gate scripts and the shared helper. This is a full cleanup, not a deprecation.

**In `tools/binding_parity_runtime_coverage.py`:**
1. Remove `"deferred"` from the `VALID_CLASSIFICATIONS` set (line 17). The remaining valid classifications are: `"runtime_verified"`, `"contract_mapped"`, `"newly_uncovered"`.
2. In `build_coverage_summary()` (line 221): remove the `deferred_registry: dict[str, Any]` parameter entirely from the function signature.
3. Remove `deferred_entries = deferred_registry.get("entries", [])` (line 232).
4. Remove `deferred_entries` from the `[*runtime_entries, *deferred_entries]` list comprehension on line 236 -- change to just `runtime_entries`.
5. Remove `_, deferred_lookup = _lookup_maps(deferred_entries)` (line 247).
6. In the gap-row loop (lines 256-261): remove the `runtime_or_deferred_lookup = dict(deferred_lookup)` and `runtime_or_deferred_lookup.update(runtime_identifier_lookup)` lines. Change `_surface_row_from_gap(binding, runtime_or_deferred_lookup, gap_row)` to `_surface_row_from_gap(binding, runtime_identifier_lookup, gap_row)`.
7. Remove `deferred_entries` from the `[*runtime_entries, *deferred_entries]` loop on line 264 -- change to just `runtime_entries`.
8. Remove `"deferred_total": classification_counts.get("deferred", 0),` from the summary dict (line 313).
9. In `render_coverage_summary_markdown()`: remove the `f"- Deferred: **{summary['deferred_total']}**",` line (line 343). Remove the `deferred=counts.get("deferred", 0),` from the per-owner table format call (line 358). Remove the `Deferred` column header from the table header string (line 349-350) and adjust the alignment row accordingly. The table should have columns: Owner Module | Runtime Verified | Contract Mapped | Newly Uncovered | Total.

**In `tools/python_api_parity/check_parity_gate.py`:**
1. Remove the `--deferred-registry` argument definition (lines 189-193).
2. Remove `deferred_registry = load_json_file(repo_root / args.deferred_registry)` (line 231).
3. Remove `deferred_registry=deferred_registry,` from the `build_coverage_summary()` call (line 237).
4. Remove `"deferred_registry": args.deferred_registry,` from the `source_paths` dict (line 241).

**In `tools/node_api_parity/check_parity_gate.py`:**
1. Remove the `--deferred-registry` argument definition (lines 293-297).
2. Remove `deferred_registry = load_json_file(repo_root / args.deferred_registry)` (line 368).
3. Remove `deferred_registry=deferred_registry,` from the `build_coverage_summary()` call (line 374).
4. Remove `"deferred_registry": args.deferred_registry,` from the `source_paths` dict (line 378).

**Also per D-08, delete these dead Tier-2 scripts:**
- `tools/python_api_parity/generate_wave_manifest.py` (226 lines, dead)
- `tools/node_api_parity/generate_wave_manifest.py` (377 lines, dead)
- `tools/node_api_parity/generate_deferred_backlog.py` (118 lines, dead)

After all changes, run both gate scripts to verify they still pass:
- `python tools/python_api_parity/check_parity_gate.py --repo-root .`
- `python tools/node_api_parity/check_parity_gate.py --repo-root .`
  </action>
  <verify>
    <automated>python tools/python_api_parity/check_parity_gate.py --repo-root . && python tools/node_api_parity/check_parity_gate.py --repo-root .</automated>
  </verify>
  <acceptance_criteria>
    - `tools/python_api_parity/check_parity_gate.py` does NOT contain the string `deferred_registry` or `--deferred-registry`
    - `tools/node_api_parity/check_parity_gate.py` does NOT contain the string `deferred_registry` or `--deferred-registry`
    - `tools/binding_parity_runtime_coverage.py` does NOT contain the string `deferred_registry` or `deferred_total` or `deferred_entries` or `deferred_lookup`
    - `tools/binding_parity_runtime_coverage.py` `VALID_CLASSIFICATIONS` set contains exactly `runtime_verified`, `contract_mapped`, `newly_uncovered` (no `deferred`)
    - `build_coverage_summary` function signature does NOT have a `deferred_registry` parameter
    - `tools/python_api_parity/generate_wave_manifest.py` does NOT exist
    - `tools/node_api_parity/generate_wave_manifest.py` does NOT exist
    - `tools/node_api_parity/generate_deferred_backlog.py` does NOT exist
    - `python tools/python_api_parity/check_parity_gate.py --repo-root .` exits zero
    - `python tools/node_api_parity/check_parity_gate.py --repo-root .` exits zero
  </acceptance_criteria>
  <done>Both gate scripts pass with deferred logic fully removed. Three dead Tier-2 scripts are deleted. No deferred-registry concept survives anywhere in the parity tooling.</done>
</task>

<task type="auto">
  <name>Task 2: Create promotion audit trail from governance files</name>
  <files>
    .planning/milestones/v9.1.0-bindings-promotion-audit.md
  </files>
  <read_first>
    docs/implementation/python_api_parity/governance/tier2_backlog_and_governance.md
    docs/implementation/python_api_parity/governance/deferred_runtime_backlog.json
    docs/implementation/python_api_parity/governance/tier2_wave_manifest.json
    docs/implementation/node_api_parity/governance/per_wave_acceptance_template.md
    docs/implementation/node_api_parity/governance/tier2_wave_manifest.json
    docs/implementation/node_api_parity/governance/tier2_backlog_and_governance.md
    docs/implementation/node_api_parity/governance/gate_contract_baseline.md
    docs/implementation/node_api_parity/governance/deferred_runtime_backlog.json
  </read_first>
  <action>
Per D-01 through D-05: Create a raw archive of all 8 governance files at `.planning/milestones/v9.1.0-bindings-promotion-audit.md`.

The document structure must be:

1. **Context header** (per D-04, 2-3 sentences): Explain that Tier-2 existed as a deferral mechanism during incremental binding promotion. Phase 3 (Python) and Phase 4 (Node) promoted all deferred entries to the single enforced tier. These files are archived here before deletion in Phase 6, as of 2026-04-10.

2. **Python Governance Files** section containing the full verbatim content of these 3 files (per D-01 raw archive), each under a subsection with its relative path as the heading:
   - `docs/implementation/python_api_parity/governance/tier2_backlog_and_governance.md` (~81 lines)
   - `docs/implementation/python_api_parity/governance/deferred_runtime_backlog.json` (~5 lines)
   - `docs/implementation/python_api_parity/governance/tier2_wave_manifest.json` (~13253 lines)

3. **Node Governance Files** section containing the full verbatim content of these 5 files:
   - `docs/implementation/node_api_parity/governance/per_wave_acceptance_template.md` (~56 lines)
   - `docs/implementation/node_api_parity/governance/tier2_wave_manifest.json` (~4467 lines)
   - `docs/implementation/node_api_parity/governance/tier2_backlog_and_governance.md` (~107 lines)
   - `docs/implementation/node_api_parity/governance/gate_contract_baseline.md` (~44 lines)
   - `docs/implementation/node_api_parity/governance/deferred_runtime_backlog.json` (~5 lines)

Each file section uses a `### path/to/file` heading followed by a fenced code block containing the file's entire content. Use appropriate language hints for the fence: `json` for `.json` files, `markdown` for `.md` files (or no language hint for markdown to avoid rendering issues in the fence).

Per D-03: Do NOT include baseline JSON files or .planning/ artifacts -- only governance files.
Per D-05: Do NOT modify baseline JSON files for stale governance references.

Note: The JSON wave manifest files are large (13K+ and 4K+ lines). This is expected -- D-01 explicitly requests "copy full content as-is". The executor should read each governance file and concatenate it into the audit document.
  </action>
  <verify>
    <automated>test -f .planning/milestones/v9.1.0-bindings-promotion-audit.md && grep -c "tier2_backlog_and_governance" .planning/milestones/v9.1.0-bindings-promotion-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/milestones/v9.1.0-bindings-promotion-audit.md` exists
    - File contains the string `Tier-2 existed as a deferral mechanism` (or equivalent context header per D-04)
    - File contains the heading `## Python Governance Files`
    - File contains the heading `## Node Governance Files`
    - File contains 8 subsection headings matching the 8 governance file paths (3 Python + 5 Node)
    - File contains the string `tier2_backlog_and_governance.md` at least twice (once for Python, once for Node)
    - File contains the string `deferred_runtime_backlog.json` at least twice (once for Python, once for Node)
    - File contains the string `tier2_wave_manifest.json` at least twice (once for Python, once for Node)
    - File contains the string `gate_contract_baseline.md` at least once (Node only)
    - File contains the string `per_wave_acceptance_template.md` at least once (Node only)
    - File is at least 17000 lines (governance files total ~18000 lines plus headers)
  </acceptance_criteria>
  <done>Promotion audit trail exists at `.planning/milestones/v9.1.0-bindings-promotion-audit.md` with the full verbatim content of all 8 governance files, ready for governance file deletion in Plan 02.</done>
</task>

</tasks>

<verification>
1. Both gate scripts pass: `python tools/python_api_parity/check_parity_gate.py --repo-root .` and `python tools/node_api_parity/check_parity_gate.py --repo-root .` both exit zero
2. No deferred-registry logic survives: `grep -r "deferred_registry\|deferred_total\|deferred_entries\|deferred_lookup\|--deferred-registry" tools/python_api_parity/ tools/node_api_parity/ tools/binding_parity_runtime_coverage.py` returns zero matches
3. Dead scripts are gone: `ls tools/python_api_parity/generate_wave_manifest.py tools/node_api_parity/generate_wave_manifest.py tools/node_api_parity/generate_deferred_backlog.py 2>&1` shows "No such file"
4. Audit trail exists: `test -f .planning/milestones/v9.1.0-bindings-promotion-audit.md`
</verification>

<success_criteria>
- Gate scripts pass without deferred logic
- Three dead Tier-2 scripts deleted
- Promotion audit trail committed with all 8 governance files archived
- D-19 ordering constraint satisfied: gate cleanup and audit trail land before any governance file deletion
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-reset/06-01-SUMMARY.md`
</output>
