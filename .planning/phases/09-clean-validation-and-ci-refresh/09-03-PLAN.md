---
phase: 09-clean-validation-and-ci-refresh
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .github/workflows/ci-python-bindings.yml
  - .github/workflows/ci-typescript.yml
  - .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
  - tests/planning/test_phase09_validation.py
autonomous: true
requirements:
  - INTG-03
must_haves:
  truths:
    - "Contributor can inspect the Python and Node PR workflows and see repo-root cache, working-directory, venv, and diagnostics paths only."
    - "The Phase 9 audit proves which Python/Node artifact directories are allowed to regenerate and which are out of scope."
  artifacts:
    - path: ".github/workflows/ci-python-bindings.yml"
      provides: "Repo-root Python bindings CI contract"
      contains: "python-bindings/.venv"
    - path: ".github/workflows/ci-typescript.yml"
      provides: "Repo-root Node bindings CI contract"
      contains: "node-bindings/classic-node"
    - path: "tests/planning/test_phase09_validation.py"
      provides: "Executable audit for Python/Node workflow and artifact-scope rules"
      contains: "test_python_node_workflows"
  key_links:
    - from: ".github/workflows/ci-python-bindings.yml"
      to: "validate_stubs.py"
      via: "repo-root rust-dir and stub-output paths"
      pattern: "--rust-dir ."
    - from: ".github/workflows/ci-typescript.yml"
      to: "node-bindings/classic-node/package.json"
      via: "repo-root working-directory and package-local parity scripts"
      pattern: "node-bindings/classic-node"
---

<objective>
Refresh the Python and Node CI workflow contracts so binding validation runs from repo root and regenerates only the artifact directories Phase 9 actually owns.

Purpose: close the remaining D-04 and D-08 path-sensitive PR workflows without reopening Phase 8’s tool-default rewires.
Output: updated Python/Node CI workflows plus executable phase-audit checks for those surfaces and their allowed artifact scope.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-RESEARCH.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
@tests/planning/test_phase09_validation.py
@.github/workflows/ci-python-bindings.yml
@.github/workflows/ci-typescript.yml
@validate_stubs.py
@node-bindings/classic-node/package.json

<interfaces>
From `.github/workflows/ci-python-bindings.yml`:
```yaml
- name: Run Python stub validation gate
  run: python ClassicLib-rs/validate_stubs.py --rust-dir ClassicLib-rs --parity-contract docs/implementation/python_api_parity/baseline/parity_contract.json --json-out ClassicLib-rs/python-bindings/parity-artifacts/stub_validation_report.json --fail-on-warnings
```

From `.github/workflows/ci-typescript.yml`:
```yaml
- name: Install npm dependencies
  working-directory: ClassicLib-rs/node-bindings/classic-node
```

From `validate_stubs.py`:
```python
if resolved == LEGACY_WORKSPACE_DIR or resolved.name == "ClassicLib-rs":
    raise FileNotFoundError(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Python and Node PR workflows to repo-root path contracts</name>
  <files>.github/workflows/ci-python-bindings.yml, .github/workflows/ci-typescript.yml</files>
  <read_first>.github/workflows/ci-python-bindings.yml
.github/workflows/ci-typescript.yml
.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
.planning/phases/09-clean-validation-and-ci-refresh/09-RESEARCH.md
validate_stubs.py
node-bindings/classic-node/package.json</read_first>
  <action>Per D-04, D-08, and D-09, update the Python and TypeScript workflows as one repo-root contract set. In `ci-python-bindings.yml`, change `python ClassicLib-rs/validate_stubs.py --rust-dir ClassicLib-rs` to `python validate_stubs.py --rust-dir .`, change the JSON output path to `python-bindings/parity-artifacts/stub_validation_report.json`, change the diagnostics upload path to `python-bindings/parity-artifacts/`, change the uv dependency cache glob to `python-bindings/requirements-ci.txt`, change the cargo build cache path from `ClassicLib-rs/target` to `target`, and change venv creation/install/test paths from `ClassicLib-rs/python-bindings/...` to `python-bindings/...`. In `ci-typescript.yml`, change every `working-directory: ClassicLib-rs/node-bindings/classic-node` to `node-bindings/classic-node`, change cargo cache paths from `ClassicLib-rs/target` to `target`, and change diagnostics upload path to `node-bindings/classic-node/parity-artifacts/`. Keep the existing package-local commands from Phase 8 intact; do not rewrite package scripts in this plan.</action>
  <acceptance_criteria>
    - `.github/workflows/ci-python-bindings.yml` does not contain `ClassicLib-rs/validate_stubs.py`.
    - `.github/workflows/ci-python-bindings.yml` contains `python validate_stubs.py --rust-dir .`.
    - `.github/workflows/ci-python-bindings.yml` contains `python-bindings/parity-artifacts/stub_validation_report.json`.
    - `.github/workflows/ci-typescript.yml` does not contain `ClassicLib-rs/node-bindings/classic-node`.
    - `.github/workflows/ci-typescript.yml` contains `working-directory: node-bindings/classic-node`.
    - `.github/workflows/ci-typescript.yml` contains `node-bindings/classic-node/parity-artifacts/`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q -k "python_node_workflows"</automated>
  </verify>
  <done>The Python and Node PR workflows use repo-root cache, working-directory, venv, and diagnostics paths only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the Phase 9 audit to lock Python/Node artifact scope</name>
  <files>.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md, tests/planning/test_phase09_validation.py</files>
  <read_first>.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
tests/planning/test_phase09_validation.py
.github/workflows/ci-python-bindings.yml
.github/workflows/ci-typescript.yml
python-bindings/parity-artifacts/stub_validation_report.json
node-bindings/classic-node/package.json</read_first>
  <behavior>
    - Test 1: `test_python_node_workflows` fails if either workflow still encodes a legacy path.
    - Test 2: `test_artifact_scope_rules` fails unless the audit document names only the allowed Python/Node regenerated artifact directories and explicitly rejects unrelated churn per D-09.
  </behavior>
  <action>Update the workflow matrix in `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` with the exact repo-root cache paths, working directories, venv locations, and diagnostics upload directories from Task 1. In the `## CI-Owned Artifact Scope` section, explicitly list the Phase 9-owned Python/Node artifact directories: `python-bindings/parity-artifacts/` and `node-bindings/classic-node/parity-artifacts/`, and state that checked-in baseline contracts under `docs/implementation/**/baseline/` should only change if the proof commands actually regenerate path-bearing metadata. Extend `tests/planning/test_phase09_validation.py` with `test_python_node_workflows` and `test_artifact_scope_rules` so those exact strings become required. Do not add CXX or benchmark artifact ownership here; those surfaces are already covered by 09-02 or 09-04.</action>
  <acceptance_criteria>
    - `tests/planning/test_phase09_validation.py` contains `def test_python_node_workflows`.
    - `tests/planning/test_phase09_validation.py` contains `def test_artifact_scope_rules`.
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `python-bindings/parity-artifacts/`.
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `node-bindings/classic-node/parity-artifacts/`.
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `docs/implementation/**/baseline/`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q -k "python_node_workflows or artifact_scope_rules"</automated>
  </verify>
  <done>The Phase 9 audit makes Python/Node artifact ownership explicit and executable, so clean proof cannot silently widen into unrelated baseline churn.</done>
</task>

</tasks>

<verification>
The Python/Node PR workflows and the phase audit agree on repo-root execution roots and the exact regenerated artifact scope Phase 9 owns.
</verification>

<success_criteria>
- `ci-python-bindings.yml` uses repo-root `validate_stubs.py`, `--rust-dir .`, `python-bindings/.venv`, `target`, and `python-bindings/parity-artifacts/`.
- `ci-typescript.yml` uses repo-root `target`, `node-bindings/classic-node`, and `node-bindings/classic-node/parity-artifacts/`.
- The Phase 9 audit and planning test explicitly lock the Python/Node artifact scope and reject unrelated churn.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clean-validation-and-ci-refresh/09-03-SUMMARY.md`
</output>
