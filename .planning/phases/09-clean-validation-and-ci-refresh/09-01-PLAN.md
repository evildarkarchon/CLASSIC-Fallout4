---
phase: 09-clean-validation-and-ci-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
  - tests/planning/test_phase09_validation.py
  - tests/planning/phase09_clean_run.ps1
autonomous: true
requirements:
  - INTG-04
must_haves:
  truths:
    - "Contributor has a committed Phase 9 proof contract instead of an ad hoc clean-run checklist."
    - "Targeted clean proof removes or quarantines the high-risk generated outputs named in D-02 before validation starts."
    - "Phase 9 fails if fresh proof recreates generated residue under `ClassicLib-rs`."
  artifacts:
    - path: ".planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md"
      provides: "Scoped audit of workflow contracts, clean targets, and artifact ownership"
      contains: "## Targeted Clean Inventory"
    - path: "tests/planning/test_phase09_validation.py"
      provides: "Executable phase audit for workflow/package surfaces and residue checks"
      contains: "test_workflow_and_package_surface"
    - path: "tests/planning/phase09_clean_run.ps1"
      provides: "Targeted clean-state harness stronger than Phase 6"
      contains: "ClassicLib-rs/target"
  key_links:
    - from: "tests/planning/phase09_clean_run.ps1"
      to: "tests/planning/test_phase09_validation.py"
      via: "final audit invocation after fresh-state proof"
      pattern: "test_phase09_validation.py"
    - from: ".planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md"
      to: "tests/planning/phase09_clean_run.ps1"
      via: "shared targeted-clean inventory"
      pattern: "repo-root target"
---

<objective>
Create the committed Phase 9 proof contract and the stronger targeted-clean harness before any workflow rewires land.

Purpose: lock D-01 through D-03 and D-10 through D-11 into executable assets so later plans can wire CI and artifact refresh against one auditable contract.
Output: a checked-in clean-validation audit, a new Phase 9 planning test, and a targeted-clean PowerShell harness.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-RESEARCH.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-VALIDATION.md
@.planning/phases/08-wrapper-and-parity-rewire/08-VALIDATION.md
@tests/planning/phase06_clean_run.ps1
@tests/planning/test_phase08_validation.py

<interfaces>
From `tests/planning/phase06_clean_run.ps1`:
```powershell
$LegacyTarget = Join-Path $RepoRoot "ClassicLib-rs/target"
$Commands = @(
    "cargo locate-project --workspace",
    "cargo metadata --format-version 1 --no-deps",
    "cargo fmt --all -- --check"
)
```

From `tests/planning/test_phase08_validation.py`:
```python
def run_command(self, command: list[str], *, cwd: Path | None = None, timeout: int = 900, expected_text: str | None = None) -> subprocess.CompletedProcess[str]:
    ...

def test_live_native_wrapper_test_flows_smoke(self) -> None:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit the Phase 9 audit artifact and executable validation scaffold</name>
  <files>.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md, tests/planning/test_phase09_validation.py</files>
  <read_first>.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
.planning/phases/09-clean-validation-and-ci-refresh/09-RESEARCH.md
.planning/phases/08-wrapper-and-parity-rewire/08-VALIDATION.md
tests/planning/test_phase08_validation.py</read_first>
  <behavior>
    - Test 1: `test_workflow_and_package_surface` fails when a required Phase 9-owned workflow or the GUI package proof surface is missing from the audit contract.
    - Test 2: `test_clean_state_and_residue` fails when the clean inventory omits repo-root `target`, binding `.venv`, Node outputs, or when the post-proof residue rule allows new generated output under `ClassicLib-rs`.
  </behavior>
  <action>Per D-01, D-02, D-03, D-06, D-07, D-08, D-09, D-10, and D-11, create `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` with these exact sections: `## Targeted Clean Inventory`, `## Workflow Contract Matrix`, `## CI-Owned Artifact Scope`, `## GUI Package Proof Surface`, and `## Legacy Residue Failure Rules`. In the targeted-clean inventory, list the exact high-risk generated outputs from D-02: `ClassicLib-rs/target`, repo-root `target`, `python-bindings/.venv`, `node-bindings/classic-node/node_modules`, `node-bindings/classic-node/dist`, built `.node` outputs under `node-bindings/classic-node`, `python-bindings/parity-artifacts`, `node-bindings/classic-node/parity-artifacts`, and `cpp-bindings/classic-cpp-bridge/parity-artifacts`. Then create `tests/planning/test_phase09_validation.py` using the Phase 8 `run_command()` pattern plus named tests for the required audit sections, workflow/package surface coverage, clean-state inventory coverage, scoped artifact refresh rules, and post-proof legacy residue rejection. Keep the audit file Phase 9-scoped only — do not widen into Phase 10 doc cleanup per D-12.</action>
  <acceptance_criteria>
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `## Targeted Clean Inventory`.
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `.github/workflows/ci-rust.yml`, `.github/workflows/ci-python-bindings.yml`, `.github/workflows/ci-typescript.yml`, `.github/workflows/ci-cpp.yml`, and `.github/workflows/benchmarks.yml`.
    - `tests/planning/test_phase09_validation.py` contains `def test_workflow_and_package_surface`.
    - `tests/planning/test_phase09_validation.py` contains `def test_clean_state_and_residue`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q -k "workflow_and_package_surface or clean_state_and_residue"</automated>
  </verify>
  <done>The phase has one committed audit document and one executable test scaffold that encode the exact clean-proof boundary and residue failure rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the stronger targeted-clean harness for Phase 9 proof</name>
  <files>tests/planning/phase09_clean_run.ps1, tests/planning/test_phase09_validation.py, .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md</files>
  <read_first>tests/planning/phase06_clean_run.ps1
tests/planning/test_phase09_validation.py
.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
classic-gui/build_gui.ps1</read_first>
  <behavior>
    - Test 1: the harness quarantines or removes every D-02 output before proof commands run.
    - Test 2: the harness records pre-proof and post-proof `ClassicLib-rs` generated-state and fails when new generated residue appears there.
  </behavior>
  <action>Use `tests/planning/phase06_clean_run.ps1` as the starting pattern, but create a new `tests/planning/phase09_clean_run.ps1` per D-01 through D-03. The script must resolve repo root, refuse to overwrite existing backup directories, quarantine `ClassicLib-rs/target` to a `.phase9-backup` sibling, remove repo-root `target`, remove `python-bindings/.venv`, remove `node-bindings/classic-node/node_modules`, remove `node-bindings/classic-node/dist`, delete built `.node` outputs under `node-bindings/classic-node`, and clear the three parity-artifact directories only for the Phase 9 proof surfaces. Record a pre-proof listing of generated paths under `ClassicLib-rs`, run placeholder proof commands in this exact order for now: `cargo locate-project --workspace`, `cargo metadata --format-version 1 --no-deps`, `python -m pytest tests/planning/test_phase09_validation.py -q`, then restore quarantined backups in `finally`. Update the Phase 9 planning test so it asserts the harness contains every D-02 cleanup target and a post-proof residue comparison hook. Do not add full live package/parity execution yet; that belongs in 09-04 after the workflows are refreshed.</action>
  <acceptance_criteria>
    - `tests/planning/phase09_clean_run.ps1` contains `ClassicLib-rs/target.phase9-backup`.
    - `tests/planning/phase09_clean_run.ps1` contains `python-bindings/.venv`.
    - `tests/planning/phase09_clean_run.ps1` contains `node-bindings/classic-node/node_modules`.
    - `tests/planning/phase09_clean_run.ps1` contains `node-bindings/classic-node/dist`.
    - `tests/planning/test_phase09_validation.py` contains `phase09_clean_run.ps1` and `ClassicLib-rs/target.phase9-backup` assertions.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q -k "clean_state_and_residue"</automated>
  </verify>
  <done>Phase 9 has a committed clean-run harness that is visibly stronger than Phase 6 and ready for the live proof commands added in later plans.</done>
</task>

</tasks>

<verification>
The audit artifact and clean harness together define the Phase 9 closure contract before any workflow rewiring begins.
</verification>

<success_criteria>
- `09-CLEAN-VALIDATION-AUDIT.md` names all five required workflows, the GUI package proof surface, the targeted clean inventory, and the legacy residue failure rule.
- `tests/planning/test_phase09_validation.py` exists and enforces the audit sections plus the clean/residue contract.
- `tests/planning/phase09_clean_run.ps1` quarantines or removes every D-02 output and prepares for post-proof residue checks.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clean-validation-and-ci-refresh/09-01-SUMMARY.md`
</output>
