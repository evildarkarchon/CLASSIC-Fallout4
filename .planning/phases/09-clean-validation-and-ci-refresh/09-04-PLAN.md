---
phase: 09-clean-validation-and-ci-refresh
plan: 04
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
files_modified:
  - .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
  - tests/planning/test_phase09_validation.py
  - tests/planning/phase09_clean_run.ps1
  - python-bindings/parity-artifacts/
  - node-bindings/classic-node/parity-artifacts/
  - cpp-bindings/classic-cpp-bridge/parity-artifacts/
  - docs/implementation/python_api_parity/baseline/
  - docs/implementation/node_api_parity/baseline/
  - docs/implementation/cxx_api_parity/baseline/
autonomous: true
requirements:
  - INTG-03
  - INTG-04
must_haves:
  truths:
    - "Contributor can rerun the required proof surfaces from a fresh state and watch the owned artifact directories regenerate under repo root."
    - "Contributor can run the required GUI package-sensitive proof surface after the targeted clean pass."
    - "Contributor can verify that fresh proof does not recreate generated outputs under `ClassicLib-rs`."
  artifacts:
    - path: "tests/planning/phase09_clean_run.ps1"
      provides: "Final targeted clean proof sequence"
      contains: "build_gui.ps1 -Package"
    - path: "tests/planning/test_phase09_validation.py"
      provides: "Final phase audit for artifact scope and legacy residue checks"
      contains: "test_no_new_legacy_residue"
    - path: "python-bindings/parity-artifacts/stub_validation_report.json"
      provides: "Refreshed Python stub-validation artifact"
      contains: "crates_passed"
  key_links:
    - from: "tests/planning/phase09_clean_run.ps1"
      to: "python-bindings/parity-artifacts/"
      via: "targeted clean then regeneration"
      pattern: "stub_validation_report.json"
    - from: "tests/planning/phase09_clean_run.ps1"
      to: "classic-gui/build_gui.ps1"
      via: "package proof step"
      pattern: "-Package"
---

<objective>
Refresh only the Phase 9-owned path-bearing artifacts and finish the clean-state + GUI package proof sequence.

Purpose: satisfy INTG-03 and INTG-04 with fresh-state evidence, scoped artifact regeneration, and a hard post-proof check that `ClassicLib-rs` stays non-authoritative.
Output: final clean-run harness, final Phase 9 audit coverage, and refreshed path-bearing artifacts only where the proof commands actually touch them.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-RESEARCH.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
@tests/planning/test_phase09_validation.py
@tests/planning/phase09_clean_run.ps1
@classic-gui/build_gui.ps1
@validate_stubs.py
@tools/python_api_parity/check_parity_gate.py
@tools/node_api_parity/check_parity_gate.py
@tools/cxx_api_parity/check_parity_gate.py
@node-bindings/classic-node/package.json

<interfaces>
From `validate_stubs.py`:
```python
if (resolved / "python-bindings").exists():
    return resolved
```

From `tools/python_api_parity/check_parity_gate.py`:
```python
parser.add_argument("--output-dir", default="python-bindings/parity-artifacts")
parser.add_argument("--baseline-output-dir", default="docs/implementation/python_api_parity/baseline")
```

From `tools/cxx_api_parity/check_parity_gate.py`:
```python
parser.add_argument("--output-dir", default="cpp-bindings/classic-cpp-bridge/parity-artifacts")
```

From `classic-gui/build_gui.ps1`:
```powershell
if ($Package) { $Install = $true }
& cpack --config $cpackConfig -B $packageDir
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate only the Phase 9-owned path-bearing artifacts after the workflow cutover</name>
  <files>python-bindings/parity-artifacts/, node-bindings/classic-node/parity-artifacts/, cpp-bindings/classic-cpp-bridge/parity-artifacts/, docs/implementation/python_api_parity/baseline/, docs/implementation/node_api_parity/baseline/, docs/implementation/cxx_api_parity/baseline/, .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md</files>
  <read_first>.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
python-bindings/parity-artifacts/stub_validation_report.json
node-bindings/classic-node/package.json
tools/python_api_parity/check_parity_gate.py
tools/node_api_parity/check_parity_gate.py
tools/cxx_api_parity/check_parity_gate.py</read_first>
  <action>Per D-08 and D-09, rerun only the proof-surface artifact producers named in the audit: `python tools/python_api_parity/check_parity_gate.py --repo-root .`, `python validate_stubs.py --rust-dir . --parity-contract docs/implementation/python_api_parity/baseline/parity_contract.json --json-out python-bindings/parity-artifacts/stub_validation_report.json --fail-on-warnings`, `bun run parity:gate` from `node-bindings/classic-node`, `bun run dts:freshness:check` from `node-bindings/classic-node`, and `python tools/cxx_api_parity/check_parity_gate.py --repo-root .`. Commit regenerated files only when those commands actually rewrite repo-root artifact directories or checked-in baseline files. Update `09-CLEAN-VALIDATION-AUDIT.md` with the exact artifact files that changed and explicitly note any owned artifact directories that remained byte-identical after rerun. Do not regenerate unrelated docs, contracts, or benchmark outputs “just in case.”</action>
  <acceptance_criteria>
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `python-bindings/parity-artifacts/`, `node-bindings/classic-node/parity-artifacts/`, and `cpp-bindings/classic-cpp-bridge/parity-artifacts/` in its refreshed-artifact section.
    - `python-bindings/parity-artifacts/stub_validation_report.json` exists after the rerun.
    - `tests/planning/test_phase09_validation.py` can assert the allowed artifact directories without mentioning any unrelated directory outside the audit scope.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q -k "artifact_scope_rules"</automated>
  </verify>
  <done>Only the artifact directories tied to the required CI/package proof surfaces are regenerated or confirmed unchanged, and the audit file records that exact scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize the clean-state harness with live package proof and post-proof residue rejection</name>
  <files>tests/planning/phase09_clean_run.ps1, tests/planning/test_phase09_validation.py, .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md</files>
  <read_first>tests/planning/phase09_clean_run.ps1
tests/planning/test_phase09_validation.py
.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
classic-gui/build_gui.ps1
.github/workflows/ci-rust.yml
.github/workflows/ci-python-bindings.yml
.github/workflows/ci-typescript.yml
.github/workflows/ci-cpp.yml
.github/workflows/benchmarks.yml</read_first>
  <behavior>
    - Test 1: `test_no_new_legacy_residue` fails if fresh proof recreates generated output under `ClassicLib-rs`.
    - Test 2: the final harness runs repo-root proof commands, then `classic-gui/build_gui.ps1 -Package`, then the Phase 9 planning audit.
  </behavior>
  <action>Finish `tests/planning/phase09_clean_run.ps1` so the placeholder proof sequence from 09-01 becomes the final Phase 9 fresh-state proof. After cleanup/quarantine, run these commands in order from repo root: `cargo locate-project --workspace --message-format plain`, `cargo metadata --format-version 1 --no-deps`, `python tools/python_api_parity/check_parity_gate.py --repo-root .`, `python validate_stubs.py --rust-dir . --parity-contract docs/implementation/python_api_parity/baseline/parity_contract.json --json-out python-bindings/parity-artifacts/stub_validation_report.json --fail-on-warnings`, `bun install` then `bun run build`, `bun run parity:gate`, and `bun run dts:freshness:check` in `node-bindings/classic-node`, `python tools/cxx_api_parity/check_parity_gate.py --repo-root .`, `pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Package`, and finally `python -m pytest tests/planning/test_phase09_validation.py -q`. Add pre-proof and post-proof `ClassicLib-rs` generated-path snapshots and fail if new generated residue appears there per D-10 and D-11. Update the audit markdown and planning test with the exact proof order and the new `test_no_new_legacy_residue` assertion. If local GUI packaging is blocked by the wrapper environment, require the harness to surface the failure clearly rather than silently skipping the package step.</action>
  <acceptance_criteria>
    - `tests/planning/phase09_clean_run.ps1` contains `classic-gui/build_gui.ps1 -Package`.
    - `tests/planning/phase09_clean_run.ps1` contains `python validate_stubs.py --rust-dir .`.
    - `tests/planning/phase09_clean_run.ps1` contains `bun run dts:freshness:check`.
    - `tests/planning/test_phase09_validation.py` contains `def test_no_new_legacy_residue`.
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `Post-proof legacy residue check`.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File tests/planning/phase09_clean_run.ps1</automated>
  </verify>
  <done>Phase 9 closes on a real fresh-state proof sequence that regenerates owned artifacts, runs the required GUI package flow, and rejects any newly generated output under `ClassicLib-rs`.</done>
</task>

</tasks>

<verification>
The final clean-run harness, artifact refresh reruns, and planning test provide the Phase 9 closure proof: repo-root workflows work from a fresh state, required artifacts regenerate in the right places, and no new legacy residue appears.
</verification>

<success_criteria>
- The targeted clean harness runs the repo-root parity/stub/package proof sequence in one order-controlled script.
- The phase audit records exactly which path-bearing artifacts regenerated and rejects unrelated churn.
- The final proof fails if new generated outputs appear under `ClassicLib-rs`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clean-validation-and-ci-refresh/09-04-SUMMARY.md`
</output>
