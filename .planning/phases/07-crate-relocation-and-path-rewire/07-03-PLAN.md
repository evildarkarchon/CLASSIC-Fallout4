---
phase: 07-crate-relocation-and-path-rewire
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - .planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md
  - tests/planning/test_phase07_validation.py
  - ClassicLib-rs/
autonomous: true
requirements:
  - MOVE-01
  - MOVE-02
must_haves:
  truths:
    - "Phase 7 ends with explicit proof of the old→new crate mapping instead of an implied move."
    - "Cargo proves the repository root is still the only live workspace root after relocation."
    - "ClassicLib-rs no longer contains live Rust crates or workspace-owned Rust authority."
  artifacts:
    - path: ".planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md"
      provides: "Completed relocation audit with 37-row mapping and stale-path sweep"
      contains: "## Old to New Crate Mapping"
    - path: "tests/planning/test_phase07_validation.py"
      provides: "Final Phase 7 audit proving mapping completeness and legacy-boundary cleanup"
      contains: "test_relocation_audit_complete"
  key_links:
    - from: ".planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md"
      to: "Cargo.toml"
      via: "member inventory and cargo proof sections"
      pattern: "cargo locate-project --workspace"
    - from: "tests/planning/test_phase07_validation.py"
      to: "ClassicLib-rs/"
      via: "legacy-boundary assertions"
      pattern: "ClassicLib-rs"
---

<objective>
Close Phase 7 with explicit relocation evidence and a hard check that `ClassicLib-rs` is no longer a live Rust workspace home.

Purpose: finish the move with auditable proof, not just a green Cargo command.
Output: a completed relocation audit artifact and final Phase 7 validation coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-CONTEXT.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-RESEARCH.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-01-PLAN.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-02-PLAN.md
@.planning/phases/06-repo-root-workspace-cutover/06-VERIFICATION.md
@Cargo.toml
@tests/planning/test_phase07_validation.py
@.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the relocation audit with mapping, cargo proof, and stale-path sweep results</name>
  <files>.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md</files>
  <action>Per D-06, D-07, D-08, and D-09, replace the Phase 7 audit template with final evidence. Record all 37 old→new crate mappings in a deterministic table, capture the actual output/summary from `cargo locate-project --workspace --message-format plain` and `cargo metadata --format-version 1 --no-deps`, and include a stale sweep covering: root workspace members, any remaining `Cargo.toml` files under `ClassicLib-rs`, and any remaining workspace-owned Rust files under `ClassicLib-rs`. If non-authoritative residue remains under `ClassicLib-rs` (for example local reports, caches, or user data), list each residue item and state why it is explicitly not part of the live build graph. Do not widen this artifact into wrapper/parity/CI proof; Phase 7 stays cargo-and-layout focused per D-09.</action>
  <verify>
    <automated>python -c "from pathlib import Path; text = Path('.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md').read_text(encoding='utf-8'); required = ['## Old to New Crate Mapping', '## Cargo Root Proof', '## Stale Member and Manifest Sweep', '## Legacy ClassicLib-rs Residue']; missing = [item for item in required if item not in text]; raise SystemExit(1 if missing else 0)"</automated>
  </verify>
  <done>The checked-in audit artifact fully documents the relocation, Cargo proof, stale sweep, and any explicitly non-authoritative residue left under `ClassicLib-rs`.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize the Phase 7 planning audit and prove the one-root workspace contract</name>
  <files>tests/planning/test_phase07_validation.py, ClassicLib-rs/</files>
  <action>Finish `tests/planning/test_phase07_validation.py` so it enforces the completed Phase 7 contract: root workspace members are all repo-root-relative, the completed `07-RELOCATION-AUDIT.md` contains the required mapping/proof sections, representative moved manifests still point at valid local crate paths, and `ClassicLib-rs` contains no live workspace member `Cargo.toml` files or workspace-owned Rust authority. If the test or audit finds lingering live Rust files under `ClassicLib-rs`, remove or neutralize them in this task so D-05 is true by phase end. Then run the required Cargo proof sequence from repo root and keep the test file aligned with those exact commands. Do not add wrapper or parity smoke checks; those are explicitly deferred to Phase 8.</action>
  <verify>
    <automated>cargo locate-project --workspace --message-format plain && cargo metadata --format-version 1 --no-deps && cargo check --workspace && python -m pytest tests/planning/test_phase07_validation.py -q</automated>
  </verify>
  <done>Phase 7 closes with one live repo-root workspace, no live crate authority under `ClassicLib-rs`, and a passing audit that proves both facts.</done>
</task>

</tasks>

<verification>
The completed relocation audit, Cargo root/member proof, workspace check, and final Phase 7 planning test together prove the move is real and `ClassicLib-rs` is no longer authoritative.
</verification>

<success_criteria>
- `07-RELOCATION-AUDIT.md` contains a complete 37-row old→new mapping plus cargo proof and stale-sweep results.
- `cargo locate-project --workspace`, `cargo metadata --format-version 1 --no-deps`, and `cargo check --workspace` succeed from repo root.
- `tests/planning/test_phase07_validation.py` passes and fails if live Rust workspace authority reappears under `ClassicLib-rs`.
</success_criteria>

<output>
After completion, create `.planning/phases/07-crate-relocation-and-path-rewire/07-03-SUMMARY.md`
</output>
