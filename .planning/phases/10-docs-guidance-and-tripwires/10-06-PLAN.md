---
phase: 10-docs-guidance-and-tripwires
plan: "06"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - Agent-consumed conventions, testing, and concern maps use current repo-root file locations and command examples.
    - Testing guidance in the codebase map stops teaching `ClassicLib-rs/Cargo.toml` or root-old binding paths as live commands.
  artifacts:
    - path: .planning/codebase/CONVENTIONS.md
      provides: Current naming/import/path examples.
    - path: .planning/codebase/TESTING.md
      provides: Current command and test-location map.
    - path: .planning/codebase/CONCERNS.md
      provides: Current issue file paths and fragile-area references.
  key_links:
    - from: .planning/codebase/TESTING.md
      to: docs/workspace-migration-matrix.md
      via: migration note or related reference
      pattern: 'workspace-migration-matrix\.md'
---

<objective>
Refresh the conventions/testing/concerns codebase maps so agent discovery and validation guidance matches the moved workspace tree.

Purpose: Implement the remaining D-03 codebase-map cleanup on the documents that agents use for naming, testing, and known-risk discovery.
Output: Updated convention, testing, and concerns maps with repo-root file paths, repo-root commands, and current fragile-area references.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@docs/workspace-migration-matrix.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update conventions and testing maps to repo-root examples</name>
  <files>.planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md
    - .planning/codebase/TESTING.md
    - docs/workspace-migration-matrix.md
    - tests/planning/test_phase06_validation.py
  </read_first>
  <action>Refresh `CONVENTIONS.md` and `TESTING.md` so their active examples use repo-root paths and commands per D-03/D-07. Replace path examples like `ClassicLib-rs/business-logic/...`, `ClassicLib-rs/node-bindings/classic-node/...`, and `ClassicLib-rs/python-bindings/...` with `business-logic/...`, `node-bindings/classic-node/...`, and `python-bindings/...` wherever the text is describing the live tree rather than a historical note. In `TESTING.md`, replace `cargo ... --manifest-path ClassicLib-rs/Cargo.toml` with plain repo-root Cargo commands, replace `ClassicLib-rs/python-bindings/.venv` with `python-bindings/.venv`, and update test-location examples to the root-level moved tree. Keep `ClassicLib-rs` only for explicitly labeled historical residue such as `ClassicLib-rs/target` when that distinction matters.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/TESTING.md` contains `cargo test --workspace --release`, `python-bindings/.venv`, and `node-bindings/classic-node/__test__/`.
    - `.planning/codebase/TESTING.md` does not contain `--manifest-path ClassicLib-rs/Cargo.toml`.
    - `.planning/codebase/CONVENTIONS.md` contains `business-logic/`, `node-bindings/classic-node/`, and `python-bindings/` as live path examples.
  </acceptance_criteria>
  <done>The conventions and testing maps now use repo-root examples and commands for the live tree.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh concern references to the moved live tree</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/ARCHITECTURE.md
  </read_first>
  <action>Update `CONCERNS.md` so every live source-file, binding, and tooling reference points at the moved repo-root tree per D-03. Replace stale live paths rooted at `ClassicLib-rs/` with the current root-level equivalents (`business-logic/...`, `foundation/...`, `cpp-bindings/...`, `node-bindings/...`, `python-bindings/...`, `ui-applications/...`). Preserve the meaning of each concern, bug, and fragile-area note; only change the path ownership and any stale layout wording. If `ClassicLib-rs/target` needs to remain as a generated-residue reference, label it as such rather than as an active source location.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/CONCERNS.md` contains `business-logic/classic-config-core`, `cpp-bindings/classic-cpp-bridge`, and `node-bindings/classic-node`.
    - `.planning/codebase/CONCERNS.md` does not use `ClassicLib-rs/...` as the active source path for fragile areas or bugs.
    - Any remaining `ClassicLib-rs` mention in `CONCERNS.md` is limited to explicitly labeled legacy residue or historical context.
  </acceptance_criteria>
  <done>The concerns map points at the current moved tree without losing the documented risk inventory.</done>
</task>

</tasks>

<verification>
Plan 10-07 will verify these files with `python -m pytest tests/planning/test_phase10_validation.py -q`.
</verification>

<success_criteria>
- Codebase-map conventions and testing examples are repo-root-first.
- Concern references point at the moved live tree instead of stale pre-move paths.
- Historical `ClassicLib-rs` mentions are clearly labeled and no longer look like live instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-06-SUMMARY.md`
</output>
