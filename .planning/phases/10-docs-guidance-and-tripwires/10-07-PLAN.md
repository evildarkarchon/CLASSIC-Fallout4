---
phase: 10-docs-guidance-and-tripwires
plan: "07"
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - tests/planning/test_phase10_validation.py
  - tests/powershell/phase10_guidance_tripwires.test.ps1
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - The repo has executable guards that fail if active docs or guidance reintroduce `ClassicLib-rs` as the live workspace root.
    - Must-read docs, skill mirrors, and codebase maps are covered by deterministic file-backed assertions.
    - The stale-path sweep excludes planning/history surfaces instead of banning every `ClassicLib-rs` mention globally.
  artifacts:
    - path: tests/planning/test_phase10_validation.py
      provides: Must-read audit plus scoped stale-path sweep for active docs, guidance, scripts, and tests.
    - path: tests/powershell/phase10_guidance_tripwires.test.ps1
      provides: PowerShell AST/text tripwire for active wrapper-script guidance.
  key_links:
    - from: tests/planning/test_phase10_validation.py
      to: README.md
      via: explicit must-read list
      pattern: 'README\.md'
    - from: tests/powershell/phase10_guidance_tripwires.test.ps1
      to: rebuild_rust.ps1
      via: AST/text scan target list
      pattern: 'rebuild_rust\.ps1'
---

<objective>
Add the hybrid regression tripwires that keep Phase 10 closed.

Purpose: Implement D-06 and D-07 by creating deterministic must-read audits plus a scoped stale-path sweep over active docs, guidance, scripts, and tests.
Output: A Python planning audit and a PowerShell script tripwire that fail when active guidance reintroduces live `ClassicLib-rs` workspace-root instructions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
@tests/planning/test_phase06_validation.py
@tests/planning/test_phase09_validation.py
@tests/powershell/rebuild_rust.general_target.test.ps1
@README.md
@docs/workspace-migration-matrix.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 10 planning audit with must-read coverage and scoped sweep</name>
  <files>tests/planning/test_phase10_validation.py</files>
  <read_first>
    - tests/planning/test_phase06_validation.py
    - tests/planning/test_phase09_validation.py
    - README.md
    - docs/workspace-migration-matrix.md
    - AGENTS.md
    - .planning/codebase/STRUCTURE.md
  </read_first>
  <behavior>
    - Test 1: the must-read list covers the matrix, top-level docs, API pages, AGENTS, all classic-project-guide mirrors, and all `.planning/codebase/*.md` files updated in Plans 10-01 through 10-06.
    - Test 2: required repo-root strings such as `cargo test --workspace`, `python-bindings/.venv`, and `node-bindings/classic-node` are present in the active guidance files.
    - Test 3: forbidden live-root phrases such as `--manifest-path ClassicLib-rs/Cargo.toml`, `uv venv ClassicLib-rs/python-bindings/.venv`, `ClassicLib-rs/node-bindings/classic-node`, and `working-directory: ClassicLib-rs` fail in the scoped sweep unless they appear inside an explicitly allowed historical or migration note.
  </behavior>
  <action>Write `tests/planning/test_phase10_validation.py` using `unittest.TestCase` plus `subTest()` in the same file-backed style as Phase 6 and Phase 9. Include explicit constants for `MUST_READ_DOCS`, `MUST_READ_API_DOCS`, `MUST_READ_AGENT_GUIDANCE`, `MUST_READ_CODEBASE_MAPS`, `SCOPED_SWEEP_PATHS`, `EXCLUDED_PATH_PARTS`, `ALLOWED_HISTORICAL_MARKERS`, and `FORBIDDEN_ACTIVE_PHRASES`. The must-read list must cover: `README.md`, `docs/workspace-migration-matrix.md`, `docs/README.md`, `docs/RUST_DOCUMENTATION_INDEX.md`, `docs/testing/TESTING_GUIDE_INDEX.md`, `docs/api/README.md`, `docs/api/QUICK_START.md`, `docs/api/binding-contract-refresh-note.md`, `AGENTS.md`, all four `SKILL.md` entrypoints, all four mirrored `repo-guide.md` files, and the seven `.planning/codebase/*.md` maps updated in this phase. The scoped sweep must target only active docs/guidance/scripts/tests (for example `README.md`, `docs/**/*.md`, `AGENTS.md`, `.agents/**/*.md`, `.opencode/**/*.md`, `.claude/**/*.md`, `.agent/**/*.md`, `.planning/codebase/*.md`, `tests/powershell/*.ps1`, and repo-root `*.ps1`) and explicitly exclude planning/history/archive surfaces such as `.planning/phases/`, `.planning/milestones/`, `.planning/research/`, `deprecated/`, and `performance_baselines/`. Assert the matrix link exists on the must-read surfaces and assert D-07 by allowing `ClassicLib-rs` only when paired with labels like `Historical note:` or `Migration note:`.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/planning/test_phase10_validation.py` contains `MUST_READ_AGENT_GUIDANCE`, `EXCLUDED_PATH_PARTS`, and `FORBIDDEN_ACTIVE_PHRASES`.
    - The test file asserts on `cargo test --workspace`, `python-bindings/.venv`, `node-bindings/classic-node`, and `workspace-migration-matrix.md`.
    - `python -m pytest tests/planning/test_phase10_validation.py -q` exits 0 after the file is written.
  </acceptance_criteria>
  <done>The Python planning audit enforces the must-read surfaces and the scoped stale-path policy for Phase 10.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the PowerShell AST/text tripwire for active wrappers</name>
  <files>tests/powershell/phase10_guidance_tripwires.test.ps1</files>
  <read_first>
    - tests/powershell/rebuild_rust.general_target.test.ps1
    - rebuild_rust.ps1
    - rebuild_node.ps1
    - classic-cli/build_cli.ps1
    - classic-gui/build_gui.ps1
  </read_first>
  <behavior>
    - Test 1: each active wrapper script parses cleanly with `Parser.ParseFile(...)`.
    - Test 2: active wrapper scripts do not contain stale live-root guidance like `ClassicLib-rs/Cargo.toml`, `ClassicLib-rs/python-bindings/.venv`, or `ClassicLib-rs/node-bindings/classic-node`.
    - Test 3: the tripwire fails fast with a descriptive message naming the offending script and phrase.
  </behavior>
  <action>Create `tests/powershell/phase10_guidance_tripwires.test.ps1` as the D-06 PowerShell half of the hybrid guard. Parse each of these files with `System.Management.Automation.Language.Parser.ParseFile`: `rebuild_rust.ps1`, `rebuild_node.ps1`, `classic-cli/build_cli.ps1`, and `classic-gui/build_gui.ps1`. After parse success, scan their text for forbidden live-root phrases including `ClassicLib-rs/Cargo.toml`, `--manifest-path ClassicLib-rs/Cargo.toml`, `ClassicLib-rs/python-bindings/.venv`, `ClassicLib-rs/node-bindings/classic-node`, and `working-directory: ClassicLib-rs`. Emit descriptive failures naming the script and the exact forbidden phrase. Keep the test file deterministic and file-backed; do not run full build workflows from it.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File tests/powershell/phase10_guidance_tripwires.test.ps1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/powershell/phase10_guidance_tripwires.test.ps1` contains `Parser]::ParseFile`, `rebuild_rust.ps1`, `rebuild_node.ps1`, `classic-cli/build_cli.ps1`, and `classic-gui/build_gui.ps1`.
    - The test file names the forbidden phrases `ClassicLib-rs/Cargo.toml`, `ClassicLib-rs/python-bindings/.venv`, and `ClassicLib-rs/node-bindings/classic-node`.
    - `pwsh -ExecutionPolicy Bypass -File tests/powershell/phase10_guidance_tripwires.test.ps1` exits 0 after the file is written.
  </acceptance_criteria>
  <done>The PowerShell tripwire enforces the script-sensitive stale-path policy without running heavyweight toolchains.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/planning/test_phase10_validation.py -q`
- `pwsh -ExecutionPolicy Bypass -File tests/powershell/phase10_guidance_tripwires.test.ps1`
</verification>

<success_criteria>
- Must-read docs, skills, and codebase maps are covered by executable tests.
- The scoped sweep rejects active live-root guidance without flagging excluded planning/history surfaces.
- Active wrapper scripts are parse-checked and scanned for forbidden stale-root phrases.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-07-SUMMARY.md`
</output>
