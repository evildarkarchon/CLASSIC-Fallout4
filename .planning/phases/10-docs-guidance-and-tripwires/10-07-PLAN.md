---
phase: 10-docs-guidance-and-tripwires
plan: "07"
type: execute
wave: 2
depends_on: ["10-00", "10-01"]
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - .agents/skills/classic-project-guide/SKILL.md
  - .opencode/skills/classic-project-guide/SKILL.md
  - .claude/skills/classic-project-guide/SKILL.md
  - .agent/skills/classic-project-guide/SKILL.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - Always-on agent entrypoints teach the repo-root contract.
    - `CLAUDE.md` is corrected alongside `AGENTS.md` and the mirrored skill entrypoints.
    - Skill mirrors stay synchronized on one migration-aware guidance contract.
  artifacts:
    - path: AGENTS.md
      provides: Repo-root always-on guidance.
    - path: CLAUDE.md
      provides: Repo-root agent-context summary.
    - path: .agents/skills/classic-project-guide/SKILL.md
      provides: Primary skill entrypoint aligned to the new root layout.
  key_links:
    - from: AGENTS.md
      to: docs/workspace-migration-matrix.md
      via: migration reference link
      pattern: workspace-migration-matrix\.md
    - from: CLAUDE.md
      to: AGENTS.md
      via: top-level reference
      pattern: @AGENTS\.md
---

<objective>
Refresh the always-on agent entrypoints so every agent environment starts from the repo-root contract.

Purpose: Implement D-03/D-04/D-05 and explicitly address the review finding that `CLAUDE.md` was missing from the original Phase 10 scope.
Output: Updated `AGENTS.md`, `CLAUDE.md`, and synchronized skill-entrypoint mirrors across all agent environments.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-REVIEWS.md
@docs/workspace-migration-matrix.md
@AGENTS.md
@CLAUDE.md
@.agents/skills/classic-project-guide/SKILL.md
@.opencode/skills/classic-project-guide/SKILL.md
@.claude/skills/classic-project-guide/SKILL.md
@.agent/skills/classic-project-guide/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AGENTS.md and CLAUDE.md to the post-move contract</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <action>Refresh `AGENTS.md` and `CLAUDE.md` per D-03/D-07 so they stop teaching `ClassicLib-rs` as the live workspace root. Keep the existing policy and guardrails intact, but replace active location examples with the repo-root directories `foundation/`, `business-logic/`, `cpp-bindings/classic-cpp-bridge/`, `node-bindings/classic-node/`, `python-bindings/`, and `ui-applications/classic-tui/`. Update cargo, Node, and Python command examples to repo-root and root-level binding paths. Add or preserve a short link to `docs/workspace-migration-matrix.md` for translation lookups. In `CLAUDE.md`, keep the generated-knowledge structure but rewrite the stale path examples and stack/architecture references so the file no longer contradicts `AGENTS.md`.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "agent_entrypoints_contract"</automated>
  </verify>
  <done>`AGENTS.md` and `CLAUDE.md` both teach the repo-root contract and no longer describe `ClassicLib-rs` as the live workspace root.</done>
</task>

<task type="auto">
  <name>Task 2: Sync all classic-project-guide entrypoint mirrors</name>
  <files>.agents/skills/classic-project-guide/SKILL.md, .opencode/skills/classic-project-guide/SKILL.md, .claude/skills/classic-project-guide/SKILL.md, .agent/skills/classic-project-guide/SKILL.md</files>
  <action>Update all four `classic-project-guide` entrypoint mirrors per D-03 so they match the live repo-root layout and matrix-routing contract. Preserve the existing guardrails, but replace active path examples like `ClassicLib-rs/`, `ClassicLib-rs/node-bindings/`, and `ClassicLib-rs/python-bindings/` with root-level `business-logic/`, `node-bindings/`, and `python-bindings/` guidance. Keep `ClassicLib-rs` only inside explicitly labeled migration or historical notes per D-07. Ensure the wording stays materially synchronized across the four mirrors.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "agent_entrypoints_contract"</automated>
  </verify>
  <done>All classic-project-guide entrypoint mirrors align to the same repo-root guidance contract.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/planning/test_phase10_validation.py -q -k "agent_entrypoints_contract"`
</verification>

<success_criteria>
- `AGENTS.md` and `CLAUDE.md` no longer contradict the repo-root workspace contract.
- All skill entrypoints are synchronized and migration-matrix-aware.
- The review concern about the missing `CLAUDE.md` surface is resolved explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-07-SUMMARY.md`
</output>
