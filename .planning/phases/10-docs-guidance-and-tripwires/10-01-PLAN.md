---
phase: 10-docs-guidance-and-tripwires
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/README.md
  - docs/RUST_DOCUMENTATION_INDEX.md
  - docs/testing/TESTING_GUIDE_INDEX.md
  - docs/workspace-migration-matrix.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - Contributor-facing entry docs teach the repo root as the live workspace shell.
    - Contributors can find one migration matrix instead of duplicated old/new path translations.
    - Active top-level docs link directly to the shared migration matrix.
  artifacts:
    - path: docs/workspace-migration-matrix.md
      provides: Single old-to-new command, path, and artifact translation page.
    - path: README.md
      provides: Repo entrypoint updated to repo-root workspace guidance.
    - path: docs/README.md
      provides: Documentation hub updated to repo-root guidance and matrix routing.
    - path: docs/RUST_DOCUMENTATION_INDEX.md
      provides: Rust documentation index aligned to repo-root commands and locations.
    - path: docs/testing/TESTING_GUIDE_INDEX.md
      provides: Testing index aligned to repo-root commands and binding paths.
  key_links:
    - from: README.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: '\[Workspace Migration Matrix\]\(docs/workspace-migration-matrix\.md\)'
    - from: docs/README.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: '\[Workspace Migration Matrix\]\(workspace-migration-matrix\.md\)'
    - from: docs/testing/TESTING_GUIDE_INDEX.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: '\[Workspace Migration Matrix\]\(\.\./workspace-migration-matrix\.md\)'
---

<objective>
Establish the single migration reference and repoint the top-level contributor entry docs to the repo-root workspace contract.

Purpose: Implement D-01, D-02, D-04, and D-05 by making the README/docs entry surfaces teach the current layout and route every old-to-new translation through one shared page.
Output: A new workspace migration matrix plus refreshed top-level docs that point contributors at repo-root commands, root-level Rust layer directories, and root-level binding paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
@README.md
@docs/README.md
@docs/RUST_DOCUMENTATION_INDEX.md
@docs/testing/TESTING_GUIDE_INDEX.md
@tests/planning/test_phase06_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared workspace migration matrix</name>
  <files>docs/workspace-migration-matrix.md</files>
  <read_first>
    - .planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
    - .planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
    - README.md
    - docs/README.md
    - docs/testing/TESTING_GUIDE_INDEX.md
  </read_first>
  <action>Create `docs/workspace-migration-matrix.md` as the single D-04/D-05 translation artifact. Use these exact sections: `# Workspace Migration Matrix`, `## Command Translation`, `## Path Root Translation`, `## Artifact And Report Locations`, and `## Historical Note Rules`. In the command table, include at minimum these rows: `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` → `cargo build --workspace`; `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` → `cargo test --workspace`; `cargo fmt --all --manifest-path ClassicLib-rs/Cargo.toml -- --check` → `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` → `cargo clippy --workspace --all-targets --all-features -- -D warnings`. In the path/artifact tables, include at minimum `ClassicLib-rs/node-bindings/classic-node` → `node-bindings/classic-node`, `ClassicLib-rs/python-bindings/.venv` → `python-bindings/.venv`, `ClassicLib-rs/python-bindings/parity-artifacts/stub_validation_report.json` → `python-bindings/parity-artifacts/stub_validation_report.json`, `ClassicLib-rs/node-bindings/classic-node/parity-artifacts/` → `node-bindings/classic-node/parity-artifacts/`, and `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/` → `cpp-bindings/classic-cpp-bridge/parity-artifacts/`. State explicitly that `ClassicLib-rs` may appear only inside labeled historical or migration notes per D-07.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `docs/workspace-migration-matrix.md` exists and contains `# Workspace Migration Matrix`.
    - The matrix file contains the exact strings `cargo build --workspace`, `python-bindings/.venv`, and `node-bindings/classic-node`.
    - The matrix file contains the phrase `Historical Note Rules` and states that `ClassicLib-rs` is allowed only in labeled historical or migration notes.
  </acceptance_criteria>
  <done>The shared matrix exists and enumerates concrete command/path/artifact replacements instead of prose-only migration advice.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint top-level contributor docs to the matrix and repo-root contract</name>
  <files>README.md, docs/README.md, docs/RUST_DOCUMENTATION_INDEX.md, docs/testing/TESTING_GUIDE_INDEX.md</files>
  <read_first>
    - README.md
    - docs/README.md
    - docs/RUST_DOCUMENTATION_INDEX.md
    - docs/testing/TESTING_GUIDE_INDEX.md
    - docs/workspace-migration-matrix.md
    - .planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
  </read_first>
  <action>Update the four entrypoint docs per D-01/D-02 so they teach the live repo-root layout, not `ClassicLib-rs` as the workspace root. Replace active path bullets with the root-level directories `foundation/`, `business-logic/`, `cpp-bindings/classic-cpp-bridge/`, `node-bindings/`, `python-bindings/`, and `ui-applications/classic-tui/`. Replace live Cargo commands with plain repo-root commands (no `--manifest-path ClassicLib-rs/Cargo.toml`). Replace live binding workflow paths with `node-bindings/classic-node` and `python-bindings/.venv`. Add a visible `[Workspace Migration Matrix]` link in each file using the correct relative target. If any `ClassicLib-rs` mention must remain, relabel it as an explicit `Historical note:` or `Migration note:` instead of an active instruction per D-07.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `[Workspace Migration Matrix](docs/workspace-migration-matrix.md)` and `cargo test --workspace`.
    - `docs/README.md` contains `[Workspace Migration Matrix](workspace-migration-matrix.md)` and `python-bindings/.venv`.
    - `docs/RUST_DOCUMENTATION_INDEX.md` no longer contains `--manifest-path ClassicLib-rs/Cargo.toml`.
    - `docs/testing/TESTING_GUIDE_INDEX.md` contains `node-bindings/classic-node`, `python-bindings/.venv`, and `[Workspace Migration Matrix](../workspace-migration-matrix.md)`.
  </acceptance_criteria>
  <done>The top-level contributor docs now route through the shared matrix and teach repo-root workspace, Node, and Python workflows without stale live-root guidance.</done>
</task>

</tasks>

<verification>
Plan 10-07 will add the executable guard for this surface via `python -m pytest tests/planning/test_phase10_validation.py -q`.
</verification>

<success_criteria>
- Contributors reach a single migration matrix from every top-level doc entrypoint.
- Active top-level docs use repo-root Cargo commands and root-level binding paths.
- Any remaining `ClassicLib-rs` mention is explicitly historical or migration-only.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-01-SUMMARY.md`
</output>
