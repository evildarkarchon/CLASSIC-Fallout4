---
phase: 10-docs-guidance-and-tripwires
plan: "03"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - AGENTS.md
  - .agents/skills/classic-project-guide/SKILL.md
  - .opencode/skills/classic-project-guide/SKILL.md
  - .claude/skills/classic-project-guide/SKILL.md
  - .agent/skills/classic-project-guide/SKILL.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - Always-on agent entrypoints teach the repo root as the live workspace shell.
    - Every mirrored skill entrypoint stays synchronized on the same repo-root guidance.
  artifacts:
    - path: AGENTS.md
      provides: Always-on repo guidance aligned to the repo-root contract.
    - path: .agents/skills/classic-project-guide/SKILL.md
      provides: Primary skill entrypoint aligned to the repo-root contract.
    - path: .opencode/skills/classic-project-guide/SKILL.md
      provides: Mirrored skill entrypoint aligned to the repo-root contract.
    - path: .claude/skills/classic-project-guide/SKILL.md
      provides: Mirrored skill entrypoint aligned to the repo-root contract.
    - path: .agent/skills/classic-project-guide/SKILL.md
      provides: Mirrored skill entrypoint aligned to the repo-root contract.
  key_links:
    - from: AGENTS.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: 'workspace-migration-matrix\.md'
---

<objective>
Refresh always-on agent entrypoints so every agent environment points at the repo-root workspace layout and shared migration matrix.

Purpose: Implement D-03, D-04, D-05, and D-07 for the always-read guidance surfaces that route planners and executors before they open detailed repo docs.
Output: Updated `AGENTS.md` plus synchronized skill entrypoints across `.agents`, `.opencode`, `.claude`, and `.agent`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@docs/workspace-migration-matrix.md
@AGENTS.md
@.agents/skills/classic-project-guide/SKILL.md
@.opencode/skills/classic-project-guide/SKILL.md
@.claude/skills/classic-project-guide/SKILL.md
@.agent/skills/classic-project-guide/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AGENTS.md to the post-migration contract</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md
    - docs/workspace-migration-matrix.md
    - .planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
  </read_first>
  <action>Rewrite the active path and quick-note sections in `AGENTS.md` per D-03/D-07 so they stop teaching `ClassicLib-rs` as the live workspace location. Replace the project overview and quick notes with the root-level directories `foundation/`, `business-logic/`, `cpp-bindings/classic-cpp-bridge/`, `node-bindings/classic-node`, `python-bindings/`, and `ui-applications/classic-tui`. Keep the existing repository rules, but update any command/path examples so repo-root Cargo and root-level binding paths are the default contract. Add a short link to `docs/workspace-migration-matrix.md` for old-to-new translations. If `ClassicLib-rs` remains anywhere, label it as a historical or migration-only note per D-07.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `foundation/`, `business-logic/`, and `node-bindings/classic-node`.
    - `AGENTS.md` contains `workspace-migration-matrix.md`.
    - `AGENTS.md` does not describe `ClassicLib-rs/` as the live workspace root.
  </acceptance_criteria>
  <done>`AGENTS.md` teaches the post-migration repo-root layout and points agents at the shared migration matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Sync all classic-project-guide entrypoint mirrors</name>
  <files>.agents/skills/classic-project-guide/SKILL.md, .opencode/skills/classic-project-guide/SKILL.md, .claude/skills/classic-project-guide/SKILL.md, .agent/skills/classic-project-guide/SKILL.md</files>
  <read_first>
    - .agents/skills/classic-project-guide/SKILL.md
    - .opencode/skills/classic-project-guide/SKILL.md
    - .claude/skills/classic-project-guide/SKILL.md
    - .agent/skills/classic-project-guide/SKILL.md
    - AGENTS.md
    - docs/workspace-migration-matrix.md
  </read_first>
  <action>Update all four `classic-project-guide` `SKILL.md` entrypoints so they mirror the same live guidance per D-03/D-07. Replace active path examples that still say `ClassicLib-rs/`, `ClassicLib-rs/node-bindings/`, or `ClassicLib-rs/python-bindings/` with the repo-root equivalents `business-logic/`, `node-bindings/`, and `python-bindings/`. Keep the same guardrails and workflow intent, but make the wording and path examples consistent across all mirrors. Add the same short migration-matrix reference to each entrypoint so every agent environment routes to one translation page instead of inventing its own migration prose.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - All four `SKILL.md` files contain `node-bindings/`, `python-bindings/`, and `workspace-migration-matrix.md`.
    - No `SKILL.md` file teaches `ClassicLib-rs/` as the live workspace root.
    - The four `SKILL.md` files are semantically synchronized on the repo-root path contract.
  </acceptance_criteria>
  <done>All skill entrypoint mirrors now point agents at the same repo-root contract and same migration reference.</done>
</task>

</tasks>

<verification>
Plan 10-07 will verify these files with `python -m pytest tests/planning/test_phase10_validation.py -q`.
</verification>

<success_criteria>
- `AGENTS.md` and all classic-project-guide entrypoints teach the same repo-root layout.
- Every mirror links to the shared migration matrix instead of duplicating path-translation prose.
- Active guidance allows `ClassicLib-rs` only as an explicitly labeled historical or migration note.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-03-SUMMARY.md`
</output>
