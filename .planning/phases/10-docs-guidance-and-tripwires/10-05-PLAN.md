---
phase: 10-docs-guidance-and-tripwires
plan: "05"
type: execute
wave: 2
depends_on: ["10-00", "10-01"]
files_modified:
  - docs/api/classic-path-core.md
  - docs/api/classic-xse-core.md
  - docs/api/game-setup-workflow.md
  - docs/api/formid-settings-boundary.md
  - docs/api/classic-file-io-core.md
  - docs/api/classic-resource-core.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - Path/setup/resource docs point at root-level source and artifact locations.
    - These workflow pages keep current ownership boundaries without stale `ClassicLib-rs` operational paths.
  artifacts:
    - path: docs/api/classic-path-core.md
      provides: Root-level path-core references.
    - path: docs/api/game-setup-workflow.md
      provides: Root-level setup workflow references.
  key_links:
    - from: docs/api/game-setup-workflow.md
      to: docs/api/classic-path-core.md
      via: workflow dependency reference
      pattern: classic-path-core\.md
---

<objective>
Refresh the path/setup/file/resource API workflow docs to the repo-root contract.

Purpose: Implement D-01 for the third cluster of active workflow-style API pages.
Output: Updated path/setup/resource docs with root-level source references and clearly bounded historical notes only.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@docs/api/classic-path-core.md
@docs/api/classic-xse-core.md
@docs/api/game-setup-workflow.md
@docs/api/formid-settings-boundary.md
@docs/api/classic-file-io-core.md
@docs/api/classic-resource-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the path and setup workflow reference pages</name>
  <files>docs/api/classic-path-core.md, docs/api/classic-xse-core.md, docs/api/game-setup-workflow.md</files>
  <action>Refresh the path/XSE/setup docs so every live source path, neighboring crate reference, and binding path uses the root-level tree per D-01/D-07. Replace `ClassicLib-rs/business-logic/...` and binding paths with `business-logic/...`, `cpp-bindings/...`, `node-bindings/...`, and `python-bindings/...`. Preserve any historical consolidation note only when explicitly labeled. Keep the workflow narrative intact; only the path and location contract should change.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "api_runtime_group_c_contract"</automated>
  </verify>
  <done>The path and setup workflow docs now reference the moved repo-root tree accurately.</done>
</task>

<task type="auto">
  <name>Task 2: Update the boundary, file I/O, and resource reference pages</name>
  <files>docs/api/formid-settings-boundary.md, docs/api/classic-file-io-core.md, docs/api/classic-resource-core.md</files>
  <action>Refresh these three docs so every live crate, binding, and artifact reference uses the root-level layout per D-01 and no active `ClassicLib-rs/...` guidance remains. Keep the current boundary explanations and neighbor-crate discussion, but convert any remaining old-root mention into a labeled historical note instead of an active path instruction.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "api_runtime_group_c_contract"</automated>
  </verify>
  <done>The boundary/file/resource docs point at root-level files and no longer teach the old workspace-root layout.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/planning/test_phase10_validation.py -q -k "api_runtime_group_c_contract"`
</verification>

<success_criteria>
- Path, setup, file, and resource workflow docs all use root-level source references.
- Historical notes remain explicit and do not masquerade as current workspace instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-05-SUMMARY.md`
</output>
