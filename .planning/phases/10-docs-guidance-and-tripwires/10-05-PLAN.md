---
phase: 10-docs-guidance-and-tripwires
plan: "05"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - Agent-consumed architecture and structure maps describe the repo-root layout instead of the legacy workspace-root layout.
    - Stack and integration maps point at repo-root manifests, commands, and artifact locations.
  artifacts:
    - path: .planning/codebase/STRUCTURE.md
      provides: Repo-root directory map for agent discovery.
    - path: .planning/codebase/STACK.md
      provides: Repo-root stack and package-manager contract.
    - path: .planning/codebase/ARCHITECTURE.md
      provides: Repo-root architecture and layer routing guidance.
    - path: .planning/codebase/INTEGRATIONS.md
      provides: Integration and automation map aligned to root-level paths.
  key_links:
    - from: .planning/codebase/STRUCTURE.md
      to: docs/workspace-migration-matrix.md
      via: migration note or related reference
      pattern: 'workspace-migration-matrix\.md'
---

<objective>
Refresh the operational codebase maps so agents see the current root-level structure, stack, architecture, and integration surfaces.

Purpose: Implement the D-03 active-agent-guidance requirement for the highest-traffic `.planning/codebase` maps that describe how the repo is laid out and how automation surfaces are wired.
Output: Updated structure, stack, architecture, and integration maps that describe root-level directories, repo-root manifests, and root-level binding/tooling paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@docs/workspace-migration-matrix.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update structure and architecture maps to the root-level layout</name>
  <files>.planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/ARCHITECTURE.md
    - docs/workspace-migration-matrix.md
    - .planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
  </read_first>
  <action>Rewrite `STRUCTURE.md` and `ARCHITECTURE.md` so active directory, layer, and entrypoint guidance uses the repo-root layout per D-03/D-07. Replace the old top-level `ClassicLib-rs/` workspace container with the root-level layer directories `foundation/`, `business-logic/`, `cpp-bindings/`, `node-bindings/`, `python-bindings/`, and `ui-applications/`. Replace references to `ClassicLib-rs/Cargo.toml` with `Cargo.toml` and `ClassicLib-rs/validate_stubs.py` with `validate_stubs.py`. Keep `ClassicLib-rs/target` only when explicitly labeled as legacy residue or generated output, not as an active source tree. Add a short migration-matrix reference where the file discusses old-versus-new path translation.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/STRUCTURE.md` contains `foundation/`, `business-logic/`, `cpp-bindings/`, and `validate_stubs.py`.
    - `.planning/codebase/ARCHITECTURE.md` contains `Cargo.toml` and root-level `node-bindings/classic-node` / `python-bindings/` paths.
    - Neither file describes `ClassicLib-rs/` as the live workspace container.
  </acceptance_criteria>
  <done>The structure and architecture maps now describe the root-level layout and active entrypoints correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Update stack and integrations maps to repo-root manifests and workflows</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/INTEGRATIONS.md</files>
  <read_first>
    - .planning/codebase/STACK.md
    - .planning/codebase/INTEGRATIONS.md
    - docs/workspace-migration-matrix.md
    - .planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
  </read_first>
  <action>Refresh `STACK.md` and `INTEGRATIONS.md` so active manifest, working-directory, package-manager, CI, and artifact examples are root-level per D-03/D-07. Replace `ClassicLib-rs/Cargo.toml`, `ClassicLib-rs/Cargo.lock`, and `ClassicLib-rs/.cargo/config.toml` with `Cargo.toml`, `Cargo.lock`, and `.cargo/config.toml`. Replace `ClassicLib-rs/node-bindings/classic-node` with `node-bindings/classic-node`, and replace `ClassicLib-rs/python-bindings/.venv` / `ClassicLib-rs/python-bindings/...` with `python-bindings/.venv` / `python-bindings/...`. Update any CI or integration references that still use `working-directory: ClassicLib-rs` or `ClassicLib-rs/target` as active guidance. Keep any remaining `ClassicLib-rs` mention explicitly historical only.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/STACK.md` contains `Cargo.toml`, `Cargo.lock`, and `node-bindings/classic-node/package.json`.
    - `.planning/codebase/STACK.md` does not contain `ClassicLib-rs/Cargo.toml` or `ClassicLib-rs/Cargo.lock` as active stack roots.
    - `.planning/codebase/INTEGRATIONS.md` contains `python-bindings/.venv` and `node-bindings/classic-node/parity-artifacts/`.
  </acceptance_criteria>
  <done>The stack and integrations maps now describe repo-root manifests, working directories, and artifact locations.</done>
</task>

</tasks>

<verification>
Plan 10-07 will verify these files with `python -m pytest tests/planning/test_phase10_validation.py -q`.
</verification>

<success_criteria>
- Operational codebase maps describe root-level directories and manifests.
- Integration and stack maps no longer teach `ClassicLib-rs` as the live workspace root.
- Any legacy mention is clearly marked historical rather than operational.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-05-SUMMARY.md`
</output>
