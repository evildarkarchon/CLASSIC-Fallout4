---
phase: 10-docs-guidance-and-tripwires
plan: "02"
type: execute
wave: 2
depends_on: ["10-00", "10-01"]
files_modified:
  - docs/api/README.md
  - docs/api/QUICK_START.md
  - docs/api/binding-contract-refresh-note.md
  - docs/api/error-contract.md
  - docs/api/binding-parity-overview.md
  - docs/api/binding-parity-policy.md
  - docs/api/node-python-contract-map.md
  - docs/api/cxx-parity-gate.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - API hub pages route contributors through repo-root paths and the shared migration matrix.
    - Binding workflow docs teach current Node, Python, and CXX maintenance paths.
    - The active API doc surface no longer leaves stale live-root instructions on the high-traffic binding pages.
  artifacts:
    - path: docs/api/README.md
      provides: Active API doc routing index aligned to the repo-root contract.
    - path: docs/api/binding-contract-refresh-note.md
      provides: Binding contract maintenance guide aligned to root-level artifact paths.
    - path: docs/api/node-python-contract-map.md
      provides: Root-level contract and parity artifact map for Node and Python.
    - path: docs/api/cxx-parity-gate.md
      provides: Root-level CXX parity gate instructions.
  key_links:
    - from: docs/api/README.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: \../workspace-migration-matrix\.md
    - from: docs/api/node-python-contract-map.md
      to: docs/workspace-migration-matrix.md
      via: migration reference link
      pattern: workspace-migration-matrix\.md
---

<objective>
Refresh the active API hubs and binding workflow docs so they match the repo-root workspace contract.

Purpose: Address D-01/D-02/D-05 and the review finding that the original API-doc plan was under-scoped relative to the later regression guard.
Output: Updated API hub pages and binding-maintenance references that use repo-root paths, commands, and artifact locations.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
@.planning/phases/10-docs-guidance-and-tripwires/10-REVIEWS.md
@docs/workspace-migration-matrix.md
@docs/api/README.md
@docs/api/QUICK_START.md
@docs/api/binding-contract-refresh-note.md
@docs/api/binding-parity-overview.md
@docs/api/binding-parity-policy.md
@docs/api/node-python-contract-map.md
@docs/api/cxx-parity-gate.md
@docs/api/error-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the API hubs and contract-maintenance pages</name>
  <files>docs/api/README.md, docs/api/QUICK_START.md, docs/api/binding-contract-refresh-note.md, docs/api/error-contract.md</files>
  <action>Update these pages per D-01/D-02/D-05. In `docs/api/README.md`, explicitly route contributors through the active docs/api surface without describing `ClassicLib-rs/business-logic/` as the live root. In `QUICK_START.md`, replace active layout references with root-level directories and link to `../workspace-migration-matrix.md`. In `binding-contract-refresh-note.md`, replace every active Node/Python/CXX path and command with the root-level equivalents, including `python validate_stubs.py --rust-dir .`, `node-bindings/classic-node/parity-artifacts/`, `python-bindings/parity-artifacts/`, and `cpp-bindings/classic-cpp-bridge/...`. In `error-contract.md`, update all source links and examples to root-level Rust/binding paths while preserving the documented error-shape intent. Keep `ClassicLib-rs` only inside clearly labeled migration or historical notes per D-07.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "api_hubs_and_binding_workflow_contract"</automated>
  </verify>
  <done>The API hub and contract-maintenance docs now point contributors at repo-root commands, source files, and artifact paths.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the binding parity workflow references</name>
  <files>docs/api/binding-parity-overview.md, docs/api/binding-parity-policy.md, docs/api/node-python-contract-map.md, docs/api/cxx-parity-gate.md</files>
  <action>Refresh the active binding workflow docs so they match the repo-root layout and matrix-routing pattern. Replace active `ClassicLib-rs/...` links with `foundation/...`, `business-logic/...`, `cpp-bindings/classic-cpp-bridge/...`, `node-bindings/classic-node/...`, and `python-bindings/...`. Update `binding-parity-policy.md` to describe the repo-root follow-up workflow for new public Rust APIs. Update `node-python-contract-map.md` so its contract locations and artifact references are root-level and include a short migration-matrix pointer rather than duplicated transition prose. Update `cxx-parity-gate.md` so its bridge paths, parity-artifact paths, and test examples are repo-root-first. Preserve historical crate-merge notes, but label them clearly and never as live workspace-root instructions.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "api_hubs_and_binding_workflow_contract"</automated>
  </verify>
  <done>The active binding parity workflow docs now teach repo-root paths and no longer leak stale `ClassicLib-rs` operational guidance.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/planning/test_phase10_validation.py -q -k "api_hubs_and_binding_workflow_contract"`
</verification>

<success_criteria>
- The active API hub pages link to the migration matrix where translation is needed.
- Binding workflow docs teach root-level Node, Python, and CXX maintenance flows.
- The review concern about under-scoped API docs is resolved by covering the full high-traffic binding workflow surface.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-02-SUMMARY.md`
</output>
