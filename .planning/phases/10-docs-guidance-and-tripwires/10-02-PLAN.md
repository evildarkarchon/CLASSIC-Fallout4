---
phase: 10-docs-guidance-and-tripwires
plan: "02"
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - docs/api/README.md
  - docs/api/QUICK_START.md
  - docs/api/binding-contract-refresh-note.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - API-facing contributor docs route through the shared migration matrix instead of repeating translations.
    - API quick-start and binding refresh guidance teach repo-root commands and root-level binding paths.
  artifacts:
    - path: docs/api/README.md
      provides: API index aligned to repo-root guidance and matrix routing.
    - path: docs/api/QUICK_START.md
      provides: Quick-start instructions aligned to repo-root commands and locations.
    - path: docs/api/binding-contract-refresh-note.md
      provides: Binding contract refresh workflow aligned to repo-root paths and artifacts.
  key_links:
    - from: docs/api/README.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: '\[Workspace Migration Matrix\]\(\.\./workspace-migration-matrix\.md\)'
    - from: docs/api/QUICK_START.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: '\[Workspace Migration Matrix\]\(\.\./workspace-migration-matrix\.md\)'
---

<objective>
Refresh the active API doc entrypoints so contributor-facing Rust and binding workflow pages match the repo-root workspace contract.

Purpose: Implement D-01, D-02, D-04, and D-05 for the active `docs/api/*.md` pages that top-level docs route contributors into.
Output: Updated API index, quick-start page, and binding refresh note that point at repo-root paths, root-level artifact locations, and the shared migration matrix.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
@docs/workspace-migration-matrix.md
@docs/api/README.md
@docs/api/QUICK_START.md
@docs/api/binding-contract-refresh-note.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire the API index and quick-start entrypoints</name>
  <files>docs/api/README.md, docs/api/QUICK_START.md</files>
  <read_first>
    - docs/api/README.md
    - docs/api/QUICK_START.md
    - docs/workspace-migration-matrix.md
    - .planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
  </read_first>
  <action>Update `docs/api/README.md` and `docs/api/QUICK_START.md` per D-01/D-02 so active examples use the repo-root layout. Replace active layer references like `ClassicLib-rs/business-logic/` with `business-logic/`, `ClassicLib-rs/node-bindings/classic-node/` with `node-bindings/classic-node/`, and `ClassicLib-rs/python-bindings/` with `python-bindings/`. Replace any live Cargo examples that use `--manifest-path ClassicLib-rs/Cargo.toml` with plain repo-root commands. Add a visible link to `../workspace-migration-matrix.md` near the top of both files and route any old/new translation discussion into that shared page instead of duplicating tables or prose. Keep `ClassicLib-rs` only inside clearly labeled historical migration notes per D-07.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `docs/api/README.md` contains `[Workspace Migration Matrix](../workspace-migration-matrix.md)`.
    - `docs/api/README.md` does not contain `ClassicLib-rs/business-logic/` as an active path example.
    - `docs/api/QUICK_START.md` contains `business-logic/`, `node-bindings/classic-node`, and `python-bindings/`.
    - `docs/api/QUICK_START.md` does not contain `ClassicLib-rs/node-bindings/classic-node/` or `ClassicLib-rs/python-bindings/` as active workflow paths.
  </acceptance_criteria>
  <done>The API index and quick-start pages now route through the matrix and teach the live repo-root layout.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the binding contract maintenance note</name>
  <files>docs/api/binding-contract-refresh-note.md</files>
  <read_first>
    - docs/api/binding-contract-refresh-note.md
    - docs/workspace-migration-matrix.md
    - docs/api/node-python-contract-map.md
    - .planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
  </read_first>
  <action>Rewrite `docs/api/binding-contract-refresh-note.md` so its binding maintenance instructions use repo-root paths and artifact locations per D-01/D-05. Replace every active `ClassicLib-rs/node-bindings/classic-node/...` path with `node-bindings/classic-node/...`; replace every active `ClassicLib-rs/python-bindings/...` path with `python-bindings/...`; replace `python ClassicLib-rs/validate_stubs.py --rust-dir ClassicLib-rs ...` with `python validate_stubs.py --rust-dir . ...`; replace active CXX bridge paths with `cpp-bindings/classic-cpp-bridge/...`. Add a short link back to `../workspace-migration-matrix.md` for old-to-new path lookups instead of repeating transition prose. Preserve historical notes only when explicitly labeled `Historical note:` or `Migration note:` per D-07.</action>
  <verify>
    <automated>MISSING — Plan 10-07 must create tests/planning/test_phase10_validation.py before this plan gets an automated assertion.</automated>
  </verify>
  <acceptance_criteria>
    - `docs/api/binding-contract-refresh-note.md` contains `python validate_stubs.py --rust-dir .`.
    - `docs/api/binding-contract-refresh-note.md` contains `node-bindings/classic-node/parity-artifacts/` and `python-bindings/parity-artifacts/`.
    - `docs/api/binding-contract-refresh-note.md` contains `[Workspace Migration Matrix](../workspace-migration-matrix.md)`.
    - The file does not contain `python ClassicLib-rs/validate_stubs.py --rust-dir ClassicLib-rs`.
  </acceptance_criteria>
  <done>The binding contract refresh note matches the repo-root binding workflow and artifact locations.</done>
</task>

</tasks>

<verification>
Plan 10-07 will verify these pages with `python -m pytest tests/planning/test_phase10_validation.py -q`.
</verification>

<success_criteria>
- API-facing contributor pages link to the shared migration matrix.
- Active API docs use repo-root paths and commands.
- Binding contract refresh guidance points at root-level Node/Python/CXX artifact locations.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-02-SUMMARY.md`
</output>
