---
phase: 10-docs-guidance-and-tripwires
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/planning/test_phase10_validation.py
  - tests/powershell/phase10_guidance_tripwires.test.ps1
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - Executors have named Phase 10 audit hooks before doc rewrites start.
    - The stale-path guard uses an explicit active-surface allowlist instead of a blunt repo-wide ban.
    - Wrapper-script guidance has a parse-backed PowerShell tripwire.
  artifacts:
    - path: tests/planning/test_phase10_validation.py
      provides: Grouped Phase 10 doc and guidance audit scaffold.
    - path: tests/powershell/phase10_guidance_tripwires.test.ps1
      provides: Parse-backed stale-path tripwire for active wrapper scripts.
  key_links:
    - from: tests/planning/test_phase10_validation.py
      to: docs/workspace-migration-matrix.md
      via: LINK_REQUIRED_SURFACES constant
      pattern: workspace-migration-matrix\.md
    - from: tests/powershell/phase10_guidance_tripwires.test.ps1
      to: rebuild_rust.ps1
      via: explicit target list
      pattern: rebuild_rust\.ps1
---

<objective>
Bootstrap the Phase 10 validation scaffold before any documentation rewrites land.

Purpose: Address the review concern about delayed validation by creating the file-backed Python and PowerShell tripwires up front per D-06 and D-07.
Output: A grouped planning audit and wrapper-script tripwire that later plans can verify against directly.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
@.planning/phases/10-docs-guidance-and-tripwires/10-REVIEWS.md
@tests/planning/test_phase06_validation.py
@tests/planning/test_phase09_validation.py
@tests/powershell/rebuild_rust.general_target.test.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the grouped Phase 10 planning audit scaffold</name>
  <files>tests/planning/test_phase10_validation.py</files>
  <action>Create `tests/planning/test_phase10_validation.py` as the Wave 1 scaffold per D-06/D-07 and the review feedback. Use `unittest.TestCase` plus `subTest()` in the same style as Phase 6 and Phase 9. Define explicit constants for: `LINK_REQUIRED_SURFACES`, `ACTIVE_TOP_LEVEL_DOCS`, `ACTIVE_API_HUB_DOCS`, `ACTIVE_API_CORE_GROUP_A`, `ACTIVE_API_CORE_GROUP_B`, `ACTIVE_API_RUNTIME_GROUP_C`, `ACTIVE_API_RUNTIME_GROUP_D`, `ACTIVE_AGENT_ENTRYPOINTS`, `ACTIVE_REPO_GUIDE_MIRRORS`, `ACTIVE_CODEBASE_MAPS`, `SCOPED_SWEEP_SURFACES`, `EXCLUDED_PATH_PARTS`, `FORBIDDEN_ACTIVE_PHRASES`, and `ALLOWED_HISTORICAL_LINE_PREFIXES`. The allowed-history logic must be simple and line-based per the reviews: permit forbidden phrases only when the exact line starts with `> Historical note:`, `> Migration note:`, `**Historical note:**`, or `**Migration note:**`, plus an explicit per-file allowlist for unavoidable special cases. Add named test methods that later plans can target directly: `test_matrix_and_top_level_docs_contract`, `test_api_hubs_and_binding_workflow_contract`, `test_api_core_group_a_contract`, `test_api_core_group_b_contract`, `test_api_runtime_group_c_contract`, `test_api_runtime_group_d_contract`, `test_agent_entrypoints_contract`, `test_repo_guide_mirrors_contract`, `test_codebase_maps_contract`, and `test_scoped_active_guidance_sweep`. Scope the sweep to the explicit active-surface lists that this phase owns; do not use `docs/**/*.md` or any other unbounded repo sweep.</action>
  <verify>
    <automated>python -m py_compile tests/planning/test_phase10_validation.py</automated>
  </verify>
  <done>`tests/planning/test_phase10_validation.py` exists, parses cleanly, and exposes named target tests for every later Phase 10 plan.</done>
</task>

<task type="auto">
  <name>Task 2: Add the wrapper-script PowerShell tripwire scaffold</name>
  <files>tests/powershell/phase10_guidance_tripwires.test.ps1</files>
  <action>Create `tests/powershell/phase10_guidance_tripwires.test.ps1` as the script-sensitive half of D-06. Parse `rebuild_rust.ps1`, `rebuild_node.ps1`, `classic-cli/build_cli.ps1`, `classic-gui/build_gui.ps1`, and `classic-cli/test_cli.ps1` with `System.Management.Automation.Language.Parser.ParseFile(...)` before any text assertions. After parse success, scan each file for forbidden live-root guidance such as `ClassicLib-rs/Cargo.toml`, `--manifest-path ClassicLib-rs/Cargo.toml`, `ClassicLib-rs/python-bindings/.venv`, `ClassicLib-rs/node-bindings/classic-node`, and `working-directory: ClassicLib-rs`. Fail with a descriptive message naming the script and offending phrase. Keep the test deterministic and file-backed; do not run build workflows from it.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$tokens=$null; $errors=$null; [System.Management.Automation.Language.Parser]::ParseFile('tests/powershell/phase10_guidance_tripwires.test.ps1',[ref]$tokens,[ref]$errors) > $null; if ($errors.Count -gt 0) { $errors | ForEach-Object { Write-Error $_.Message }; exit 1 }"</automated>
  </verify>
  <done>The PowerShell tripwire file exists, parses cleanly, and names the active wrapper surfaces and forbidden stale-root phrases.</done>
</task>

</tasks>

<verification>
- `python -m py_compile tests/planning/test_phase10_validation.py`
- `pwsh -NoProfile -Command "$tokens=$null; $errors=$null; [System.Management.Automation.Language.Parser]::ParseFile('tests/powershell/phase10_guidance_tripwires.test.ps1',[ref]$tokens,[ref]$errors) > $null; if ($errors.Count -gt 0) { exit 1 }"`
</verification>

<success_criteria>
- Later plans can target named Phase 10 tests instead of waiting for a late end-of-phase audit.
- The stale-path policy is encoded as explicit allowlists and line-based history markers, not a brittle Markdown parser.
- Wrapper scripts have a parse-backed tripwire before doc rewrites begin.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-00-SUMMARY.md`
</output>
