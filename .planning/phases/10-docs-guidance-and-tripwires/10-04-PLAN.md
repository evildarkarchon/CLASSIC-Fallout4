---
phase: 10-docs-guidance-and-tripwires
plan: "04"
type: execute
wave: 2
depends_on: ["10-00", "10-01"]
files_modified:
  - docs/api/classic-version-registry-core.md
  - docs/api/classic-version-core.md
  - docs/api/classic-web-core.md
  - docs/api/classic-update-core.md
  - docs/api/classic-config-core.md
  - docs/api/classic-config-core-yaml-schema.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - Version, config, web, and update API docs point at root-level source locations.
    - The config-side crate docs preserve merger history without keeping stale operational paths.
  artifacts:
    - path: docs/api/classic-config-core.md
      provides: Root-level config-core and crashgen-rule references.
    - path: docs/api/classic-version-registry-core.md
      provides: Root-level version-registry references.
  key_links:
    - from: docs/api/classic-config-core.md
      to: docs/api/classic-config-core-yaml-schema.md
      via: schema cross-reference
      pattern: classic-config-core-yaml-schema\.md
---

<objective>
Refresh the version/config/web/update API reference pages to the repo-root contract.

Purpose: Implement D-01 for the second cluster of active crate reference docs linked from the API hub.
Output: Updated version/config/web/update docs with root-level source paths and accurate current ownership notes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@docs/api/classic-version-registry-core.md
@docs/api/classic-version-core.md
@docs/api/classic-web-core.md
@docs/api/classic-update-core.md
@docs/api/classic-config-core.md
@docs/api/classic-config-core-yaml-schema.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the version, web, and update reference docs</name>
  <files>docs/api/classic-version-registry-core.md, docs/api/classic-version-core.md, docs/api/classic-web-core.md, docs/api/classic-update-core.md</files>
  <action>Rewrite live source links, dependency paths, and binding references in these four API pages to the root-level layout per D-01/D-07. Replace `ClassicLib-rs/business-logic/...`, `ClassicLib-rs/cpp-bindings/...`, `ClassicLib-rs/node-bindings/...`, and `ClassicLib-rs/python-bindings/...` with the repo-root equivalents. Preserve merger/context notes only when labeled historically; do not leave any active workspace-root path examples behind.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "api_core_group_b_contract"</automated>
  </verify>
  <done>The version/web/update docs use repo-root source links and no longer teach stale workspace-root locations.</done>
</task>

<task type="auto">
  <name>Task 2: Update the config reference and schema docs</name>
  <files>docs/api/classic-config-core.md, docs/api/classic-config-core-yaml-schema.md</files>
  <action>Refresh `classic-config-core.md` and `classic-config-core-yaml-schema.md` so all live source links and cross-crate references use `business-logic/...`, `foundation/...`, `node-bindings/...`, and `python-bindings/...` at repo root. Keep the crashgen/settings consolidation history, but convert it into explicit historical-note wording per D-07. Make sure the schema doc and config doc still cross-link cleanly after the path updates.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "api_core_group_b_contract"</automated>
  </verify>
  <done>The config doc pair reflects current root-level ownership and keeps merger history clearly labeled instead of operational.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/planning/test_phase10_validation.py -q -k "api_core_group_b_contract"`
</verification>

<success_criteria>
- Version, config, web, and update docs align to root-level source paths.
- Config merger notes are retained as history, not live workspace-root instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-04-SUMMARY.md`
</output>
