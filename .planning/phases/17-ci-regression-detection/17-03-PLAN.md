# Plan 17-03: PR Reporting and Regression Enforcement

---
phase: 17-ci-regression-detection
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - .github/workflows/benchmarks.yml
autonomous: true

must_haves:
  truths:
    - "PR comment shows benchmark results in table format"
    - "Existing comment is updated, not duplicated"
    - "Performance regression >10% fails the build"
    - "Bypass label allows intentional regressions"
    - "Required check blocks merge on failure"
  artifacts:
    - path: ".github/workflows/benchmarks.yml"
      provides: "PR comment posting and failure enforcement"
      contains: "peter-evans/create-or-update-comment"
  key_links:
    - from: "failures.md"
      to: "PR comment body"
      via: "cat failures.md in comment builder"
      pattern: "cat failures.md"
    - from: "steps.analyze.outputs.has_failure"
      to: "fail step condition"
      via: "if condition checks analysis output"
      pattern: "has_failure.*true"
---

<objective>
Complete the benchmark regression detection system by adding PR comment posting and fail-on-regression enforcement. This plan transforms the analysis results from Plan 17-02 into actionable feedback for developers.

Purpose: Provide clear feedback on benchmark results and enforce regression thresholds.
Output: PR comment posting and failure enforcement steps in workflow.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ci-regression-detection/17-01-SUMMARY.md
@.planning/phases/17-ci-regression-detection/17-02-SUMMARY.md
@.github/workflows/benchmarks.yml

Plan 17-02 added comparison logic, threshold analysis with config file support, and label bypass detection. This plan:
- Posts/updates PR comments with benchmark results
- Fails the build when regressions exceed 10%
- Respects bypass label for intentional regressions

**User decisions (LOCKED):**
- PR comment: table format, update existing comment
- Failure messages: actionable summary with % regression
- Required check: benchmark must pass before merge
- Label bypass: `perf-regression-accepted` allows merge
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure PR comment posting with branch protection documentation</name>
  <files>.github/workflows/benchmarks.yml</files>
  <action>
Add peter-evans/find-comment and peter-evans/create-or-update-comment actions.

**Add workflow-level documentation comment for branch protection:**
```yaml
# IMPORTANT: Configure branch protection for main:
# - Require status checks to pass before merging
# - Required check: "Run Benchmarks" (this workflow's job name)
# - This ensures PRs with performance regressions cannot merge
#
# To configure: Repository Settings > Branches > Branch protection rules
# 1. Add rule for "main"
# 2. Enable "Require status checks to pass before merging"
# 3. Select "Run Benchmarks" from status checks list
```

**Find existing comment:**
```yaml
- name: Find existing comment
  if: github.event_name == 'pull_request'
  uses: peter-evans/find-comment@v3
  id: fc
  with:
    issue-number: ${{ github.event.pull_request.number }}
    comment-author: 'github-actions[bot]'
    body-includes: '<!-- benchmark-results -->'
```

**Build PR comment body:**
```yaml
- name: Build PR comment body
  if: github.event_name == 'pull_request'
  id: comment-body
  working-directory: rust
  shell: bash
  run: |
    {
      echo '<!-- benchmark-results -->'
      echo '## Benchmark Results'
      echo ''
      echo "**Commit:** ${{ github.event.pull_request.head.sha }}"
      echo "**Baseline:** main branch"
      echo "**Mode:** quick (50 samples)"
      echo ''

      if [ "${{ steps.baseline-check.outputs.baseline_exists }}" != "true" ]; then
        echo '> [!WARNING]'
        echo '> No baseline found. Comparison skipped.'
        echo '> Baseline will be established when this PR merges to main.'
      else
        status="${{ steps.analyze.outputs.status }}"
        case "$status" in
          failure) echo '> [!CAUTION]' && echo '> Performance regressions detected (>10%)' ;;
          warning) echo '> [!WARNING]' && echo '> Performance warnings detected (5-10%)' ;;
          success) echo '> [!TIP]' && echo '> No significant performance changes detected' ;;
          *) echo '> [!NOTE]' && echo '> Comparison status: '"$status" ;;
        esac

        echo ''

        if [ -s failures.md ]; then
          echo '### Regressions (>10%)'
          echo ''
          echo '| Benchmark | Change | Current | Baseline |'
          echo '|-----------|--------|---------|----------|'
          cat failures.md
          echo ''
        fi

        if [ -s warnings.md ]; then
          echo '### Warnings (5-10%)'
          echo ''
          echo '| Benchmark | Change | Current | Baseline |'
          echo '|-----------|--------|---------|----------|'
          cat warnings.md
          echo ''
        fi

        if [ -s improvements.md ]; then
          echo '### Improvements'
          echo ''
          echo '| Benchmark | Change | Current | Baseline |'
          echo '|-----------|--------|---------|----------|'
          cat improvements.md
          echo ''
        fi

        if [ ! -s failures.md ] && [ ! -s warnings.md ] && [ ! -s improvements.md ]; then
          echo 'All benchmarks within threshold (no changes >5%).'
          echo ''
        fi
      fi

      echo '---'
      echo '*Benchmarks run on GitHub Actions. Results may vary due to shared infrastructure.*'
      echo '*To accept regressions, add label `perf-regression-accepted` with justification.*'
    } > comment.md

    body=$(cat comment.md)
    echo "body<<EOF" >> $GITHUB_OUTPUT
    echo "$body" >> $GITHUB_OUTPUT
    echo "EOF" >> $GITHUB_OUTPUT
```

**Post or update comment:**
```yaml
- name: Post or update PR comment
  if: github.event_name == 'pull_request'
  uses: peter-evans/create-or-update-comment@v4
  with:
    comment-id: ${{ steps.fc.outputs.comment-id }}
    issue-number: ${{ github.event.pull_request.number }}
    body: ${{ steps.comment-body.outputs.body }}
    edit-mode: replace
```

The comment uses GitHub's alert syntax (> [!CAUTION], etc.) for visual clarity.
  </action>
  <verify>
Verify workflow contains: `peter-evans/find-comment@v3`, `peter-evans/create-or-update-comment@v4`, `<!-- benchmark-results -->`, branch protection documentation comment.
  </verify>
  <done>
Workflow has PR comment posting with find-existing, build-body, and post-or-update steps. Branch protection configuration is documented in workflow comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fail-on-regression enforcement step</name>
  <files>.github/workflows/benchmarks.yml</files>
  <action>
Add final step that fails the workflow when regressions exceed threshold and bypass label is not present:

```yaml
- name: Fail on regression
  if: |
    github.event_name == 'pull_request' &&
    steps.analyze.outputs.has_failure == 'true' &&
    steps.check-label.outputs.bypass != 'true'
  shell: bash
  run: |
    echo "::error::Performance regression detected (>10%)"
    echo ""
    echo "One or more benchmarks showed significant regression."
    echo "Options:"
    echo "  1. Fix the performance regression"
    echo "  2. Add label 'perf-regression-accepted' with justification comment"
    echo ""
    echo "See the PR comment for detailed benchmark results."
    exit 1
```

This step uses `if` conditions to only fail when:
- This is a PR (not main branch push)
- Analysis found failures (>10% regressions)
- Bypass label is NOT present
  </action>
  <verify>
Verify workflow contains: `Fail on regression` step, `has_failure == 'true'`, `bypass != 'true'`, `exit 1`.
  </verify>
  <done>
Workflow has fail-on-regression step that fails the build when regressions exceed 10% and bypass label is not present. Error message provides actionable guidance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify end-to-end workflow behavior</name>
  <files>.github/workflows/benchmarks.yml</files>
  <action>
Verify the complete workflow covers all scenarios by reviewing the workflow file:

1. **Missing baseline case**: Workflow should produce warning annotation and pass
   - Check: `baseline_exists == 'false'` path in comment builder shows warning
   - Check: No failure step runs when baseline missing

2. **No regression case**: Comment should show "No significant changes", check passes
   - Check: `status == 'success'` path in comment builder

3. **Warning case**: Comment should show warnings table, check passes
   - Check: `status == 'warning'` path shows warnings table
   - Check: Fail step only triggers on `has_failure == 'true'`

4. **Failure case**: Comment should show regressions, check fails
   - Check: `status == 'failure'` path shows regressions table
   - Check: Fail step triggers and exits with code 1

5. **Bypass case**: With `perf-regression-accepted` label, check passes
   - Check: Fail step condition includes `bypass != 'true'`

6. **Main branch update**: Baseline cache saved
   - Check: Cache save step has `if: github.ref == 'refs/heads/main'`

Review workflow file and confirm all conditional paths are correctly implemented.
  </action>
  <verify>
Read through `.github/workflows/benchmarks.yml` and verify all 6 scenarios have correct conditional logic. Run `python -c "import yaml; yaml.safe_load(open('.github/workflows/benchmarks.yml'))"` to validate syntax.
  </verify>
  <done>
Workflow file passes YAML validation and all scenario paths (missing baseline, no regression, warning, failure, bypass, main branch) have correct conditional logic implemented.
  </done>
</task>

</tasks>

<verification>
1. **PR comment posted**: Comment appears on PR with benchmark results
2. **Comment updated**: Subsequent pushes update existing comment (not create new)
3. **Failure enforced**: >10% regression fails build when no bypass label
4. **Bypass works**: `perf-regression-accepted` label prevents failure
5. **Missing baseline handled**: Warning shown, check passes
6. **Failure message actionable**: Error message explains options to fix
7. **Branch protection documented**: Configuration steps in workflow comments
</verification>

<success_criteria>
- PR comments are posted with clear benchmark result tables
- Existing comments are updated (not duplicated)
- Build fails when regression >10% and no bypass label
- Bypass label allows intentional regressions through
- All scenario paths work correctly
- Branch protection configuration is documented for manual setup
</success_criteria>

<output>
After completion, create `.planning/phases/17-ci-regression-detection/17-03-SUMMARY.md`
</output>
