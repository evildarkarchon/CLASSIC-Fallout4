---
phase: 02-crashgen-config-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs
  - ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs
  - ClassicLib-rs/business-logic/classic-config-core/src/lib.rs
  - ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs
  - ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs.bak
  - ClassicLib-rs/business-logic/classic-config-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/settings_validator.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/crashgen_registry.rs
  - ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-scangame-core/src/orchestrator.rs
  - ClassicLib-rs/business-logic/classic-scangame-core/src/crashgen_orchestrator.rs
  - ClassicLib-rs/business-logic/classic-scangame-core/src/toml.rs
  - ClassicLib-rs/node-bindings/classic-node/Cargo.toml
  - ClassicLib-rs/node-bindings/classic-node/src/crashgen_rules.rs
  - ClassicLib-rs/python-bindings/classic-config-py/Cargo.toml
  - ClassicLib-rs/python-bindings/classic-config-py/src/lib.rs
  - ClassicLib-rs/python-bindings/classic-scanlog-py/Cargo.toml
  - ClassicLib-rs/python-bindings/classic-scanlog-py/src/crashgen_rules.rs
  - ClassicLib-rs/python-bindings/classic-scanlog-py/src/settings_validator.rs
  - ClassicLib-rs/python-bindings/classic-scangame-py/Cargo.toml
  - ClassicLib-rs/python-bindings/classic-scangame-py/src/crashgen_rules.rs
  - ClassicLib-rs/Cargo.toml
autonomous: true
requirements: [CGEN-01, CGEN-02, CGEN-03]

must_haves:
  truths:
    - "classic_config_core re-exports every public type and function formerly in classic_crashgen_settings_core"
    - "No Cargo.toml in the workspace has a classic-crashgen-settings-core dependency"
    - "The classic-crashgen-settings-core directory no longer exists"
    - "classic-crashgen-settings-core is removed from the workspace members list"
    - "classic-scangame-core depends on classic-config-core (new dep per D-05)"
    - "classic-scangame-py depends on classic-config-core (new dep per D-10)"
    - "yamldata.rs.bak no longer exists and was deleted in a separate commit per D-17"
    - "git log shows the crashgen_rules.rs rename commit separate from the content-edit commit per D-13"
    - "cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml exits 0"
    - "cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml exits 0"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs"
      provides: "RuleSeverity, ConfigLayout, TargetValueType, ExpectedValue, Predicate, PreflightActionKind, RuleReportBucket, PreflightAction, PreflightRule, RuleTarget, RuleMessages, CheckRule, CrashgenSettingsRules, EvaluationContext, OutcomeKind, EvaluationOutcome, EvaluationResult, evaluate_rules"
      min_lines: 500
    - path: "ClassicLib-rs/business-logic/classic-config-core/src/lib.rs"
      provides: "mod crashgen_rules + pub use crashgen_rules::* re-export"
      contains: "pub use crashgen_rules"
    - path: "ClassicLib-rs/business-logic/classic-config-core/Cargo.toml"
      provides: "config-core deps with classic-crashgen-settings-core REMOVED"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs"
      to: "crate::crashgen_rules"
      via: "use statement (same-crate path, no external dep)"
      pattern: "crate::"
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs"
      to: "classic_config_core crashgen types"
      via: "use classic_config_core::{...}"
      pattern: "use classic_config_core::"
    - from: "ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml"
      to: "classic-config-core"
      via: "new Cargo dep"
      pattern: "classic-config-core\\s*=\\s*\\{\\s*path"
    - from: "ClassicLib-rs/python-bindings/classic-scangame-py/Cargo.toml"
      to: "classic-config-core"
      via: "new Cargo dep"
      pattern: "classic-config-core\\s*=\\s*\\{\\s*path"
---

<objective>
Absorb `classic-crashgen-settings-core` into `classic-config-core` at the Rust level: move source via `git mv`, re-export the rule model from config-core, migrate all 3 Rust core consumers and 4 binding crates to the new import path, add the new `classic-config-core` dep to `classic-scangame-core` and `classic-scangame-py`, delete the crate directory and workspace-member entry, and clean up the stray `yamldata.rs.bak`. All commits preserve blame and stay buildable.

Purpose: Satisfy CGEN-01, CGEN-02, CGEN-03 with zero consumer-visible behavior change.
Output: 16-crate business-logic topology (down from 17 after Phase 1, or from the original 19); single source of truth for crashgen rule model lives in config-core.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-crashgen-config-merge/02-CONTEXT.md
@.planning/phases/02-crashgen-config-merge/02-RESEARCH.md
@.planning/phases/02-crashgen-config-merge/02-VALIDATION.md
@.planning/phases/01-yaml-settings-merge/01-01-PLAN.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Public API exported by classic_crashgen_settings_core that must become available from classic_config_core -->
<!-- Source of truth: ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs (573 lines, single file) -->

From classic_crashgen_settings_core (all will be re-exported from classic_config_core):
- Enums: RuleSeverity, ConfigLayout, TargetValueType, ExpectedValue, Predicate,
  PreflightActionKind, RuleReportBucket, OutcomeKind
- Structs: PreflightAction, PreflightRule, RuleTarget, RuleMessages, CheckRule,
  CrashgenSettingsRules, EvaluationContext, EvaluationOutcome, EvaluationResult
- Functions: evaluate_rules(rules, context) -> EvaluationResult

Consumer call sites (all use path `classic_crashgen_settings_core::X`):
- classic-config-core/src/yamldata.rs (1 ref)
- classic-scanlog-core/src/orchestrator.rs (11 refs)
- classic-scanlog-core/src/settings_validator.rs (2 refs)
- classic-scanlog-core/src/crashgen_registry.rs (1 ref)
- classic-scangame-core/src/orchestrator.rs (1 ref)
- classic-scangame-core/src/crashgen_orchestrator.rs (1 ref)
- classic-scangame-core/src/toml.rs (4 refs)
- classic-node/src/crashgen_rules.rs (binding)
- classic-config-py/src/lib.rs (binding)
- classic-scanlog-py/src/crashgen_rules.rs (binding)
- classic-scanlog-py/src/settings_validator.rs (binding)
- classic-scangame-py/src/crashgen_rules.rs (binding)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv source file (rename-only commit, preserve blame)</name>
  <files>
    ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs -> ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs
  </files>
  <read_first>
    - .planning/phases/02-crashgen-config-merge/02-CONTEXT.md (especially D-01, D-02, D-13, D-17, D-18)
    - ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs (full source being moved)
    - ClassicLib-rs/business-logic/classic-config-core/src/lib.rs (target crate root, 27 lines)
    - .planning/phases/01-yaml-settings-merge/01-01-PLAN.md (Phase 1 precedent — D-15 git mv then edit pattern)
  </read_first>
  <action>
    Per D-13, perform ONLY the file rename in this commit — do NOT edit the moved file's contents, do NOT update any other files. This preserves `git blame` cleanly.

    Commands (run from repo root in PowerShell):
    ```
    git mv ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs
    ```

    At this instant the workspace will NOT compile — `classic-crashgen-settings-core/src/lib.rs` is gone but the crate's `Cargo.toml` and everyone who depends on it still references it. That is expected and intentional per D-13; compilation is restored in Task 2.

    Commit as its own standalone commit (do NOT bundle anything else):
    ```
    git commit -m "Refactor: git mv crashgen-settings-core lib.rs to config-core crashgen_rules.rs"
    ```
    Do NOT run `cargo build` between the git mv and the commit — it will fail. Build verification happens at the end of Task 2.
  </action>
  <verify>
    <automated>git log -1 --name-status | Select-String -Pattern "R.*classic-crashgen-settings-core/src/lib.rs.*classic-config-core/src/crashgen_rules.rs"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-status` shows a rename (R100 or similar) from `ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs` to `ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs`
    - Commit message starts with `Refactor:` per project convention
    - `test -f ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs` → true
    - `test -f ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs` → false
    - The commit touches exactly ONE rename pair and no other files (`git show --stat HEAD` shows exactly 2 file entries: the old path and the new path, or a single rename line)
    - Contents of `crashgen_rules.rs` are byte-identical to the former `lib.rs` (blame preserved)
  </acceptance_criteria>
  <done>
    Source file has been moved via `git mv` in an isolated rename-only commit. Workspace is temporarily in a non-compiling state — this is expected and resolved by Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rust core merge — config-core re-exports, consumer import swaps, dep updates, crate deletion, workspace member removal</name>
  <files>
    ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs
    ClassicLib-rs/business-logic/classic-config-core/src/lib.rs
    ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs
    ClassicLib-rs/business-logic/classic-config-core/Cargo.toml
    ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
    ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs
    ClassicLib-rs/business-logic/classic-scanlog-core/src/settings_validator.rs
    ClassicLib-rs/business-logic/classic-scanlog-core/src/crashgen_registry.rs
    ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml
    ClassicLib-rs/business-logic/classic-scangame-core/src/orchestrator.rs
    ClassicLib-rs/business-logic/classic-scangame-core/src/crashgen_orchestrator.rs
    ClassicLib-rs/business-logic/classic-scangame-core/src/toml.rs
    ClassicLib-rs/business-logic/classic-crashgen-settings-core/ (directory, deleted via git rm -r)
    ClassicLib-rs/Cargo.toml
  </files>
  <read_first>
    - .planning/phases/02-crashgen-config-merge/02-CONTEXT.md (D-01..D-11, D-18 — all Rust-side decisions)
    - ClassicLib-rs/business-logic/classic-config-core/src/lib.rs (target crate root to add `mod crashgen_rules; pub use crashgen_rules::*;`)
    - ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs (the file just moved; needs no content change except possibly import cleanup if its former `lib.rs` had crate-root-specific attributes)
    - ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs (1 crashgen ref to swap to `crate::`)
    - ClassicLib-rs/business-logic/classic-config-core/Cargo.toml — **the exact dep line to delete is line 22: `classic-crashgen-settings-core = { path = "../classic-crashgen-settings-core" }`** (verified by read; no workspace-qualified alternative exists). Pre-edit verification grep: `Select-String -Path ClassicLib-rs/business-logic/classic-config-core/Cargo.toml -Pattern 'classic-crashgen-settings-core' -SimpleMatch` must return exactly 1 match (line 22).
    - ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml (remove `classic-crashgen-settings-core`, keep existing `classic-config-core` per D-07)
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs (11 `classic_crashgen_settings_core::` refs)
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/settings_validator.rs (2 refs)
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/crashgen_registry.rs (1 ref)
    - ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml (remove `classic-crashgen-settings-core`, ADD `classic-config-core = { path = "../classic-config-core" }` per D-05/D-08)
    - ClassicLib-rs/business-logic/classic-scangame-core/src/orchestrator.rs (1 ref)
    - ClassicLib-rs/business-logic/classic-scangame-core/src/crashgen_orchestrator.rs (1 ref)
    - ClassicLib-rs/business-logic/classic-scangame-core/src/toml.rs (4 refs)
    - ClassicLib-rs/Cargo.toml (workspace members — remove `"business-logic/classic-crashgen-settings-core"` per D-18)
    - .planning/phases/01-yaml-settings-merge/01-01-PLAN.md (Phase 1 precedent for this exact sequence)
  </read_first>
  <action>
    Single content-edit commit that restores the workspace to a buildable state. Do ALL edits below before committing. Per D-13 this is a separate commit from the Task 1 rename.

    Step A — Wire the moved file into config-core:
    1. Edit `ClassicLib-rs/business-logic/classic-config-core/src/lib.rs`: add at crate root (per D-01, D-03):
       ```rust
       mod crashgen_rules;
       pub use crashgen_rules::*;
       ```
       Place alongside the existing re-exports; do NOT disturb any other lines.
    2. Inspect `ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs`. It was formerly a crate root (`lib.rs`), so:
       - Strip any `#![...]` inner crate-level attributes that are illegal inside a module file (e.g., `#![deny(...)]`, `#![allow(...)]` at top of file). Move them to the config-core `lib.rs` crate root if they are still wanted, or drop them if they duplicate existing config-core lints.
       - Leave all item-level `#[derive(...)]`, `#[allow(...)]`, doc comments, and code bodies unchanged. Claude's discretion (per D-discretion) for any `cargo fmt` churn on adjusted lines.
    3. Edit `ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs` per D-06: replace the single `use classic_crashgen_settings_core::...;` (or path-qualified uses) with `use crate::...;` pointing at the same items. The crashgen types are now in the same crate.

    Step B — config-core Cargo.toml cleanup (D-06):
    4. Edit `ClassicLib-rs/business-logic/classic-config-core/Cargo.toml`: delete exactly this literal line (verified at line 22 during planning):
       ```
       classic-crashgen-settings-core = { path = "../classic-crashgen-settings-core" }
       ```
       Do NOT touch any other dep. After the edit, `Select-String -Path ClassicLib-rs/business-logic/classic-config-core/Cargo.toml -Pattern 'classic-crashgen-settings-core' -SimpleMatch` must return 0 matches.

    Step C — scanlog-core consumer migration (D-07):
    5. `ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml`: remove the `classic-crashgen-settings-core` entry. Do NOT add anything — `classic-config-core` is already present.
    6. In each of `orchestrator.rs` (11 refs), `settings_validator.rs` (2 refs), `crashgen_registry.rs` (1 ref): replace every occurrence of `classic_crashgen_settings_core::` with `classic_config_core::`. No other edits. Leave `use` grouping as-is unless `cargo fmt` rewrites it.

    Step D — scangame-core consumer migration (D-05, D-08 — ONLY dep-graph-affecting change):
    7. `ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml`:
       - Remove `classic-crashgen-settings-core = { path = "../classic-crashgen-settings-core" }`
       - ADD `classic-config-core = { path = "../classic-config-core" }` in the `[dependencies]` section, alphabetically placed.
    8. In each of `orchestrator.rs` (1 ref), `crashgen_orchestrator.rs` (1 ref), `toml.rs` (4 refs): replace `classic_crashgen_settings_core::` with `classic_config_core::`.

    Step E — Delete the source crate and workspace member (D-11, D-18):
    9. `git rm -r ClassicLib-rs/business-logic/classic-crashgen-settings-core/` — this removes the (now nearly-empty) crate directory including its `Cargo.toml`, `src/` (whatever remains), and any stray files.
    10. Edit `ClassicLib-rs/Cargo.toml`: remove the `"business-logic/classic-crashgen-settings-core"` entry from the `members = [...]` list. Do NOT disturb list ordering of other entries.

    Step F — Build verification BEFORE committing:
    11. Run `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml`. Must exit 0. If it fails, fix the import/dep issue before committing. Per 02-VALIDATION.md this is the post-task sampling command.

    Step G — Commit (explicit file paths per CLAUDE.md git safety — do NOT use `git add -A` or `git add .`):
    ```
    git add ClassicLib-rs/business-logic/classic-config-core/src/lib.rs `
            ClassicLib-rs/business-logic/classic-config-core/src/crashgen_rules.rs `
            ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs `
            ClassicLib-rs/business-logic/classic-config-core/Cargo.toml `
            ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml `
            ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs `
            ClassicLib-rs/business-logic/classic-scanlog-core/src/settings_validator.rs `
            ClassicLib-rs/business-logic/classic-scanlog-core/src/crashgen_registry.rs `
            ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml `
            ClassicLib-rs/business-logic/classic-scangame-core/src/orchestrator.rs `
            ClassicLib-rs/business-logic/classic-scangame-core/src/crashgen_orchestrator.rs `
            ClassicLib-rs/business-logic/classic-scangame-core/src/toml.rs `
            ClassicLib-rs/Cargo.toml `
            ClassicLib-rs/Cargo.lock
    # Also stage the `git rm -r` directory deletion — `git rm` already stages, so no extra `git add` needed for that.
    git commit -m "Refactor: absorb classic-crashgen-settings-core into classic-config-core (CGEN-01, CGEN-02, CGEN-03)"
    ```

    Step H — Post-commit test run (wave sampling per 02-VALIDATION.md):
    12. Run `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml`. Must exit 0. The 4 inline unit tests that were inside the moved file are now inside `config-core::crashgen_rules` and must pass unchanged.

    Note: Binding crates (classic-node, classic-config-py, classic-scanlog-py, classic-scangame-py) are handled in Task 3 — this task focuses on the Rust core workspace.

    Trap guard: The research/CONTEXT flagged that `classic-scangame-core` gaining `classic-config-core` as a new dep is the ONLY dep-graph-affecting change in this phase. Verify with `cargo tree -p classic-scangame-core --manifest-path ClassicLib-rs/Cargo.toml | Select-String classic-config-core` — it must now appear in the output.
  </action>
  <verify>
    <automated>cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml</automated>
    <automated>cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `Select-String -Path ClassicLib-rs/business-logic/classic-config-core/src/lib.rs -Pattern 'pub use crashgen_rules'` returns a match
    - `Select-String -Path ClassicLib-rs/business-logic/classic-config-core/src/lib.rs -Pattern 'mod crashgen_rules'` returns a match
    - `Select-String -Path ClassicLib-rs/business-logic/classic-config-core/Cargo.toml -Pattern 'classic-crashgen-settings-core'` returns no matches
    - `Get-ChildItem -Recurse -Path ClassicLib-rs/business-logic/classic-scanlog-core/src -Include *.rs | Select-String -Pattern 'classic_crashgen_settings_core'` returns no matches
    - `Get-ChildItem -Recurse -Path ClassicLib-rs/business-logic/classic-scangame-core/src -Include *.rs | Select-String -Pattern 'classic_crashgen_settings_core'` returns no matches
    - `Select-String -Path ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml -Pattern 'classic-config-core'` returns a match (NEW dep per D-05)
    - `Select-String -Path ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml -Pattern 'classic-crashgen-settings-core'` returns no matches
    - `Test-Path ClassicLib-rs/business-logic/classic-crashgen-settings-core` returns False
    - `Select-String -Path ClassicLib-rs/Cargo.toml -Pattern 'classic-crashgen-settings-core'` returns no matches
    - `cargo tree -p classic-scangame-core --manifest-path ClassicLib-rs/Cargo.toml | Select-String classic-config-core` returns a match
    - Git log shows two commits: HEAD is a content-edit commit with prefix `Refactor:` and HEAD~1 is the Task 1 rename commit
  </acceptance_criteria>
  <done>
    All 3 Rust core consumers import crashgen types from `classic_config_core`. The crate directory and workspace member entry are removed. `cargo build --workspace` and `cargo test --workspace` are both green. Blame is preserved via the two-commit pattern.
  </done>
</task>

<task type="auto">
  <name>Task 3: Binding consumer migration — node, config-py, scanlog-py, scangame-py (import swaps + Cargo.toml edits, NO file renames)</name>
  <files>
    ClassicLib-rs/node-bindings/classic-node/Cargo.toml
    ClassicLib-rs/node-bindings/classic-node/src/crashgen_rules.rs
    ClassicLib-rs/python-bindings/classic-config-py/Cargo.toml
    ClassicLib-rs/python-bindings/classic-config-py/src/lib.rs
    ClassicLib-rs/python-bindings/classic-scanlog-py/Cargo.toml
    ClassicLib-rs/python-bindings/classic-scanlog-py/src/crashgen_rules.rs
    ClassicLib-rs/python-bindings/classic-scanlog-py/src/settings_validator.rs
    ClassicLib-rs/python-bindings/classic-scangame-py/Cargo.toml
    ClassicLib-rs/python-bindings/classic-scangame-py/src/crashgen_rules.rs
  </files>
  <read_first>
    - .planning/phases/02-crashgen-config-merge/02-CONTEXT.md (D-09, D-10 — binding rules: NO renames, each binding's Cargo.toml audited before editing)
    - ClassicLib-rs/node-bindings/classic-node/Cargo.toml (verify whether classic-config-core is already present)
    - ClassicLib-rs/node-bindings/classic-node/src/crashgen_rules.rs
    - ClassicLib-rs/python-bindings/classic-config-py/Cargo.toml
    - ClassicLib-rs/python-bindings/classic-config-py/src/lib.rs (scan for any classic_crashgen_settings_core refs)
    - ClassicLib-rs/python-bindings/classic-scanlog-py/Cargo.toml
    - ClassicLib-rs/python-bindings/classic-scanlog-py/src/crashgen_rules.rs
    - ClassicLib-rs/python-bindings/classic-scanlog-py/src/settings_validator.rs
    - ClassicLib-rs/python-bindings/classic-scangame-py/Cargo.toml (TRAP: this one needs the NEW classic-config-core dep added — matching D-05 for scangame-core. Per deep_work_rules, this is explicit.)
    - ClassicLib-rs/python-bindings/classic-scangame-py/src/crashgen_rules.rs
  </read_first>
  <action>
    Per D-09: NO file renames. Per D-10: each binding's Cargo.toml must have `classic-crashgen-settings-core` removed, and `classic-config-core` must be present — add it only if not already present, to avoid duplicate entries.

    Step A — Audit each binding Cargo.toml BEFORE editing:
    For each of the 4 binding Cargo.toml files, run:
    ```
    Select-String -Path <path> -Pattern 'classic-config-core|classic-crashgen-settings-core'
    ```
    Record which bindings already have `classic-config-core` and which don't. Only `classic-scangame-py` is known to need the NEW dep per the deep_work_rules trap guard, but verify the others too — especially `classic-node` which may already include config-core.

    Step B — classic-node (NAPI):
    1. `ClassicLib-rs/node-bindings/classic-node/Cargo.toml`: remove `classic-crashgen-settings-core` entry. If `classic-config-core` is not already present, add `classic-config-core = { path = "../../business-logic/classic-config-core" }` (use whichever exact path style the file already uses for sibling business-logic deps — mirror the existing format).
    2. `ClassicLib-rs/node-bindings/classic-node/src/crashgen_rules.rs`: replace every `classic_crashgen_settings_core::` with `classic_config_core::`. Do NOT rename the file (D-09).

    Step C — classic-config-py (PyO3):
    3. `ClassicLib-rs/python-bindings/classic-config-py/Cargo.toml`: remove `classic-crashgen-settings-core`. `classic-config-core` should already be present (this binding wraps config-core) — verify and skip add if so.
    4. `ClassicLib-rs/python-bindings/classic-config-py/src/lib.rs`: replace any `classic_crashgen_settings_core::` with `classic_config_core::`. Grep the full src tree for the binding to confirm nothing is missed.

    Step D — classic-scanlog-py (PyO3):
    5. `ClassicLib-rs/python-bindings/classic-scanlog-py/Cargo.toml`: remove `classic-crashgen-settings-core`. Add `classic-config-core` ONLY if not already present.
    6. `ClassicLib-rs/python-bindings/classic-scanlog-py/src/crashgen_rules.rs`: swap `classic_crashgen_settings_core::` → `classic_config_core::`.
    7. `ClassicLib-rs/python-bindings/classic-scanlog-py/src/settings_validator.rs`: same swap.

    Step E — classic-scangame-py (PyO3) — TRAP GUARD:
    8. `ClassicLib-rs/python-bindings/classic-scangame-py/Cargo.toml`: remove `classic-crashgen-settings-core`. ADD `classic-config-core = { path = "../../business-logic/classic-config-core" }` (mirror existing dep path format). This is a NEW dep per D-10, analogous to D-05 for scangame-core. Do NOT skip this — without it the crate will fail to compile after the import swap.
    9. `ClassicLib-rs/python-bindings/classic-scangame-py/src/crashgen_rules.rs`: swap `classic_crashgen_settings_core::` → `classic_config_core::`.

    Step F — Full grep sweep for safety:
    10. Run `Get-ChildItem -Recurse -Path ClassicLib-rs -Include *.rs,*.toml | Select-String -Pattern 'classic[-_]crashgen[-_]settings[-_]core'`. Output must be empty. If any match remains, fix it before committing.

    Step G — Build verification:
    11. Run `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` (note: Python binding crates are excluded from default test run but they ARE in the workspace and get built).
    12. Run `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml`.

    Step H — Commit (explicit file paths per CLAUDE.md git safety — do NOT use `git add -A` or `git add .`):
    ```
    git add ClassicLib-rs/node-bindings/classic-node/Cargo.toml `
            ClassicLib-rs/node-bindings/classic-node/src/crashgen_rules.rs `
            ClassicLib-rs/python-bindings/classic-config-py/Cargo.toml `
            ClassicLib-rs/python-bindings/classic-config-py/src/lib.rs `
            ClassicLib-rs/python-bindings/classic-scanlog-py/Cargo.toml `
            ClassicLib-rs/python-bindings/classic-scanlog-py/src/crashgen_rules.rs `
            ClassicLib-rs/python-bindings/classic-scanlog-py/src/settings_validator.rs `
            ClassicLib-rs/python-bindings/classic-scangame-py/Cargo.toml `
            ClassicLib-rs/python-bindings/classic-scangame-py/src/crashgen_rules.rs `
            ClassicLib-rs/Cargo.lock
    git commit -m "Refactor: migrate binding consumers to classic-config-core for crashgen rules (CGEN-02)"
    ```
  </action>
  <verify>
    <automated>cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `Get-ChildItem -Recurse -Path ClassicLib-rs -Include *.rs,*.toml | Select-String -Pattern 'classic_crashgen_settings_core|classic-crashgen-settings-core'` returns NO matches anywhere in the workspace
    - `Select-String -Path ClassicLib-rs/python-bindings/classic-scangame-py/Cargo.toml -Pattern 'classic-config-core'` returns a match (NEW dep)
    - `Select-String -Path ClassicLib-rs/node-bindings/classic-node/src/crashgen_rules.rs -Pattern 'classic_config_core'` returns matches (proving import swap)
    - All 4 binding `crashgen_rules.rs` / lib.rs files retain their original filenames (no renames per D-09) — `test -f` on each original path succeeds
    - HEAD commit is a `Refactor:`-prefixed content edit touching only binding crate files
  </acceptance_criteria>
  <done>
    All 4 binding crates import crashgen types from `classic_config_core`. No binding file was renamed. `classic-scangame-py` now has the NEW `classic-config-core` dep per the trap guard. Workspace builds and tests clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Delete stray yamldata.rs.bak (separate commit per D-17)</name>
  <files>
    ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs.bak
  </files>
  <read_first>
    - .planning/phases/02-crashgen-config-merge/02-CONTEXT.md (D-17 — separate commit requirement)
    - ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs.bak (confirm it exists and is the 21 KB stray from 2025-12-13)
  </read_first>
  <action>
    Per D-17, this deletion is its OWN commit — do NOT bundle with the merge commits above. The separate commit keeps `git log` audit-clean.

    Commands:
    ```
    git rm ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs.bak
    cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml
    git commit -m "Chore: delete stray yamldata.rs.bak from classic-config-core"
    ```

    The build after the `git rm` is defensive — removing a `.bak` file should not affect compilation because Rust's module system only reads files named in `mod` declarations, but we verify to be safe.
  </action>
  <verify>
    <automated>cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs.bak` returns False
    - HEAD commit message starts with `Chore:` and mentions `yamldata.rs.bak`
    - HEAD commit touches exactly one file (the deleted .bak)
    - `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `git log --oneline -5` shows this commit as distinct from the Task 3 binding-migration commit and the Task 2 Rust-core-merge commit
  </acceptance_criteria>
  <done>
    The stray backup file is removed in an isolated commit. Workspace still builds.
  </done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks in this plan complete:

1. `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` → exit 0
2. `cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml` → exit 0
3. Full workspace grep:
   ```
   Get-ChildItem -Recurse -Path ClassicLib-rs -Include *.rs,*.toml | Select-String -Pattern 'classic[-_]crashgen[-_]settings[-_]core'
   ```
   Must return zero matches.
4. `Test-Path ClassicLib-rs/business-logic/classic-crashgen-settings-core` → False
5. `Test-Path ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs.bak` → False
6. `git log --oneline -6` shows the 4 distinct commits:
   - Task 1 (rename-only)
   - Task 2 (Rust core merge content edits)
   - Task 3 (binding migration)
   - Task 4 (yamldata.rs.bak deletion)
7. Workspace member count in `ClassicLib-rs/Cargo.toml` is one less than before this phase began.
</verification>

<success_criteria>
- CGEN-01: crashgen_rules.rs lives inside classic-config-core; public API surface preserved via `pub use crashgen_rules::*;` at the crate root
- CGEN-02: Every former consumer (3 Rust cores + 4 bindings) imports from classic_config_core; zero references to classic_crashgen_settings_core remain
- CGEN-03: classic-crashgen-settings-core directory deleted; workspace members list cleaned
- `cargo build --workspace` and `cargo test --workspace` both green
- Blame preserved via separate git mv commit
- yamldata.rs.bak deleted in its own commit
</success_criteria>

<output>
After completion, create `.planning/phases/02-crashgen-config-merge/02-01-SUMMARY.md` documenting:
- Commits created (expect 4: rename, core merge, binding migration, bak cleanup)
- Final crate count in workspace
- `cargo tree` delta for scangame-core and scangame-py showing new config-core dep
- Parity gate status (deferred to Plan 02-02)
</output>
</content>
</invoke>