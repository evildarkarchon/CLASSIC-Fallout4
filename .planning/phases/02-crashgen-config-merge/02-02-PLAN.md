---
phase: 02-crashgen-config-merge
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tools/node_api_parity/generate_baseline.py
  - tools/node_api_parity/tests/test_generate_baseline_targets.py
  - docs/api/classic-config-core.md
  - docs/api/classic-crashgen-settings-core.md
  - docs/api/README.md
  - docs/api/binding-parity-overview.md
  - docs/api/classic-scanlog-core.md
  - docs/api/classic-scangame-core.md
  - docs/RUST_DOCUMENTATION_INDEX.md
  - CLAUDE.md
  - AGENTS.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements: [CGEN-01, CGEN-02, CGEN-03]

must_haves:
  truths:
    - "tools/node_api_parity/generate_baseline.py no longer lists classic-crashgen-settings-core in RUST_TARGET_CRATES, RUST_OWNER_BY_CRATE, or SQUAD_BY_OWNER (all three dicts cleaned, plus the inline Phase-4-A1 comment removed)"
    - "tools/node_api_parity/tests/test_generate_baseline_targets.py expected-targets list no longer includes classic-crashgen-settings-core, and the Amendment A1 docstring assertions are gone"
    - "docs/api/classic-crashgen-settings-core.md no longer exists"
    - "docs/api/classic-config-core.md contains a 'Crashgen rule model' section merged from the deleted doc"
    - "docs/api/README.md index no longer references the deleted doc"
    - "docs/api/classic-scanlog-core.md, docs/api/classic-scangame-core.md, and docs/RUST_DOCUMENTATION_INDEX.md no longer reference the deleted crate"
    - "CXX parity gate exits 0 with zero drift"
    - "Python parity gate exits 0 with deferred_total == 0"
    - "Node parity gate exits 0 without a generator crash on the missing directory"
    - "CLAUDE.md and AGENTS.md no longer reference classic-crashgen-settings-core as a separate crate"
  artifacts:
    - path: "tools/node_api_parity/generate_baseline.py"
      provides: "Updated RUST_TARGET_CRATES + RUST_OWNER_BY_CRATE + SQUAD_BY_OWNER with no classic-crashgen-settings-core or crashgen_settings entries"
    - path: "tools/node_api_parity/tests/test_generate_baseline_targets.py"
      provides: "Updated expected-targets tests without classic-crashgen-settings-core or Amendment-A1 references"
    - path: "docs/api/classic-config-core.md"
      provides: "Merged crashgen rule model section"
      contains: "Crashgen rule model"
    - path: "docs/api/README.md"
      provides: "Index without crashgen-settings-core entry"
  key_links:
    - from: "tools/node_api_parity/generate_baseline.py"
      to: "ClassicLib-rs/business-logic/classic-config-core"
      via: "RUST_TARGET_CRATES entry (not classic-crashgen-settings-core)"
      pattern: "classic-config-core"
    - from: "docs/api/README.md"
      to: "docs/api/classic-config-core.md"
      via: "markdown link"
      pattern: "classic-config-core\\.md"
---

<objective>
Finish Phase 2 by (1) updating the Node parity generator tooling so the Node parity gate does not crash on a missing `classic-crashgen-settings-core` directory, (2) consolidating API docs into `classic-config-core.md`, (3) sweeping active project docs for stale crate references per D-15, and (4) running all 3 parity gates (CXX + Python + Node) verify-only per D-12 to prove zero drift.

Purpose: Without the Node parity tooling update this plan MUST run FIRST — per research, the parity gate will CRASH (not just drift) when it tries to scan a directory that no longer exists. Docs cleanup is latent but honors D-14 and D-15.

Output: All 3 parity gates green; docs reflect the 16-crate topology; CGEN-01/02/03 fully closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-crashgen-config-merge/02-CONTEXT.md
@.planning/phases/02-crashgen-config-merge/02-RESEARCH.md
@.planning/phases/02-crashgen-config-merge/02-VALIDATION.md
@.planning/phases/02-crashgen-config-merge/02-01-PLAN.md
@docs/api/README.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Node parity generator tooling (MUST run before Node parity gate — CRASH risk)</name>
  <files>
    tools/node_api_parity/generate_baseline.py
    tools/node_api_parity/tests/test_generate_baseline_targets.py
  </files>
  <read_first>
    - tools/node_api_parity/generate_baseline.py — confirm FOUR stale items still present per 02-RESEARCH.md lines 181-184:
      1. Line ~43: `"classic-crashgen-settings-core": "ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs",` inside `RUST_TARGET_CRATES`
      2. Line ~72: `"classic-crashgen-settings-core": "crashgen_settings",` inside `RUST_OWNER_BY_CRATE`
      3. An entry mapping the `"crashgen_settings"` owner to a squad label inside `SQUAD_BY_OWNER` (approximately lines 100-110)
      4. Lines ~36-40: an inline multi-line comment describing the Phase-4-Plan-1 "Amendment A1" decision to track this crate
      Pre-edit verification: `Select-String -Path tools/node_api_parity/generate_baseline.py -Pattern 'classic-crashgen-settings-core|crashgen_settings' -SimpleMatch` MUST return matches for all four items before editing.
    - tools/node_api_parity/tests/test_generate_baseline_targets.py — note the `tests/` subdirectory in the path. Confirm FOUR stale items per 02-RESEARCH.md lines 185-188:
      1. Line ~32-39: `def test_crashgen_settings_core_is_present` with its docstring (mentions "Amendment A1") and the `assert "classic-crashgen-settings-core" in gb.RUST_TARGET_CRATES` body
      2. Line ~70-79: `def test_crashgen_settings_core_has_explicit_owner` with its docstring (mentions "A1/MEDIUM concern") and the `assert "classic-crashgen-settings-core" in gb.RUST_OWNER_BY_CRATE` body
      The two docstring "Amendment A1" references are inside the two test functions above — they disappear when those functions are deleted.
    - .planning/phases/02-crashgen-config-merge/02-CONTEXT.md (D-12 verify-only strategy rationale)
    - .planning/STATE.md Decisions block — "Parity generator scripts now scan sub-module files recursively" (Phase 1 precedent)
  </read_first>
  <action>
    CRITICAL TRAP GUARD per deep_work_rules: Research flagged that tools/node_api_parity/generate_baseline.py contains THREE hardcoded dicts plus an inline comment all referencing the deleted crate. After Phase 2 Plan 01 deletes the `classic-crashgen-settings-core` directory, if this list is not updated the Node parity gate will CRASH — not merely drift. Worse, even if `RUST_TARGET_CRATES` is cleaned, a stale `crashgen_settings` entry in `SQUAD_BY_OWNER` without a corresponding owner in `RUST_OWNER_BY_CRATE` (or vice versa) will raise `KeyError` at handoff-rendering time. This task MUST remove ALL FOUR items and run before Task 3's Node parity gate invocation.

    Step A — Update generate_baseline.py (FOUR removals):
    1. Delete the `"classic-crashgen-settings-core": "ClassicLib-rs/business-logic/classic-crashgen-settings-core/src/lib.rs",` line from the `RUST_TARGET_CRATES` dict (~line 43). Do NOT replace with `classic-config-core` — config-core should already be in the list (verified at ~line 27). After deletion, `classic-config-core` must still appear in `RUST_TARGET_CRATES`.
    2. Delete the `"classic-crashgen-settings-core": "crashgen_settings",` line from the `RUST_OWNER_BY_CRATE` dict (~line 72). Do NOT add anything — config-core already has its own `"config"` owner entry.
    3. Delete the `SQUAD_BY_OWNER` entry that maps the `"crashgen_settings"` owner label to a squad. This is the MOST IMPORTANT removal — without it, the dict-audit assertion in `test_squad_by_owner_covers_every_owner` will still pass (because its other direction checks that owners have squads, not that squads have owners), but downstream code that iterates `SQUAD_BY_OWNER.items()` may still reference the dead owner. Grep for `crashgen_settings` in the file — after removal, `Select-String -Path tools/node_api_parity/generate_baseline.py -Pattern 'crashgen_settings'` must return 0 matches.
    4. Delete the inline Phase-4-Plan-1 Amendment-A1 comment at ~lines 36-40 (the paragraph that explains why the crate was being tracked). Leave surrounding comments intact.

    After all four removals, run:
    ```
    Select-String -Path tools/node_api_parity/generate_baseline.py -Pattern 'classic-crashgen-settings-core|crashgen_settings'
    ```
    This MUST return exactly 0 matches.

    Step B — Update tests/test_generate_baseline_targets.py (FOUR assertions / two functions):
    5. Delete the entire `def test_crashgen_settings_core_is_present():` function (~lines 32-39), including its docstring that references "Amendment A1". This removes the assertion at line 39 AND one of the two Amendment-A1 docstring references.
    6. Delete the entire `def test_crashgen_settings_core_has_explicit_owner():` function (~lines 70-79), including its docstring that references "A1/MEDIUM concern fix". This removes the assertion at line 76 AND the second Amendment-A1 docstring reference.
    7. Review the `test_rust_target_crates_floor_is_nineteen` function (~line 21): the floor is `>= 19`. After Phase 2 removes one crate, the expected count becomes 18. Update the floor to `>= 18` (NOT tight equality) so future growth is permitted, and update the docstring count from "10 to 19+" to "10 to 18+".
    8. Review `test_every_target_crate_lib_rs_path_exists` and `test_every_target_crate_parses_non_empty_symbols`: these are generic across all crates so they need no direct edits — but they WILL fail if Step A is incomplete (the missing lib.rs path will surface here first).

    After edits, run:
    ```
    Select-String -Path tools/node_api_parity/tests/test_generate_baseline_targets.py -Pattern 'classic-crashgen-settings-core|Amendment A1'
    ```
    This MUST return exactly 0 matches.

    Step C — Sanity run:
    9. Run the tests to confirm the generator script still loads and the tests still pass:
       ```
       uv run pytest tools/node_api_parity/tests/test_generate_baseline_targets.py -q
       ```
       Must exit 0.

    Step D — Commit (explicit file paths per CLAUDE.md git safety):
    ```
    git add tools/node_api_parity/generate_baseline.py `
            tools/node_api_parity/tests/test_generate_baseline_targets.py
    git commit -m "Update: remove classic-crashgen-settings-core from Node parity generator targets"
    ```
  </action>
  <verify>
    <automated>uv run pytest tools/node_api_parity/tests/test_generate_baseline_targets.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path tools/node_api_parity/generate_baseline.py -Pattern 'classic-crashgen-settings-core|crashgen_settings'` returns 0 matches
    - `Select-String -Path tools/node_api_parity/tests/test_generate_baseline_targets.py -Pattern 'classic-crashgen-settings-core|Amendment A1'` returns 0 matches
    - `Select-String -Path tools/node_api_parity/generate_baseline.py -Pattern 'classic-config-core'` returns at least one match (config-core remains a scan target)
    - `uv run pytest tools/node_api_parity/tests/test_generate_baseline_targets.py -q` exits 0
    - HEAD commit message starts with `Update:` and references the parity generator
  </acceptance_criteria>
  <done>
    Node parity generator tooling no longer tries to scan the deleted classic-crashgen-settings-core directory, AND the orphaned `crashgen_settings` owner/squad entries are gone. The companion test file is aligned (no stale assertions, no dead Amendment-A1 docstrings). The generator's unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Merge API doc, update index, sweep cross-references in active docs</name>
  <files>
    docs/api/classic-config-core.md
    docs/api/classic-crashgen-settings-core.md
    docs/api/README.md
    docs/api/binding-parity-overview.md
    docs/api/classic-scanlog-core.md
    docs/api/classic-scangame-core.md
    docs/RUST_DOCUMENTATION_INDEX.md
    CLAUDE.md
    AGENTS.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    .planning/codebase/STACK.md
    .planning/codebase/ARCHITECTURE.md
    .planning/codebase/STRUCTURE.md
  </files>
  <read_first>
    - .planning/phases/02-crashgen-config-merge/02-CONTEXT.md (D-14 merge strategy, D-15 cross-ref scope)
    - .planning/phases/02-crashgen-config-merge/02-RESEARCH.md lines 196-213 — full active-docs sweep table
    - docs/api/classic-crashgen-settings-core.md (full content to merge)
    - docs/api/classic-config-core.md (target for merged section)
    - docs/api/README.md (index file to update)
    - docs/api/binding-parity-overview.md (per D-15 scope)
    - docs/api/classic-scanlog-core.md (per RESEARCH line 207 — import-path references in examples)
    - docs/api/classic-scangame-core.md (per RESEARCH line 208 — import-path references in examples)
    - docs/RUST_DOCUMENTATION_INDEX.md (per RESEARCH line 209 — crashgen entry to remove or redirect)
    - CLAUDE.md (has a crate list mentioning crashgen-settings-core)
    - AGENTS.md (per D-15 scope)
    - .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md (per D-15 scope — the ROADMAP/REQUIREMENTS files intentionally KEEP references as historical phase records; only remove references that describe the crate as currently existing)
    - .planning/codebase/*.md (per D-15 scope — architecture/structure files)
  </read_first>
  <action>
    Per D-14, merge the crashgen-settings-core API doc INTO config-core as a section, then delete the source. Per D-15, sweep ONLY active docs — skip archived milestone plans and historical snapshots.

    Step A — Merge the API doc (D-14):
    1. Read `docs/api/classic-crashgen-settings-core.md` in full.
    2. Append its content to `docs/api/classic-config-core.md` as a new top-level section titled `## Crashgen rule model` (or equivalent heading matching the target doc's heading style). Preserve the merged content's sub-headings verbatim but bump each heading level by one so it nests cleanly under the new section. Update any internal links that pointed at the source doc to point at the new section anchor.
    3. `git rm docs/api/classic-crashgen-settings-core.md`.

    Step B — Update API doc index:
    4. Edit `docs/api/README.md`: remove the numbered entry for `classic-crashgen-settings-core.md` (currently item 12 per the provided index). Renumber subsequent items if the file uses strict sequential numbering, OR if the file uses plain bullet style, just delete the line. Expand the config-core description to note that it now contains the crashgen rule model.
    5. In the `order matches` bullet list further down README.md, remove the `classic-crashgen-settings-core` bullet and update the config-core bullet description.

    Step C — Update binding-parity-overview.md (D-15):
    6. `docs/api/binding-parity-overview.md`: remove any `classic-crashgen-settings-core` entry from the per-crate binding surface reference. Where the crashgen surface is documented, fold it into the config-core entry.

    Step D — Update adjacent API docs with example import paths (per RESEARCH lines 207-208):
    7. `docs/api/classic-scanlog-core.md`: grep for `classic_crashgen_settings_core` and `classic-crashgen-settings-core`. In any code examples or reference paragraphs that reference these names, update the import path to `classic_config_core` (matching the Plan 02-01 Task 2 source-level change). Do NOT rewrite unrelated content.
    8. `docs/api/classic-scangame-core.md`: same sweep and update — any example import paths referencing `classic_crashgen_settings_core` become `classic_config_core`.

    Step E — Update docs/RUST_DOCUMENTATION_INDEX.md (per RESEARCH line 209):
    9. Open `docs/RUST_DOCUMENTATION_INDEX.md` and locate the `classic-crashgen-settings-core` entry. Remove the entry entirely (preferred — the crate no longer exists), OR redirect it to point at the config-core "Crashgen rule model" section if the index format expects a fully-populated row. Match the surrounding entry style.

    Step F — Update CLAUDE.md and AGENTS.md (D-15):
    10. `CLAUDE.md`: search for `classic-crashgen-settings-core` — the Technology Stack / Architecture sections mention it. Update any crate-count numbers (e.g., "17 business-logic crates" → "16") and remove the crashgen-settings-core bullet from the layer list. If CLAUDE.md has a summary of what config-core does, note that it now owns the crashgen rule model.
    11. `AGENTS.md`: same sweep. Likely minimal — AGENTS.md is thin (RESEARCH noted no matches found).

    Step G — Update .planning/ docs (D-15, scoped):
    12. `.planning/ROADMAP.md`: DO NOT remove Phase 2 history or requirement IDs. Only update crate-count text (e.g., any "17 crates → 16 crates" aspirational text can now say "completed"). Search for `classic-crashgen-settings-core` and update only present-tense claims about the crate existing.
    13. `.planning/REQUIREMENTS.md`: same rule — CGEN-01/02/03 requirements text stays.
    14. `.planning/PROJECT.md`: update any architecture description text mentioning the crate.
    15. `.planning/codebase/STACK.md`, `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/STRUCTURE.md`: remove crashgen-settings-core from any crate inventory, update crate counts, fold its description under config-core where descriptive text exists.

    Step H — Full active-docs sweep:
    16. Run:
        ```
        Get-ChildItem -Path CLAUDE.md,AGENTS.md,docs/api,docs/RUST_DOCUMENTATION_INDEX.md,.planning/codebase,.planning/ROADMAP.md,.planning/REQUIREMENTS.md,.planning/PROJECT.md -Recurse -Include *.md | Select-String -Pattern 'classic-crashgen-settings-core|classic_crashgen_settings_core'
        ```
        Review each match. Remove if it describes the crate as a current artifact. Keep if it is a historical reference inside a phase narrative (e.g., "Phase 2 absorbed classic-crashgen-settings-core" is valid history). Per D-15, do NOT touch `.planning/milestones/*`, `docs/plans/*`, `docs/prd/complete/*`.

    Step I — Commit (explicit file paths per CLAUDE.md git safety — do NOT use `git add -A` or `git add .`):
    ```
    git add docs/api/classic-config-core.md `
            docs/api/README.md `
            docs/api/binding-parity-overview.md `
            docs/api/classic-scanlog-core.md `
            docs/api/classic-scangame-core.md `
            docs/RUST_DOCUMENTATION_INDEX.md `
            CLAUDE.md `
            AGENTS.md `
            .planning/ROADMAP.md `
            .planning/REQUIREMENTS.md `
            .planning/PROJECT.md `
            .planning/codebase/STACK.md `
            .planning/codebase/ARCHITECTURE.md `
            .planning/codebase/STRUCTURE.md
    # The git rm from Step A (classic-crashgen-settings-core.md) is already staged
    git commit -m "Docs: consolidate classic-crashgen-settings-core into classic-config-core documentation"
    ```
  </action>
  <verify>
    <automated>Select-String -Path docs/api/classic-config-core.md -Pattern 'Crashgen rule model'</automated>
  </verify>
  <acceptance_criteria>
    - `Test-Path docs/api/classic-crashgen-settings-core.md` returns False
    - `Select-String -Path docs/api/classic-config-core.md -Pattern 'Crashgen rule model'` returns a match
    - `Select-String -Path docs/api/README.md -Pattern 'classic-crashgen-settings-core'` returns no matches
    - `Select-String -Path docs/api/binding-parity-overview.md -Pattern 'classic-crashgen-settings-core'` returns no matches
    - `Select-String -Path docs/api/classic-scanlog-core.md,docs/api/classic-scangame-core.md,docs/RUST_DOCUMENTATION_INDEX.md -Pattern 'classic_crashgen_settings_core|classic-crashgen-settings-core'` returns 0 matches
    - `Select-String -Path CLAUDE.md -Pattern 'classic-crashgen-settings-core'` returns no matches that describe the crate as currently existing
    - Active-docs sweep (from Step H) returns only historical-context matches, if any
    - HEAD commit has `Docs:` prefix
  </acceptance_criteria>
  <done>
    API docs consolidated, adjacent `classic-scanlog-core.md` / `classic-scangame-core.md` / `RUST_DOCUMENTATION_INDEX.md` example references swept, active project docs cleaned, no present-tense references to the deleted crate remain in scoped-active documents.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run all 3 parity gates (verify-only per D-12) — CXX, Python, Node</name>
  <files>
    (verification only; no source files modified in this task)
  </files>
  <read_first>
    - .planning/phases/02-crashgen-config-merge/02-CONTEXT.md (D-12 — verify-only, expect zero drift)
    - .planning/phases/02-crashgen-config-merge/02-01-PLAN.md (must be complete)
    - .planning/phases/02-crashgen-config-merge/02-VALIDATION.md (parity gate section)
    - tools/cxx_api_parity/README.md (confirmed entry script: `python tools/cxx_api_parity/check_parity_gate.py`)
    - tools/python_api_parity/check_parity_gate.py
    - ClassicLib-rs/node-bindings/classic-node/package.json (locate `parity:gate:local` script)
  </read_first>
  <action>
    Per D-12, Phase 2 uses verify-only strategy: gates run, must exit 0 with zero drift. If drift appears it is a BUG to investigate, NOT a signal to regenerate baselines. Phase 4 does final cross-merge baseline work.

    Step A — CXX parity gate:
    1. Run the CXX parity gate entry (concrete command confirmed via `tools/cxx_api_parity/README.md`):
       ```
       python tools/cxx_api_parity/check_parity_gate.py
       ```
       Must exit 0.

    Step B — Python parity gate:
    2. Run:
       ```
       python tools/python_api_parity/check_parity_gate.py --repo-root .
       ```
       Must exit 0 AND the report's `deferred_total` field must equal 0 (per GATE-03 wording, even though GATE-03 is a Phase 4 requirement — same principle applies at the per-phase check).

    Step C — Node parity gate:
    3. From `ClassicLib-rs/node-bindings/classic-node/`:
       ```
       bun run parity:gate:local
       ```
       Must exit 0. This is the gate that Task 1's tooling update was defensive against — if Task 1 was skipped, this would CRASH on the missing directory.

    Step D — Document outcomes:
    4. Capture each command's exit code and summary line in the plan's summary artifact (02-02-SUMMARY.md). If any gate drifts or fails:
       - Do NOT regenerate baselines.
       - Halt and flag the issue for investigation (likely an import path the migration missed, or a generator tool that still scans the deleted directory).
       - Report the failure in the summary so /gsd:verify-work can act on it.

    No commit for this task — verification only.
  </action>
  <verify>
    <automated>python tools/python_api_parity/check_parity_gate.py --repo-root .</automated>
  </verify>
  <acceptance_criteria>
    - CXX parity gate exits 0 with zero drift vs committed baseline
    - Python parity gate exits 0 AND `deferred_total == 0` in its report output
    - Node parity gate (`bun run parity:gate:local`) exits 0 — does NOT crash on missing directory (validates Task 1)
    - Outcomes recorded in 02-02-SUMMARY.md
    - No baseline files were regenerated (`git status` shows no modifications to parity baseline directories)
  </acceptance_criteria>
  <done>
    All 3 parity gates are green with zero drift. Phase 2 is ready for `/gsd:verify-work`.
  </done>
</task>

</tasks>

<verification>
Phase-level verification after this plan completes:

1. Node parity generator tooling sweep clean:
   ```
   Select-String -Path tools/node_api_parity/generate_baseline.py,tools/node_api_parity/tests/test_generate_baseline_targets.py -Pattern 'classic-crashgen-settings-core|crashgen_settings|Amendment A1'
   ```
   Zero matches.
2. All 3 parity gates exit 0 with zero drift.
3. Active-docs sweep shows only historical-context matches:
   ```
   Get-ChildItem -Path CLAUDE.md,AGENTS.md,docs/api,docs/RUST_DOCUMENTATION_INDEX.md,.planning/codebase,.planning/ROADMAP.md,.planning/REQUIREMENTS.md,.planning/PROJECT.md -Recurse -Include *.md | Select-String -Pattern 'classic-crashgen-settings-core'
   ```
4. `docs/api/classic-crashgen-settings-core.md` does not exist.
5. `docs/api/classic-config-core.md` contains a `Crashgen rule model` section.
6. `docs/api/classic-scanlog-core.md`, `docs/api/classic-scangame-core.md`, and `docs/RUST_DOCUMENTATION_INDEX.md` contain zero references to the deleted crate.
</verification>

<success_criteria>
- CGEN-01, CGEN-02, CGEN-03 fully closed (source merge + consumer migration + deletion from 02-01, now validated by parity gates)
- Node parity generator no longer scans the deleted directory and no longer has an orphaned `crashgen_settings` owner/squad entry
- API docs consolidated per D-14
- Cross-reference cleanup per D-15 honored (active docs only), including the three additional targets from RESEARCH lines 207-209
- All 3 parity gates green with zero drift (verify-only per D-12)
- Phase 2 matches ROADMAP success criteria 1-4
</success_criteria>

<output>
After completion, create `.planning/phases/02-crashgen-config-merge/02-02-SUMMARY.md` documenting:
- The 2 commits created by this plan (Node parity tooling update + docs consolidation)
- Parity gate results: CXX/Python/Node exit codes + drift summary (must all be 0 / zero)
- Final state of classic-config-core docs (merged section heading)
- List of files swept during D-15 cross-reference cleanup
- Any historical-context references deliberately preserved (e.g., phase narratives)
</output>
</content>
</invoke>