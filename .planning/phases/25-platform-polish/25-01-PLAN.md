---
phase: 25-platform-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rust/Cargo.toml
  - rust/ui-applications/classic-gui/Cargo.toml
  - rust/ui-applications/classic-gui/src/logging.rs
  - rust/ui-applications/classic-gui/src/lib.rs
  - rust/ui-applications/classic-gui/src/main.rs
  - rust/ui-applications/classic-gui/src/settings.rs
autonomous: true

must_haves:
  truths:
    - "All application errors are written to a log file, not stderr"
    - "Slint renderer-software feature is compiled in as a fallback path"
    - "tracing-appender WorkerGuard is held for entire application lifetime"
  artifacts:
    - path: "rust/ui-applications/classic-gui/src/logging.rs"
      provides: "File logging initialization with tracing-appender"
      exports: ["init_logging"]
    - path: "rust/ui-applications/classic-gui/Cargo.toml"
      provides: "Dependencies for tracing-appender, tauri-winres, renderer-software"
      contains: "tracing-appender"
    - path: "rust/Cargo.toml"
      provides: "Workspace dependency for tracing-appender"
      contains: "tracing-appender"
  key_links:
    - from: "rust/ui-applications/classic-gui/src/main.rs"
      to: "logging.rs"
      via: "init_logging() call returns WorkerGuard"
      pattern: "init_logging"
    - from: "rust/ui-applications/classic-gui/src/main.rs"
      to: "tracing"
      via: "tracing::warn!/error! replaces eprintln!"
      pattern: "tracing::(warn|error)"
    - from: "rust/ui-applications/classic-gui/src/settings.rs"
      to: "tracing"
      via: "tracing::warn!/error! replaces eprintln!"
      pattern: "tracing::(warn|error)"
---

<objective>
Add file logging infrastructure and replace all eprintln! calls with tracing macros, preparing for console-less Windows distribution.

Purpose: With `windows_subsystem = "windows"` (added in Plan 02), stderr is null. All 12 eprintln! calls across main.rs and settings.rs would be silently lost. This plan creates the logging infrastructure and replaces those calls so errors are captured to a log file.

Output: logging.rs module with init_logging(), all eprintln! replaced with tracing macros, renderer-software feature added to Slint dependency.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-platform-polish/25-RESEARCH.md
@rust/ui-applications/classic-gui/Cargo.toml
@rust/ui-applications/classic-gui/src/main.rs
@rust/ui-applications/classic-gui/src/settings.rs
@rust/ui-applications/classic-gui/src/lib.rs
@rust/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and create logging module</name>
  <files>
    rust/Cargo.toml
    rust/ui-applications/classic-gui/Cargo.toml
    rust/ui-applications/classic-gui/src/logging.rs
    rust/ui-applications/classic-gui/src/lib.rs
  </files>
  <action>
1. Add `tracing-appender` to workspace dependencies in `rust/Cargo.toml`:
   ```toml
   tracing-appender = "0.2"
   ```

2. Update `rust/ui-applications/classic-gui/Cargo.toml`:
   - Add to `[dependencies]`:
     ```toml
     tracing = { workspace = true }
     tracing-subscriber = { workspace = true, features = ["fmt"] }
     tracing-appender = { workspace = true }
     ```
   - Add `renderer-software` to the Slint features list (secondary fallback if Skia entirely fails):
     ```toml
     slint = { workspace = true, features = ["backend-winit", "renderer-skia", "renderer-software", "image-default-formats"] }
     ```
   - Add to `[build-dependencies]`:
     ```toml
     tauri-winres = "0.3"
     ```

3. Create `rust/ui-applications/classic-gui/src/logging.rs`:
   - Module-level doc comment explaining file logging for the GUI app
   - `pub fn init_logging() -> tracing_appender::non_blocking::WorkerGuard`:
     - Get log directory via `directories::ProjectDirs::from("com", "classic", "classic-gui")`, use `data_dir()` (not config_dir -- logs are data, not config). Fall back to `std::env::current_dir()` if ProjectDirs returns None.
     - Create the log directory if it doesn't exist (use `std::fs::create_dir_all`)
     - Use `tracing_appender::rolling::never(&log_dir, "classic-gui.log")` for non-rotating log file (overwrite each launch per user decision)
     - Wrap in `tracing_appender::non_blocking()` to get `(NonBlocking, WorkerGuard)`
     - Initialize `tracing_subscriber::fmt()` with `.with_writer(non_blocking)`, `.with_ansi(false)`, `.with_target(true)`, `.with_level(true)`, `.init()`
     - Return the `WorkerGuard` (caller MUST hold it for entire app lifetime)
   - Add `/// # Important` doc section explaining the guard must be held

4. Register the new module in `rust/ui-applications/classic-gui/src/lib.rs`:
   - Add `pub mod logging;`
   - Add re-export: `pub use logging::init_logging;`

NOTE: `tracing-appender::rolling::never()` creates a file that is overwritten each launch, not appended to. This is the desired behavior per user decision (no rotation needed for a desktop app).
  </action>
  <verify>
Run `cargo check -p classic-gui` from `rust/` directory. Should compile without errors. Verify logging.rs exists and exports `init_logging`.
  </verify>
  <done>
- `tracing-appender` in workspace Cargo.toml
- `tracing`, `tracing-subscriber`, `tracing-appender` in classic-gui dependencies
- `renderer-software` in Slint features
- `tauri-winres` in build-dependencies
- `logging.rs` created with `init_logging()` function
- Module registered in lib.rs
- `cargo check -p classic-gui` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all eprintln! calls with tracing macros</name>
  <files>
    rust/ui-applications/classic-gui/src/main.rs
    rust/ui-applications/classic-gui/src/settings.rs
  </files>
  <action>
Replace all 12 `eprintln!` calls with appropriate `tracing` macros. The replacements are:

**In `main.rs` (10 calls):**
1. Line ~164 `eprintln!("Failed to save window state: {}", e)` -> `tracing::warn!("Failed to save window state: {}", e)`
2. Line ~175 `eprintln!("Failed to save window state: {}", e)` -> `tracing::warn!("Failed to save window state: {}", e)`
3. Line ~650 `eprintln!("Failed to save game_version: {}", e)` -> `tracing::warn!("Failed to save game_version: {}", e)`
4. Line ~674 `eprintln!("Failed to save update_check: {}", e)` -> `tracing::warn!("Failed to save update_check: {}", e)`
5. Line ~691 `eprintln!("Failed to save update_source: {}", e)` -> `tracing::warn!("Failed to save update_source: {}", e)`
6. Line ~705 `eprintln!("Failed to save fcx_mode: {}", e)` -> `tracing::warn!("Failed to save fcx_mode: {}", e)`
7. Line ~722 `eprintln!("Failed to save simplify_logs: {}", e)` -> `tracing::warn!("Failed to save simplify_logs: {}", e)`
8. Line ~738 `eprintln!("Failed to save show_formid_values: {}", e)` -> `tracing::warn!("Failed to save show_formid_values: {}", e)`
9. Line ~754 `eprintln!("Failed to save move_unsolved_logs: {}", e)` -> `tracing::warn!("Failed to save move_unsolved_logs: {}", e)`
10. Line ~770 `eprintln!("Failed to save auto_switch_to_results: {}", e)` -> `tracing::warn!("Failed to save auto_switch_to_results: {}", e)`

**In `settings.rs` (2 calls):**
1. Line ~40 `eprintln!("Failed to load settings, using defaults: {}", e)` -> `tracing::warn!("Failed to load settings, using defaults: {}", e)`
2. Line ~177 `eprintln!("Failed to save default settings: {}", e)` -> `tracing::warn!("Failed to save default settings: {}", e)`

Use `tracing::warn!` for all of these (they are non-fatal save/load errors that fall back gracefully). Do NOT use `tracing::error!` -- these are expected recovery paths, not application errors.

After replacing, verify there are ZERO remaining `eprintln!` calls in the entire `src/` directory of classic-gui. The crate lint `unsafe_code = "deny"` is already set; no new unsafe code should be introduced.
  </action>
  <verify>
1. Run `cargo check -p classic-gui` -- should compile without errors
2. Search for remaining `eprintln!` in `rust/ui-applications/classic-gui/src/` -- should find zero matches
3. Run `cargo test -p classic-gui` -- all existing tests should still pass
  </verify>
  <done>
- All 12 eprintln! calls replaced with tracing::warn!
- Zero eprintln! calls remain in the classic-gui crate
- `cargo check` and `cargo test` pass
  </done>
</task>

</tasks>

<verification>
1. `cargo check -p classic-gui` compiles without errors
2. `cargo test -p classic-gui` -- all existing tests pass
3. No `eprintln!` calls remain in `rust/ui-applications/classic-gui/src/`
4. `logging.rs` exports `init_logging()` returning `WorkerGuard`
5. Slint features include `renderer-software`
</verification>

<success_criteria>
- File logging infrastructure is ready (logging.rs with init_logging)
- All stderr output replaced with structured tracing macros
- renderer-software feature compiled in for GPU fallback path
- All build dependencies for Plan 02 are in place (tauri-winres)
- Existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/25-platform-polish/25-01-SUMMARY.md`
</output>
