---
phase: 12-integration-replay-and-verification-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/planning/phase09_clean_run.ps1
  - tests/planning/test_phase09_validation.py
  - .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
autonomous: true
requirements: [INTG-01, INTG-04]
must_haves:
  truths:
    - A targeted clean-state proof recreates `python-bindings/.venv` before Python wrapper replay runs.
    - After the Phase 9 clean proof completes, the repo-root Python rebuild wrapper is still runnable without manual bootstrap.
    - The committed Phase 9 audit contract explicitly checks both venv recreation and post-proof wrapper replay.
  artifacts:
    - path: tests/planning/phase09_clean_run.ps1
      provides: Fresh-state harness that restores the Python binding environment during proof.
    - path: tests/planning/test_phase09_validation.py
      provides: Executable audit coverage for venv recreation and replayability.
    - path: .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
      provides: Human-readable clean-proof record aligned to the repaired replay flow.
  key_links:
    - from: tests/planning/phase09_clean_run.ps1
      to: rebuild_rust.ps1
      via: repo-root PowerShell proof step after venv bootstrap
      pattern: rebuild_rust\.ps1
    - from: tests/planning/phase09_clean_run.ps1
      to: python-bindings/.venv
      via: `uv venv` and dependency install before wrapper replay
      pattern: python-bindings/\.venv|uv venv
    - from: tests/planning/test_phase09_validation.py
      to: tests/planning/phase09_clean_run.ps1
      via: ordered proof-step assertions
      pattern: phase09_clean_run\.ps1
---

<objective>
Repair the clean-state replay path so the Python rebuild wrapper stays usable after the Phase 9 proof.

Purpose: Close the only remaining broken end-to-end integration flow called out by the milestone audit.
Output: A durable Phase 9 clean harness plus aligned audit/test coverage proving `.venv` recreation and wrapper replay.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v9.1.0-MILESTONE-AUDIT.md
@.planning/phases/08-wrapper-and-parity-rewire/08-CONTEXT.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-VALIDATION.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
@tests/planning/phase09_clean_run.ps1
@tests/planning/test_phase09_validation.py
@rebuild_rust.ps1

<interfaces>
Current Python wrapper prerequisite from `rebuild_rust.ps1`:

```powershell
if (-not (Test-Path $PythonBindingsPython)) {
    Write-Error "Python bindings virtual environment not found at '$PythonBindingsVenv'. Create it first with 'uv venv python-bindings/.venv' and install dependencies into that interpreter."
    exit 1
}
```

Current clean harness gap from `tests/planning/phase09_clean_run.ps1`:

```powershell
$CleanupTargets = @(
    (Join-Path $RepoRoot "target"),
    (Join-Path $RepoRoot "python-bindings/.venv"),
    ...
)

$ProofSteps = @(
    @{ Command = "cargo locate-project --workspace --message-format plain"; WorkingDirectory = $RepoRoot },
    ...
    @{ Command = "python -m pytest tests/planning/test_phase09_validation.py -q"; WorkingDirectory = $RepoRoot }
)
```

The repaired proof must preserve Phase 9 D-01/D-02/D-03 targeted-clean behavior while making the wrapper replay durable, not manual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the Python binding environment inside the clean-proof harness</name>
  <files>tests/planning/phase09_clean_run.ps1, .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md</files>
  <action>Update the Phase 9 clean harness so it still deletes `python-bindings/.venv` per Phase 9 D-01/D-02, but then recreates the bindings-local environment with the canonical repo-root bootstrap (`uv venv python-bindings/.venv` plus dependency install into `python-bindings/.venv/Scripts/python.exe`) before any Python wrapper proof runs. Keep the proof repo-root-first and targeted-clean only; do not weaken `rebuild_rust.ps1`'s fail-fast prerequisite or add permanent dual-layout compatibility. Extend the ordered proof steps in `09-CLEAN-VALIDATION-AUDIT.md` so they explicitly record the venv bootstrap, the repo-root Python wrapper replay, and the fact that no new `ClassicLib-rs/` residue appears afterward, per Phase 8 D-01/D-02 and Phase 9 D-01 through D-03.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File tests/planning/phase09_clean_run.ps1</automated>
  </verify>
  <done>The clean proof recreates `.venv`, completes from a targeted-clean state, and leaves the Python wrapper runnable without manual setup.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten the Phase 9 audit to prove replayability instead of one-shot success</name>
  <files>tests/planning/test_phase09_validation.py</files>
  <action>Update `tests/planning/test_phase09_validation.py` so its ordered proof assertions require the new venv-bootstrap commands, require the repo-root Python wrapper replay step after bootstrap, and keep the legacy-residue checks intact. Preserve the existing CI/package/path assertions; this task should only strengthen the clean-replay contract that the milestone audit flagged, not broaden Phase 9 scope into unrelated docs or artifact churn.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q</automated>
  </verify>
  <done>The committed Phase 9 validation suite fails if the clean proof deletes `.venv` without restoring wrapper replayability.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/planning/test_phase09_validation.py -q`
- `pwsh -ExecutionPolicy Bypass -File tests/planning/phase09_clean_run.ps1`
</verification>

<success_criteria>
- `tests/planning/phase09_clean_run.ps1` recreates `python-bindings/.venv` before Python wrapper replay.
- The clean proof finishes with no new generated residue under `ClassicLib-rs/`.
- `tests/planning/test_phase09_validation.py` explicitly guards the repaired replay flow.
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-replay-and-verification-closure/12-01-SUMMARY.md`
</output>
