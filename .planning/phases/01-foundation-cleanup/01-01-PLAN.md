---
phase: 01-foundation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib/io/database/database_rust.py
  - ClassicLib/core/constants.py
autonomous: true

must_haves:
  truths:
    - "No file in ClassicLib/ contains DEPRECATED markers after cleanup"
    - "All existing tests still pass after deprecated code removal"
    - "cargo build --workspace succeeds confirming Rust crate health"
    - "Coverage baseline report exists showing per-module coverage percentages"
    - "0% coverage modules outside integration/python/ are evaluated and documented"
  artifacts:
    - path: "ClassicLib/io/database/database_rust.py"
      provides: "DELETED -- deprecated re-export shim with zero callers"
    - path: "ClassicLib/core/constants.py"
      provides: "Live code only -- _DeprecatedVersion class and deprecated constants removed"
  key_links:
    - from: "ClassicLib/core/constants.py"
      to: "all importers of NULL_VERSION, YAML, GameID, DB_PATHS"
      via: "imports must still work after removing deprecated block"
      pattern: "from ClassicLib.core.constants import"
---

<objective>
Remove all deprecated code from ClassicLib/ and establish a test coverage baseline.

Purpose: Eliminate dead code noise so subsequent phases work with only live code. Coverage baseline identifies deletion candidates and prevents coverage regression.
Output: Deprecated files deleted, deprecated code blocks removed from mixed files, coverage baseline report generated and evaluated.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cleanup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated Python code</name>
  <files>ClassicLib/io/database/database_rust.py, ClassicLib/core/constants.py</files>
  <action>
1. **Delete `ClassicLib/io/database/database_rust.py` entirely.** This file is a deprecated re-export shim with zero callers (verified in research). Before deleting, run a final confirmation: `grep -r "database_rust" ClassicLib/ tests/` to ensure no imports exist. If any imports found, update them to import from the actual source instead.

2. **Remove the deprecated block from `ClassicLib/core/constants.py`.** This file contains BOTH deprecated and live code. Remove ONLY:
   - The `_DeprecatedVersion` class (wrapper class with `__getattr__`)
   - The deprecated version constants block (e.g., `OG_VERSION`, `NG_VERSION`, `BEST_VERSION`, etc.) that use `_DeprecatedVersion`
   - Any `warnings.warn(..., DeprecationWarning)` calls in the deprecated block
   - The associated imports for `warnings` if only used by the deprecated block

   **KEEP all of these (they are live code):**
   - `NULL_VERSION` constant
   - `YAML` enum class
   - `GameID` enum/class
   - `SETTINGS_IGNORE_NONE` constant
   - `DB_PATHS` dict
   - `get_db_paths()` function
   - Any other non-deprecated constants or functions

   Before removing, confirm no callers reference the deprecated constant names: `grep -rn "OG_VERSION\|NG_VERSION\|BEST_VERSION\|_DeprecatedVersion" ClassicLib/ tests/` -- expect zero matches.

3. **Search for any other DEPRECATED markers.** Run `grep -r "DEPRECATED\|deprecated" ClassicLib/ --include="*.py" -l` and evaluate each result. If deprecation markers exist only in docstrings describing *external* deprecation (e.g., "this replaces the deprecated X"), leave them. If they mark code that should be deleted, delete it.

4. **Run all tests** to confirm nothing breaks: `uv run pytest -m "unit and not slow" -x --tb=short`

5. **Verify no DEPRECATED markers remain** in live code: `grep -r "DEPRECATED" ClassicLib/ --include="*.py" -l` should return 0 files.
  </action>
  <verify>
- `grep -r "DEPRECATED" ClassicLib/ --include="*.py" -l` returns empty (0 files)
- `uv run pytest -m "unit and not slow" -x` passes
- `python -c "from ClassicLib.core.constants import NULL_VERSION, YAML, GameID, DB_PATHS, get_db_paths; print('OK')"` succeeds
- `ClassicLib/io/database/database_rust.py` does not exist
  </verify>
  <done>All deprecated code removed from ClassicLib/. No DEPRECATED markers remain in any .py file. All existing tests pass. Live imports from constants.py still work.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Rust workspace health and establish coverage baseline</name>
  <files>(no files modified -- verification and reporting only)</files>
  <action>
1. **Verify Rust workspace builds cleanly:** Run `cargo build --workspace` from the `rust/` directory. All 39 crates should compile without errors. This is a verification task -- the research confirmed no stub/empty crates exist, so the expected outcome is a clean build.

2. **Run cargo clippy for dead code detection:** Run `cargo clippy --workspace -- -D warnings` from the `rust/` directory. The workspace already has `unused = "deny"` lint configured. Any clippy warnings about dead Rust code should be noted but NOT fixed in this plan (they are informational for the audit record).

3. **Generate test coverage baseline:** Run `uv run pytest -m "unit and not slow" --cov=ClassicLib --cov-report=term --cov-report=html:htmlcov` to produce both terminal and HTML coverage reports. Save the terminal output to `.planning/phases/01-foundation-cleanup/coverage-baseline.txt` for reference.

4. **Evaluate 0% coverage modules:** From the coverage report, identify all modules showing 0% coverage. For each:
   - If in `ClassicLib/integration/python/`: Mark as "expected 0% -- Rust active, Phase 5 candidate". Do NOT flag for deletion.
   - If in `ClassicLib/integration/rust/`: Mark as "expected low -- tested through Rust, not directly". Do NOT flag for deletion.
   - If elsewhere in ClassicLib/: Evaluate whether the module is dead code or simply untested. Check if it has callers (`grep -r "import module_name" ClassicLib/`). Document findings.

5. **Document the evaluation** in the coverage-baseline.txt file with a section at the bottom listing each 0% module and its disposition (keep/investigate/Phase 5 candidate).

NOTE: Do not redirect output to `nul` or `NUL` on Windows. Use file redirection to actual files only.
  </action>
  <verify>
- `cargo build --workspace` succeeds (run from J:/CLASSIC-Fallout4/rust/)
- `.planning/phases/01-foundation-cleanup/coverage-baseline.txt` exists and contains coverage data
- Coverage report includes per-module percentages
- 0% modules are listed with evaluation notes
  </verify>
  <done>Rust workspace verified healthy (all crates compile, clippy passes). Coverage baseline captured with per-module data. All 0% coverage modules evaluated and categorized (Phase 5 candidates vs dead code vs untested).</done>
</task>

</tasks>

<verification>
Phase-level checks after both tasks complete:
1. `grep -r "DEPRECATED" ClassicLib/ --include="*.py" -l` returns 0 files
2. `cargo build --workspace` succeeds (from rust/ directory)
3. `uv run pytest -m "unit and not slow"` passes with 0 failures
4. `.planning/phases/01-foundation-cleanup/coverage-baseline.txt` exists with evaluation notes
</verification>

<success_criteria>
- All deprecated Python code removed (DEAD-01 complete)
- Rust workspace verified healthy, no stub crates (DEAD-03 complete -- verification confirms all crates are real)
- Coverage baseline established with 0% module evaluation (DEAD-04 complete)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cleanup/01-01-SUMMARY.md`
</output>
