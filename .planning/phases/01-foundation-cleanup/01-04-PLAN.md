---
phase: 01-foundation-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/gui/settings/test_settings_persistence_e2e.py
  - tests/performance/test_async_pipeline_performance.py
  - tests/performance/test_crash_log_processing_performance.py
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 4 previously-failing tests pass with reset_all_singletons fixture active"
    - "Phase 1 success criterion 1 accurately scopes deprecated code check"
  artifacts:
    - path: "tests/gui/settings/test_settings_persistence_e2e.py"
      provides: "Settings e2e tests with proper MessageHandler initialization"
      contains: "gui_message_handler"
    - path: "tests/performance/test_async_pipeline_performance.py"
      provides: "Pipeline perf test with MessageHandler init"
      contains: "init_message_handler"
    - path: "tests/performance/test_crash_log_processing_performance.py"
      provides: "Crash log perf test with MessageHandler init"
      contains: "init_message_handler"
  key_links:
    - from: "tests/gui/settings/test_settings_persistence_e2e.py"
      to: "tests/fixtures/qt_fixtures.py"
      via: "gui_message_handler fixture"
      pattern: "gui_message_handler"
    - from: "tests/performance/*.py"
      to: "tests/fixtures/registry_fixtures.py"
      via: "init_message_handler_fixture"
      pattern: "init_message_handler_fixture"
---

<objective>
Close 2 UAT gaps from Phase 1 verification: fix 4 tests that fail with "MessageHandler not initialized" after reset_all_singletons fixture was added, and refine the overly-broad DEPRECATED grep success criterion.

Purpose: Phase 1 is functionally complete but UAT found pre-existing state-leakage bugs exposed by the new singleton reset fixture, plus a success criterion that catches Phase 4 scope items.
Output: All tests pass, ROADMAP success criterion refined.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cleanup/01-03-SUMMARY.md
@.planning/phases/01-foundation-cleanup/01-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MessageHandler initialization in failing tests</name>
  <files>
    tests/gui/settings/test_settings_persistence_e2e.py
    tests/performance/test_async_pipeline_performance.py
    tests/performance/test_crash_log_processing_performance.py
  </files>
  <action>
Fix 4 tests that fail with "RuntimeError: Message handler not initialized" because reset_all_singletons now properly clears MessageHandler between tests.

**Settings e2e tests** (`tests/gui/settings/test_settings_persistence_e2e.py`):

The `TestPersistenceAcrossInstances` class methods use `app` and `reset_settings` fixtures but NOT `gui_message_handler`. The `SettingsDialog` constructor requires MessageHandler to be initialized. Compare with `test_integration_e2e.py` which correctly uses `gui_message_handler` alongside `app`.

Fix: Add `gui_message_handler` to the fixture parameters of:
- `test_settings_persistence_across_instances(self, app, reset_settings, gui_message_handler)`
- `test_settings_reload_after_save(self, app, reset_settings, gui_message_handler)`

The `gui_message_handler` fixture is already available (defined in `tests/fixtures/qt_fixtures.py` and registered via conftest plugin loading). It initializes MessageHandler in GUI mode with a mocked show method to prevent blocking dialogs.

Do NOT change `TestDefaultValues` tests unless they also fail -- `test_missing_settings_use_defaults` and `test_invalid_game_version_uses_default` were not reported as failing.

**Performance tests** (`tests/performance/test_async_pipeline_performance.py` and `tests/performance/test_crash_log_processing_performance.py`):

These tests create `AsyncCrashLogPipeline` which needs MessageHandler. They have no conftest.py and no MessageHandler initialization.

Fix: Add `init_message_handler_fixture` to the test method/class fixture parameters. This fixture is defined in `tests/fixtures/registry_fixtures.py` and provides a non-GUI MessageHandler instance with proper cleanup.

For `test_async_pipeline_performance.py`: Add `init_message_handler_fixture` parameter to test functions or the class that creates `AsyncCrashLogPipeline`.

For `test_crash_log_processing_performance.py`: Add `init_message_handler_fixture` parameter to the `TestRealWorldCrashLogProcessing` class test methods.

IMPORTANT: Read each test file fully before modifying to verify the exact test signatures and understand which tests actually need the fixture. Only add the fixture to tests that create objects requiring MessageHandler.
  </action>
  <verify>
Run the 4 previously-failing tests:
```
uv run pytest tests/gui/settings/test_settings_persistence_e2e.py::TestPersistenceAcrossInstances -v
uv run pytest tests/performance/test_async_pipeline_performance.py tests/performance/test_crash_log_processing_performance.py -v
```
All 4 tests must pass (no "Message handler not initialized" errors).

Then run the full quick test suite to confirm no regressions:
```
uv run pytest -x -m "unit and not slow" --timeout=60 -q
```
  </verify>
  <done>All 4 previously-failing tests pass. No regressions in unit test suite.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Phase 1 DEPRECATED success criterion in ROADMAP</name>
  <files>.planning/ROADMAP.md</files>
  <action>
The Phase 1 success criterion #1 currently reads:
```
`grep -r "DEPRECATED" ClassicLib/ --include="*.py" -l` returns 0 files (all deprecated modules removed)
```

This is too broad. `ClassicLib/scanning/logs/analyzers/FormIDAnalyzer.py` contains `DEPRECATED:` markers in docstrings flagging sync wrappers for Phase 4 removal. These are active deprecation notices, not dead code -- Phase 4 (Interface Consolidation) explicitly plans to remove the FormID sync wrapper (plan 04-01).

Update ROADMAP.md Phase 1 success criterion #1 to scope-check only Phase 1 targets:
```
`grep -r "DEPRECATED" ClassicLib/ --include="*.py" -l` returns only files with Phase 4+ deprecation notices (FormIDAnalyzer.py sync wrappers); no Phase 1 deprecated modules (database_rust.py, _DeprecatedVersion) remain
```

This makes it clear that the grep finding FormIDAnalyzer.py is expected and within scope of a later phase.
  </action>
  <verify>Read the updated ROADMAP.md and confirm criterion #1 is refined to scope Phase 1 targets only.</verify>
  <done>Phase 1 success criterion #1 distinguishes between Phase 1 deprecated code (removed) and Phase 4 deprecation notices (expected to remain).</done>
</task>

</tasks>

<verification>
1. Run the 4 previously-failing tests individually and confirm all pass
2. Run `uv run pytest -x -m "unit and not slow" --timeout=60 -q` -- no regressions
3. Read ROADMAP.md Phase 1 criterion #1 -- properly scoped
</verification>

<success_criteria>
- 4 tests that failed with "Message handler not initialized" now pass
- No test regressions in unit suite
- ROADMAP Phase 1 success criterion #1 accurately reflects Phase 1 scope
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cleanup/01-04-SUMMARY.md`
</output>
