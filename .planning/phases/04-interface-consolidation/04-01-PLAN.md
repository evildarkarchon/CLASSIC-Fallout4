---
phase: 04-interface-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib/scanning/logs/analyzers/FormIDAnalyzer.py
  - ClassicLib/scanning/logs/analyzers/__init__.py
  - ClassicLib/scanning/logs/__init__.py
  - ClassicLib/scanning/logs/orchestrator_core.py
  - ClassicLib/integration/rust/formid_rust.py
autonomous: true

must_haves:
  truths:
    - "FormIDAnalyzer.py sync wrapper file does not exist"
    - "All callers that previously referenced FormIDAnalyzer now use FormIDAnalyzerCore directly or are removed"
    - "uv run pytest passes with no regressions from removal"
  artifacts:
    - path: "ClassicLib/scanning/logs/analyzers/FormIDAnalyzer.py"
      provides: "MUST NOT EXIST after plan completes"
    - path: "ClassicLib/scanning/logs/analyzers/__init__.py"
      provides: "No FormIDAnalyzer re-export"
    - path: "ClassicLib/scanning/logs/__init__.py"
      provides: "No FormIDAnalyzer re-export"
  key_links:
    - from: "ClassicLib/scanning/logs/orchestrator_core.py"
      to: "FormIDAnalyzerCore (directly)"
      via: "Dead FormIDAnalyzer assignment removed"
      pattern: "no.*FormIDAnalyzer.*import"
    - from: "ClassicLib/integration/rust/formid_rust.py"
      to: "FormIDAnalyzerCore"
      via: "Direct import instead of FormIDAnalyzer alias"
      pattern: "from.*FormIDAnalyzerCore import"
---

<objective>
Remove the deprecated FormIDAnalyzer.py sync wrapper (155 lines) and update all callers to use FormIDAnalyzerCore directly.

Purpose: Eliminates the first of three deprecated sync wrapper patterns, removing 155 lines of dead indirection that wraps FormIDAnalyzerCore with create_sync_wrapper calls.
Output: FormIDAnalyzer.py deleted, all imports updated, tests passing.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interface-consolidation/04-RESEARCH.md
@ClassicLib/scanning/logs/analyzers/FormIDAnalyzer.py
@ClassicLib/scanning/logs/orchestrator_core.py
@ClassicLib/integration/rust/formid_rust.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove FormIDAnalyzer references from all callers</name>
  <files>
    ClassicLib/scanning/logs/orchestrator_core.py
    ClassicLib/integration/rust/formid_rust.py
    ClassicLib/scanning/logs/__init__.py
    ClassicLib/scanning/logs/analyzers/__init__.py
  </files>
  <action>
1. **orchestrator_core.py** (~line 21): Remove the import `from ClassicLib.scanning.logs.analyzers.FormIDAnalyzer import FormIDAnalyzer`. Remove the assignment `self.formid_analyzer = FormIDAnalyzer(...)` in __init__ (this is a dead assignment -- only `_async_formid_analyzer` which uses FormIDAnalyzerCore is used at runtime, see lines ~485-488, ~661-662). Search the file for any other references to `self.formid_analyzer` and remove them. Do NOT touch `self._async_formid_analyzer` or `FormIDAnalyzerCore` references.

2. **formid_rust.py** (~line 69): Change the fallback import from:
   ```python
   from ClassicLib.scanning.logs.analyzers.FormIDAnalyzer import FormIDAnalyzer as PyFormIDAnalyzer
   ```
   to:
   ```python
   from ClassicLib.scanning.logs.analyzers.FormIDAnalyzerCore import FormIDAnalyzerCore as PyFormIDAnalyzer
   ```
   This is safe because FormIDAnalyzerCore has the same `__init__(self, yamldata, show_formid_values, formid_db_exists, db_pool=None)` signature -- FormIDAnalyzer was a sync wrapper around it. Verify by reading FormIDAnalyzerCore.py line ~64 to confirm the __init__ signature matches what formid_rust.py passes.

3. **scanning/logs/__init__.py** (~line 29): Remove the `from ClassicLib.scanning.logs.analyzers.FormIDAnalyzer import FormIDAnalyzer` import and any re-export of `FormIDAnalyzer` in `__all__`. Also check line 13 which has a comment referencing FormIDAnalyzer -- update or remove it.

4. **scanning/logs/analyzers/__init__.py** (~line 13): Remove the `from ClassicLib.scanning.logs.analyzers.FormIDAnalyzer import FormIDAnalyzer` import and any re-export of `FormIDAnalyzer` in `__all__`.
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.scanning.logs import orchestrator_core; print('OK')"`
Run: `uv run python -c "from ClassicLib.integration.rust import formid_rust; print('OK')"`
Run: `uv run python -c "from ClassicLib.scanning.logs import analyzers; print('OK')"`
All three must print OK with no ImportError.
Run: `uv run python -c "import inspect; from ClassicLib.scanning.logs.analyzers.FormIDAnalyzerCore import FormIDAnalyzerCore; print(inspect.signature(FormIDAnalyzerCore.__init__))"` -- verify it accepts (self, yamldata, show_formid_values, formid_db_exists, db_pool=None).
  </verify>
  <done>No Python file in ClassicLib/ imports FormIDAnalyzer (the sync wrapper class). orchestrator_core no longer has the dead formid_analyzer assignment.</done>
</task>

<task type="auto">
  <name>Task 2: Delete FormIDAnalyzer.py and run tests</name>
  <files>
    ClassicLib/scanning/logs/analyzers/FormIDAnalyzer.py
  </files>
  <action>
1. Delete the file `ClassicLib/scanning/logs/analyzers/FormIDAnalyzer.py` entirely.

2. Verify no remaining references exist by searching the entire ClassicLib/ directory and tests/ directory for "FormIDAnalyzer" (the class name, not FormIDAnalyzerCore). Any remaining references in test files should be updated to use FormIDAnalyzerCore instead or removed if they test the sync wrapper specifically.

3. Search tests/ for imports of FormIDAnalyzer (not FormIDAnalyzerCore) and update them. Test files that specifically tested the sync wrapper behavior should be removed or rewritten to test FormIDAnalyzerCore directly.

4. Run the full test suite: `uv run pytest -x -q` (stop on first failure for fast feedback). If tests fail due to FormIDAnalyzer references, fix them by updating to FormIDAnalyzerCore.
  </action>
  <verify>
Run: `uv run pytest -x -q`
Confirm: `grep -r "FormIDAnalyzer" ClassicLib/ --include="*.py"` returns ONLY references to FormIDAnalyzerCore (the file FormIDAnalyzerCore.py and its class).
  </verify>
  <done>FormIDAnalyzer.py does not exist. All tests pass. No stale references to the sync wrapper class remain.</done>
</task>

</tasks>

<verification>
1. `ls ClassicLib/scanning/logs/analyzers/FormIDAnalyzer.py` should fail (file not found)
2. `grep -r "from.*FormIDAnalyzer import FormIDAnalyzer" ClassicLib/ --include="*.py"` returns 0 results
3. `grep -r "create_sync_wrapper" ClassicLib/scanning/ --include="*.py"` returns only executor.py (not FormIDAnalyzer.py)
4. `uv run pytest -x -q` passes
</verification>

<success_criteria>
FormIDAnalyzer.py sync wrapper file does not exist. All callers updated to use FormIDAnalyzerCore directly. Test suite passes with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-interface-consolidation/04-01-SUMMARY.md`
</output>
