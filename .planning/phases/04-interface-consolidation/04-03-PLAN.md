---
phase: 04-interface-consolidation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - ClassicLib/io/yaml/sync/cache.py
  - ClassicLib/io/yaml/sync/convenience.py
  - ClassicLib/io/yaml/sync/__init__.py
  - ClassicLib/io/yaml/__init__.py
  - ClassicLib/io/yaml/cache.py
  - ClassicLib/io/yaml/convenience.py
  - ClassicLib/support/resources.py
  - tests/fixtures/singleton_fixtures.py
  - tests/core/test_resource_loader_unit.py
autonomous: false

must_haves:
  truths:
    - "ClassicLib/io/yaml/sync/ directory does not exist"
    - "yaml_settings, classic_settings, yaml_cache, YamlSettingsCache are importable from ClassicLib.io.yaml"
    - "All 60+ callers of yaml_settings/classic_settings continue to work without any import changes (they import from ClassicLib.io.yaml)"
    - "GUI launches and performs crash log scan without freezing or deadlocking"
    - "uv run pytest passes with no regressions"
  artifacts:
    - path: "ClassicLib/io/yaml/sync/"
      provides: "MUST NOT EXIST after plan completes (directory deleted)"
    - path: "ClassicLib/io/yaml/cache.py"
      provides: "YamlSettingsCache singleton (moved from sync/cache.py)"
    - path: "ClassicLib/io/yaml/convenience.py"
      provides: "yaml_settings, classic_settings, yaml_cache (moved from sync/convenience.py)"
    - path: "ClassicLib/io/yaml/__init__.py"
      provides: "Re-exports from new locations (cache.py, convenience.py)"
  key_links:
    - from: "ClassicLib/io/yaml/__init__.py"
      to: "ClassicLib/io/yaml/cache.py"
      via: "Updated import path"
      pattern: "from ClassicLib\\.io\\.yaml\\.cache import"
    - from: "ClassicLib/io/yaml/__init__.py"
      to: "ClassicLib/io/yaml/convenience.py"
      via: "Updated import path"
      pattern: "from ClassicLib\\.io\\.yaml\\.convenience import"
---

<objective>
Move YAML sync cache and convenience modules from the sync/ subdirectory to the parent yaml/ directory, then delete the sync/ directory entirely.

Purpose: Eliminates the artificial sync/async split in the YAML module structure. The sync convenience functions (yaml_settings, classic_settings) are legitimate GUI entry points that use AsyncBridge.run_async() internally -- they belong at the top level, not in a "sync" subdirectory. This is the final structural change for Phase 4.
Output: sync/ directory deleted, files moved to parent, all imports working, GUI smoke test passes.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interface-consolidation/04-RESEARCH.md
@.planning/phases/04-interface-consolidation/04-02-SUMMARY.md
@ClassicLib/io/yaml/__init__.py
@ClassicLib/io/yaml/sync/__init__.py
@ClassicLib/io/yaml/sync/cache.py
@ClassicLib/io/yaml/sync/convenience.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move sync files to parent yaml/ directory and update imports</name>
  <files>
    ClassicLib/io/yaml/cache.py
    ClassicLib/io/yaml/convenience.py
    ClassicLib/io/yaml/__init__.py
    ClassicLib/io/yaml/sync/cache.py
    ClassicLib/io/yaml/sync/convenience.py
    ClassicLib/io/yaml/sync/__init__.py
  </files>
  <action>
**Step 1: Move cache.py**

Copy `ClassicLib/io/yaml/sync/cache.py` to `ClassicLib/io/yaml/cache.py`. Update any internal imports within the file:
- Change any `from ClassicLib.io.yaml.sync.` references to `from ClassicLib.io.yaml.`
- Check for relative imports like `from .convenience` and update to reflect new location
- Preserve all class functionality, lazy imports, and AsyncBridge usage exactly as-is

**Step 2: Move convenience.py**

Copy `ClassicLib/io/yaml/sync/convenience.py` to `ClassicLib/io/yaml/convenience.py`. Update any internal imports:
- Change any `from ClassicLib.io.yaml.sync.cache` to `from ClassicLib.io.yaml.cache`
- The `write_file_sync` import was already removed in 04-02. If convenience.py still has file I/O imports, replace them with AsyncBridge pattern.
- Preserve all function signatures and behavior exactly

**Step 3: Update yaml/__init__.py**

Change the sync import block from:
```python
from ClassicLib.io.yaml.sync import (
    YamlSettingsCache,
    classic_settings,
    yaml_cache,
    yaml_settings,
)
```
to:
```python
from ClassicLib.io.yaml.cache import YamlSettingsCache
from ClassicLib.io.yaml.convenience import (
    classic_settings,
    yaml_cache,
    yaml_settings,
)
```

Update the module docstring to remove references to the sync/ subdirectory. Keep `__all__` unchanged since the exported names are the same.

**Step 4: Delete sync/ directory**

Delete `ClassicLib/io/yaml/sync/cache.py`, `ClassicLib/io/yaml/sync/convenience.py`, `ClassicLib/io/yaml/sync/__init__.py`, and the `ClassicLib/io/yaml/sync/__pycache__/` directory. Remove the `ClassicLib/io/yaml/sync/` directory itself.

**Step 5: Search for stale references (including direct sync submodule imports)**

Search entire codebase for `ClassicLib.io.yaml.sync` or `from ClassicLib.io.yaml.sync` and fix any remaining references. Known files that import directly from the sync submodule (bypassing `ClassicLib.io.yaml.__init__`):
- `ClassicLib/support/resources.py` (line ~506): `from ClassicLib.io.yaml.sync.convenience import yaml_settings` -- change to `from ClassicLib.io.yaml.convenience import yaml_settings`
- `tests/fixtures/singleton_fixtures.py` (line ~55): `from ClassicLib.io.yaml.sync.cache import YamlSettingsCache` -- change to `from ClassicLib.io.yaml.cache import YamlSettingsCache`
- `tests/core/test_resource_loader_unit.py` (lines ~578, ~609): references to `ClassicLib.io.yaml.sync.convenience` in mock paths -- update mock paths to `ClassicLib.io.yaml.convenience`

Also run: `grep -r "from ClassicLib.io.yaml.sync" ClassicLib/ tests/ --include="*.py"` and update any remaining results not listed above.

**Step 6: Run tests**

Run `uv run pytest -x -q` to verify no regressions. All 60+ callers import from `ClassicLib.io.yaml` (the __init__.py), not from `ClassicLib.io.yaml.sync`, so they should all work without changes.

**CRITICAL: Preserve lazy import patterns.** The cache.py and convenience.py files use lazy imports (importing yaml_settings/classic_settings inside functions) to avoid circular imports. Do NOT change these to top-level imports.
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.io.yaml import yaml_settings, classic_settings, YamlSettingsCache, yaml_cache; print('OK')"`
Run: `uv run python -c "from ClassicLib.io.yaml import yaml_settings_async, classic_settings_async; print('OK')"`
Run: `ls ClassicLib/io/yaml/sync/ 2>/dev/null && echo 'FAIL: sync/ still exists' || echo 'OK: sync/ removed'`
Run: `uv run pytest -x -q`
  </verify>
  <done>sync/ directory does not exist. cache.py and convenience.py live at ClassicLib/io/yaml/ level. All imports work. Tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 4 Interface Consolidation:
- FormIDAnalyzer.py sync wrapper removed (04-01)
- create_sync_wrapper, bridge_helpers.py, sync_adapters.py removed (04-02)
- YAML sync/ directory consolidated into parent yaml/ (04-03)
The codebase now has exactly two async patterns: native async (CLI/TUI) and AsyncBridge (GUI).
  </what-built>
  <how-to-verify>
1. Launch the GUI: `uv run python CLASSIC_Interface.py`
2. Verify the GUI window opens without errors
3. Load a crash log (or use File > Open to select one)
4. Click "Scan" to perform a crash log analysis
5. Verify the scan completes without freezing, deadlocking, or error dialogs
6. Check the scan results display correctly
7. Close the application normally (no crash on exit)
  </how-to-verify>
  <resume-signal>Type "approved" if GUI works correctly, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
1. `ls ClassicLib/io/yaml/sync/` should fail (directory not found)
2. `uv run python -c "from ClassicLib.io.yaml import yaml_settings, classic_settings, YamlSettingsCache; print('OK')"` succeeds
3. `grep -r "ClassicLib.io.yaml.sync" ClassicLib/ tests/ --include="*.py"` returns 0 results (including tests and fixtures)
4. `uv run pytest -x -q` passes
5. GUI smoke test passes (manual verification)
</verification>

<success_criteria>
ClassicLib/io/yaml/sync/ directory does not exist. All YAML imports work from ClassicLib.io.yaml. GUI launches and performs crash log scan without freezing. Test suite passes with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-interface-consolidation/04-03-SUMMARY.md`
</output>
