---
phase: 04-interface-consolidation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - ClassicLib/scanning/logs/executor.py
  - ClassicLib/io/files/sync_adapters.py
  - ClassicLib/io/files/__init__.py
  - ClassicLib/_async_utils/bridge_helpers.py
  - ClassicLib/_async_utils/__init__.py
  - ClassicLib/core/async_bridge.py
  - ClassicLib/__init__.py
  - ClassicLib/scanning/game/wrye_check.py
  - ClassicLib/Interface/controllers/results_viewer.py
  - ClassicLib/support/xse.py
  - ClassicLib/support/docs_path.py
  - ClassicLib/support/papyrus.py
  - ClassicLib/io/yaml/sync/convenience.py
autonomous: true

must_haves:
  truths:
    - "grep -r 'create_sync_wrapper' ClassicLib/ returns 0 results"
    - "_async_utils/bridge_helpers.py does not exist"
    - "io/files/sync_adapters.py does not exist"
    - "All callers of sync file I/O functions migrated to AsyncBridge.run_async() or preserved as pure sync (stream_lines_sync)"
    - "uv run pytest passes with no regressions"
  artifacts:
    - path: "ClassicLib/_async_utils/bridge_helpers.py"
      provides: "MUST NOT EXIST after plan completes"
    - path: "ClassicLib/io/files/sync_adapters.py"
      provides: "MUST NOT EXIST after plan completes"
    - path: "ClassicLib/io/files/sync_helpers.py"
      provides: "Preserved stream_lines_sync function (pure sync, not create_sync_wrapper)"
    - path: "ClassicLib/scanning/logs/executor.py"
      provides: "execute_scan_sync uses AsyncBridge.run_async() directly"
  key_links:
    - from: "ClassicLib/scanning/logs/executor.py"
      to: "AsyncBridge.run_async()"
      via: "Direct call replacing create_sync_wrapper"
      pattern: "AsyncBridge.*run_async.*execute_scan"
    - from: "caller files (wrye_check, xse, docs_path, results_viewer)"
      to: "AsyncBridge.run_async() + FileIOCore"
      via: "Direct async bridge call"
      pattern: "AsyncBridge.*run_async|get_file_io"
---

<objective>
Remove all create_sync_wrapper usage, delete bridge_helpers.py, delete sync_adapters.py, and migrate all callers to use AsyncBridge.run_async() directly or native async.

Purpose: Eliminates the deprecated create_sync_wrapper mechanism (the second of three deprecated patterns) and its two implementation files, leaving only AsyncBridge.run_async() and native async as the two async patterns.
Output: bridge_helpers.py and sync_adapters.py deleted, all callers migrated, tests passing.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interface-consolidation/04-RESEARCH.md
@.planning/phases/04-interface-consolidation/04-01-SUMMARY.md
@ClassicLib/io/files/sync_adapters.py
@ClassicLib/_async_utils/bridge_helpers.py
@ClassicLib/scanning/logs/executor.py
@ClassicLib/io/files/__init__.py
@ClassicLib/core/async_bridge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate sync file I/O callers and executor, preserve stream_lines_sync</name>
  <files>
    ClassicLib/scanning/logs/executor.py
    ClassicLib/scanning/game/wrye_check.py
    ClassicLib/Interface/controllers/results_viewer.py
    ClassicLib/support/xse.py
    ClassicLib/support/docs_path.py
    ClassicLib/support/papyrus.py
    ClassicLib/io/yaml/sync/convenience.py
    ClassicLib/io/files/sync_helpers.py
    ClassicLib/io/files/__init__.py
    ClassicLib/__init__.py
  </files>
  <action>
**Step 1: Create sync_helpers.py to preserve stream_lines_sync**

Create `ClassicLib/io/files/sync_helpers.py` containing ONLY the `stream_lines_sync` function extracted from sync_adapters.py (lines 119-143). This function is a pure sync generator that does NOT use create_sync_wrapper. Copy it exactly, including its imports (`Path`, `Iterator`, `open_file_with_encoding`, the `_core` helper, and `get_file_io`).

**Step 2: Migrate executor.py**

In `ClassicLib/scanning/logs/executor.py`:
- Remove `from ClassicLib.core.async_bridge import create_sync_wrapper` (line 16)
- Replace the `execute_scan_sync` method body (~line 425) which uses `create_sync_wrapper(self.execute_scan, strict=True)` with:
  ```python
  from ClassicLib.core.async_bridge import AsyncBridge
  bridge = AsyncBridge.get_instance()
  return bridge.run_async(self.execute_scan())
  ```
  Keep the method's existing docstring and GUI-only documentation.

**Step 3: Migrate sync file I/O callers**

For each caller that imports from `ClassicLib.io.files` (the sync adapter functions), replace with direct AsyncBridge.run_async() + FileIOCore calls:

- **wrye_check.py** (imports `read_file_sync`): Replace with a local helper:
  ```python
  from ClassicLib.core.async_bridge import AsyncBridge
  from ClassicLib.integration.factory import get_file_io
  def _read_file(path):
      io_core = get_file_io()
      return AsyncBridge.get_instance().run_async(io_core.read_file(path))
  ```
  Then replace `read_file_sync(path)` calls with `_read_file(path)`.

- **results_viewer.py** (imports `read_file_sync`): Same pattern as wrye_check.py.

- **xse.py** (imports `read_bytes_sync`, `read_lines_sync`): Replace with local helpers using AsyncBridge pattern for each.

- **docs_path.py** (imports `append_file_sync`, `read_lines_sync`, `write_file_sync`): Replace with local helpers. Check each function -- some may be in async methods already (those inside `async def` functions should use `await io_core.method()` directly, NOT AsyncBridge).

- **papyrus.py** (imports `stream_lines_sync`): Change import to `from ClassicLib.io.files import stream_lines_sync` (this will come from the new sync_helpers.py via __init__.py).

- **yaml/sync/convenience.py** (~line 204, imports `write_file_sync`): Replace with AsyncBridge pattern. This is already in a sync context so AsyncBridge is correct.

**Step 4: Update io/files/__init__.py**

Replace the import from `sync_adapters` with import from `sync_helpers`:
```python
from ClassicLib.io.files.sync_helpers import stream_lines_sync
```
Remove all other sync adapter re-exports (`read_file_sync`, `write_file_sync`, etc.) from `__init__.py` and `__all__`. Keep only `FileIOCore`, path utilities, and `stream_lines_sync`.

**Step 5: Update ClassicLib/__init__.py**

Remove `read_file_sync, write_file_sync` from the import `from ClassicLib.io.files import ...` (line 58). These are no longer package-level exports.

**IMPORTANT NOTES:**
- When migrating callers, check whether each call site is inside an `async def` function. If so, use `await io_core.method()` directly instead of AsyncBridge. AsyncBridge is ONLY for sync contexts (GUI callbacks, sync functions).
- For docs_path.py, carefully check each function -- some are async, some are sync. Apply the appropriate pattern for each.
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.io.files import stream_lines_sync; print('OK')"`
Run: `uv run python -c "from ClassicLib.scanning.logs.executor import CrashLogExecutor; print('OK')"`
Run: `uv run python -c "from ClassicLib.scanning.game.wrye_check import *; print('OK')"`
All must succeed with no ImportError.
  </verify>
  <done>All callers of sync file I/O functions migrated. executor.py uses AsyncBridge directly. stream_lines_sync preserved in sync_helpers.py.</done>
</task>

<task type="auto">
  <name>Task 2: Delete sync_adapters.py, bridge_helpers.py, clean up re-exports, run tests</name>
  <files>
    ClassicLib/io/files/sync_adapters.py
    ClassicLib/_async_utils/bridge_helpers.py
    ClassicLib/_async_utils/__init__.py
    ClassicLib/core/async_bridge.py
  </files>
  <action>
1. **Delete** `ClassicLib/io/files/sync_adapters.py`.

2. **Delete** `ClassicLib/_async_utils/bridge_helpers.py`.

3. **Update** `ClassicLib/_async_utils/__init__.py`: Remove the import of `create_sync_wrapper` and any other symbols from `bridge_helpers`. Remove `create_sync_wrapper` from `__all__`. If bridge_helpers was the only content, check what else the `_async_utils` package exports -- if it's now empty or only has unused re-exports, evaluate if the entire `_async_utils/` directory can be removed. If other important symbols exist, keep the package but remove bridge_helpers references.

4. **Update** `ClassicLib/core/async_bridge.py`: Remove the import of `create_sync_wrapper` from `ClassicLib.AsyncBridge` (line ~52) and from `ClassicLib._async_utils` (line ~607). Remove `create_sync_wrapper` from `__all__` (line ~104). Remove the docstring references to create_sync_wrapper (line ~76). Keep all other AsyncBridge functionality intact.

5. **Search** for any remaining references to `create_sync_wrapper` across the entire codebase (including tests/):
   - `grep -r "create_sync_wrapper" . --include="*.py"` should return 0 results
   - Fix any remaining references found in test files by updating them

6. **Search** for any remaining imports of the deleted modules:
   - `grep -r "bridge_helpers" . --include="*.py"` should return 0 results
   - `grep -r "sync_adapters" . --include="*.py"` should return 0 results
   - Fix any remaining references found

7. **Run tests**: `uv run pytest -x -q`. Fix any failures caused by the removal.
  </action>
  <verify>
Run: `uv run pytest -x -q` -- all tests pass
Run: `grep -r "create_sync_wrapper" ClassicLib/ --include="*.py"` -- returns 0 results
Run: `grep -r "bridge_helpers" ClassicLib/ --include="*.py"` -- returns 0 results
Run: `grep -r "sync_adapters" ClassicLib/ --include="*.py"` -- returns 0 results
  </verify>
  <done>bridge_helpers.py and sync_adapters.py do not exist. create_sync_wrapper has zero references in ClassicLib/. All tests pass.</done>
</task>

</tasks>

<verification>
1. `ls ClassicLib/_async_utils/bridge_helpers.py` should fail (file not found)
2. `ls ClassicLib/io/files/sync_adapters.py` should fail (file not found)
3. `grep -r "create_sync_wrapper" ClassicLib/ --include="*.py"` returns 0 results
4. `ls ClassicLib/io/files/sync_helpers.py` should succeed (stream_lines_sync preserved)
5. `uv run pytest -x -q` passes
</verification>

<success_criteria>
create_sync_wrapper has zero references in ClassicLib/. bridge_helpers.py and sync_adapters.py do not exist. stream_lines_sync is preserved and functional. executor.py uses AsyncBridge.run_async() directly. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-interface-consolidation/04-02-SUMMARY.md`
</output>
