---
phase: 08-report-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib/integration/rust/report/generator.py
  - ClassicLib/scanning/logs/report_generator.py
  - ClassicLib/scanning/logs/orchestrator_core.py
  - ClassicLib/integration/factory.py
autonomous: true

must_haves:
  truths:
    - "RustAcceleratedReportGenerator delegates all section generation to Rust (no Python fallback)"
    - "generate_header() has no is_vr_log parameter (VR indicator removed)"
    - "generate_error_section() computes is_outdated in Python, passes boolean to Rust"
    - "Static header methods (suspect_section_header, settings_section_header, etc.) call Rust generator"
  artifacts:
    - path: "ClassicLib/integration/rust/report/generator.py"
      provides: "Rust-only report generator wrapper"
      contains: "raise RuntimeError"
    - path: "ClassicLib/scanning/logs/report_generator.py"
      provides: "Fragment-based report generator (VR removed)"
      exports: ["ReportGeneratorFragments"]
    - path: "ClassicLib/scanning/logs/orchestrator_core.py"
      provides: "Orchestrator without VR indicator passing"
  key_links:
    - from: "ClassicLib/integration/rust/report/generator.py"
      to: "classic_scanlog.ReportGenerator"
      via: "direct import"
      pattern: "from classic_scanlog import"
    - from: "ClassicLib/integration/factory.py"
      to: "RustAcceleratedReportGenerator"
      via: "get_report_generator factory"
      pattern: "raise RuntimeError.*Rust module"
---

<objective>
Wire all ReportGenerator section methods to Rust implementation and remove VR mode indicators from report generation.

Purpose: Complete the Phase 8 goal of having all report sections generated by Rust. VR is being deprecated and removed as part of this phase per user decision.
Output: Report generator wrapper that delegates all calls to Rust with no Python fallback. VR indicators removed from header and error section generation.
</objective>

<execution_context>
@C:\Users\evild\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\evild\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-report-generation/08-CONTEXT.md
@.planning/phases/08-report-generation/08-RESEARCH.md

@ClassicLib/integration/rust/report/generator.py
@ClassicLib/scanning/logs/report_generator.py
@ClassicLib/scanning/logs/orchestrator_core.py
@ClassicLib/integration/factory.py
@rust/python-bindings/classic-scanlog-py/src/report.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire RustAcceleratedReportGenerator to Rust-only (no fallback)</name>
  <files>ClassicLib/integration/rust/report/generator.py</files>
  <action>
Remove all Python fallback paths from RustAcceleratedReportGenerator. Make it Rust-only with hard fail.

1. Remove the lazy imports for Python fallback (_get_py_report_generator)
2. Remove the RUST_AVAILABLE check that creates Python fallback
3. Change __init__ to raise RuntimeError if Rust not available (consistent with Phase 7 pattern)
4. Remove the `_use_rust` flag - always use Rust
5. Update generate_header():
   - Remove the is_vr_log parameter (VR removal per decision)
   - Call self._generator.generate_header(crashlog_filename) directly
   - No fallback path
6. Update generate_error_section():
   - Keep the version comparison logic (compute is_outdated in Python)
   - Remove VR-specific version comparison (game_is_vr check)
   - Call self._generator.generate_error_section(main_error, crashgen_version, is_outdated)
   - No fallback path
7. Update generate_suspect_section() - call Rust directly, no fallback
8. Update wrapper methods that are currently @staticmethod (generate_suspect_section_header, generate_suspect_found_footer, generate_settings_section_header, generate_mod_check_header, generate_plugin_suspect_header, generate_formid_section_header, generate_record_section_header):
   - Remove @staticmethod decorator and add self parameter (Rust methods are already instance methods — no conversion needed on Rust side)
   - Call self._generator.method_name() instead of using Python ReportFragment
   - Remove Python fallback fragment generation

Error handling pattern (from factory.py):
```python
def __init__(self, yamldata: Any = None) -> None:
    if not RUST_AVAILABLE:
        msg = "Required Rust module for ReportGenerator not available. Reinstall CLASSIC."
        raise RuntimeError(msg)
    self._generator = RustReportGenerator()
    self.yamldata = yamldata
```
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.integration.rust.report.generator import RustAcceleratedReportGenerator; gen = RustAcceleratedReportGenerator(); print(gen.generate_header('test.log').to_list()[:2])"`
Should output first 2 lines of header without VR indicator.

Verify static header methods are wired to Rust:
Run: `uv run python -c "from ClassicLib.integration.factory import get_report_generator; gen = get_report_generator(); print(gen.generate_suspect_section_header().to_list()[:1])"`
Run: `uv run python -c "from ClassicLib.integration.factory import get_report_generator; gen = get_report_generator(); print(gen.generate_settings_section_header().to_list()[:1])"`
Run: `uv run python -c "from ClassicLib.integration.factory import get_report_generator; gen = get_report_generator(); print(gen.generate_suspect_found_footer(True).to_list()[:1])"`
Each command should return section header content from Rust (no Python fallback errors).
  </verify>
  <done>
RustAcceleratedReportGenerator raises RuntimeError if Rust unavailable. All section generation methods call Rust directly with no Python fallback. VR parameters removed from method signatures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove VR indicators from Python ReportGeneratorFragments</name>
  <files>ClassicLib/scanning/logs/report_generator.py</files>
  <action>
Remove VR mode indicators from the Python ReportGeneratorFragments class. This class is still used by OrchestratorCore directly in some paths.

1. Update generate_header():
   - Remove `is_vr_log: bool = False` parameter
   - Remove the VR indicator text block:
     ```python
     if is_vr_log:
         lines.extend([
             "> ⚠️ **VR MODE AUTO-DETECTED**: This crash log is from Fallout 4 VR\n",
             "> VR-specific settings and version comparisons are being used.\n\n",
         ])
     ```

2. Update generate_error_section():
   - Remove `is_vr_log: bool = False` parameter
   - Keep the game_version_id parameter (used for list-based version checking)
   - Simplify the legacy version comparison (remove VR check):
     - Currently: `game_is_vr = is_vr_log or (GlobalRegistry.get_vr() == "VR")`
     - After: Remove game_is_vr entirely for legacy path
     - For legacy path (no game_version_id), just compare version_current < version_latest
   - Keep the new list-based version checking (game_version_id path) unchanged

Note: VR version comparison for crashgen is handled by the VersionRegistry in the new path, not by explicit VR parameter checking.
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.scanning.logs.report_generator import ReportGeneratorFragments; gen = ReportGeneratorFragments(None); h = gen.generate_header('test.log'); print('VR' not in ''.join(h.to_list()))"`
Should output True (no VR text in header).
  </verify>
  <done>
ReportGeneratorFragments.generate_header() has no is_vr_log parameter and produces no VR indicator text. generate_error_section() has no is_vr_log parameter.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update OrchestratorCore to remove VR indicator passing</name>
  <files>ClassicLib/scanning/logs/orchestrator_core.py</files>
  <action>
Update OrchestratorCore to stop passing is_vr_log to report generator methods.

1. In process_crash_log():
   - Keep VR detection for version matching (is_vr_log used for get_crashgen_name, get_game_root_name)
   - Update the generate_header() call to remove is_vr_log parameter:
     - Before: `self.report_generator.generate_header(crashlog_file.name, is_vr_log)`
     - After: `self.report_generator.generate_header(crashlog_file.name)`
   - Remove the VR logging: "VR mode auto-detected for log" (optional - can keep as informational)

2. In _process_log_sections_async():
   - Keep is_vr_log parameter (used for version registry matching)
   - Update generate_error_section() call to remove is_vr_log parameter:
     - Before: `self.report_generator.generate_error_section(..., is_vr_log, game_version_id=game_version_id)`
     - After: `self.report_generator.generate_error_section(..., game_version_id=game_version_id)`
   - The game_version_id already captures VR vs non-VR through the VersionRegistry match

Note: is_vr_log is still used internally for:
- get_crashgen_name(is_vr_log) - returns VR-specific crashgen name
- get_game_root_name(is_vr_log) - returns VR-specific root name
- registry.match_version(game_version, "Fallout4", is_vr=is_vr_log) - VR-aware version matching

The removal is only for REPORT DISPLAY, not for VERSION LOGIC.
  </action>
  <verify>
Run: `uv run python -c "from ClassicLib.scanning.logs.orchestrator_core import OrchestratorCore; print('is_vr_log' not in open('ClassicLib/scanning/logs/orchestrator_core.py').read().split('generate_header')[1][:200])"`
Should output True (no is_vr_log near generate_header call).
  </verify>
  <done>
OrchestratorCore.process_crash_log() calls generate_header() without is_vr_log. _process_log_sections_async() calls generate_error_section() without is_vr_log. VR detection still works for version matching but is not displayed in reports.
  </done>
</task>

</tasks>

<verification>
1. Import test: `uv run python -c "from ClassicLib.integration.rust.report.generator import RustAcceleratedReportGenerator; gen = RustAcceleratedReportGenerator()"`
2. Header generation: `uv run python -c "from ClassicLib.integration.factory import get_report_generator; gen = get_report_generator(); print(gen.generate_header('test.log').to_list()[:3])"`
3. Error section: `uv run python -c "from ClassicLib.integration.factory import get_report_generator; gen = get_report_generator(); from packaging.version import Version; print(gen.generate_error_section('ERROR', '1.0', Version('1.0'), Version('2.0'), Version('2.0')).to_list()[:4])"`
4. Unit tests: `uv run pytest tests/rust_integration/parity/test_report_parity.py -v --tb=short`
</verification>

<success_criteria>
- RustAcceleratedReportGenerator has no Python fallback code
- All generate_* methods call Rust directly
- VR indicators removed from report header and error section generation
- OrchestratorCore passes no is_vr_log to report generation methods
- Existing parity tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-report-generation/08-01-SUMMARY.md`
</output>
