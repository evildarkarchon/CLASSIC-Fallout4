---
phase: 01-cxx-parity-gate-tooling
plan: 03
type: execute
wave: 3
depends_on: [01-cxx-parity-gate-tooling/02]
files_modified:
  - docs/api/cxx-parity-gate.md
  - docs/api/README.md
  - tools/cxx_api_parity/README.md
  - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore
  - .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md
autonomous: true
requirements:
  - CXXG-04
  - CXXG-05
must_haves:
  truths:
    - "A contributor can follow docs/api/cxx-parity-gate.md to run the gate locally"
    - "A contributor can follow the doc to refresh the baseline after an intentional bridge change"
    - "ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/ is gitignored (does NOT appear in git status after gate runs)"
    - "docs/api/README.md links to cxx-parity-gate.md so contributors can discover it"
    - "The gate runs cleanly end-to-end (exit 0 on the committed born-green baseline) after all plan work is complete"
    - "VALIDATION.md per-task table is updated with concrete Task IDs from Plans 01 and 02"
  artifacts:
    - path: "docs/api/cxx-parity-gate.md"
      provides: "Contributor doc with sections: Overview, Local Run, Refresh Workflow, Bootstrap From Scratch, Contract Row Schema, build.rs Relationship, Ephemeral vs Committed Artifacts, CI Integration preview"
      min_lines: 80
      contains: "## Local Run"
    - path: "tools/cxx_api_parity/README.md"
      provides: "One-line pointer to docs/api/cxx-parity-gate.md (D-15)"
      min_lines: 3
    - path: "ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore"
      provides: "Gitignore entry for parity-artifacts/ (D-08 / Pitfall 7 from RESEARCH.md)"
      contains: "parity-artifacts"
  key_links:
    - from: "docs/api/README.md"
      to: "docs/api/cxx-parity-gate.md"
      via: "Numbered list entry added to the existing doc index"
      pattern: "cxx-parity-gate\\.md"
    - from: "tools/cxx_api_parity/README.md"
      to: "docs/api/cxx-parity-gate.md"
      via: "Markdown link to the canonical contributor doc"
      pattern: "docs/api/cxx-parity-gate"
---

<objective>
Finish the CXX parity gate with the three remaining Phase 1 deliverables: the contributor doc (CXXG-05), the `.gitignore` entry preventing parity-artifacts pollution (Pitfall 7 from research), and the VALIDATION.md task-id backfill (quality-gate requirement from the planning context). Also run one last end-to-end smoke to prove the whole phase hangs together.

Purpose: CXXG-05 explicitly requires `docs/api/cxx-parity-gate.md`. Without the gitignore entry, every contributor run of the gate creates untracked files that pollute `git status`. Without the VALIDATION.md backfill, the Nyquist sampling contract cannot verify that each task has the right automated coverage.

Output: Discoverable contributor doc; gitignored ephemeral artifacts dir; up-to-date VALIDATION.md task-id mapping; final end-to-end gate run exits 0 with clean `git status` (no untracked parity-artifacts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-cxx-parity-gate-tooling/01-CONTEXT.md
@.planning/phases/01-cxx-parity-gate-tooling/01-RESEARCH.md
@.planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md
@.planning/phases/01-cxx-parity-gate-tooling/01-01-SUMMARY.md
@.planning/phases/01-cxx-parity-gate-tooling/01-02-SUMMARY.md

# Doc index — this plan adds an entry
@docs/api/README.md

# Reference gitignore style (Node binding has the same pattern)
@ClassicLib-rs/node-bindings/classic-node/.gitignore

# Working gate scripts from Plan 02
@tools/cxx_api_parity/check_parity_gate.py
@tools/cxx_api_parity/generate_baseline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contributor doc + gitignore + doc index entry</name>

  <files>
    - docs/api/cxx-parity-gate.md
    - tools/cxx_api_parity/README.md
    - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore
    - docs/api/README.md
  </files>

  <read_first>
    - docs/api/README.md (the doc index — understand the existing numbered layout before inserting a new entry)
    - .planning/phases/01-cxx-parity-gate-tooling/01-CONTEXT.md decisions D-08 (ephemeral vs committed), D-15 (doc location), D-16 (local invocation command)
    - .planning/phases/01-cxx-parity-gate-tooling/01-RESEARCH.md §"Contributor Doc Content (CXXG-05)", §"Gitignore Coverage Analysis", §"Pitfall 7"
    - ClassicLib-rs/node-bindings/classic-node/.gitignore (pattern to copy for the bridge crate gitignore)
    - tools/cxx_api_parity/check_parity_gate.py (cite the exact CLI in the doc — D-16)
    - tools/cxx_api_parity/generate_baseline.py (cite the bootstrap command in the doc)
  </read_first>

  <action>
  ## Part A — Create `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore`

  File content (create new file):

  ```
  # Ephemeral parity gate artifacts (D-08 / Phase 1 CXX gate).
  # The committed baseline lives at docs/implementation/cxx_api_parity/baseline/.
  # Regenerate with: python tools/cxx_api_parity/check_parity_gate.py --repo-root .
  parity-artifacts/
  ```

  Verify git sees the new file and the artifacts dir is now ignored:

  ```
  git status --short ClassicLib-rs/cpp-bindings/classic-cpp-bridge/
  ```

  Expected: `.gitignore` appears as `??` (new untracked file) but `parity-artifacts/` does NOT appear. Existing files inside `parity-artifacts/` that were never committed are now invisible to git.

  If `parity-artifacts/` files were accidentally staged before this point, unstage them first with `git rm --cached -r ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/`. (Verify with `git ls-files ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/` — should print nothing after the cleanup.)

  ## Part B — Create `docs/api/cxx-parity-gate.md`

  Mirror the tone and structure of existing `docs/api/classic-*.md` pages (which you should sample 1-2 of for style). The doc MUST contain these section headings exactly so the acceptance-criteria greps match:

  ```markdown
  # CXX Parity Gate

  The CXX parity gate is a Python source-parsing tool that enumerates the CXX bridge
  surface exposed by `ClassicLib-rs/cpp-bindings/classic-cpp-bridge` and compares it
  against a committed baseline. It runs cross-platform Python — no Rust build, no
  MSVC, no `cxx-build` invocation.

  The gate is the acceptance criterion for any change that adds, removes, or modifies
  a `#[cxx::bridge]` item. Phase 2 of the `v9.1.0-bindings` milestone uses this gate
  to accept widened bridge entry points as they land.

  ## Overview

  - **Purpose:** Detect drift between the committed CXX bridge contract and the
    current bridge source tree.
  - **Scope:** All files listed in `classic-cpp-bridge/build.rs::cxx_build::bridges([...])`
    (14 files as of this milestone).
  - **Single source of truth for file list:** `build.rs`. The gate parses it dynamically
    — there is no hardcoded list to keep in sync.
  - **Single source of truth for the contract:** `docs/implementation/cxx_api_parity/baseline/parity_contract.json`.
  - **Exit codes:** `0` = pass (no drift, no stale artifacts); `1` = drift detected,
    stale committed artifacts, or baseline file missing.

  ## Local Run

  Run the gate from the repo root:

  ```bash
  python tools/cxx_api_parity/check_parity_gate.py --repo-root .
  ```

  A clean run prints `CXX parity gate passed.` and exits 0. Drift is reported to
  stderr with a summary of missing, added, and signature-mismatched rows.

  The gate is **source-only** — it does not invoke Cargo, `cxx-build`, or MSVC. All
  you need is Python 3.12+.

  ## Refresh Workflow

  After an intentional bridge change (adding, removing, or modifying a `#[cxx::bridge]`
  item), refresh the committed baseline in the same commit that touches the bridge
  source:

  ```bash
  python tools/cxx_api_parity/check_parity_gate.py --repo-root . --update-baseline
  git add docs/implementation/cxx_api_parity/baseline/
  git commit -m "Docs: refresh CXX parity baseline"
  ```

  `--update-baseline` copies the fresh generated artifacts into the committed
  baseline directory and re-runs the comparison. A follow-up plain run should then
  exit 0.

  **Never use `--update-baseline` to mask unintentional drift** — if you didn't
  mean to add or remove a bridge item, fix the source, not the baseline.

  ## Bootstrap From Scratch

  If the committed baseline is ever missing or corrupted, regenerate it from source
  with the bootstrap entry point:

  ```bash
  python tools/cxx_api_parity/generate_baseline.py --repo-root . --write-baseline
  ```

  This rewrites every file under `docs/implementation/cxx_api_parity/baseline/`
  using a fresh parse of the bridge source tree. Review the diff before committing.

  ## Contract Row Schema

  Each row in `parity_contract.json` has these fields:

  | Field | Kinds | Meaning |
  |-------|-------|---------|
  | `id` | all | Stable `sha256("{rustSymbol}:{kind}:{bridgeModule}")[:16]` hash |
  | `rustSymbol` | all | Identifier name as it appears in the bridge source |
  | `kind` | all | `function` \| `struct` \| `enum` \| `opaque` |
  | `bridgeModule` | all | Filename stem (e.g. `scanner`, `scangame`, `yaml`) |
  | `sourceFile` | all | Repo-relative path with forward slashes |
  | `blockOrigin` | all | `Rust` for items in `extern "Rust"`/top-level structs/enums; `C++` for items inside `unsafe extern "C++"` |
  | `signature` | function only | `{"args": [{"name", "type"}, ...], "returnType": str \| null}` |
  | `fields` | struct only | Ordered list of `{"name", "type"}` — source order preserved |
  | `variants` | enum only | Ordered list of variant name strings (discriminants stripped) |

  Doc comments (`///` and `//!`) are NOT compared. Lifetimes, `&`/`&mut`,
  `Pin<&mut T>`, `Box<T>`, and `UniquePtr<T>` wrappers ARE part of the signature
  and ARE compared — they are ABI-relevant.

  ## build.rs Relationship

  The gate parses `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/build.rs` for the
  `cxx_build::bridges([...])` array. This is the single source of truth for which
  bridge files participate in the gate.

  Adding a new bridge source file is a two-step workflow:

  1. Add the new `src/foo.rs` file to the bridge crate and add its path to
     `build.rs::cxx_build::bridges([...])`.
  2. Run `python tools/cxx_api_parity/check_parity_gate.py --repo-root . --update-baseline`
     to accept the new entries into the committed baseline.

  Removing a bridge file uses the same `--update-baseline` workflow.

  ## Ephemeral vs Committed Artifacts

  The gate writes two sets of files:

  - **Ephemeral (gitignored):** `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/`
    contains the output of the current run. These files are regenerated every
    invocation and should not be committed.
  - **Committed (tracked):** `docs/implementation/cxx_api_parity/baseline/` contains
    the golden baseline. `--update-baseline` is the only supported way to modify
    these files; manual edits will be detected as drift by the next gate run.

  ## CI Integration

  Phase 5 of the `v9.1.0-bindings` milestone wires a `cxx-parity-gate` job into
  `.github/workflows/ci-cpp.yml` that runs before `cli-tests` and `gui-tests`. The
  CI job requires only Python and a checkout — no Rust toolchain, no MSVC, no
  vcpkg setup. It blocks merges on a non-zero exit just like the Python and Node
  parity gates.

  Until Phase 5 lands, contributors are expected to run the gate locally before
  opening PRs that touch the bridge crate.

  ## Troubleshooting

  **"Contract file not found" error.** The committed baseline is missing from
  `docs/implementation/cxx_api_parity/baseline/parity_contract.json`. Bootstrap it
  with the command from the "Bootstrap From Scratch" section.

  **"Checked-in CXX parity artifacts are stale" error.** One or more files under
  `docs/implementation/cxx_api_parity/baseline/` no longer match a fresh source
  scan. If you intended the change, run `--update-baseline`. If not, investigate
  what changed in the bridge source.

  **"No `#[cxx::bridge] mod ffi` block found" error.** A file listed in `build.rs`
  is missing its `#[cxx::bridge]` attribute. Add the attribute or remove the file
  from `build.rs`.

  **"build.rs does not contain a cxx_build::bridges([...]) call" error.** Someone
  rewrote `build.rs` into a form the parser does not recognize. Restore the
  standard form or update the parser.

  ## Related Docs

  - [`binding-parity-overview.md`](binding-parity-overview.md) — Cross-binding
    surface comparison (C++, Node, Python)
  - [`classic-cpp-bridge-game-entrypoints.md`](classic-cpp-bridge-game-entrypoints.md) —
    Current path/game/scangame bridge entry points
  - [`classic-cpp-bridge-data-entrypoints.md`](classic-cpp-bridge-data-entrypoints.md) —
    Current config/file/database/scanner bridge entry points
  ```

  Write the doc verbatim (adapting prose voice to match the existing `docs/api/` pages if you sample one for style). Aim for ~100 lines of prose.

  ## Part C — Create `tools/cxx_api_parity/README.md`

  File content (create new file, 3 lines minimum per D-15):

  ```markdown
  # CXX Parity Gate Tooling

  Contributor reference: [`docs/api/cxx-parity-gate.md`](../../docs/api/cxx-parity-gate.md)
  ```

  This is intentionally thin — D-15 says the canonical doc lives under `docs/api/`, not under the tool directory.

  ## Part D — Update `docs/api/README.md` to link the new doc

  Read the file first and find the existing numbered list of docs. Insert a new entry for `cxx-parity-gate.md` in a position that makes sense given the existing ordering (probably near the other `classic-cpp-bridge-*.md` entries, since this doc is conceptually about the bridge crate).

  Because the existing list is explicitly numbered, adding a new entry in the middle means renumbering every subsequent entry is optional — the list is a reading order, not a formal taxonomy. The simplest approach is to ADD the entry at the end (or just after `binding-parity-overview.md` if that ordering reads more naturally). Keep the change minimal — one new line.

  Example diff location (insert after the last `classic-cpp-bridge-*.md` entry — line around 28-29):

  ```diff
   29. [`classic-gui-scan-result-ordering.md`](classic-gui-scan-result-ordering.md) - current Qt-side behavior for completion-order batch results, `input_index` correlation, and Results-tab ordering boundaries
   30. [`binding-parity-overview.md`](binding-parity-overview.md) - current C++ bridge, Node, and Python exposure comparison for shared Rust crates
  +31. [`cxx-parity-gate.md`](cxx-parity-gate.md) - contributor guide for the CXX parity gate that enumerates the bridge surface from `build.rs` and detects drift against a committed baseline
   31. [`node-python-contract-map.md`](node-python-contract-map.md) - where the active Node and Python public contracts, wrapper files, and parity artifacts live
  ```

  Renumber only if the list is already strictly sequential — otherwise append to the end. Do NOT disturb unrelated entries.

  ## Part E — Run the final end-to-end smoke

  After the doc and gitignore are in place, run the gate one more time and confirm nothing is disturbed:

  ```
  python tools/cxx_api_parity/check_parity_gate.py --repo-root .
  git status --short
  ```

  Expected:
  - Gate: exit 0, "CXX parity gate passed."
  - git status: shows the new files this task added (docs/api/cxx-parity-gate.md, tools/cxx_api_parity/README.md, ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore, docs/api/README.md) and NOTHING under `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/` (because it is now gitignored)

  If `parity-artifacts/` still shows up, the `.gitignore` path is wrong — fix it before committing.

  Commit: `docs(01-cxx-parity-gate): contributor doc, .gitignore, doc index entry`
  </action>

  <verify>
    <automated>python tools/cxx_api_parity/check_parity_gate.py --repo-root . 2>&1 | tail -5 && git status --short | grep -v parity-artifacts</automated>
  </verify>

  <acceptance_criteria>
    - `test -f docs/api/cxx-parity-gate.md` exits 0
    - `test -f tools/cxx_api_parity/README.md` exits 0
    - `test -f ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore` exits 0
    - `grep -q 'parity-artifacts' ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore`
    - `grep -q '## Local Run' docs/api/cxx-parity-gate.md`
    - `grep -q '## Refresh Workflow' docs/api/cxx-parity-gate.md`
    - `grep -q '## Bootstrap From Scratch' docs/api/cxx-parity-gate.md`
    - `grep -q '## Contract Row Schema' docs/api/cxx-parity-gate.md`
    - `grep -q '## build.rs Relationship' docs/api/cxx-parity-gate.md`
    - `grep -q '## Ephemeral vs Committed Artifacts' docs/api/cxx-parity-gate.md`
    - `grep -q '## CI Integration' docs/api/cxx-parity-gate.md`
    - `grep -q 'python tools/cxx_api_parity/check_parity_gate.py --repo-root .' docs/api/cxx-parity-gate.md`
    - `grep -q -- '--update-baseline' docs/api/cxx-parity-gate.md`
    - `grep -q 'cxx-parity-gate' docs/api/README.md`
    - `grep -q 'docs/api/cxx-parity-gate' tools/cxx_api_parity/README.md`
    - `wc -l < docs/api/cxx-parity-gate.md | awk '{exit ($1 >= 80 ? 0 : 1)}'` (doc is at least 80 lines)
    - `python tools/cxx_api_parity/check_parity_gate.py --repo-root . 2>&1 | grep -q 'CXX parity gate passed'`
    - `python tools/cxx_api_parity/check_parity_gate.py --repo-root . ; echo $?` ends with 0
    - `git check-ignore ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/rust_api_surface.json 2>&1 | grep -q parity-artifacts` (git recognizes the gitignore pattern)
    - `git status --short ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/ 2>&1 | grep -cv '.' | grep -q 0 || true` (parity-artifacts produces no git status output — files are gitignored)
  </acceptance_criteria>

  <done>
    Contributor doc exists at `docs/api/cxx-parity-gate.md` with all 7 required sections. The bridge crate has a local `.gitignore` that hides the ephemeral `parity-artifacts/` directory. The doc index at `docs/api/README.md` links the new page. Final end-to-end gate run exits 0 and `git status` is clean of any parity-artifacts pollution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backfill VALIDATION.md task-id map + finalize phase</name>

  <files>
    - .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md
  </files>

  <read_first>
    - .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md (the table with TBD task IDs to fill)
    - .planning/phases/01-cxx-parity-gate-tooling/01-01-PLAN.md (for Task IDs from Plan 01)
    - .planning/phases/01-cxx-parity-gate-tooling/01-02-PLAN.md (for Task IDs from Plan 02)
    - .planning/phases/01-cxx-parity-gate-tooling/01-01-SUMMARY.md
    - .planning/phases/01-cxx-parity-gate-tooling/01-02-SUMMARY.md
  </read_first>

  <action>
  VALIDATION.md has a per-task verification map with `TBD` in the Task ID column. Fill it in using the concrete task IDs from the plans executed in Waves 1 and 2.

  Task ID convention: `{phase}-{plan}-T{task-number}` (e.g., `01-01-T1`, `01-01-T2`, `01-02-T1`, `01-02-T2`).

  Mapping:

  | Current (TBD) | New Task ID | Which Task Satisfies It |
  |---|---|---|
  | Wave 0 scaffolding (`test -d tools/cxx_api_parity/tests/fixtures`) | `01-01-T1` | Plan 01 Task 1 (RED phase — creates the directory) |
  | Parser unit tests (all 9 tests under `test_parser.py`) | `01-01-T2` | Plan 01 Task 2 (GREEN phase — implements parser so tests pass) |
  | Baseline exists + 14 modules (CXXG-02 integration) | `01-02-T2` | Plan 02 Task 2 (bootstrap + integration tests) |
  | Gate passes on unchanged source | `01-02-T2` | Plan 02 Task 2 |
  | Gate fails on added/removed function, struct rename | `01-02-T2` | Plan 02 Task 2 |
  | Gate fails on stale artifact | `01-02-T2` | Plan 02 Task 2 |
  | No `--deferred-registry` arg | `01-02-T1` | Plan 02 Task 1 (CLI surface is locked when check_parity_gate.py is written) — and re-verified in `01-02-T2` integration tests |
  | `check_parity_gate.py --help` exits 0 | `01-02-T1` | Plan 02 Task 1 (the script is runnable after Task 1 completes) |
  | End-to-end `--repo-root .` exits 0 against committed baseline | `01-02-T2` and `01-03-T1` | Plan 02 bootstraps, Plan 03 re-runs the smoke after doc/gitignore work |
  | Doc presence (`test -f docs/api/cxx-parity-gate.md` + grep `## Local Run`) | `01-03-T1` | Plan 03 Task 1 (this plan's first task) |

  Read the current VALIDATION.md, then edit each row in the "Per-Task Verification Map" table:
  - Replace `TBD` in the Task ID column with the mapping above
  - Replace the "❌ W0" file-exists marker with "✅ created" where appropriate (Wave 0 files were created in Plan 01 Task 1)
  - Update the Status column: leave "⬜ pending" (plan-phase doesn't execute); execute-phase will flip to "✅ green" after commits
  - Also update `wave_0_complete: false` → `wave_0_complete: true` in the frontmatter since Plan 01 folds Wave 0 scaffolding into its Task 1
  - Update `nyquist_compliant: false` → `nyquist_compliant: true` in the frontmatter once the task IDs are filled in
  - Update the "Approval" line at the bottom: "pending — gsd-planner fills concrete task IDs in Step 1" → "pending — awaiting execute-phase runs (task IDs filled 2026-04-07 by gsd-planner)"

  Use an Edit-style targeted replacement (read the file first, then write specific updated sections — do NOT rewrite the whole file and disturb the rest of the content).

  Specifically the Per-Task Verification Map table should look like this after the edit:

  ```markdown
  | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
  |---------|------|------|-------------|-----------|-------------------|-------------|--------|
  | 01-01-T1 | 01 | 1 | CXXG-01 | scaffolding | `test -d tools/cxx_api_parity/tests/fixtures` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestParseExternRust::test_parse_extern_rust_functions -x` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestParseSharedStructs::test_parse_shared_structs -x` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestParseEnums::test_parse_enums -x` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestParseOpaqueTypes::test_parse_opaque_types -x` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestParseExternCpp::test_parse_extern_cpp -x` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestParseBuildRs::test_parse_build_rs -x` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestParseBuildRs::test_build_rs_missing_bridges -x` | ✅ created | ⬜ pending |
  | 01-01-T2 | 01 | 1 | CXXG-01 | unit | `uv run pytest tools/cxx_api_parity/tests/test_parser.py::TestDeterminism::test_deterministic_output -x` | ✅ created | ⬜ pending |
  | 01-02-T2 | 02 | 2 | CXXG-02 | integration | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestBaselineExists::test_baseline_file_exists -x` | ✅ created | ⬜ pending |
  | 01-02-T2 | 02 | 2 | CXXG-02 | integration | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestBaselineExists::test_baseline_covers_14_modules -x` | ✅ created | ⬜ pending |
  | 01-02-T2 | 02 | 2 | CXXG-03 | smoke | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestGateSmoke::test_gate_passes_on_unchanged_source -x` | ✅ created | ⬜ pending |
  | 01-02-T2 | 02 | 2 | CXXG-03 | drift | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestDriftDetection::test_gate_fails_on_added_function -x` | ✅ created | ⬜ pending |
  | 01-02-T2 | 02 | 2 | CXXG-03 | drift | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestDriftDetection::test_gate_fails_on_removed_function -x` | ✅ created | ⬜ pending |
  | 01-02-T2 | 02 | 2 | CXXG-03 | drift | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestDriftDetection::test_gate_fails_on_struct_field_rename -x` | ✅ created | ⬜ pending |
  | 01-02-T2 | 02 | 2 | CXXG-03 | stale-artifact | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestStaleArtifact::test_gate_fails_on_stale_artifact -x` | ✅ created | ⬜ pending |
  | 01-02-T1 | 02 | 2 | CXXG-04 | CLI surface | `uv run pytest tools/cxx_api_parity/tests/test_gate.py::TestNoDeferredRegistry::test_no_deferred_registry_arg -x` | ✅ created | ⬜ pending |
  | 01-02-T1 | 02 | 2 | CXXG-04 | smoke | `python tools/cxx_api_parity/check_parity_gate.py --help` exits 0 | ✅ created | ⬜ pending |
  | 01-03-T1 | 03 | 3 | CXXG-02 | end-to-end | `python tools/cxx_api_parity/check_parity_gate.py --repo-root .` exits 0 against committed baseline | ✅ created | ⬜ pending |
  | 01-03-T1 | 03 | 3 | CXXG-05 | doc presence | `test -f docs/api/cxx-parity-gate.md && grep -q '## Local Run' docs/api/cxx-parity-gate.md` | ✅ created | ⬜ pending |
  ```

  Also update the frontmatter at the top:

  ```yaml
  ---
  phase: 1
  slug: cxx-parity-gate-tooling
  status: planned
  nyquist_compliant: true
  wave_0_complete: true
  created: 2026-04-06
  ---
  ```

  And update the sign-off line at the bottom from:
  ```
  **Approval:** pending — gsd-planner fills concrete task IDs in Step 1; gsd-plan-checker verifies before VERIFICATION PASSED
  ```
  to:
  ```
  **Approval:** task IDs filled 2026-04-07 by gsd-planner; gsd-plan-checker verifies before VERIFICATION PASSED
  ```

  Commit: `docs(01-cxx-parity-gate): backfill VALIDATION.md task IDs`
  </action>

  <verify>
    <automated>grep -c 'TBD' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'TBD' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md` returns 0
    - `grep -q '01-01-T1' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - `grep -q '01-01-T2' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - `grep -q '01-02-T1' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - `grep -q '01-02-T2' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - `grep -q '01-03-T1' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - `grep -q 'nyquist_compliant: true' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - `grep -q 'wave_0_complete: true' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - `grep -q 'task IDs filled 2026-04-07' .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md`
    - The Per-Task Verification Map table still has the same number of rows (header + 20 data rows) as the original — rows were EDITED, not deleted or added
  </acceptance_criteria>

  <done>
    VALIDATION.md has concrete Task IDs for every row. Frontmatter reflects `nyquist_compliant: true` and `wave_0_complete: true`. Approval note updated. No TBD markers remain in the file. The validation contract is now ready for `/gsd:verify-work` after Plans 01 and 02 execute.
  </done>
</task>

</tasks>

<verification>
Final end-to-end check:

```
python tools/cxx_api_parity/check_parity_gate.py --repo-root .
uv run pytest tools/cxx_api_parity/tests/ -q
git status --short
grep -c TBD .planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md
test -f docs/api/cxx-parity-gate.md && grep -q '## Local Run' docs/api/cxx-parity-gate.md
test -f ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore
```

Expected:
- Gate: exit 0, "CXX parity gate passed."
- Pytest: 19+ tests PASSED, 0 FAILED
- git status: NO files under `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/` listed (gitignored)
- VALIDATION.md TBD count: 0
- Doc present with the required heading
- Bridge .gitignore present
</verification>

<success_criteria>
- `docs/api/cxx-parity-gate.md` contains all 7 required section headings (Local Run, Refresh Workflow, Bootstrap From Scratch, Contract Row Schema, build.rs Relationship, Ephemeral vs Committed Artifacts, CI Integration) — CXXG-05 satisfied
- `tools/cxx_api_parity/README.md` points to the canonical doc — D-15 convention honored
- `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/.gitignore` hides `parity-artifacts/` — Pitfall 7 avoided
- `docs/api/README.md` links the new contributor doc so it's discoverable
- Final end-to-end gate run exits 0 with a clean `git status` (no parity-artifacts pollution)
- `.planning/phases/01-cxx-parity-gate-tooling/01-VALIDATION.md` has concrete Task IDs for every row and no TBD markers
- `nyquist_compliant: true` and `wave_0_complete: true` set in VALIDATION.md frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/01-cxx-parity-gate-tooling/01-03-SUMMARY.md` documenting:
- Final doc structure (section count, total line count)
- Where the doc was inserted in `docs/api/README.md`
- Confirmation that `git check-ignore` recognizes `parity-artifacts/` as ignored
- The task-id map applied to VALIDATION.md (paste the filled table)
- Commit hashes for Plan 03 tasks
- Explicit statement: "Phase 1 (CXX Parity Gate Tooling) is complete; Phase 2 (CXX Bridge Surface Expansion) can now start and use `--update-baseline` to accept new bridge entries as it widens the surface."
</output>
