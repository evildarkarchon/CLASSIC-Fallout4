---
phase: 11-workspace-infra-verification-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, TEST-03]
must_haves:
  truths:
    - "Phase 8 has one authoritative verification report covering all six workspace and infrastructure requirements with evidence, not summary-only claims"
    - "The verification report separately proves Proton workflow wiring and the Proton integration test instead of collapsing INFRA-03 and TEST-03 into one vague claim"
    - "Requirement traceability no longer leaves any Phase 8 workspace/infra requirements orphaned once the report exists"
  artifacts:
    - path: ".planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md"
      provides: "Initial authoritative verification artifact for Phase 8"
      contains: "# Phase 08: Workspace and Infrastructure Verification Report"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Checklist and traceability synchronized to the new Phase 8 verification artifact"
      contains: "INFRA-05"
  key_links:
    - from: ".planning/phases/08-workspace-and-infrastructure/08-VALIDATION.md"
      to: ".planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md"
      via: "promote the existing validation commands into explicit requirement-by-requirement evidence rows"
      pattern: "08-01-01|08-01-02|08-02-01|08-02-02|08-03-01|08-03-02"
    - from: "ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs"
      to: ".planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md"
      via: "record the real Proton-first Linux documents-path wiring alongside the integration proof file"
      pattern: "find_docs_path_linux_with|construct_proton_docs_path|local_share_path"
    - from: "ClassicLib-rs/node-bindings/classic-node/package.json"
      to: ".planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md"
      via: "record tracked declaration freshness and parity/runtime enforcement from the real Node gate scripts"
      pattern: "parity:gate:local|dts:freshness:check|test:bun|test:node"
    - from: ".planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "close the orphaned-requirement gap by aligning the checklist and traceability rows to the new verification artifact"
      pattern: "INFRA-01|INFRA-02|INFRA-03|INFRA-04|INFRA-05|TEST-03"
---

<objective>
Create the missing authoritative Phase 8 verification artifact and synchronize requirement traceability so the milestone can prove the workspace/infrastructure work is actually verified.

Purpose: Phase 11 is a verification-closure phase, not a new implementation phase. Phase 8 already shipped the code, docs, and tests, but the milestone audit still fails because `08-VERIFICATION.md` does not exist and the six Phase 8 requirements cannot be traced to one concrete report.
Output: A new `.planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md` plus a synchronized `.planning/REQUIREMENTS.md` that maps all six Phase 8 requirements to the closure artifact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/11-workspace-infra-verification-completion/11-RESEARCH.md
@.planning/phases/08-workspace-and-infrastructure/08-VALIDATION.md
@.planning/phases/08-workspace-and-infrastructure/08-01-SUMMARY.md
@.planning/phases/08-workspace-and-infrastructure/08-02-SUMMARY.md
@.planning/phases/08-workspace-and-infrastructure/08-03-SUMMARY.md
@docs/api/classic-shared-core.md
@docs/api/classic-path-core.md
@docs/api/game-setup-workflow.md
@docs/api/binding-parity-overview.md
@docs/api/binding-contract-refresh-note.md
@docs/implementation/node_api_parity/governance/gate_contract_baseline.md
@.opencode/skills/classic-project-guide/references/repo-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the authoritative Phase 8 verification report for workspace ownership, Proton wiring, and gui-bridge proof</name>
  <files>.planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-workspace-and-infrastructure/08-VALIDATION.md
    - .planning/phases/08-workspace-and-infrastructure/08-01-SUMMARY.md
    - .planning/phases/08-workspace-and-infrastructure/08-02-SUMMARY.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/v1.0-MILESTONE-AUDIT.md
    - ClassicLib-rs/Cargo.toml
    - ClassicLib-rs/business-logic/classic-path-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml
    - ClassicLib-rs/foundation/classic-shared-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs
    - ClassicLib-rs/business-logic/classic-path-core/tests/linux_proton_docs_path.rs
    - docs/api/classic-shared-core.md
    - docs/api/classic-path-core.md
    - docs/api/game-setup-workflow.md
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md` exists and contains `# Phase 08: Workspace and Infrastructure Verification Report`.
    - The report frontmatter identifies `phase: 08-workspace-and-infrastructure` and does not include a `re_verification:` block because this is the initial authoritative Phase 8 verification artifact.
    - The report has explicit requirement rows for `INFRA-01`, `INFRA-02`, `INFRA-03`, `INFRA-04`, and `TEST-03`.
    - `INFRA-03` and `TEST-03` are separate rows with distinct evidence text; neither row says only "see summary" or "same as above".
    - The evidence text cites the real implementation and proof surfaces: `ClassicLib-rs/Cargo.toml`, `classic-path-core/Cargo.toml`, `classic-constants-core/Cargo.toml`, `classic-shared-core/Cargo.toml`, `classic-path-core/src/docs_path.rs`, `classic-path-core/tests/linux_proton_docs_path.rs`, and the touched docs pages.
  </acceptance_criteria>
  <action>Create `.planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md` as the single authoritative Phase 8 verification report. Use repo-standard verification-report sections: frontmatter, Goal Achievement, Observable Truths, Required Artifacts, Key Link Verification, Behavioral Spot-Checks, Requirements Coverage, Anti-Patterns Found, Human Verification Required, and Gaps Summary. Set frontmatter to the Phase 8 identity (`phase: 08-workspace-and-infrastructure`) and initial-verification semantics rather than a Phase 11 or re-verification story. Promote the exact Phase 8 validation commands into evidence-backed rows for `INFRA-01`, `INFRA-02`, `INFRA-03`, `INFRA-04`, and `TEST-03`: use `cargo check -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml`, `cargo check -p classic-constants-core --manifest-path ClassicLib-rs/Cargo.toml`, `cargo test -p classic-shared-core --features gui-bridge --manifest-path ClassicLib-rs/Cargo.toml`, `cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml proton -- --nocapture`, and `cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml`. For the report body, cite the actual source/doc evidence already in the repo: `ClassicLib-rs/Cargo.toml` for workspace-owned `winreg`, `classic-path-core/Cargo.toml` for target-gated `winreg = { workspace = true }`, `classic-constants-core/Cargo.toml` for `phf = { workspace = true }`, `classic-shared-core/Cargo.toml` plus `docs/api/classic-shared-core.md` for the removed `zerovec` workaround and validated `gui-bridge` state, and `classic-path-core/src/docs_path.rs` plus `tests/linux_proton_docs_path.rs` plus the path docs for Proton-first Linux discovery. Use the three Phase 8 summaries as provenance only; do not treat them as proof.</action>
  <verify>
    <automated>cargo check -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml &amp;&amp; cargo check -p classic-constants-core --manifest-path ClassicLib-rs/Cargo.toml &amp;&amp; cargo test -p classic-shared-core --features gui-bridge --manifest-path ClassicLib-rs/Cargo.toml &amp;&amp; cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml proton -- --nocapture &amp;&amp; cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>`08-VERIFICATION.md` exists as a Phase 8-owned verification artifact with explicit, command-backed coverage for INFRA-01/02/03/04 and TEST-03 instead of summary-only closure claims.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Node declaration-governance evidence bundle and close Phase 11 traceability</name>
  <files>.planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md
    - .planning/phases/08-workspace-and-infrastructure/08-VALIDATION.md
    - .planning/phases/08-workspace-and-infrastructure/08-03-SUMMARY.md
    - .planning/REQUIREMENTS.md
    - ClassicLib-rs/node-bindings/classic-node/package.json
    - ClassicLib-rs/node-bindings/classic-node/.gitignore
    - ClassicLib-rs/node-bindings/classic-node/index.d.ts
    - tools/node_api_parity/check_dts_freshness.py
    - .github/workflows/ci-typescript.yml
    - docs/api/binding-parity-overview.md
    - docs/api/binding-contract-refresh-note.md
    - docs/implementation/node_api_parity/governance/gate_contract_baseline.md
  </read_first>
  <acceptance_criteria>
    - `08-VERIFICATION.md` contains an explicit `INFRA-05` requirement row that cites the tracked `index.d.ts` artifact, `.gitignore`, `package.json`, `check_dts_freshness.py`, and `.github/workflows/ci-typescript.yml` together.
    - `08-VERIFICATION.md` contains behavioral spot-check text for `bun run parity:gate:local`, `bun run test:bun`, `bun run test:node`, and `bun run dts:freshness:check`.
    - `.planning/REQUIREMENTS.md` marks `INFRA-01`, `INFRA-02`, `INFRA-03`, `INFRA-04`, `INFRA-05`, and `TEST-03` as checked and maps each of those IDs to `Phase 11 | Complete` in the traceability table.
    - The verification report and traceability table both mention all six Phase 8 requirements; none remain pending or orphaned.
  </acceptance_criteria>
  <action>Finish `08-VERIFICATION.md` by adding the full `INFRA-05` evidence bundle and then synchronize `.planning/REQUIREMENTS.md`. In the verification report, add or complete the Node-governance artifact rows, key-link entries, and behavioral spot-checks so `INFRA-05` is backed by the real tracked declaration policy: `ClassicLib-rs/node-bindings/classic-node/index.d.ts`, `.gitignore`, `package.json` scripts (`parity:gate:local`, `test:bun`, `test:node`, `dts:freshness:check`), `tools/node_api_parity/check_dts_freshness.py`, and `.github/workflows/ci-typescript.yml`. Use the repo-governed command set from `08-VALIDATION.md` and `gate_contract_baseline.md`; do not invent a new Node gate sequence and do not handwave freshness as "unchanged so fine". After the report explicitly covers all six requirement IDs, update `.planning/REQUIREMENTS.md` in the same change: flip the checkbox lines for `INFRA-01` through `INFRA-05` and `TEST-03` to `[x]`, update the traceability table so those six rows point to `Phase 11` with `Complete` status, and refresh the footer `Last updated` note for the Phase 11 closure. Do not create `11-VERIFICATION.md`, and do not move any requirement out of Phase 11.</action>
  <verify>
    <automated>bun run parity:gate:local &amp;&amp; bun run test:bun &amp;&amp; bun run test:node &amp;&amp; bun run dts:freshness:check &amp;&amp; rg -n "INFRA-01|INFRA-02|INFRA-03|INFRA-04|INFRA-05|TEST-03" .planning/phases/08-workspace-and-infrastructure/08-VERIFICATION.md .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>`08-VERIFICATION.md` tells the full six-requirement Phase 8 verification story including Node freshness governance, and `.planning/REQUIREMENTS.md` no longer leaves any Phase 8 workspace/infra requirements orphaned.</done>
</task>

</tasks>

<verification>
- `cargo check -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo check -p classic-constants-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo test -p classic-shared-core --features gui-bridge --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml proton -- --nocapture` passes.
- `cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- From `ClassicLib-rs/node-bindings/classic-node`, `bun run parity:gate:local && bun run test:bun && bun run test:node && bun run dts:freshness:check` passes.
- `08-VERIFICATION.md` explicitly covers INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, and TEST-03 with direct evidence.
- `.planning/REQUIREMENTS.md` marks all six Phase 11 requirements complete and maps them to Phase 11 in the traceability table.
</verification>

<success_criteria>
- `08-VERIFICATION.md` exists and is the authoritative Phase 8 verification report.
- The report separately proves workspace dependency ownership, Proton workflow wiring, the Proton integration test, gui-bridge/workaround cleanup, and Node declaration freshness/runtime governance.
- All six Phase 11 requirement IDs are explicitly represented in both the verification report and `.planning/REQUIREMENTS.md`.
- The milestone can trace the entire Phase 8 workspace/infra story to one concrete verification artifact instead of orphaned summaries.
</success_criteria>

<output>
After completion, create `.planning/phases/11-workspace-infra-verification-completion/11-01-SUMMARY.md`
</output>
