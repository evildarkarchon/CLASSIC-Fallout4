---
phase: 06-mmap-toctou-safety
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
  - .planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md
  - performance_baselines/README.md
autonomous: true
requirements: [SAFE-05]
must_haves:
  truths:
    - "Phase 6 benchmark coverage compares `map()`, `map_copy()`, and `map_copy_read_only()` on files that definitely exercise the mmap branch"
    - "The committed proof artifact records commands, sizes, results, and an acceptability call for Windows validation"
    - "Raw Criterion baselines stay local-only while the repo keeps a shareable markdown proof"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs"
      provides: "Phase 6 mmap throughput benchmark group in the existing file-io harness"
      contains: "phase6_mmap_variants"
    - path: ".planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md"
      provides: "Committed Windows-focused mmap throughput proof"
      contains: "map_copy_read_only"
    - path: "performance_baselines/README.md"
      provides: "Contributor workflow for local Phase 6 benchmark baselines"
      contains: "Phase 6"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs"
      to: "ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs"
      via: "benchmark-local helpers mirror the production decode path after 06-01"
      pattern: "phase6_mmap_variants|map_copy_read_only"
    - from: ".planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md"
      to: "performance_baselines/README.md"
      via: "same local-only baseline workflow and artifact location"
      pattern: "save-baseline|baseline"
---

<objective>
Add the locked Phase 6 mmap throughput proof to the existing file-io Criterion harness and commit the resulting markdown evidence.

Purpose: SAFE-05 is not just a constructor swap; Phase 6 must prove that the safer mapping is acceptable on Windows by benchmarking the three mmap variants in the existing `file_io_benchmarks.rs` harness per D-03, D-04, and D-05.
Output: A `phase6_mmap_variants` benchmark group, a committed `06-BENCHMARK-PROOF.md`, and an updated local baseline workflow note.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-mmap-toctou-safety/06-CONTEXT.md
@.planning/phases/06-mmap-toctou-safety/06-RESEARCH.md
@.planning/phases/06-mmap-toctou-safety/06-VALIDATION.md
@.planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md
@performance_baselines/README.md
@ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs
@ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs

<interfaces>
From `ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs` after 06-01:
```rust
pub async fn read_file_mmap(&self, path: &Path) -> Result<String, FileIOError>;
```

Existing Criterion entrypoint in `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs`:
```rust
criterion_group! {
    name = benches;
    config = common::config::configure_criterion();
    targets =
        encoding_detection_benchmarks,
        path_filtering_benchmarks,
        file_io_core_benchmarks,
        log_pattern_benchmarks,
        dds_parsing_benchmarks
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `file_io_benchmarks.rs` with a Phase 6 three-way mmap throughput group</name>
  <files>ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
    - ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs
    - .planning/phases/06-mmap-toctou-safety/06-CONTEXT.md
    - .planning/phases/06-mmap-toctou-safety/06-RESEARCH.md
    - .planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md
  </read_first>
  <action>Per D-03, D-04, and D-05, keep the benchmark work inside `file_io_benchmarks.rs` and add one focused group named `phase6_mmap_variants`. Benchmark exactly three variants: `map`, `map_copy`, and `map_copy_read_only`. Use synthetic UTF-8 crash-log-like files sized above the 1 MB threshold so the mmap branch is guaranteed to run, with at least three sizes that bracket and scale beyond the cutoff (use `1_048_576 + 4_096`, `4 * 1_048_576`, and `16 * 1_048_576` unless execution reveals a clear repo-specific reason to adjust). Report `Throughput::Bytes`, create files and any runtime/setup outside `b.iter`, and keep the timed loop focused on the mapping plus the same decode path production pays. Do not create a new bench harness and do not benchmark unrelated cache-hit paths.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `phase6_mmap_variants`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `map_copy_read_only`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `map_copy`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `Throughput::Bytes`
    - `ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs` contains `1_048_576 + 4_096`
  </acceptance_criteria>
  <verify>
    <automated>cargo bench -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml --bench file_io_benchmarks phase6_mmap_variants -- --test</automated>
  </verify>
  <done>The existing file-io Criterion harness contains a compilable Phase 6 group that compares all three mmap strategies on representative large files.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the committed Phase 6 benchmark proof and document the local baseline workflow</name>
  <files>.planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md, performance_baselines/README.md</files>
  <read_first>
    - .planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md
    - performance_baselines/README.md
    - .planning/phases/06-mmap-toctou-safety/06-RESEARCH.md
    - .planning/phases/06-mmap-toctou-safety/06-VALIDATION.md
    - ClassicLib-rs/business-logic/classic-file-io-core/benches/file_io_benchmarks.rs
  </read_first>
  <action>Create `.planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md` using the Phase 5 proof structure: include capture date, harness path, BENCH_MODE, exact benchmark commands, covered group name, tested sizes, and a results table for `map`, `map_copy`, and `map_copy_read_only`. State explicitly that raw Criterion artifacts stay local-only under `ClassicLib-rs/target/criterion/`. Update `performance_baselines/README.md` with a Phase 6 section that shows the local save/compare commands for `phase6_mmap_variants` and points contributors to the committed proof artifact. The proof text must make a clear acceptability call for Windows validation without overstating upstream guarantees: document the measured throughput tradeoff and whether `map_copy_read_only()` remains within the repo's acceptable bar relative to the alternatives.</action>
  <acceptance_criteria>
    - `.planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md` contains `phase6_mmap_variants`
    - `.planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md` contains `map_copy_read_only`
    - `.planning/phases/06-mmap-toctou-safety/06-BENCHMARK-PROOF.md` contains `--save-baseline`
    - `performance_baselines/README.md` contains `Phase 6`
    - `performance_baselines/README.md` contains `file_io_benchmarks phase6_mmap_variants`
  </acceptance_criteria>
  <verify>
    <automated>$env:BENCH_MODE='thorough'; cargo bench -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml --bench file_io_benchmarks phase6_mmap_variants -- --save-baseline phase6-mmap-baseline; cargo bench -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml --bench file_io_benchmarks phase6_mmap_variants -- --baseline phase6-mmap-baseline</automated>
  </verify>
  <done>The repo has a shareable Phase 6 proof artifact and a documented local-only baseline workflow for the mmap comparison group.</done>
</task>

</tasks>

<verification>
- `cargo bench -p classic-file-io-core --manifest-path ClassicLib-rs/Cargo.toml --bench file_io_benchmarks phase6_mmap_variants -- --test` passes.
- The committed `06-BENCHMARK-PROOF.md` records commands, sizes, and results for all three mmap variants.
- `performance_baselines/README.md` documents the local save/compare workflow without committing raw Criterion directories.
</verification>

<success_criteria>
- SAFE-05 includes empirical Windows-target benchmark evidence instead of an inferred performance claim.
- The benchmark harness stays in `classic-file-io-core` per D-03 and covers files that definitely exercise the mmap path per D-05.
- The repo keeps only the markdown proof artifact under version control while raw baselines remain local-only per D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mmap-toctou-safety/06-02-SUMMARY.md`
</output>
