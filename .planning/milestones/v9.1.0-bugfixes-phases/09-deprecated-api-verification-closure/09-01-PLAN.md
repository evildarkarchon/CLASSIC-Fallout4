---
phase: 09-deprecated-api-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-deprecated-api-migration/01-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEBT-05, DEBT-06, DEBT-07, DEBT-10]
must_haves:
  truths:
    - "Phase 1 verification no longer reports a stale `gaps_found` status once current evidence is rerun"
    - "DEBT-05, DEBT-06, DEBT-07, and DEBT-10 are each backed by explicit current evidence in `01-VERIFICATION.md`"
    - "Python and Node parity proof for the deprecated API migration is either rerun and recorded or explicitly called out as residual proof, never assumed"
  artifacts:
    - path: ".planning/phases/01-deprecated-api-migration/01-VERIFICATION.md"
      provides: "Repo-standard re-verification artifact for the Phase 1 closure"
      contains: "re_verification:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement checklist and traceability aligned with the closure result"
      contains: "DEBT-05"
  key_links:
    - from: ".planning/phases/01-deprecated-api-migration/01-VALIDATION.md"
      to: ".planning/phases/01-deprecated-api-migration/01-VERIFICATION.md"
      via: "rerun the declared Rust and Python commands, then record the fresh results"
      pattern: "version::tests|parse_segments_parallel|generate_suspect_section|formid_analyzer_legacy_dict_deprecation_warning"
    - from: "ClassicLib-rs/node-bindings/classic-node/package.json"
      to: ".planning/phases/01-deprecated-api-migration/01-VERIFICATION.md"
      via: "record the local Node parity-gate result for SC5 instead of inferring it from unchanged files"
      pattern: "parity:gate:local"
---

<objective>
Refresh the stale Phase 1 verification artifact so the milestone audit can see current, explicit proof for the deprecated API migration requirements.

Purpose: Phase 09 exists because `.planning/phases/01-deprecated-api-migration/01-VERIFICATION.md` still reflects an old documentation-gap story, not the current closure evidence the audit consumes. This plan reruns the declared proof, rewrites the artifact in repo-standard re-verification form, and aligns requirement bookkeeping.
Output: A refreshed `01-VERIFICATION.md` with re-verification metadata plus a synchronized `.planning/REQUIREMENTS.md` traceability state for DEBT-05, DEBT-06, DEBT-07, and DEBT-10.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-deprecated-api-verification-closure/09-RESEARCH.md
@.planning/phases/01-deprecated-api-migration/01-VALIDATION.md
@.planning/phases/01-deprecated-api-migration/01-VERIFICATION.md
@.planning/phases/01-deprecated-api-migration/01-01-SUMMARY.md
@.planning/phases/01-deprecated-api-migration/01-02-SUMMARY.md
@.opencode/skills/classic-project-guide/references/repo-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rerun the Phase 1 Rust and Python proof and replace the stale gap narrative with current evidence</name>
  <files>.planning/phases/01-deprecated-api-migration/01-VERIFICATION.md</files>
  <action>Use `01-VALIDATION.md` and `09-RESEARCH.md` as the execution contract. Rerun the current Rust and Python proof for DEBT-05, DEBT-06, DEBT-07, and DEBT-10: the `classic-scanlog-core` `version::tests` command, the targeted Python deprecation-warning pytest selector, and `python tools/python_api_parity/check_parity_gate.py --repo-root .`. Then rewrite the body of `01-VERIFICATION.md` so it stops centering the obsolete REQUIREMENTS-sync gap and instead records fresh command results, current requirement coverage, and evidence grounded in the rerun outputs plus the existing Phase 1 summaries. Do not mark anything verified from summary prose alone, and do not preserve the old human-verification text if the commands were actually rerun now.</action>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml -- version::tests && uv run --python ClassicLib-rs/python-bindings/.venv/Scripts/python.exe python -m pytest ClassicLib-rs/python-bindings/tests/test_tier1_parity_smoke.py -q -k "parse_segments_parallel or generate_suspect_section or formid_analyzer_legacy_dict_deprecation_warning" && python tools/python_api_parity/check_parity_gate.py --repo-root .</automated>
  </verify>
  <done>`01-VERIFICATION.md` contains fresh Rust/Python evidence for all four requirements and no longer relies on the obsolete "REQUIREMENTS.md is out of sync" story as the primary blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize repo-standard re-verification metadata and close the Phase 9 traceability loop</name>
  <files>.planning/phases/01-deprecated-api-migration/01-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>Run the repo-standard Node local parity gate from `ClassicLib-rs/node-bindings/classic-node` so Success Criterion 5 has fresh cross-binding proof, then finalize `01-VERIFICATION.md` frontmatter as a closure artifact: set `re_verification.previous_status` to `gaps_found`, carry forward the prior score, populate `gaps_closed`, leave `gaps_remaining` empty only if the rerun commands are green, and update the phase status/score accordingly. Reconcile the requirements triangle called out in research by updating `.planning/REQUIREMENTS.md` so DEBT-05, DEBT-06, DEBT-07, and DEBT-10 show completed checklist state and completed Phase 9 traceability only when the refreshed verification artifact now contains the current evidence. Do not create a separate Phase 09 verification file, and do not edit `v1.0-MILESTONE-AUDIT.md` directly; the next audit should consume the refreshed artifacts.</action>
  <verify>
    <automated>bun run parity:gate:local</automated>
  </verify>
  <done>`01-VERIFICATION.md` is in repo-standard re-verification format with honest closure metadata, and `.planning/REQUIREMENTS.md` no longer leaves the four Phase 9 requirements pending once the new proof exists.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml -- version::tests` passes.
- `uv run --python ClassicLib-rs/python-bindings/.venv/Scripts/python.exe python -m pytest ClassicLib-rs/python-bindings/tests/test_tier1_parity_smoke.py -q -k "parse_segments_parallel or generate_suspect_section or formid_analyzer_legacy_dict_deprecation_warning"` passes.
- `python tools/python_api_parity/check_parity_gate.py --repo-root .` passes.
- From `ClassicLib-rs/node-bindings/classic-node`, `bun run parity:gate:local` passes.
- `01-VERIFICATION.md` status, score, and requirements table match the rerun evidence instead of the pre-Phase-09 gap narrative.
</verification>

<success_criteria>
- `01-VERIFICATION.md` is updated from its stale `gaps_found` state to a status justified by fresh rerun evidence.
- DEBT-05, DEBT-06, DEBT-07, and DEBT-10 are each explicitly covered in `01-VERIFICATION.md` with current command-backed evidence.
- `.planning/REQUIREMENTS.md` no longer shows the four Phase 9 requirements as pending after the closure proof lands.
- Python and Node parity proof for the deprecated API migration is fresh and recorded; no claim depends on "no files changed" reasoning.
</success_criteria>

<output>
After completion, create `.planning/phases/09-deprecated-api-verification-closure/09-01-SUMMARY.md`
</output>
