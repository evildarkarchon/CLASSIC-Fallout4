---
phase: 04-bounded-cache-replacement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs
  - docs/api/classic-settings-core.md
autonomous: true
requirements: [CACHE-02, CONS-03]
must_haves:
  truths:
    - "Settings cache is bounded to 64 entries without changing caller-managed invalidation semantics"
    - "Settings cache stats expose hits, misses, hit_rate, size, and capacity instead of key lists"
    - "Settings clear/reset helpers still work as the public isolation surface"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs"
      provides: "Bounded settings cache plus canonical stats"
      contains: "Cache::new(64)"
    - path: "docs/api/classic-settings-core.md"
      provides: "Updated settings cache contract"
      contains: "capacity"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs"
      to: "quick_cache::sync::Cache"
      via: "SETTINGS_CACHE initialization"
      pattern: "Cache::new\(64\)"
    - from: "ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs"
      to: "invalidate|clear_cache|reset_cache_stats"
      via: "public cache management API"
      pattern: "pub fn (invalidate|clear_cache|reset_cache_stats)"
---

<objective>
Replace the settings cache with a bounded quick_cache implementation while keeping its current explicit invalidation model and shrinking the shared stats contract to the canonical Phase 4 shape.

Purpose: CACHE-02 and CONS-03 require the settings cache to adopt a 64-entry bound per D-01 without changing the manual cache lifecycle or exposing `keys` inside the canonical stats struct per D-02, D-03, and D-06. This plan must honor the roadmap's `LRU` wording by implementing the locked `quick_cache` crate and validating bounded eviction behavior without asserting strict victim order.
Output: A bounded settings cache, canonical stats, updated inline coverage, and synced API docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bounded-cache-replacement/04-CONTEXT.md
@.planning/phases/04-bounded-cache-replacement/04-RESEARCH.md
@docs/api/README.md
@docs/api/classic-settings-core.md

<interfaces>
From `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs`:
```rust
static SETTINGS_CACHE: Lazy<DashMap<String, Arc<Vec<Yaml>>>> = Lazy::new(DashMap::new);

pub struct CacheStats {
    pub hits: u64,
    pub misses: u64,
    pub hit_rate: f64,
    pub size: usize,
    pub keys: Vec<String>,
}

pub fn get_cached(key: &str) -> Option<Arc<Vec<Yaml>>> { ... }
pub fn invalidate(key: &str) -> bool { ... }
pub fn clear_cache() { ... }
pub fn cache_keys() -> Vec<String> { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace SETTINGS_CACHE with a 64-entry quick_cache and canonical stats</name>
  <files>ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs
    - ClassicLib-rs/business-logic/classic-file-io-core/src/core.rs
    - docs/api/classic-settings-core.md
    - .planning/phases/04-bounded-cache-replacement/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: inserting and reading a cached settings key records a hit.
    - Test 2: missing keys record misses without panicking.
    - Test 3: `cache_stats()` reports `capacity == 64` and no longer exposes `keys` in the canonical struct.
    - Test 4: bounded-cache checks verify the cache never exceeds capacity without asserting a specific evicted key, because `quick_cache` is required per D-01.
  </behavior>
  <action>Update the failing settings cache tests first, then swap `SETTINGS_CACHE` from `Lazy<DashMap<String, Arc<Vec<Yaml>>>>` to `LazyLock<Cache<String, Arc<Vec<Yaml>>>>` with `Cache::new(64)` per D-01. Keep `get_cached`, `invalidate`, `clear_cache`, and `cache_keys` behavior caller-managed per D-06 and D-07: settings freshness must still be controlled only by explicit reload/invalidate/clear calls, not file mtimes. Retain atomic hit/miss counters and change the canonical `CacheStats` struct to exactly `hits`, `misses`, `hit_rate`, `size`, and `capacity` per D-02. Remove `keys` from the canonical struct per D-03, but preserve `cache_keys()` as a separate helper per D-05. Treat roadmap `LRU eviction` wording as bounded eviction implemented by the locked `quick_cache`; do not add tests that require exact eviction order.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs` contains `use quick_cache::sync::Cache`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs` contains `static SETTINGS_CACHE: LazyLock<Cache<String, Arc<Vec<Yaml>>>>`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs` contains `Cache::new(64)`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs` contains `pub capacity: usize`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs` does not contain `pub keys: Vec<String>`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>`classic-settings-core` uses a bounded 64-entry quick_cache, keeps manual invalidation behavior, and exposes only the canonical CacheStats fields.</done>
</task>

<task type="auto">
  <name>Task 2: Keep cache management helpers stable and update the settings API guide</name>
  <files>ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs, docs/api/classic-settings-core.md</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs
    - docs/api/classic-settings-core.md
    - .planning/phases/04-bounded-cache-replacement/04-VALIDATION.md
  </read_first>
  <action>Finish the settings cache refactor by ensuring `invalidate`, `clear_cache`, `cache_size`, `cache_keys`, and `reset_cache_stats` all operate correctly against the quick_cache backing store per D-07. Update the inline/unit coverage so it proves `cache_keys()` still works as a separate helper while `cache_stats()` reports only the canonical five fields per D-03 and D-05. Then revise `docs/api/classic-settings-core.md` to document the 64-entry capacity, the new `capacity` field, and the fact that `cache_keys()` is now the only place where key listings remain public. Add an explicit note that Phase 4 accepts bounded `quick_cache` eviction semantics here, not strict LRU victim-order assertions.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs` contains `pub fn cache_keys() -> Vec<String>`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs` contains `capacity: SETTINGS_CACHE.capacity()`
    - `docs/api/classic-settings-core.md` contains `capacity`
    - `docs/api/classic-settings-core.md` contains `cache_keys()`
    - `docs/api/classic-settings-core.md` does not list `keys` as a `CacheStats` field
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>Settings cache lifecycle helpers remain stable, and the contributor docs reflect the bounded/cache-key split correctly.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo clippy -p classic-settings-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` passes.
- `docs/api/classic-settings-core.md` documents `capacity` and keeps `cache_keys()` outside the canonical stats struct.
</verification>

<success_criteria>
- CACHE-02: the settings cache is bounded to 64 entries with quick_cache.
- CONS-03: settings cache stats match the five-field canonical contract.
- Manual invalidation and reset helpers still behave exactly as the public cache lifecycle surface.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bounded-cache-replacement/04-02-SUMMARY.md`
</output>
