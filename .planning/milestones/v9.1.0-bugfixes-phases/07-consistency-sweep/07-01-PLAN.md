---
phase: 07-consistency-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/version.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/plugin_analyzer.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/report.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/formid_analyzer.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/formid.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
  - docs/api/classic-scanlog-core.md
autonomous: true
requirements: [CONS-01]
must_haves:
  truths:
    - "classic-scanlog-core no longer imports `once_cell::sync::Lazy` or `once_cell::sync::OnceCell` directly"
    - "`RecordScanner` keeps its existing lazy per-instance matcher behavior via `std::sync::OnceLock`"
    - "The scanlog contributor doc and crate manifest move in the same change as the source sweep"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs"
      provides: "RecordScanner one-time matcher initialization migrated to std"
      contains: "OnceLock"
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs"
      provides: "Global FCX handler migrated to repo-standard std lazy static"
      contains: "LazyLock"
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml"
      provides: "Direct `once_cell` dependency removed after source migration"
      contains: "classic-scanlog-core"
    - path: "docs/api/classic-scanlog-core.md"
      provides: "Contributor-facing scanlog docs aligned to std lazy primitives"
      contains: "LazyLock"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs"
      to: "aho_corasick::AhoCorasickBuilder"
      via: "per-instance matcher caching still goes through `get_or_init`"
      pattern: "OnceLock|get_or_init"
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs"
      to: "parking_lot::Mutex"
      via: "global FCX handler remains a lazily initialized process-global mutex"
      pattern: "LazyLock::new"
    - from: "docs/api/classic-scanlog-core.md"
      to: "ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs"
      via: "doc wording matches the new std primitive names"
      pattern: "LazyLock|OnceLock"
---

<objective>
Migrate the remaining direct `once_cell` usage inside `classic-scanlog-core` to `std::sync::{LazyLock, OnceLock}` without changing scanlog behavior.

Purpose: Phase 7 only succeeds if the biggest remaining `once_cell` surface, `classic-scanlog-core`, moves to the Phase 4/5 std pattern and drops its direct manifest dependency in the same change per D-01 through D-04.
Output: Updated scanlog source, a cleaned `classic-scanlog-core/Cargo.toml`, and aligned contributor docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-consistency-sweep/07-CONTEXT.md
@.planning/phases/07-consistency-sweep/07-RESEARCH.md
@.planning/phases/07-consistency-sweep/07-VALIDATION.md
@docs/api/README.md
@docs/api/classic-scanlog-core.md
@ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
@ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/version.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/plugin_analyzer.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/report.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/formid_analyzer.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/formid.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs

<interfaces>
From `ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs`:
```rust
pub static GLOBAL_FCX_HANDLER: Lazy<Mutex<FcxModeHandler>>;
```

From `ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs`:
```rust
pub struct RecordScanner {
    record_matcher: OnceCell<AhoCorasick>,
    ignore_matcher: OnceCell<AhoCorasick>,
}

pub fn scan_named_records(&self, segment_callstack: &[String]) -> (Vec<String>, Vec<String>);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the remaining scanlog lazy statics with `std::sync::LazyLock`</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/version.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/plugin_analyzer.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/report.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/formid_analyzer.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/formid.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
    - .planning/phases/07-consistency-sweep/07-CONTEXT.md
    - .planning/phases/07-consistency-sweep/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `classic-scanlog-core` still passes its existing crate tests after the lazy static type swap.
    - Test 2: FCX reset/state tests still observe the same process-global behavior.
    - Test 3: Parser/formid/plugin/report/orchestrator statics still initialize transparently at their current call sites.
  </behavior>
  <action>Convert each remaining `once_cell::sync::Lazy` import and static in `classic-scanlog-core` to `std::sync::LazyLock`, following the exact Phase 4/5 style of `use std::sync::LazyLock;` plus `LazyLock::new(...)`. Keep initializer closures semantically identical and do not redesign surrounding modules. Use the existing tests as the behavior lock. Do not broaden this task into registry/perf or workspace-wide manifest cleanup yet.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs` contains `LazyLock`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/parser.rs` contains `LazyLock`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/orchestrator.rs` contains `LazyLock`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/formid.rs` contains `LazyLock`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>The scanlog crate uses `LazyLock` for all remaining module-level lazy statics and crate tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace `RecordScanner` field caches with `OnceLock` and remove the scanlog direct manifest dependency</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs, ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml, docs/api/classic-scanlog-core.md</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs
    - ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
    - docs/api/classic-scanlog-core.md
    - docs/api/README.md
    - .planning/phases/07-consistency-sweep/07-CONTEXT.md
    - .planning/phases/07-consistency-sweep/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `RecordScanner` still lazily builds matcher automatons on first use rather than at constructor time.
    - Test 2: repeated scans still reuse the same cached matcher state for a given instance.
    - Test 3: the crate manifest and scanlog contributor doc no longer describe direct `once_cell` usage.
  </behavior>
  <action>Migrate `RecordScanner` from `once_cell::sync::OnceCell` to `std::sync::OnceLock`, preserving the current `get_or_init(|| ...)` flow so instance data still drives matcher construction lazily at call time. After the source sweep compiles, remove `once_cell = { workspace = true }` from `classic-scanlog-core/Cargo.toml`. Update `docs/api/classic-scanlog-core.md` anywhere it still names `once_cell` internals so the doc matches the new `LazyLock`/`OnceLock` implementation. Keep the scope tight: no new abstractions, no binding changes, and no workspace/root manifest edits in this task.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs` contains `OnceLock`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/src/record_scanner.rs` contains `get_or_init`
    - `docs/api/classic-scanlog-core.md` contains `OnceLock`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml` contains `classic-shared-core`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml && pwsh -Command "if (rg -n 'once_cell' 'ClassicLib-rs/business-logic/classic-scanlog-core' 'ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml' 'docs/api/classic-scanlog-core.md') { exit 1 }"</automated>
  </verify>
  <done>`classic-scanlog-core` is fully on std lazy primitives, its direct manifest dependency is removed, and its contributor doc no longer names `once_cell`.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `pwsh -Command "if (rg -n 'once_cell' 'ClassicLib-rs/business-logic/classic-scanlog-core' 'ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml' 'docs/api/classic-scanlog-core.md') { exit 1 }"` returns success after the manifest cleanup.
</verification>

<success_criteria>
- `classic-scanlog-core` no longer directly depends on `once_cell` in source or `Cargo.toml`.
- `RecordScanner` keeps lazy per-instance matcher construction through `OnceLock` instead of forcing a redesign.
- The scanlog API doc stays aligned with the source migration in the same plan wave.
</success_criteria>

<output>
After completion, create `.planning/phases/07-consistency-sweep/07-01-SUMMARY.md`
</output>
