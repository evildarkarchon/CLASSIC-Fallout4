---
phase: 07-consistency-sweep
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs
  - ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs
  - ClassicLib-rs/business-logic/classic-registry-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-perf-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml
  - ClassicLib-rs/Cargo.toml
  - docs/api/classic-registry-core.md
  - docs/api/classic-perf-core.md
  - docs/api/classic-settings-core.md
autonomous: true
requirements: [CONS-01]
must_haves:
  truths:
    - "`classic-registry-core` and `classic-perf-core` use `std::sync::LazyLock` for their process-global stores"
    - "No owned workspace manifest in the Phase 7 target set retains a direct `once_cell` declaration"
    - "The touched `docs/api` pages stop describing registry, perf, or settings internals in terms of `once_cell`"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs"
      provides: "Registry singleton migrated to std lazy initialization"
      contains: "LazyLock"
    - path: "ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs"
      provides: "Perf metrics singleton migrated to std lazy initialization"
      contains: "LazyLock"
    - path: "ClassicLib-rs/Cargo.toml"
      provides: "Workspace direct `once_cell` declaration removed after final audit"
      contains: "workspace.dependencies"
    - path: "docs/api/classic-registry-core.md"
      provides: "Registry contributor doc aligned to std lazy storage"
      contains: "LazyLock"
    - path: "docs/api/classic-perf-core.md"
      provides: "Perf contributor doc aligned to std lazy storage"
      contains: "LazyLock"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs"
      to: "dashmap::DashMap"
      via: "global registry map still initializes lazily with `DashMap::new`"
      pattern: "LazyLock::new\(DashMap::new\)"
    - from: "ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs"
      to: "dashmap::DashMap"
      via: "global metrics map still initializes lazily with `DashMap::new`"
      pattern: "LazyLock::new\(DashMap::new\)"
    - from: "docs/api/classic-settings-core.md"
      to: "ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs"
      via: "dependency note matches the already-migrated settings cache implementation"
      pattern: "LazyLock|quick_cache"
---

<objective>
Finish the Phase 7 sweep outside scanlog-core by migrating registry/perf globals, removing the remaining direct manifest declarations, and aligning the touched contributor docs.

Purpose: After 07-01 clears the scanlog crate, Phase 7 still needs the workspace-wide consistency pass promised in D-01 through D-03: migrate the remaining owned lazy statics, remove stale direct dependency declarations from both active and already-converted crates, and make the docs stop naming `once_cell` internals.
Output: Updated registry/perf source, a clean set of owned manifests, and aligned docs for registry/perf/settings.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-consistency-sweep/07-CONTEXT.md
@.planning/phases/07-consistency-sweep/07-RESEARCH.md
@.planning/phases/07-consistency-sweep/07-VALIDATION.md
@docs/api/README.md
@docs/api/classic-registry-core.md
@docs/api/classic-perf-core.md
@docs/api/classic-settings-core.md
@ClassicLib-rs/Cargo.toml
@ClassicLib-rs/business-logic/classic-registry-core/Cargo.toml
@ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs
@ClassicLib-rs/business-logic/classic-perf-core/Cargo.toml
@ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs
@ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml
@ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
@ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml

<interfaces>
From `ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs`:
```rust
pub fn register<K, V>(key: K, value: V)
where
    K: Into<String>,
    V: Any + Send + Sync + 'static;
```

From `ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs`:
```rust
pub struct MetricsSummary {
    pub count: usize,
    pub total: f64,
    pub average: f64,
    pub min: f64,
    pub max: f64,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace registry and perf direct `once_cell` statics with `LazyLock`</name>
  <files>ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs, ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs
    - ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs
    - .planning/phases/07-consistency-sweep/07-CONTEXT.md
    - .planning/phases/07-consistency-sweep/07-RESEARCH.md
    - ClassicLib-rs/business-logic/classic-settings-core/src/cache.rs
  </read_first>
  <behavior>
    - Test 1: registry crate tests still pass with unchanged global storage semantics.
    - Test 2: perf crate tests still pass with unchanged metric collection semantics.
    - Test 3: both statics keep the same lazy first-use initialization behavior after the type swap.
  </behavior>
  <action>Convert the process-global `REGISTRY` and `METRICS` statics from direct `once_cell::sync::Lazy` usage to `std::sync::LazyLock`, reusing the same `DashMap::new` constructor shape the repo already standardized elsewhere. Keep the public APIs exactly as-is and do not fold in unrelated cleanup. This task is source-only; manifest and doc cleanup follows once the source migration is green.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs` contains `LazyLock`
    - `ClassicLib-rs/business-logic/classic-registry-core/src/registry.rs` contains `DashMap::new`
    - `ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs` contains `LazyLock`
    - `ClassicLib-rs/business-logic/classic-perf-core/src/metrics.rs` contains `DashMap::new`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-registry-core --manifest-path ClassicLib-rs/Cargo.toml && cargo test -p classic-perf-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>The registry and perf crates both use `LazyLock` for their global stores and their crate tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the remaining owned direct `once_cell` manifest entries and align touched docs</name>
  <files>ClassicLib-rs/business-logic/classic-registry-core/Cargo.toml, ClassicLib-rs/business-logic/classic-perf-core/Cargo.toml, ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml, ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml, ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml, ClassicLib-rs/Cargo.toml, docs/api/classic-registry-core.md, docs/api/classic-perf-core.md, docs/api/classic-settings-core.md</files>
  <read_first>
    - ClassicLib-rs/Cargo.toml
    - ClassicLib-rs/business-logic/classic-registry-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-perf-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml
    - docs/api/classic-registry-core.md
    - docs/api/classic-perf-core.md
    - docs/api/classic-settings-core.md
    - docs/api/README.md
    - .planning/phases/07-consistency-sweep/07-CONTEXT.md
  </read_first>
  <action>After confirming 07-01 and Task 1 removed the remaining owned code usage, delete the direct `once_cell` dependency entries from the affected crate manifests plus the root `ClassicLib-rs/Cargo.toml` workspace dependency list. Then update the touched API docs so they no longer describe registry, perf, or settings internals as `once_cell`-backed. Keep the wording precise: success means no direct repo-owned source/manifests still use `once_cell`, not that transitive lockfile entries disappear. Finish with a workspace build to catch integration fallout from the manifest cleanup.</action>
  <acceptance_criteria>
    - `docs/api/classic-registry-core.md` contains `LazyLock`
    - `docs/api/classic-perf-core.md` contains `LazyLock`
    - `docs/api/classic-settings-core.md` contains `quick_cache`
    - `ClassicLib-rs/Cargo.toml` contains `serial_test`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -Command "if (rg -n 'once_cell' 'ClassicLib-rs/Cargo.toml' 'ClassicLib-rs/business-logic/classic-registry-core' 'ClassicLib-rs/business-logic/classic-perf-core' 'ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml' 'ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml' 'ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml' 'docs/api/classic-registry-core.md' 'docs/api/classic-perf-core.md' 'docs/api/classic-settings-core.md') { exit 1 } else { cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml }"</automated>
  </verify>
  <done>All Phase 7 owned manifests and touched docs are free of direct `once_cell` declarations or wording, and the workspace still builds.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-registry-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo test -p classic-perf-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` passes after the manifest cleanup.
- `pwsh -Command "if (rg -n 'once_cell' 'ClassicLib-rs/Cargo.toml' 'ClassicLib-rs/business-logic/classic-registry-core' 'ClassicLib-rs/business-logic/classic-perf-core' 'ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml' 'ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml' 'ClassicLib-rs/business-logic/classic-scangame-core/Cargo.toml' 'docs/api/classic-registry-core.md' 'docs/api/classic-perf-core.md' 'docs/api/classic-settings-core.md') { exit 1 }"` returns success after the cleanup.
</verification>

<success_criteria>
- The remaining non-scanlog direct lazy statics now use `LazyLock` with no public API churn.
- All owned direct `once_cell` manifest declarations targeted by Phase 7 are removed.
- The touched API docs describe current std/quick_cache internals instead of stale `once_cell` wording.
</success_criteria>

<output>
After completion, create `.planning/phases/07-consistency-sweep/07-02-SUMMARY.md`
</output>
