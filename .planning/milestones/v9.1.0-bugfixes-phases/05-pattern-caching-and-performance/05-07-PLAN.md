---
phase: 05-pattern-caching-and-performance
plan: 07
type: execute
wave: 3
depends_on: [05-02, 05-06]
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
  - .planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md
autonomous: true
requirements: [PERF-02, PERF-04]
must_haves:
  truths:
    - "`detect_mods_important` still matches the legacy helper on the locked fixture and overlap-parity cases"
    - "The Phase 5 important-mod benchmark group no longer shows a >5% regression versus the legacy replica on the synthetic and real-fixture surfaces"
    - "The final proof artifact explains which cost center caused the regression and records the post-fix deltas without weakening the benchmark bar"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      provides: "Optimized important-mod matcher path that preserves Aho-Corasick semantics"
      contains: "AhoCorasick"
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs"
      provides: "Root-cause and no-regression benchmarks for the important-mod hotspot"
      contains: "phase5_detect_mods_important"
    - path: ".planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md"
      provides: "Updated benchmark proof with root-cause notes and post-fix deltas"
      contains: "phase5_detect_mods_important"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs"
      to: "quick_cache::sync::Cache"
      via: "bounded important-mod matcher reuse keyed by normalized detect literals"
      pattern: "Cache<|IMPORTANT"
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs"
      to: "detect_mods_important"
      via: "legacy/current benchmark variants in the existing Phase 5 harness"
      pattern: "phase5_detect_mods_important|legacy_regex|real_fixture_plugin_surface"
---

<objective>
Eliminate or materially reduce the residual `detect_mods_important` regression that remained after Phase 5 verification, while keeping the locked Aho-Corasick implementation and the existing benchmark-proof bar intact.

Purpose: `05-VERIFICATION.md` passed the phase but explicitly recorded one remaining risk: in this environment the current important-mod path is still slower than the legacy benchmark replica. This follow-up plan keeps the work inside Phase 5 because it targets the same hotspot, same proof artifact, and same PERF-02/PERF-04 contracts.
Output: A root-cause-isolated important-mod benchmark slice, a targeted optimization in `mod_detector.rs`, and an updated benchmark-proof artifact showing the post-fix deltas.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
@.planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
@.planning/phases/05-pattern-caching-and-performance/05-VERIFICATION.md
@.planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md
@.planning/phases/05-pattern-caching-and-performance/05-02-SUMMARY.md
@.planning/phases/05-pattern-caching-and-performance/05-06-SUMMARY.md
@ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
@ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
@performance_baselines/README.md

<interfaces>
From `ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs`:
```rust
pub fn detect_mods_important(
    entries: &[CoreModEntry],
    crashlog_plugins: &IndexMap<String, String>,
    user_gpu: Option<&str>,
    xse_modules: &HashSet<String>,
) -> Result<Vec<String>>;

fn build_important_mod_haystack(
    crashlog_plugins: &IndexMap<String, String>,
    xse_modules: &HashSet<String>,
) -> (HashSet<String>, String);
```

Existing benchmark helpers in `ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs`:
```rust
fn detect_mods_important_legacy_regex_count(
    entries: &[CoreModEntry],
    plugins: &IndexMap<String, String>,
    xse_modules: &HashSet<String>,
) -> usize;

fn detect_mods_important_aho_count(
    entries: &[CoreModEntry],
    plugins: &IndexMap<String, String>,
    xse_modules: &HashSet<String>,
) -> usize;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isolate the important-mod slowdown with benchmark slices tied to the existing Phase 5 harness</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs, ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs</files>
  <action>Per D-09, D-10, D-11, and D-12, extend the existing `phase5_detect_mods_important` group so the executor can see whether the regression is dominated by one-per-call automaton construction, haystack preparation, or match-result collection. Keep the work inside `scanlog_benchmarks.rs` rather than creating a new harness, and add only the smallest production or test-only helper needed in `mod_detector.rs` to expose reuse or compile-count behavior. Preserve the legacy replica benchmark and keep the Aho-Corasick path as the production path per D-01 through D-04; this task is investigation, not rollback.</action>
  <verify>
    <automated>cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks phase5_detect_mods_important -- --test</automated>
  </verify>
  <done>The important-mod benchmark group has named variants that attribute the slowdown to specific cost centers instead of only reporting one aggregate regression number.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize detect_mods_important without violating the locked parity and matcher decisions</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs, ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs</files>
  <action>Use the Task 1 evidence to remove the dominant cost while staying with Aho-Corasick per D-01, `MatchKind::LeftmostLongest` per D-02, and the combined lowercase plugin-plus-XSE haystack per D-03. Prefer the Phase 4/05 cache pattern if matcher construction is the root cause: a bounded process-wide `LazyLock<quick_cache::sync::Cache<...>>` keyed from the normalized `entry.detect` literals per D-06, D-07, and D-08. If a different hotspot dominates, optimize that path instead, but do not revert to per-entry regex, do not delete the legacy helper or parity assertions required by D-04, and do not broaden scope into unrelated Node or mmap work. The resulting path must still produce the same observable lines as the legacy helper on existing parity cases.</action>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_important</automated>
  </verify>
  <done>`detect_mods_important` keeps the locked semantics and parity coverage while removing the cost center identified in Task 1.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the proof artifact and enforce the no-regression bar for the important-mod hotspot</name>
  <files>.planning/phases/05-pattern-caching-and-performance/05-BENCHMARK-PROOF.md, ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs</files>
  <action>Rerun the focused important-mod benchmark workflow in thorough mode and update `05-BENCHMARK-PROOF.md` with the root-cause findings, the final before/after medians, and an explicit pass/fail call against the repo's existing `warning > 5%` threshold. The follow-up only counts as complete if both the synthetic and real-fixture important-mod comparisons are at or better than the no-regression bar relative to the legacy benchmark replica; if not, document the remaining delta as a blocker instead of weakening PERF-04. Keep raw Criterion data local-only per D-11.</action>
  <verify>
    <automated>$env:BENCH_MODE='thorough'; cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks phase5_detect_mods_important -- --save-baseline phase5-important-followup; cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks phase5_detect_mods_important -- --baseline phase5-important-followup</automated>
  </verify>
  <done>`05-BENCHMARK-PROOF.md` records post-fix important-mod deltas and shows both tracked surfaces are no worse than the repo's 5% warning bar versus the legacy replica.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml detect_mods_important` passes with the existing parity coverage still intact.
- `cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks phase5_detect_mods_important -- --test` runs the focused important-mod group successfully.
- The refreshed `05-BENCHMARK-PROOF.md` shows both important-mod benchmark surfaces at or inside the repo's no-regression threshold versus the legacy replica.
</verification>

<success_criteria>
- Root cause is explicitly identified in the benchmark/proof trail instead of inferred from a single regression number.
- `detect_mods_important` remains parity-safe and Aho-Corasick-based, honoring D-01 through D-04.
- The synthetic and real-fixture important-mod benchmarks are each no worse than 5% relative to the legacy benchmark replica; faster results are preferred, but anything beyond that threshold is a failed follow-up.
- The benchmark-proof requirement stays strict: the plan does not succeed by redefining the hotspot, hiding the legacy comparator, or weakening the threshold language.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-caching-and-performance/05-07-SUMMARY.md`
</output>
