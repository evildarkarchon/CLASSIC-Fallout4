---
phase: 05-pattern-caching-and-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs
  - docs/api/classic-cpp-bridge-data-entrypoints.md
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "detect_crash_pattern reuses one module-level LogParser instead of constructing a new parser per call"
    - "Bridge tests cover at least one positive crash-pattern path in addition to empty-input behavior"
    - "The bridge remains adapter-only and does not reimplement parser logic"
  artifacts:
    - path: "ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs"
      provides: "LazyLock<LogParser> bridge reuse"
      contains: "LogParser::new(None)"
    - path: "docs/api/classic-cpp-bridge-data-entrypoints.md"
      provides: "Contributor note on cached parser reuse for detect_crash_pattern"
      contains: "detect_crash_pattern"
  key_links:
    - from: "ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs"
      to: "classic_scanlog_core::LogParser"
      via: "module-level bridge parser singleton"
      pattern: "LazyLock<LogParser>"
---

<objective>
Remove per-call parser construction from the C++ bridge crash-pattern helper without widening the bridge beyond thin adapter responsibilities.

Purpose: PERF-03 is isolated to one bridge hotspot and is low semantic risk compared with the mod-detector work. This plan keeps the bridge thin by caching only the parser instance, not by duplicating core behavior or creating a separate benchmark harness.
Output: A module-level `LazyLock<LogParser>`, improved bridge tests, and a small bridge API note.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
@.planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
@docs/api/README.md
@docs/api/classic-cpp-bridge-data-entrypoints.md

<interfaces>
From `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs`:
```rust
pub fn detect_crash_pattern(input: &str) -> Result<String> { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bridge coverage for positive crash-pattern detection and parser reuse</name>
  <files>ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs</files>
  <read_first>
    - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs
    - .planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a representative crash header produces a positive pattern result.
    - Test 2: repeated calls keep the same outward behavior while using the cached parser singleton.
  </behavior>
  <action>Add the smallest bridge-level regression coverage needed to prove `detect_crash_pattern` still works on non-empty inputs, not just empty strings. Reuse one of the existing scanlog benchmark fixtures or a minimal header excerpt derived from it so the positive case reflects real bridge input. Keep assertions on observable output, not on parser internals.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs` contains at least one positive `detect_crash_pattern` test
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-cpp-bridge --manifest-path ClassicLib-rs/Cargo.toml detect_crash_pattern</automated>
  </verify>
  <done>Bridge coverage protects the parser reuse change with one real positive case.</done>
</task>

<task type="auto">
  <name>Task 2: Cache the bridge parser with a module-level LazyLock and document the helper contract</name>
  <files>ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs, docs/api/classic-cpp-bridge-data-entrypoints.md</files>
  <read_first>
    - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs
    - docs/api/classic-cpp-bridge-data-entrypoints.md
    - .planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
  </read_first>
  <action>Replace per-call `LogParser::new(None)` construction in `detect_crash_pattern` with a module-level `LazyLock<LogParser>` initialized once. As part of the change, verify the real `LogParser` type compiles cleanly in a static `LazyLock` and that sharing its bounded internal caches does not change observable `detect_crash_pattern` behavior across repeated calls; if either assumption fails, stop and record the constraint instead of broadening the bridge design. Keep error handling unchanged unless startup construction now makes an impossible path explicit. Update the bridge API guide with one concise note that this helper reuses a cached default parser internally and still exposes the same fail-soft bridge behavior.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs` contains `LazyLock<LogParser>`
    - `ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs` contains `LogParser::new(None)` exactly in singleton initialization, not per call
    - `docs/api/classic-cpp-bridge-data-entrypoints.md` contains `detect_crash_pattern`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-cpp-bridge --manifest-path ClassicLib-rs/Cargo.toml detect_crash_pattern</automated>
  </verify>
  <done>The bridge reuses one parser instance and the contributor docs state that contract clearly.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-cpp-bridge --manifest-path ClassicLib-rs/Cargo.toml detect_crash_pattern` passes.
- `cargo build -p classic-cpp-bridge --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `docs/api/classic-cpp-bridge-data-entrypoints.md` documents the cached parser reuse note for `detect_crash_pattern`.
</verification>

<success_criteria>
- PERF-03: `detect_crash_pattern` no longer constructs a parser per call.
- The bridge remains a thin adapter over `classic-scanlog-core::LogParser` behavior.
- Positive bridge coverage exists for the helper before benchmark work begins.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-caching-and-performance/05-03-SUMMARY.md`
</output>
