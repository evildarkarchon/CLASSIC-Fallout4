---
phase: 05-pattern-caching-and-performance
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
  - performance_baselines/README.md
autonomous: true
requirements: [PERF-04]
must_haves:
  truths:
    - "Phase 5 benchmark proof lives in the existing classic-scanlog-core Criterion harness"
    - "Bench groups cover important-mod matching, cached regex paths, and bridge-style crash-pattern parsing"
    - "Baseline workflow stays local and raw captures remain out of git by default"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs"
      provides: "Phase 5 Criterion groups for before/after hotspot proof"
      contains: "detect_mods_important"
    - path: "performance_baselines/README.md"
      provides: "Contributor guidance for local Phase 5 baseline save/compare usage"
      contains: "save-baseline"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs"
      to: "Criterion baseline workflow"
      via: "Phase 5 benchmark groups"
      pattern: "benchmark_group"
---

<objective>
Prove the Phase 5 hot-path changes with Criterion in the existing scanlog benchmark harness and make the local before/after workflow explicit for future contributors.

Purpose: PERF-04 is phase-gating evidence, not just extra instrumentation. The research locked the benchmark home, input shape, and baseline policy, so this plan depends on the implementation plans and focuses only on credible before/after proof.
Output: New scanlog benchmark groups, bridge-style parser benchmark coverage inside the existing harness, and updated local baseline instructions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-pattern-caching-and-performance/05-CONTEXT.md
@.planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
@ClassicLib-rs/criterion.toml
@performance_baselines/README.md

<interfaces>
Existing benchmark home:
```rust
// ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
criterion_group!(...)
criterion_main!(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend scanlog_benchmarks with Phase 5 hotspot groups</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs
    - ClassicLib-rs/business-logic/classic-scanlog-core/src/mod_detector.rs
    - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs
    - .planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
  </read_first>
  <action>Add dedicated Criterion groups for: cached `detect_mods_single`/`detect_mods_batch`, `detect_mods_important`, and a small Rust helper that mirrors the bridge `detect_crash_pattern` parser-allocation hotspot so the benchmark remains inside the existing harness. Be explicit in code/comments that the crash-pattern benchmark is a Rust-side replica of the bridge pattern, not an FFI benchmark. Use both existing real crash-log fixtures and compact synthetic plugin/mod lists that isolate compile-vs-match cost. Keep setup out of the inner timing loop unless a benchmark is explicitly measuring setup cost.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs` contains a Phase 5 group for `detect_mods_important`
    - `ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs` contains a Phase 5 group for cached regex paths
    - `ClassicLib-rs/business-logic/classic-scanlog-core/benches/scanlog_benchmarks.rs` contains a bridge-style crash-pattern benchmark helper
  </acceptance_criteria>
  <verify>
    <automated>cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks -- --test</automated>
  </verify>
  <done>The existing Criterion harness now measures every locked Phase 5 hotspot.</done>
</task>

<task type="auto">
  <name>Task 2: Document the local baseline save/compare flow for Phase 5 proof</name>
  <files>performance_baselines/README.md</files>
  <read_first>
    - performance_baselines/README.md
    - ClassicLib-rs/criterion.toml
    - .planning/phases/05-pattern-caching-and-performance/05-RESEARCH.md
  </read_first>
  <action>Update `performance_baselines/README.md` with the exact local Criterion commands Phase 5 should use for save/compare verification, including `--save-baseline` and `--baseline`, and restate that raw Criterion baseline directories stay local unless a later request asks for a shareable export. Define "measurable improvement" for this phase as a Criterion baseline comparison that clears normal noise and shows at least a meaningful directional win (use the repo's existing 5% warning threshold as the minimum bar when the signal is stable); if a hotspot does not clear that bar, require the summary to explain why the structural change still shipped per D-12.</action>
  <acceptance_criteria>
    - `performance_baselines/README.md` contains `--save-baseline`
    - `performance_baselines/README.md` contains `--baseline`
    - `performance_baselines/README.md` contains `scanlog_benchmarks`
  </acceptance_criteria>
  <verify>
    <automated>cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks -- --test</automated>
  </verify>
  <done>The repo documents a local-only baseline workflow that future Phase 5 verification can repeat.</done>
</task>

</tasks>

<verification>
- `cargo bench -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml --bench scanlog_benchmarks -- --test` passes.
- Local baseline instructions in `performance_baselines/README.md` match the existing Criterion harness and repo policy.
- The benchmark file covers all locked Phase 5 hotspots in one harness.
</verification>

<success_criteria>
- PERF-04: Criterion benchmark coverage exists for the important matcher, cached regex paths, and bridge-style crash-pattern parsing in the existing harness.
- Contributors have explicit local baseline save/compare instructions and do not need to guess the repo policy.
- Benchmark setup isolates hot-path work rather than accidentally timing unrelated initialization noise.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pattern-caching-and-performance/05-04-SUMMARY.md`
</output>
