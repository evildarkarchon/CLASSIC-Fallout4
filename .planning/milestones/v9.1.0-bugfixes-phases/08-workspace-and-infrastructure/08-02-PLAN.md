---
phase: 08-workspace-and-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs
  - ClassicLib-rs/business-logic/classic-path-core/src/platform/linux.rs
  - ClassicLib-rs/business-logic/classic-path-core/src/platform/mod.rs
  - ClassicLib-rs/business-logic/classic-path-core/tests/linux_proton_docs_path.rs
  - docs/api/classic-path-core.md
  - docs/api/game-setup-workflow.md
autonomous: true
requirements: [INFRA-03, TEST-03]
must_haves:
  truths:
    - "Linux docs-path discovery prefers a valid Proton documents path after cached-path checks"
    - "If Steam metadata fails or the Proton candidate is invalid, Linux falls back to the legacy local-share path before returning not found"
    - "Crate-level integration proof covers the Proton happy path, both fallback branches, and the legacy local-share regression"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs"
      provides: "Shared Proton-aware Linux docs-path workflow"
      contains: "construct_proton_docs_path"
    - path: "ClassicLib-rs/business-logic/classic-path-core/tests/linux_proton_docs_path.rs"
      provides: "Cross-platform integration proof for Proton ordering and fallbacks"
      contains: "proton"
    - path: "docs/api/classic-path-core.md"
      provides: "Contributor-facing Linux docs-path contract aligned to Proton-first behavior"
      contains: "Proton"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs"
      to: "ClassicLib-rs/business-logic/classic-path-core/src/platform/linux.rs"
      via: "shared Linux path workflow now calls the existing Proton helper"
      pattern: "construct_proton_docs_path|parse_steam_library_vdf"
    - from: "ClassicLib-rs/business-logic/classic-path-core/tests/linux_proton_docs_path.rs"
      to: "ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs"
      via: "integration tests prove candidate ordering and fallback behavior without binding duplication"
      pattern: "proton|local_share"
---

<objective>
Make Linux documents-path discovery actually use the existing Steam/Proton helper through the shared `DocsPathFinder` workflow and lock that behavior with crate-level integration coverage.

Purpose: Phase 8 only fulfills the Linux side if Proton support becomes real shared behavior instead of a dormant helper. The proof must live in `classic-path-core`, run with tempdir-backed fixtures, and preserve the existing non-Proton Linux fallback contract.
Output: Updated Linux docs-path selection logic, a new `classic-path-core` integration test file, and aligned API/setup docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
@.planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
@.planning/phases/08-workspace-and-infrastructure/08-VALIDATION.md
@docs/api/README.md
@docs/api/classic-path-core.md
@docs/api/game-setup-workflow.md
@ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs
@ClassicLib-rs/business-logic/classic-path-core/src/platform/linux.rs
@ClassicLib-rs/business-logic/classic-path-core/src/platform/mod.rs

<interfaces>
From `ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs`:
```rust
pub struct DocsPathFinder {
    pub fn find_docs_path(&self) -> DocsPathResult<PathBuf>;
}
```

From `ClassicLib-rs/business-logic/classic-path-core/src/platform/linux.rs`:
```rust
pub fn parse_steam_library_vdf(app_id: u32) -> DocsPathResult<PathBuf>;
pub fn construct_proton_docs_path(steam_library: &Path, app_id: u32, relative_path: &str) -> PathBuf;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Proton-first Linux candidate selection into `DocsPathFinder`</name>
  <files>ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs, ClassicLib-rs/business-logic/classic-path-core/src/platform/linux.rs, ClassicLib-rs/business-logic/classic-path-core/src/platform/mod.rs</files>
  <read_first>
    - ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs
    - ClassicLib-rs/business-logic/classic-path-core/src/platform/linux.rs
    - ClassicLib-rs/business-logic/classic-path-core/src/platform/mod.rs
    - .planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
    - .planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: after cached-path checks, a valid Proton docs path wins over a valid local-share path on Linux.
    - Test 2: if Steam metadata lookup fails, the legacy `~/.local/share/<relative_path>` fallback still succeeds.
    - Test 3: if Steam metadata resolves but the Proton path is invalid, the local-share fallback still succeeds.
  </behavior>
  <action>Keep the public `DocsPathFinder` contract stable, but make its Linux branch use the existing `parse_steam_library_vdf()` plus `construct_proton_docs_path()` helpers in Proton-first order per D-01 through D-04. Prefer a small extracted helper or injected candidate-selection path if needed so the new behavior can be tested on this Windows-hosted workflow without requiring native Linux execution. Do not move this logic into bindings, do not add Fallout 4 VR Proton logic, and do not weaken the legacy local-share fallback.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs` contains `construct_proton_docs_path`
    - `ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs` contains `parse_steam_library_vdf`
    - `ClassicLib-rs/business-logic/classic-path-core/src/platform/linux.rs` contains `377160`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml proton -- --nocapture</automated>
  </verify>
  <done>The shared Linux docs-path workflow becomes Proton-aware with Proton-first ordering and retained local-share fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add crate-level Proton integration proof and align the path-detection docs</name>
  <files>ClassicLib-rs/business-logic/classic-path-core/tests/linux_proton_docs_path.rs, docs/api/classic-path-core.md, docs/api/game-setup-workflow.md</files>
  <read_first>
    - docs/api/classic-path-core.md
    - docs/api/game-setup-workflow.md
    - ClassicLib-rs/business-logic/classic-path-core/src/docs_path.rs
    - .planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
    - .planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a mock Proton prefix structure yields the Fallout 4 documents path.
    - Test 2: invalid Proton path and Steam lookup failure both fall back to local-share.
    - Test 3: the old local-share-only case still has explicit regression coverage.
  </behavior>
  <action>Create `classic-path-core/tests/linux_proton_docs_path.rs` as the required Phase 8 integration proof using temp directories and injected path inputs. Cover the happy path, both fallback branches, and the legacy local-share regression named in D-06. Update `docs/api/classic-path-core.md` and `docs/api/game-setup-workflow.md` so contributor-visible path detection describes cached path first, Proton second, and local-share fallback third. Keep the docs focused on active behavior; do not document out-of-scope Fallout 4 VR Linux support.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-path-core/tests/linux_proton_docs_path.rs` contains `TempDir`
    - `ClassicLib-rs/business-logic/classic-path-core/tests/linux_proton_docs_path.rs` contains `local_share`
    - `docs/api/classic-path-core.md` contains `Proton`
    - `docs/api/game-setup-workflow.md` contains `documents path`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml proton && cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>`classic-path-core` owns the Linux Proton proof and the active docs describe the same ordering the code implements.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml proton` passes.
- `cargo test -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
</verification>

<success_criteria>
- Proton path support is implemented once in shared Rust path discovery.
- Linux callers get Proton-first behavior with explicit local-share fallback.
- The required integration proof runs from `classic-path-core` and active docs match it.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-and-infrastructure/08-02-SUMMARY.md`
</output>
