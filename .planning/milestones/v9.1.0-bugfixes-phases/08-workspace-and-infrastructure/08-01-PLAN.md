---
phase: 08-workspace-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/Cargo.toml
  - ClassicLib-rs/business-logic/classic-path-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml
  - ClassicLib-rs/foundation/classic-shared-core/Cargo.toml
  - ClassicLib-rs/foundation/classic-shared-core/src/lib.rs
  - ClassicLib-rs/foundation/classic-shared-core/src/async_bridge.rs
  - docs/api/classic-shared-core.md
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-04]
must_haves:
  truths:
    - "`winreg` and `phf` are owned from `ClassicLib-rs/Cargo.toml` with member manifests inheriting via `workspace = true`"
    - "The `classic-shared-core` `zerovec` workaround is removed rather than preserved as a historical note"
    - "Any direct blocker cleanup stays tightly adjacent to `gui-bridge` and the touched docs stop implying the workaround still exists"
  artifacts:
    - path: "ClassicLib-rs/Cargo.toml"
      provides: "Workspace dependency source of truth for `winreg` and `phf`"
      contains: "workspace.dependencies"
    - path: "ClassicLib-rs/business-logic/classic-path-core/Cargo.toml"
      provides: "Windows-only inherited `winreg` dependency"
      contains: "workspace = true"
    - path: "ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml"
      provides: "Inherited `phf` dependency"
      contains: "workspace = true"
    - path: "ClassicLib-rs/foundation/classic-shared-core/Cargo.toml"
      provides: "`zerovec` workaround removal point"
      contains: "classic-shared-core"
    - path: "docs/api/classic-shared-core.md"
      provides: "Contributor-facing gui-bridge contract aligned after workaround removal"
      contains: "gui-bridge"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-path-core/Cargo.toml"
      to: "ClassicLib-rs/Cargo.toml"
      via: "Windows registry dependency inherits from the workspace instead of a crate-local pin"
      pattern: "winreg|workspace = true"
    - from: "ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml"
      to: "ClassicLib-rs/Cargo.toml"
      via: "`phf` version ownership moves to the workspace"
      pattern: "phf|workspace = true"
    - from: "ClassicLib-rs/foundation/classic-shared-core/Cargo.toml"
      to: "ClassicLib-rs/foundation/classic-shared-core/src/lib.rs"
      via: "`gui-bridge` still compiles after the workaround removal"
      pattern: "gui-bridge"
---

<objective>
Promote the remaining shared dependency pins to workspace ownership and remove the lingering `zerovec` workaround from `classic-shared-core` with build proof.

Purpose: Phase 8 should start by resolving the manifest-level drift and the stale ICU/Slint workaround before touching Linux path behavior or Node contract governance. This keeps the infra cleanup small, proves whether `gui-bridge` still needs the workaround, and avoids mixing it with Proton-specific logic.
Output: Updated workspace/member manifests, cleaned `classic-shared-core` dependency wiring, and aligned `classic-shared-core` API docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
@.planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
@.planning/phases/08-workspace-and-infrastructure/08-VALIDATION.md
@docs/api/README.md
@docs/api/classic-shared-core.md
@ClassicLib-rs/Cargo.toml
@ClassicLib-rs/business-logic/classic-path-core/Cargo.toml
@ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml
@ClassicLib-rs/foundation/classic-shared-core/Cargo.toml
@ClassicLib-rs/foundation/classic-shared-core/src/lib.rs
@ClassicLib-rs/foundation/classic-shared-core/src/async_bridge.rs

<interfaces>
From `ClassicLib-rs/foundation/classic-shared-core/src/lib.rs`:
```rust
#[cfg(feature = "gui-bridge")]
pub mod async_bridge;
```

From `ClassicLib-rs/business-logic/classic-path-core/Cargo.toml`:
```toml
[target.'cfg(windows)'.dependencies]
winreg = "..."
```

From `ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml`:
```toml
[dependencies]
phf = { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote `winreg` and `phf` to workspace-owned dependency declarations</name>
  <files>ClassicLib-rs/Cargo.toml, ClassicLib-rs/business-logic/classic-path-core/Cargo.toml, ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml</files>
  <read_first>
    - ClassicLib-rs/Cargo.toml
    - ClassicLib-rs/business-logic/classic-path-core/Cargo.toml
    - ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml
    - .planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
    - .planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
  </read_first>
  <action>Move `winreg` and `phf` version ownership into `[workspace.dependencies]` in `ClassicLib-rs/Cargo.toml` without changing versions. Update `classic-path-core` to inherit `winreg` through `workspace = true` inside its existing `target.'cfg(windows)'.dependencies` block, and update `classic-constants-core` to inherit `phf` via `workspace = true`. Keep this scoped to ownership cleanup only: no opportunistic dependency upgrades, no unrelated manifest reshuffling, and no new shared deps.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/Cargo.toml` contains `winreg`
    - `ClassicLib-rs/Cargo.toml` contains `phf`
    - `ClassicLib-rs/business-logic/classic-path-core/Cargo.toml` contains `workspace = true`
    - `ClassicLib-rs/business-logic/classic-constants-core/Cargo.toml` contains `workspace = true`
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml && cargo check -p classic-constants-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>The shared dependency pins are centrally owned in the workspace and the affected crates build using inherited declarations.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the `zerovec` workaround and any directly blocking stale gui-bridge code</name>
  <files>ClassicLib-rs/foundation/classic-shared-core/Cargo.toml, ClassicLib-rs/foundation/classic-shared-core/src/lib.rs, ClassicLib-rs/foundation/classic-shared-core/src/async_bridge.rs, docs/api/classic-shared-core.md</files>
  <read_first>
    - ClassicLib-rs/foundation/classic-shared-core/Cargo.toml
    - ClassicLib-rs/foundation/classic-shared-core/src/lib.rs
    - ClassicLib-rs/foundation/classic-shared-core/src/async_bridge.rs
    - docs/api/classic-shared-core.md
    - docs/api/README.md
    - .planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
    - .planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
  </read_first>
  <action>Delete the direct `zerovec` workaround from `classic-shared-core` first, then fix only the narrowly adjacent `gui-bridge` code or docs required to keep the crate healthy. Do not preserve historical workaround comments, do not broaden into a repo-wide Slint cleanup, and do not redesign the public `gui-bridge` surface unless a minimal blocker removal demands it. If source or docs still describe the workaround after the manifest cleanup, rewrite them to match the post-removal state in the same change.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/foundation/classic-shared-core/Cargo.toml` contains `gui-bridge`
    - `ClassicLib-rs/foundation/classic-shared-core/src/lib.rs` contains `async_bridge`
    - `docs/api/classic-shared-core.md` contains `gui-bridge`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p classic-shared-core --features gui-bridge --manifest-path ClassicLib-rs/Cargo.toml && cargo test --workspace --release --all-features --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <done>The workaround is gone, any direct blocker cleanup stays local to `classic-shared-core`, and the crate still passes the locked build/test proof.</done>
</task>

</tasks>

<verification>
- `cargo check -p classic-path-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo check -p classic-constants-core --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo test -p classic-shared-core --features gui-bridge --manifest-path ClassicLib-rs/Cargo.toml` passes.
- `cargo test --workspace --release --all-features --manifest-path ClassicLib-rs/Cargo.toml` passes after the workaround removal.
</verification>

<success_criteria>
- The workspace, not member manifests, is the source of truth for `winreg` and `phf`.
- `classic-shared-core` no longer carries the Phase 8-targeted `zerovec` workaround.
- Any blocker cleanup is narrow and the touched API doc matches the post-removal state.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-and-infrastructure/08-01-SUMMARY.md`
</output>
