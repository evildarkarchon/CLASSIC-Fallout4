---
phase: 08-workspace-and-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - ClassicLib-rs/node-bindings/classic-node/.gitignore
  - ClassicLib-rs/node-bindings/classic-node/package.json
  - ClassicLib-rs/node-bindings/classic-node/index.d.ts
  - docs/api/binding-parity-overview.md
  - docs/api/binding-contract-refresh-note.md
  - docs/implementation/node_api_parity/governance/gate_contract_baseline.md
autonomous: true
requirements: [INFRA-05]
must_haves:
  truths:
    - "`classic-node/index.d.ts` is treated as a tracked generated contract artifact rather than a build-only byproduct"
    - "Public Node export changes require same-change declaration refresh plus the existing freshness/parity checks"
    - "Contributor-facing docs stop implying a build is required just to inspect Node types"
  artifacts:
    - path: "ClassicLib-rs/node-bindings/classic-node/.gitignore"
      provides: "Tracking policy no longer contradicts the committed declaration snapshot"
      contains: "index.d.ts"
    - path: "ClassicLib-rs/node-bindings/classic-node/package.json"
      provides: "Repo-standard declaration freshness and parity command source"
      contains: "dts:freshness:check"
    - path: "ClassicLib-rs/node-bindings/classic-node/index.d.ts"
      provides: "Committed NAPI-RS declaration snapshot"
      contains: "export"
    - path: "docs/api/binding-contract-refresh-note.md"
      provides: "Contributor workflow for same-change Node contract refresh"
      contains: "index.d.ts"
  key_links:
    - from: "ClassicLib-rs/node-bindings/classic-node/package.json"
      to: "tools/node_api_parity/check_dts_freshness.py"
      via: "existing freshness script remains the authoritative check-only gate"
      pattern: "dts:freshness:check|check_dts_freshness.py"
    - from: "docs/api/binding-parity-overview.md"
      to: "ClassicLib-rs/node-bindings/classic-node/index.d.ts"
      via: "public Node contract docs point directly at the committed declaration snapshot"
      pattern: "index.d.ts"
---

<objective>
Formalize the Node declaration snapshot as a tracked contract artifact and align the existing freshness/parity workflow around that policy.

Purpose: The repo already has the right freshness commands and a committed `index.d.ts`, but the `.gitignore` and contributor docs still tell a contradictory build-first story. This plan makes the tracked snapshot authoritative without inventing new scripts or CI jobs.
Output: Clean `classic-node` tracking policy, refreshed declaration snapshot, and contributor/parity docs that require same-change refresh and verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
@.planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
@.planning/phases/08-workspace-and-infrastructure/08-VALIDATION.md
@docs/api/README.md
@docs/api/binding-parity-overview.md
@docs/api/binding-contract-refresh-note.md
@docs/implementation/node_api_parity/governance/gate_contract_baseline.md
@ClassicLib-rs/node-bindings/classic-node/package.json
@ClassicLib-rs/node-bindings/classic-node/.gitignore
@ClassicLib-rs/node-bindings/classic-node/index.d.ts
@ClassicLib-rs/node-bindings/classic-node/__test__/regression_drift.spec.ts
@tools/node_api_parity/check_dts_freshness.py

<interfaces>
From `ClassicLib-rs/node-bindings/classic-node/package.json`:
```json
{
  "scripts": {
    "dts:freshness:check": "...",
    "dts:freshness:local": "...",
    "parity:gate:local": "..."
  }
}
```

From `ClassicLib-rs/node-bindings/classic-node/index.d.ts`:
```ts
export ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align tracking policy and docs around the committed `index.d.ts` contract</name>
  <files>ClassicLib-rs/node-bindings/classic-node/.gitignore, ClassicLib-rs/node-bindings/classic-node/package.json, docs/api/binding-parity-overview.md, docs/api/binding-contract-refresh-note.md, docs/implementation/node_api_parity/governance/gate_contract_baseline.md</files>
  <read_first>
    - ClassicLib-rs/node-bindings/classic-node/.gitignore
    - ClassicLib-rs/node-bindings/classic-node/package.json
    - docs/api/binding-parity-overview.md
    - docs/api/binding-contract-refresh-note.md
    - docs/implementation/node_api_parity/governance/gate_contract_baseline.md
    - .planning/phases/08-workspace-and-infrastructure/08-CONTEXT.md
  </read_first>
  <action>Remove the `.gitignore` contradiction around `index.d.ts` and keep `package.json` focused on the existing `dts:freshness:*` and parity commands rather than adding a second freshness workflow. Update the touched docs so they explicitly say `index.d.ts` is a tracked generated artifact, public export changes must refresh it in the same change, and contributors can inspect the committed snapshot without building first. Do not promise handwritten editing of the declaration file, and do not add new CI jobs unless the existing gate is objectively missing.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/node-bindings/classic-node/package.json` contains `dts:freshness:check`
    - `docs/api/binding-parity-overview.md` contains `index.d.ts`
    - `docs/api/binding-contract-refresh-note.md` contains `same change`
    - `docs/implementation/node_api_parity/governance/gate_contract_baseline.md` contains `dts:freshness:check`
  </acceptance_criteria>
  <verify>
    <automated>cd ClassicLib-rs/node-bindings/classic-node && bun run dts:freshness:check</automated>
  </verify>
  <done>The repo no longer has conflicting guidance about whether `index.d.ts` should be tracked or whether a build is required to inspect it.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the Node declaration snapshot and run the existing parity/freshness gate sequence</name>
  <files>ClassicLib-rs/node-bindings/classic-node/index.d.ts</files>
  <read_first>
    - ClassicLib-rs/node-bindings/classic-node/index.d.ts
    - ClassicLib-rs/node-bindings/classic-node/__test__/regression_drift.spec.ts
    - tools/node_api_parity/check_dts_freshness.py
    - .planning/phases/08-workspace-and-infrastructure/08-RESEARCH.md
  </read_first>
  <action>Refresh `ClassicLib-rs/node-bindings/classic-node/index.d.ts` using the repo's existing NAPI-RS regeneration path, then commit the generated result as the contract snapshot for the current public exports. Treat the generator output as authoritative: do not hand-edit the declaration file except for repository-owned generated headers if the toolchain already does so. Finish by running the full local Node gate sequence already captured in research so the phase proves freshness, Bun tests, Node tests, and parity together.</action>
  <acceptance_criteria>
    - `ClassicLib-rs/node-bindings/classic-node/index.d.ts` contains `export`
  </acceptance_criteria>
  <verify>
    <automated>cd ClassicLib-rs/node-bindings/classic-node && bun run parity:gate:local && bun run test:bun && bun run test:node && bun run dts:freshness:check</automated>
  </verify>
  <done>The committed declaration snapshot matches generated output and the existing local Node contract gates are green.</done>
</task>

</tasks>

<verification>
- `cd ClassicLib-rs/node-bindings/classic-node && bun run dts:freshness:check` passes.
- `cd ClassicLib-rs/node-bindings/classic-node && bun run parity:gate:local && bun run test:bun && bun run test:node` passes.
</verification>

<success_criteria>
- `index.d.ts` is clearly a tracked generated contract artifact.
- The repo reuses the existing freshness/parity workflow instead of inventing a duplicate.
- The touched docs consistently require same-change refresh for public Node contract updates.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-and-infrastructure/08-03-SUMMARY.md`
</output>
