---
phase: 03-fcx-state-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/src/lib.rs
  - docs/api/classic-scanlog-core.md
autonomous: true
requirements: [SAFE-01, CONS-02, TEST-01]
must_haves:
  truths:
    - "FCX reset waits for the mutex instead of silently skipping under contention"
    - "Core callers can distinguish successful reset from the unnecessary/no-op path"
    - "A contention test proves stale FCX state is cleared once the mutex becomes available"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs"
      provides: "Blocking FCX reset contract plus contention coverage"
      contains: "FcxResetError"
    - path: "ClassicLib-rs/business-logic/classic-scanlog-core/src/lib.rs"
      provides: "Public re-export of the reset error contract"
      contains: "FcxResetError"
    - path: "docs/api/classic-scanlog-core.md"
      provides: "Contributor-facing FCX reset contract documentation"
      contains: "reset_global_state"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs"
      to: "GLOBAL_FCX_HANDLER"
      via: "blocking lock() inside reset_global_state"
      pattern: "GLOBAL_FCX_HANDLER\\.lock\\(\\)"
    - from: "ClassicLib-rs/business-logic/classic-scanlog-core/src/lib.rs"
      to: "ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs"
      via: "pub use"
      pattern: "pub use fcx_handler::.*FcxResetError"
---

<objective>
Harden the core FCX reset contract before any binding work so every downstream caller gets deterministic reset behavior under contention.

Purpose: SAFE-01 and CONS-02 both originate in the core singleton. Fixing them first prevents the C++ and Node plans from baking in ad-hoc recovery logic.
Output: A blocking `reset_global_state() -> Result<(), FcxResetError>` contract with contention tests and updated API docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fcx-state-hardening/03-CONTEXT.md
@.planning/phases/03-fcx-state-hardening/03-RESEARCH.md
@docs/api/README.md

<interfaces>
From `ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs`:
```rust
pub static GLOBAL_FCX_HANDLER: Lazy<Mutex<FcxModeHandler>> =
    Lazy::new(|| Mutex::new(FcxModeHandler::new(false)));

pub struct ConfigIssue {
    pub file_path: String,
    pub section: Option<String>,
    pub setting: String,
    pub current_value: String,
    pub recommended_value: String,
    pub description: String,
    pub severity: String,
}

pub struct FcxModeHandler {
    pub fcx_mode: bool,
    pub main_files_check: Option<String>,
    pub game_files_check: Option<String>,
    pub detected_issues: Vec<ConfigIssue>,
    pub checks_run: bool,
}
```

Current reset implementation to replace:
```rust
pub fn reset_global_state() {
    if let Some(mut handler) = GLOBAL_FCX_HANDLER.try_lock() {
        handler.reset();
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the FCX reset contract and contention tests</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/fcx_handler.rs</files>
  <behavior>
    - Test 1: dirty global FCX state returns success and clears cached results/issues/checks_run.
    - Test 2: already-clean global FCX state returns the non-fatal `Unnecessary` outcome.
    - Test 3: a caller blocked behind another thread holding the mutex eventually succeeds once the lock is released.
  </behavior>
  <action>Write the failing tests first in `fcx_handler.rs`, then implement the contract in the same file. Replace `try_lock()` with blocking `lock()` per D-05, keep the API shape `Result<(), FcxResetError>` per D-05, and treat the already-clean path as a typed non-fatal outcome per D-07. Use only the existing `GLOBAL_FCX_HANDLER` + `FcxModeHandler` state to determine whether a reset is necessary; do not introduce a second singleton or alternate runtime. The contention test should hold the parking_lot mutex on one thread, call `reset_global_state()` on another, then release the lock and assert the stale FCX fields were actually cleared (implements TEST-01).</action>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml -- fcx_reset</automated>
  </verify>
  <done>`fcx_handler.rs` exposes `FcxResetError`, `reset_global_state()` blocks on the mutex instead of skipping, and the targeted FCX reset tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export and document the new reset contract</name>
  <files>ClassicLib-rs/business-logic/classic-scanlog-core/src/lib.rs, docs/api/classic-scanlog-core.md</files>
  <action>Update the crate root to re-export `FcxResetError` so bindings can pattern-match on the core contract without deep module imports. Then update `docs/api/classic-scanlog-core.md` to describe the blocking reset behavior, the typed `Result<(), FcxResetError>` contract, and the expectation that bindings auto-reset at scan start while treating `Unnecessary` as benign. Keep the documentation scoped to contributor-visible behavior; do not promise C++ issue inspection or scan-result payload changes because D-08 and D-09 forbid them.</action>
  <verify>
    <automated>cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml -- fcx_reset && cargo clippy -p classic-scanlog-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings</automated>
  </verify>
  <done>`classic-scanlog-core` publicly exports `FcxResetError`, the API guide reflects the new contract, and targeted tests/lints stay green.</done>
</task>

</tasks>

<verification>
- `cargo test -p classic-scanlog-core --manifest-path ClassicLib-rs/Cargo.toml -- fcx_reset` passes.
- `cargo clippy -p classic-scanlog-core --all-targets --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` passes.
- `docs/api/classic-scanlog-core.md` documents the new reset contract and no longer implies resets can silently skip.
</verification>

<success_criteria>
- SAFE-01: the core reset path uses `lock()` and cannot disappear under contention.
- CONS-02: callers can distinguish success from the unnecessary/no-op outcome via `FcxResetError`.
- TEST-01: a contention test proves the reset eventually succeeds once the mutex is released.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fcx-state-hardening/03-01-SUMMARY.md`
</output>
