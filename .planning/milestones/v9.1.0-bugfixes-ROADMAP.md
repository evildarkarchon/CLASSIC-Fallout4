# Milestone v1.0: CLASSIC Codebase Cleanup

**Status:** ✅ SHIPPED 2026-02-02
**Phases:** 1-5
**Total Plans:** 14

## Overview

This cleanup milestone proceeded bottom-up through dependency layers: remove dead code and fix global state (low risk, high noise reduction), simplify the integration layer (clarify ownership boundaries), thin out fat Python wrappers by moving logic to Rust (highest impact), consolidate dual sync/async interfaces (remove deprecated patterns), and finally prune Python fallback files (irreversible, requires proven Rust stability). Each phase unblocked the next by reducing ambiguity about what is live, what is dead, and where logic belongs.

## Phases

### Phase 1: Foundation Cleanup
**Goal**: The codebase contains only live code, global state is test-friendly, and CI prevents dead code regression
**Depends on**: Nothing (first phase)
**Requirements**: DEAD-01, DEAD-02, DEAD-03, DEAD-04, GLOB-01, GLOB-02, GLOB-03
**Plans**: 4 plans

Plans:
- [x] 01-01: Dead code removal (remove deprecated files, verify Rust crates, establish coverage baseline)
- [x] 01-02: CI tooling setup (install vulture, curate whitelist, add to CI pipeline)
- [x] 01-03: Global state cleanup (replace mutable flags, audit globals, add reset fixture)
- [x] 01-04: Gap closure (fix test MessageHandler init, refine DEPRECATED criterion scope)

**Details:**
- Removed deprecated code (_DeprecatedVersion class, database_rust.py shim)
- Established 71% coverage baseline
- Installed Vulture 2.14 with CI enforcement; removed true dead code
- Replaced mutable _VERSION_WARNING_LOGGED flag with lru_cache
- Created comprehensive reset_all_singletons autouse fixture covering 19+ globals
- Fixed 4 tests with missing MessageHandler initialization

### Phase 2: Integration Layer Simplification
**Goal**: The Python-Rust integration boundary uses a single-layer factory with direct try-import, no redundant detection/caching layers
**Depends on**: Phase 1
**Requirements**: ARCH-01, ARCH-02, ARCH-03
**Plans**: 2 plans

Plans:
- [x] 02-01: Factory flattening (collapse factory/ subpackage + detector.py + status.py into single flat factory.py)
- [x] 02-02: Acceleration removal and type narrowing (delete acceleration/ package, create Protocol types, narrow factory return types)

**Details:**
- Consolidated 8 factory submodules + detector.py + status.py + config.py into single flat factory.py (~948 lines)
- 80+ files updated with new import paths
- Deleted 960-line ClassicLib/acceleration/ package
- Created ClassicLib/integration/types.py with 13 Protocol classes
- pyright passes with 0 errors

### Phase 3: Wrapper Thinning
**Goal**: Python wrappers in ClassicLib/integration/rust/ are thin adapters (type conversion only), with business logic living in Rust -core crates
**Depends on**: Phase 2
**Requirements**: ARCH-04
**Plans**: 2 plans

Plans:
- [x] 03-01: FileIO wrapper thinning (937 lines → 230, removed SyncWrapper and convenience functions)
- [x] 03-02: Parser and FormID wrapper thinning (321 + 326 lines → 122 + 128, removed hasattr/legacy patterns)

**Details:**
- file_io_rust.py thinned 75% (937→230 lines)
- parser_rust.py 62% reduction (321→122 lines)
- formid_rust.py 61% reduction (326→128 lines)
- Established thin delegation pattern: convert args → call Rust → convert return
- Accepted deviation: file_io at 230 lines (15% over 200-line target due to fallback paths, resolved in Phase 5)

### Phase 4: Interface Consolidation
**Goal**: The codebase has exactly two async patterns — native async (CLI/TUI) and AsyncBridge (GUI) — with no deprecated sync wrappers
**Depends on**: Phase 3
**Requirements**: REDN-01, REDN-02, REDN-04
**Plans**: 3 plans

Plans:
- [x] 04-01: FormID sync wrapper removal (delete FormIDAnalyzer.py, update callers to FormIDAnalyzerCore)
- [x] 04-02: Bridge helper + sync adapter removal (remove create_sync_wrapper, delete bridge_helpers.py and sync_adapters.py)
- [x] 04-03: YAML sync/ directory consolidation (move sync/ files to parent yaml/, delete sync/ directory)

**Details:**
- Removed FormIDAnalyzer.py sync wrapper (155 lines)
- Deleted create_sync_wrapper + 396 lines of sync infrastructure
- Migrated 8 callers to direct AsyncBridge.run_async() calls
- Moved yaml/sync/cache.py and yaml/sync/convenience.py to yaml/ level
- Deleted entire yaml/sync/ directory
- Pre-existing GUI file path resolution bug discovered (not Phase 4 regression)

### Phase 5: Fallback Pruning
**Goal**: Python fallback implementations are removed where Rust is proven stable, and factory returns typed Rust objects directly
**Depends on**: Phase 4
**Requirements**: REDN-03
**Plans**: 3 plans

Plans:
- [x] 05-01: Remove 5 easy fallbacks (database, file_io, formid, record, report) + remove CLASSIC_DISABLE_RUST mechanism
- [x] 05-02: Remove 3 hard fallbacks (parser, mod_detector, plugin) + delete integration/python/ directory
- [x] 05-03: Add startup Rust validation, clean spec files, PyInstaller build verification

**Details:**
- Deleted all 8 fallback files (database_py, file_io_py, formid_py, record_py, report_py, parser_py, mod_detector_py, plugin_py)
- Eliminated integration/python/ directory entirely
- Completely removed _is_rust_disabled() and CLASSIC_DISABLE_RUST mechanism
- Established RuntimeError pattern for all 7 factory functions
- Added validate_rust_modules() startup validation for 6 required Rust modules
- Cleaned stale hiddenimports from all 6 spec files
- Fixed pre-existing pyffi VERSION file packaging gap
- Verified PyInstaller build succeeds

---

## Milestone Summary

**Key Decisions:**
- 5-phase bottom-up approach: dead code first, fallback pruning last (irreversible)
- Phase 1 combines DEAD and GLOB requirements (both foundation, no dependencies)
- lru_cache(maxsize=1) replaces mutable global flags (testable via cache_clear)
- Autouse reset_all_singletons fixture covers 19+ globals in 4 categories
- get_yamldata keeps Any (Rust/Python interfaces incompatible)
- Protocols are static-only (no @runtime_checkable)
- Thin delegation pattern: convert args → call Rust → convert return
- Two async patterns only: native async (CLI/TUI) and AsyncBridge (GUI)
- RuntimeError pattern for missing Rust modules (fail-fast, no silent degradation)
- validate_rust_modules() at startup for early failure detection

**Issues Resolved:**
- Fixed 4 tests with missing MessageHandler initialization
- Fixed 7 stale test references to removed Python fallbacks
- Fixed pre-existing pyffi VERSION file packaging gap
- Fixed _check_source_installation and _check_local_dir path depth bugs

**Issues Deferred:**
- Pre-existing classic-yaml-core test_clear_cache failure (cache size assertion)
- Pre-existing GUI file path resolution bug in classic_settings() (relative path)
- Settings e2e test state leakage (pre-existing, documented)

**Technical Debt Incurred:**
- cache.py line 431: return {} metrics placeholder (not implemented)
- _async_utils package kept for backward compatibility re-exports

---

_For current project status, see .planning/ROADMAP.md_
