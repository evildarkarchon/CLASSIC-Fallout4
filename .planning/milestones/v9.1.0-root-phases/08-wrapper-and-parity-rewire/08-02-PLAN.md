---
phase: 08-wrapper-and-parity-rewire
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - classic-cli/CMakeLists.txt
  - classic-gui/CMakeLists.txt
  - ui-applications/classic-tui/src/main.rs
  - tests/powershell/cpp_build_scripts.test.ps1
autonomous: true
requirements: [INTG-01]
must_haves:
  truths:
    - "CLI and GUI native integration flows resolve the relocated CXX bridge from repo-root paths."
    - "The TUI stays a direct repo-root Cargo entrypoint per D-04."
    - "Phase 8 can prove a lightweight TUI smoke run without entering the full alternate-screen UI per D-15."
  artifacts:
    - path: "classic-cli/CMakeLists.txt"
      provides: "Repo-root CXX bridge include path for CLI builds"
    - path: "classic-gui/CMakeLists.txt"
      provides: "Repo-root CXX bridge include path for GUI builds"
    - path: "ui-applications/classic-tui/src/main.rs"
      provides: "Fast-exit TUI smoke flag handling before terminal setup"
  key_links:
    - from: "classic-cli/CMakeLists.txt"
      to: "cpp-bindings/classic-cpp-bridge/include"
      via: "target_include_directories"
      pattern: "cpp-bindings/classic-cpp-bridge/include"
    - from: "classic-gui/CMakeLists.txt"
      to: "cpp-bindings/classic-cpp-bridge/include"
      via: "target_include_directories"
      pattern: "cpp-bindings/classic-cpp-bridge/include"
    - from: "ui-applications/classic-tui/src/main.rs"
      to: "cargo run -p classic-tui -- --version"
      via: "pre-terminal CLI arg handling"
      pattern: "std::env::args|--version|--help"
---

<objective>
Finish the native frontend and TUI repo-root proof surfaces after crate relocation.

Purpose: Close the native frontend half of INTG-01 with real repo-root paths instead of legacy includes or build-only proof.
Output: Rewired CLI/GUI bridge includes, lightweight TUI smoke support, and native-path regression coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wrapper-and-parity-rewire/08-CONTEXT.md
@.planning/phases/08-wrapper-and-parity-rewire/08-RESEARCH.md
@classic-cli/CMakeLists.txt
@classic-gui/CMakeLists.txt
@ui-applications/classic-tui/src/main.rs
@tests/powershell/cpp_build_scripts.test.ps1

<interfaces>
From `classic-cli/CMakeLists.txt`:
```cmake
corrosion_import_crate(
    MANIFEST_PATH ${CMAKE_SOURCE_DIR}/../Cargo.toml
    CRATES classic-cpp-bridge
)
target_include_directories(classic_cxx_bridge PUBLIC
    "${CMAKE_SOURCE_DIR}/../ClassicLib-rs/cpp-bindings/classic-cpp-bridge/include"
)
```

From `ui-applications/classic-tui/src/main.rs`:
```rust
fn main() -> color_eyre::Result<()> {
    color_eyre::install()?;
    let _log_guard = init_logging();
    let _ = classic_shared_core::get_runtime();
    execute!(stderr_handle, EnterAlternateScreen, EnableMouseCapture, Hide)?;
    enable_raw_mode()?;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock native path and smoke-proof expectations</name>
  <files>tests/powershell/cpp_build_scripts.test.ps1</files>
  <action>Extend the existing PowerShell build-script audit so it asserts the CLI and GUI CMake entrypoints no longer reference `ClassicLib-rs/cpp-bindings/...` and instead include the repo-root `cpp-bindings/classic-cpp-bridge/include` path per D-05. Add behavior-oriented assertions that the chosen TUI smoke flag is handled before alternate-screen setup, using minimal pre-terminal logic instead of text-shape checks alone. Addresses review concerns: stronger TUI proof and no accidental CLI dependency creep.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File tests/powershell/cpp_build_scripts.test.ps1</automated>
  </verify>
  <done>The native/TUI contract test fails if CMake include paths or TUI smoke behavior drift back to the legacy layout.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire CLI/GUI bridge includes and add a lightweight TUI smoke flag</name>
  <files>classic-cli/CMakeLists.txt, classic-gui/CMakeLists.txt, ui-applications/classic-tui/src/main.rs</files>
  <action>Update both native CMake files so `target_include_directories(classic_cxx_bridge ...)` points at repo-root `cpp-bindings/classic-cpp-bridge/include` per D-05 and D-14. In `ui-applications/classic-tui/src/main.rs`, add minimal pre-terminal argument handling implemented with `std::env::args()` only so `cargo run -p classic-tui -- --version` (or the chosen equivalent) prints and exits before `EnterAlternateScreen`/`enable_raw_mode()` per D-04 and D-15. Do not add a new TUI wrapper script, do not pull in a new CLI parsing crate, and do not broaden this into general CLI redesign.</action>
  <verify>
    <automated>pwsh -ExecutionPolicy Bypass -File tests/powershell/cpp_build_scripts.test.ps1</automated>
  </verify>
  <done>CLI and GUI CMake files use repo-root bridge includes, and the TUI exposes one lightweight repo-root smoke command that exits before full UI startup.</done>
</task>

</tasks>

<verification>
During execution, finish proof with the real native/TUI entrypoints:
- `pwsh -ExecutionPolicy Bypass -File classic-cli/build_cli.ps1 -Test`
- `pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test`
- `cargo run -p classic-tui -- --version`

If local Qt is unavailable, use the existing wrapper-supported fallback preset rather than changing the proof surface.
</verification>

<success_criteria>
- Native CLI/GUI builds no longer depend on `ClassicLib-rs/cpp-bindings/...` include paths.
- The TUI remains a repo-root Cargo flow and exposes a lightweight smoke command implemented without a new CLI dependency.
- Regression coverage catches any return of legacy include paths or interactive-only TUI startup.
</success_criteria>

<output>
After completion, create `.planning/phases/08-wrapper-and-parity-rewire/08-02-SUMMARY.md`
</output>
