---
phase: 10-docs-guidance-and-tripwires
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/workspace-migration-matrix.md
  - README.md
  - docs/README.md
  - docs/RUST_DOCUMENTATION_INDEX.md
  - docs/testing/TESTING_GUIDE_INDEX.md
autonomous: true
requirements: [DOCS-01, DOCS-02]
must_haves:
  truths:
    - Contributors can find one migration matrix for old-to-new workflow translation.
    - Top-level docs teach repo-root commands and root-level workspace paths.
    - High-traffic contributor entrypoints link to the shared migration matrix.
  artifacts:
    - path: docs/workspace-migration-matrix.md
      provides: Shared command, path, and artifact translation reference.
    - path: README.md
      provides: Repo-root contributor entrypoint.
    - path: docs/README.md
      provides: Docs hub routed through the migration matrix.
    - path: docs/RUST_DOCUMENTATION_INDEX.md
      provides: Rust contributor index aligned to repo-root commands.
    - path: docs/testing/TESTING_GUIDE_INDEX.md
      provides: Testing guide aligned to repo-root workflows.
  key_links:
    - from: README.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: docs/workspace-migration-matrix\.md
    - from: docs/testing/TESTING_GUIDE_INDEX.md
      to: docs/workspace-migration-matrix.md
      via: relative Markdown link
      pattern: \../workspace-migration-matrix\.md
---

<objective>
Publish the shared migration matrix and repoint the top-level contributor doc hubs to the repo-root contract.

Purpose: Implement D-01, D-02, D-04, and D-05 by establishing one source-of-truth translation page and routing high-traffic entry docs through it.
Output: A new migration matrix plus refreshed top-level docs that teach repo-root Cargo, Node, Python, and artifact paths.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
@.planning/phases/10-docs-guidance-and-tripwires/10-REVIEWS.md
@README.md
@docs/README.md
@docs/RUST_DOCUMENTATION_INDEX.md
@docs/testing/TESTING_GUIDE_INDEX.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared workspace migration matrix</name>
  <files>docs/workspace-migration-matrix.md</files>
  <action>Create `docs/workspace-migration-matrix.md` as the single D-04/D-05 translation artifact. Use GitHub-flavored Markdown tables and keep the page easy to link from both contributor docs and agent guidance. Include sections for command translation, path-root translation, artifact/report translation, and historical-note rules. Per the review feedback, include Node and Python parity-gate translations explicitly: `bun run parity:gate`, `python tools/python_api_parity/check_parity_gate.py --repo-root .`, `python tools/cxx_api_parity/check_parity_gate.py --repo-root .`, `python validate_stubs.py --rust-dir .`, and the root-level artifact/report locations for Node, Python, and CXX parity outputs. State D-07 explicitly: `ClassicLib-rs` may appear only inside labeled migration or historical notes, never as live workspace-root guidance.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "matrix_and_top_level_docs_contract"</automated>
  </verify>
  <done>The migration matrix exists and gives contributors one concrete old-to-new command/path/artifact lookup page.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the top-level contributor doc entrypoints</name>
  <files>README.md, docs/README.md, docs/RUST_DOCUMENTATION_INDEX.md, docs/testing/TESTING_GUIDE_INDEX.md</files>
  <action>Update the four top-level doc hubs per D-01/D-02 so they teach the live repo-root layout: `foundation/`, `business-logic/`, `cpp-bindings/classic-cpp-bridge/`, `node-bindings/classic-node/`, `python-bindings/`, and `ui-applications/classic-tui/`. Replace live Cargo commands that still use `--manifest-path ClassicLib-rs/Cargo.toml` with plain repo-root commands. Replace live Node and Python workflow paths with `node-bindings/classic-node` and `python-bindings/.venv`. Add visible matrix links near the workflow sections, and keep any remaining `ClassicLib-rs` mention only as a clearly labeled migration or historical note per D-07.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "matrix_and_top_level_docs_contract"</automated>
  </verify>
  <done>The top-level contributor docs now route through the shared matrix and stop teaching `ClassicLib-rs` as the live workspace root.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/planning/test_phase10_validation.py -q -k "matrix_and_top_level_docs_contract"`
</verification>

<success_criteria>
- One migration matrix owns old-to-new translations.
- README/docs entrypoints use repo-root commands and paths.
- Contributors can reach the migration matrix from the high-traffic doc hubs.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-01-SUMMARY.md`
</output>
