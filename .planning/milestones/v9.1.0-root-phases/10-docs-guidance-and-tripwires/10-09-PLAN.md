---
phase: 10-docs-guidance-and-tripwires
plan: "09"
type: execute
wave: 2
depends_on: ["10-00", "10-01"]
files_modified:
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: [DOCS-01, DOCS-03]
must_haves:
  truths:
    - Agent-consumed codebase maps describe the root-level layout and commands.
    - Testing and concern maps stop teaching stale workspace-root paths.
    - The codebase-map surface is covered by the Phase 10 audit scaffold.
  artifacts:
    - path: .planning/codebase/STRUCTURE.md
      provides: Root-level repo layout map for agents.
    - path: .planning/codebase/TESTING.md
      provides: Root-level testing command and location guidance.
    - path: .planning/codebase/CONCERNS.md
      provides: Fragile-area map aligned to the moved live tree.
  key_links:
    - from: .planning/codebase/TESTING.md
      to: tests/planning/test_phase10_validation.py
      via: Phase 10 audit coverage
      pattern: test_phase10_validation\.py
---

<objective>
Refresh the operational codebase maps so agent discovery and validation guidance matches the moved repo-root tree.

Purpose: Implement D-03 for the `.planning/codebase/*.md` surfaces and keep them inside the Phase 10 regression audit.
Output: Updated structure, stack, architecture, integrations, conventions, testing, and concerns maps aligned to repo-root paths and commands.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-docs-guidance-and-tripwires/10-CONTEXT.md
@.planning/phases/10-docs-guidance-and-tripwires/10-RESEARCH.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the structure, stack, architecture, and integrations maps</name>
  <files>.planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/INTEGRATIONS.md</files>
  <action>Refresh these four operational maps per D-03/D-07 so they describe the repo-root layout and command contract. Replace `ClassicLib-rs/` as the live workspace container with `foundation/`, `business-logic/`, `cpp-bindings/`, `node-bindings/`, `python-bindings/`, and `ui-applications/`. Replace `ClassicLib-rs/Cargo.toml`, `ClassicLib-rs/Cargo.lock`, `ClassicLib-rs/node-bindings/...`, and `ClassicLib-rs/python-bindings/...` active examples with `Cargo.toml`, `Cargo.lock`, `node-bindings/...`, and `python-bindings/...`. Keep `ClassicLib-rs/target` only when explicitly labeled as generated residue, not as active source layout.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "codebase_maps_contract"</automated>
  </verify>
  <done>The structure/stack/architecture/integrations maps now describe the moved repo-root tree and live command roots accurately.</done>
</task>

<task type="auto">
  <name>Task 2: Update the conventions, testing, and concerns maps</name>
  <files>.planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md</files>
  <action>Refresh these three maps so their naming examples, testing commands, test locations, and fragile-area references all use the repo-root tree per D-03/D-07. Replace `cargo ... --manifest-path ClassicLib-rs/Cargo.toml` with plain repo-root cargo commands. Replace `ClassicLib-rs/python-bindings/.venv` with `python-bindings/.venv` and `ClassicLib-rs/node-bindings/classic-node/...` with `node-bindings/classic-node/...`. Update concern references to the moved live tree while keeping any remaining `ClassicLib-rs` mention explicitly historical or residue-only.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase10_validation.py -q -k "codebase_maps_contract"</automated>
  </verify>
  <done>The conventions/testing/concerns maps now use repo-root examples and no longer teach stale workspace-root commands or paths.</done>
</task>

</tasks>

<verification>
`python -m pytest tests/planning/test_phase10_validation.py -q -k "codebase_maps_contract"`
</verification>

<success_criteria>
- All seven active codebase-map surfaces are aligned to the moved repo-root tree.
- Testing and concern guidance no longer rely on stale `ClassicLib-rs` operational paths.
- The codebase-map surface is covered by executable Phase 10 audit hooks.
</success_criteria>

<output>
After completion, create `.planning/phases/10-docs-guidance-and-tripwires/10-09-SUMMARY.md`
</output>
