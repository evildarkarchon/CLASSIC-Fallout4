---
phase: 06-repo-root-workspace-cutover
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .github/workflows/ci-rust.yml
  - .github/workflows/benchmarks.yml
  - README.md
  - AGENTS.md
  - CLAUDE.md
  - docs/README.md
  - docs/api/QUICK_START.md
  - .claude/skills/ci-check/SKILL.md
  - .claude/skills/classic-project-guide/SKILL.md
  - .claude/skills/classic-project-guide/references/repo-guide.md
  - .agents/skills/classic-project-guide/SKILL.md
  - .agents/skills/classic-project-guide/references/repo-guide.md
  - .opencode/skills/classic-project-guide/SKILL.md
  - .opencode/skills/classic-project-guide/references/repo-guide.md
  - .agent/skills/classic-project-guide/SKILL.md
  - .agent/skills/classic-project-guide/references/repo-guide.md
  - tests/planning/test_phase06_validation.py
  - tests/planning/phase06_clean_run.ps1
autonomous: true
requirements:
  - ROOT-01
  - ROOT-02
must_haves:
  truths:
    - "Contributor can run `cargo fmt --all`, `cargo clippy --workspace`, `cargo test --workspace`, and `cargo build -p classic-scanlog-core` from repo root."
    - "Rust CI runs against the repo-root workspace and no longer names `ClassicLib-rs/Cargo.toml` as an active manifest."
    - "Phase proof includes Cargo-native root detection, a clean run that does not rely on `ClassicLib-rs/target`, and active doc surfaces aligned to the repo-root contract."
  artifacts:
    - path: ".github/workflows/ci-rust.yml"
      provides: "Repo-root Rust CI workflow"
      contains: "cargo clippy --workspace --all-targets --all-features -- -D warnings"
    - path: ".github/workflows/benchmarks.yml"
      provides: "Benchmark workflow with explicit minimum viable repo-root path fixes"
      contains: "target/criterion"
    - path: "CLAUDE.md"
      provides: "Always-on Claude instructions aligned to repo-root Cargo"
      contains: "cargo build --workspace"
    - path: "docs/api/QUICK_START.md"
      provides: "Contributor quick-start guidance aligned to repo-root Cargo"
      contains: "cargo fmt --all"
    - path: ".claude/skills/ci-check/SKILL.md"
      provides: "Repo-approved CI check commands for agents"
      contains: "cargo clippy --workspace"
    - path: ".agents/skills/classic-project-guide/references/repo-guide.md"
      provides: "Project skill reference with repo-root Cargo commands"
      contains: "cargo test --workspace"
    - path: "tests/planning/test_phase06_validation.py"
      provides: "Audit coverage for CI/workflow/root-detection/clean-run/doc-sync invariants"
      contains: "cargo_root_detection"
  key_links:
    - from: ".github/workflows/ci-rust.yml"
      to: "Cargo.toml"
      via: "plain repo-root cargo commands"
      pattern: "cargo test --workspace --release -- --nocapture"
    - from: ".github/workflows/benchmarks.yml"
      to: "benchmark-config.yaml"
      via: "explicit repo-root config lookup"
      pattern: "CONFIG_FILE=\"benchmark-config.yaml\""
    - from: "tests/planning/test_phase06_validation.py"
      to: "CLAUDE.md"
      via: "active-doc audit"
      pattern: "CLAUDE.md"
---

<objective>
Rewire cargo-based automation to the repo-root workspace and prove the cutover with Cargo-native root detection, a clean validation pass, and aligned active docs.

Purpose: satisfy the Phase 6 closure rule that repo-root Cargo workflows and Rust CI both operate without `ClassicLib-rs/Cargo.toml`, while keeping every active workflow/doc surface aligned to that contract.
Output: repo-root `ci-rust.yml`, explicit minimum benchmark workflow path fixes, synchronized active workflow docs/skills, and a passing clean proof suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repo-root-workspace-cutover/06-CONTEXT.md
@.planning/phases/06-repo-root-workspace-cutover/06-RESEARCH.md
@.planning/phases/06-repo-root-workspace-cutover/06-REVIEWS.md
@.planning/phases/06-repo-root-workspace-cutover/06-VALIDATION.md
@.planning/phases/06-repo-root-workspace-cutover/06-01-PLAN.md
@.planning/phases/06-repo-root-workspace-cutover/06-02-PLAN.md
@.github/workflows/ci-rust.yml
@.github/workflows/benchmarks.yml
@README.md
@AGENTS.md
@CLAUDE.md
@docs/README.md
@docs/api/QUICK_START.md
@tests/planning/test_phase06_validation.py
@tests/planning/phase06_clean_run.ps1

<interfaces>
From `.github/workflows/ci-rust.yml`:
```yaml
- name: Run rustfmt check
  run: cargo fmt --all --manifest-path ClassicLib-rs/Cargo.toml -- --check

- name: Cache cargo build
  with:
    path: ClassicLib-rs/target
```

From `.github/workflows/benchmarks.yml`:
```yaml
- name: Restore baseline cache
  with:
    path: ClassicLib-rs/target/criterion/baseline

- name: Check baseline exists
  working-directory: ClassicLib-rs

- name: Analyze thresholds
  working-directory: ClassicLib-rs
  run: |
    CONFIG_FILE="benchmark-config.yaml"
```

From `CLAUDE.md`:
```md
cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml
cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml
```

From `docs/api/QUICK_START.md`:
```md
cargo test --workspace --manifest-path ClassicLib-rs/Cargo.toml
cargo fmt --all --manifest-path ClassicLib-rs/Cargo.toml -- --check
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Rust CI and enumerate the minimum repo-root benchmark workflow fixes</name>
  <files>.github/workflows/ci-rust.yml, .github/workflows/benchmarks.yml, tests/planning/test_phase06_validation.py</files>
  <action>Per D-07, D-09, D-10, D-13, and D-16, rewrite `.github/workflows/ci-rust.yml` to use plain repo-root Cargo commands with no `--manifest-path` references, and change cargo cache/output paths from `ClassicLib-rs/target` to `target`. Keep the same jobs (`format`, `lint`, `build`, `test`) and the same crate-specific test intent, but run them from repo root: `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo build --workspace --release`, `cargo test -p classic-message-core -- --nocapture`, `cargo test --workspace --release -- --nocapture`, and `cargo test --workspace --release --all-features -- --nocapture`. For `.github/workflows/benchmarks.yml`, make the Phase 6 minimum-fix scope explicit rather than implicit: update any dead `working-directory: ClassicLib-rs` steps, baseline cache paths under `ClassicLib-rs/target/criterion/...`, and any repo-root benchmark config lookups so the moved `criterion.toml`, `benchmark-config.yaml`, `benches/`, and `target/criterion/**` paths stay viable. Extend `tests/planning/test_phase06_validation.py` so it asserts `ci-rust.yml` has no `ClassicLib-rs/Cargo.toml` usage and `benchmarks.yml` no longer depends on old working directories or old target paths. Addresses review concern: enumerate the exact `benchmarks.yml` assumptions that must change in Phase 6.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "repo_root_workflows or benchmark_workflow_paths"</automated>
  </verify>
  <done>Active cargo-based CI surfaces target the repo-root workspace, and the benchmark workflow's minimum viable Phase 6 path rewires are explicit and auditable.</done>
</task>

<task type="auto">
  <name>Task 2: Add Cargo-native proof audits and run the clean root-workflow validation suite</name>
  <files>tests/planning/test_phase06_validation.py, tests/planning/phase06_clean_run.ps1</files>
  <action>Per D-11, D-14, D-15, and D-16, finish `tests/planning/test_phase06_validation.py` so it shells out to `cargo locate-project --workspace` and `cargo metadata --format-version 1 --no-deps`, parses the JSON response, and asserts `workspace_root == REPO_ROOT` and `target_directory == REPO_ROOT / "target"`. Add an audit over active cargo-first surfaces (`.github/workflows/ci-rust.yml`, `.github/workflows/benchmarks.yml`, `rebuild_rust.ps1`, `validate_stubs.py`, `README.md`, `CLAUDE.md`, and `docs/api/QUICK_START.md`) so any remaining `--manifest-path` usage or any `ClassicLib-rs/Cargo.toml` reference fails the phase audit. Update `tests/planning/phase06_clean_run.ps1` if needed so it performs the exact clean pass required for Phase 6 with plain repo-root Cargo invocation only: guard or rename `ClassicLib-rs/target`, run `cargo locate-project --workspace`, `cargo metadata --format-version 1 --no-deps`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace --release -- --nocapture`, `cargo build -p classic-scanlog-core`, and finally `python -m pytest tests/planning/test_phase06_validation.py -q`, then restore any renamed target directory. If the clean pass exposes stale-path breakage, fix it only in the already-in-scope active Phase 6 surfaces. Addresses review concern: closure proof must cover the expanded active-doc and workflow set, not just CI YAML.</action>
  <verify>
    <automated>pwsh -File tests/planning/phase06_clean_run.ps1</automated>
  </verify>
  <done>The repo-root Cargo contract, Rust CI rewiring, root detection, and clean-state proof are all green.</done>
</task>

<task type="auto">
  <name>Task 3: Sync always-on and quick-start docs/skills to the repo-root Cargo contract</name>
  <files>README.md, AGENTS.md, CLAUDE.md, docs/README.md, docs/api/QUICK_START.md, .claude/skills/ci-check/SKILL.md, .claude/skills/classic-project-guide/SKILL.md, .claude/skills/classic-project-guide/references/repo-guide.md, .agents/skills/classic-project-guide/SKILL.md, .agents/skills/classic-project-guide/references/repo-guide.md, .opencode/skills/classic-project-guide/SKILL.md, .opencode/skills/classic-project-guide/references/repo-guide.md, .agent/skills/classic-project-guide/SKILL.md, .agent/skills/classic-project-guide/references/repo-guide.md, tests/planning/test_phase06_validation.py</files>
  <action>Per the repo rule in `AGENTS.md` to keep docs synchronized with workflow changes, update the active always-loaded or quick-start surfaces that currently teach old-root Cargo usage: `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/README.md`, `docs/api/QUICK_START.md`, `.claude/skills/ci-check/SKILL.md`, and each `classic-project-guide` skill/reference copy under `.claude/`, `.agents/`, `.opencode/`, and `.agent/`. Replace active workflow guidance that routes contributors or agents through `ClassicLib-rs/Cargo.toml` with the plain repo-root commands `cargo fmt --all`, `cargo clippy --workspace`, `cargo test --workspace`, and `cargo build -p classic-scanlog-core`. Keep later-phase caveats intact: broad historical-doc cleanup remains Phase 10 work, but no active always-on or quick-start doc may present `ClassicLib-rs/Cargo.toml` as the live workspace root after this task. Extend `tests/planning/test_phase06_validation.py` with `readme_sync` and `agents_sync` audits that explicitly cover `CLAUDE.md` and `docs/api/QUICK_START.md` in addition to the existing README/skill surfaces. Addresses review concern: include always-on agent docs and active quick-start/setup docs in the Phase 6 sync scope.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase06_validation.py -q -k "readme_sync or agents_sync"</automated>
  </verify>
  <done>Contributor-facing and agent-facing active docs no longer contradict the Phase 6 repo-root Cargo contract.</done>
</task>

</tasks>

<verification>
Root Cargo commands, Rust CI, benchmark-path viability, active always-on docs, and clean-state proof all operate from repo root with Cargo-native root detection.
</verification>

<success_criteria>
  - `ci-rust.yml` no longer uses `ClassicLib-rs/Cargo.toml` or `ClassicLib-rs/target`.
  - `benchmarks.yml` has explicit repo-root working-directory, config, and `target/criterion` path fixes where needed.
  - Cargo reports the repository root as the only workspace root and uses `target/` at repo root.
  - `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/api/QUICK_START.md`, and active skill docs no longer present `ClassicLib-rs/Cargo.toml` as the active workspace root.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repo-root-workspace-cutover/06-03-SUMMARY.md`
</output>
