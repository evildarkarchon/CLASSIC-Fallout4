---
phase: 07-crate-relocation-and-path-rewire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/planning/test_phase07_validation.py
  - .planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md
autonomous: true
requirements:
  - MOVE-01
  - MOVE-02
must_haves:
  truths:
    - "Phase 7 has a deterministic audit surface before any crate move starts."
    - "Executors can prove relocation with Cargo metadata plus a checked-in old→new mapping artifact instead of ad hoc notes."
    - "Representative moved-manifest checks are defined up front so broken path edges fail immediately after the move."
  artifacts:
    - path: "tests/planning/test_phase07_validation.py"
      provides: "Phase 7 planning audit scaffold with named relocation checks"
      contains: "class Phase07ValidationAuditTests"
    - path: ".planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md"
      provides: "Checked-in relocation audit contract for old→new mapping and stale-path sweep"
      contains: "# Phase 7 Relocation Audit"
  key_links:
    - from: "tests/planning/test_phase07_validation.py"
      to: "Cargo.toml"
      via: "future cargo metadata assertions"
      pattern: "cargo metadata --format-version 1 --no-deps"
    - from: "tests/planning/test_phase07_validation.py"
      to: ".planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md"
      via: "audit-artifact presence and section checks"
      pattern: "07-RELOCATION-AUDIT.md"
---

<objective>
Create the Wave 0 relocation-proof contract before any crate directories move.

Purpose: make Phase 7 verification deterministic so the physical move can be executed against a known audit/test surface instead of relying on memory.
Output: a checked-in relocation audit template and a Phase 7 planning test scaffold.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-CONTEXT.md
@.planning/phases/07-crate-relocation-and-path-rewire/07-RESEARCH.md
@.planning/phases/06-repo-root-workspace-cutover/06-VERIFICATION.md
@.planning/phases/06-repo-root-workspace-cutover/06-03-SUMMARY.md
@Cargo.toml
@tests/planning/test_phase06_validation.py

<interfaces>
From `Cargo.toml`:
```toml
[workspace]
members = [
    "ClassicLib-rs/foundation/classic-shared-core",
    "ClassicLib-rs/business-logic/classic-scanlog-core",
    "ClassicLib-rs/python-bindings/classic-config-py",
    "ClassicLib-rs/node-bindings/classic-node",
    "ClassicLib-rs/ui-applications/classic-tui",
    "ClassicLib-rs/cpp-bindings/classic-cpp-bridge",
]
resolver = "2"
```

From `07-RESEARCH.md`:
```text
MOVE-01 => all 37 workspace members are relocated out of ClassicLib-rs with preserved layout.
MOVE-02 => cargo locate-project + cargo metadata + planning audit prove one live root and valid local path relationships.
```

From `tests/planning/test_phase06_validation.py`:
```python
class Phase06ValidationAuditTests(unittest.TestCase):
    def test_cargo_root_detection(self) -> None: ...
    def test_clean_target_guard(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the checked-in relocation audit contract</name>
  <files>.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md</files>
  <action>Per D-07 and D-08, create `.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md` as the canonical Phase 7 proof artifact. Give it stable sections for: (1) old→new crate mapping covering all 37 current workspace members, (2) `cargo locate-project --workspace` proof, (3) `cargo metadata --format-version 1 --no-deps` proof, (4) stale workspace-member / stale-manifest sweep, and (5) `ClassicLib-rs/` residue inventory with an explicit non-authoritative rationale per D-06. Keep it as a template/contract in this plan; do not fill in post-move results yet. The goal is to define exactly what later plans must populate, not to perform the relocation early.</action>
  <verify>
    <automated>python -c "from pathlib import Path; text = Path('.planning/phases/07-crate-relocation-and-path-rewire/07-RELOCATION-AUDIT.md').read_text(encoding='utf-8'); required = ['# Phase 7 Relocation Audit', '## Old to New Crate Mapping', '## Cargo Root Proof', '## Stale Member and Manifest Sweep', '## Legacy ClassicLib-rs Residue']; missing = [item for item in required if item not in text]; raise SystemExit(1 if missing else 0)"</automated>
  </verify>
  <done>The phase directory contains a stable relocation-audit artifact with the required proof sections spelled out before implementation starts.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap the Phase 7 planning audit scaffold</name>
  <files>tests/planning/test_phase07_validation.py</files>
  <action>Per D-07, D-08, and the Wave 0 gaps from `07-RESEARCH.md`, create `tests/planning/test_phase07_validation.py` using the Phase 6 unittest-under-pytest style. Add a bootstrap test that proves repo root, phase dir, and `07-RELOCATION-AUDIT.md` exist. Reserve stable named hooks for the later relocation checks: workspace members moved out of `ClassicLib-rs`, representative manifest path edges (`classic-cpp-bridge`, `classic-node`, `classic-config-py`, `classic-tui`), audit completeness, and cargo root detection. It is acceptable for later-hook tests to be skipped or scaffolded in this plan, but the file must already name the final checks so later plans fill them in rather than inventing new audit shapes. Do not assert post-move truths yet.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase07_validation.py -q -k "bootstrap or audit"</automated>
  </verify>
  <done>A Phase 7 planning test file exists, passes its bootstrap subset, and names the final relocation checks that later plans must complete.</done>
</task>

</tasks>

<verification>
The phase has a deterministic audit artifact and a runnable Wave 0 planning test scaffold before any crate relocation begins.
</verification>

<success_criteria>
- `07-RELOCATION-AUDIT.md` exists with stable sections for mapping, Cargo proof, stale sweep, and legacy residue.
- `tests/planning/test_phase07_validation.py` exists and passes its bootstrap subset.
- Later Phase 7 plans can verify against named tests instead of inventing new proof surfaces mid-move.
</success_criteria>

<output>
After completion, create `.planning/phases/07-crate-relocation-and-path-rewire/07-01-SUMMARY.md`
</output>
