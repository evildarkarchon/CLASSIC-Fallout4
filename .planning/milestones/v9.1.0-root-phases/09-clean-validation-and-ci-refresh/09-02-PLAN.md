---
phase: 09-clean-validation-and-ci-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .github/workflows/ci-rust.yml
  - .github/workflows/ci-cpp.yml
  - .github/workflows/benchmarks.yml
  - .planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
  - tests/planning/test_phase09_validation.py
autonomous: true
requirements:
  - INTG-03
must_haves:
  truths:
    - "Contributor can inspect the Rust, CXX, and benchmark workflows and see repo-root command, cache, hash, and upload paths together."
    - "The required GUI package-sensitive proof surface is encoded as a first-class Phase 9 invariant."
  artifacts:
    - path: ".github/workflows/ci-rust.yml"
      provides: "Repo-root Rust cache and hash contract"
      contains: "hashFiles('foundation/**/*.rs'"
    - path: ".github/workflows/ci-cpp.yml"
      provides: "Repo-root CXX cache and diagnostics contract"
      contains: "cpp-bindings/classic-cpp-bridge/parity-artifacts/"
    - path: ".github/workflows/benchmarks.yml"
      provides: "Repo-root benchmark cache and target contract"
      contains: "target/criterion/baseline"
    - path: "tests/planning/test_phase09_validation.py"
      provides: "Executable audit for Rust/CXX/benchmark workflow and GUI package invariants"
      contains: "test_rust_cpp_benchmark_workflows"
  key_links:
    - from: ".github/workflows/ci-rust.yml"
      to: "Cargo.toml"
      via: "repo-root rust source hash inputs"
      pattern: "foundation/\*\*/\*.rs"
    - from: ".github/workflows/ci-cpp.yml"
      to: "classic-gui/build_gui.ps1"
      via: "wrapper-owned native proof"
      pattern: "build_gui.ps1"
---

<objective>
Refresh the Rust, native C++, and benchmark workflow contracts so repo-root caches, hashes, diagnostics, and GUI package proof all match the relocated workspace.

Purpose: satisfy the D-04, D-05, D-06, and D-07 Phase 9 closure surface for Rust/native automation without relying on stale `ClassicLib-rs` paths.
Output: updated Rust/C++/benchmark workflows plus executable phase-audit checks for those surfaces.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-RESEARCH.md
@.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
@tests/planning/test_phase09_validation.py
@.github/workflows/ci-rust.yml
@.github/workflows/ci-cpp.yml
@.github/workflows/benchmarks.yml
@classic-gui/build_gui.ps1

<interfaces>
From `.github/workflows/ci-rust.yml`:
```yaml
- uses: actions/cache@v5
  with:
    path: target
    key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('ClassicLib-rs/**/*.rs') }}
```

From `.github/workflows/ci-cpp.yml`:
```yaml
with:
  path: ClassicLib-rs/cpp-bindings/classic-cpp-bridge/parity-artifacts/
```

From `classic-gui/build_gui.ps1`:
```powershell
if ($Package) { $Install = $true }
& cpack --config $cpackConfig -B $packageDir
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Rust, CXX, and benchmark workflow path contracts to repo root</name>
  <files>.github/workflows/ci-rust.yml, .github/workflows/ci-cpp.yml, .github/workflows/benchmarks.yml</files>
  <read_first>.github/workflows/ci-rust.yml
.github/workflows/ci-cpp.yml
.github/workflows/benchmarks.yml
.planning/phases/09-clean-validation-and-ci-refresh/09-CONTEXT.md
.planning/phases/09-clean-validation-and-ci-refresh/09-RESEARCH.md</read_first>
  <action>Per D-04, D-05, D-06, and D-07, update the three native/benchmark workflows as one path contract set. In `ci-rust.yml`, keep the existing jobs and repo-root commands, but replace every `hashFiles('ClassicLib-rs/**/*.rs')` key input with repo-root Rust source globs covering `foundation/**/*.rs`, `business-logic/**/*.rs`, `cpp-bindings/**/*.rs`, `node-bindings/**/*.rs`, `python-bindings/**/*.rs`, and `ui-applications/**/*.rs`. In `ci-cpp.yml`, keep wrapper-owned commands, but change cargo cache paths from `ClassicLib-rs/target` to `target`, change the CXX parity diagnostics upload path to `cpp-bindings/classic-cpp-bridge/parity-artifacts/`, and keep native diagnostics rooted under `classic-cli/` and `classic-gui/`. In `benchmarks.yml`, replace any remaining `ClassicLib-rs/**/*.rs` key input with the same repo-root Rust source globs, keep `target/criterion/baseline` as the baseline cache path, and do not introduce new benchmark outputs outside repo root. Do not widen into Python/Node workflow edits here; those belong to 09-03.</action>
  <acceptance_criteria>
    - `.github/workflows/ci-rust.yml` does not contain `ClassicLib-rs/**/*.rs`.
    - `.github/workflows/ci-cpp.yml` does not contain `ClassicLib-rs/target`.
    - `.github/workflows/ci-cpp.yml` contains `cpp-bindings/classic-cpp-bridge/parity-artifacts/`.
    - `.github/workflows/benchmarks.yml` contains `target/criterion/baseline`.
    - `.github/workflows/benchmarks.yml` does not contain `ClassicLib-rs/**/*.rs`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q -k "rust_cpp_benchmark_workflows"</automated>
  </verify>
  <done>The Rust/native/benchmark workflows use repo-root cache and hash inputs only, and their diagnostics paths no longer point into legacy generated directories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the Phase 9 audit to prove the native workflow and GUI package surfaces</name>
  <files>.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md, tests/planning/test_phase09_validation.py</files>
  <read_first>.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md
tests/planning/test_phase09_validation.py
.github/workflows/ci-rust.yml
.github/workflows/ci-cpp.yml
.github/workflows/benchmarks.yml
classic-gui/build_gui.ps1</read_first>
  <behavior>
    - Test 1: `test_rust_cpp_benchmark_workflows` fails if any of the three workflows still encode a legacy cache/hash/upload path.
    - Test 2: `test_gui_package_surface` fails unless the audit document and harness both treat `classic-gui/build_gui.ps1 -Package` as the required package-sensitive proof surface.
  </behavior>
  <action>Update `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` so the workflow matrix records, for each of `ci-rust.yml`, `ci-cpp.yml`, and `benchmarks.yml`, the exact repo-root command roots, cache paths, hash inputs, and failure-diagnostic upload paths after Task 1. Then extend `tests/planning/test_phase09_validation.py` with exact-string assertions for those fields plus a dedicated `test_gui_package_surface` that checks both the audit document and `tests/planning/phase09_clean_run.ps1` reference `classic-gui/build_gui.ps1 -Package`. Use D-07 verbatim in the action text and assertions; do not downgrade the GUI proof to `-Test` or build-only.</action>
  <acceptance_criteria>
    - `tests/planning/test_phase09_validation.py` contains `def test_rust_cpp_benchmark_workflows`.
    - `tests/planning/test_phase09_validation.py` contains `def test_gui_package_surface`.
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `classic-gui/build_gui.ps1 -Package`.
    - `.planning/phases/09-clean-validation-and-ci-refresh/09-CLEAN-VALIDATION-AUDIT.md` contains `.github/workflows/benchmarks.yml` and `target/criterion/baseline` in the same workflow matrix section.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/planning/test_phase09_validation.py -q -k "rust_cpp_benchmark_workflows or gui_package_surface"</automated>
  </verify>
  <done>Phase 9’s native workflow contract is executable and explicitly includes the required GUI package proof surface.</done>
</task>

</tasks>

<verification>
The Rust/native/benchmark workflow files and the phase audit agree on one repo-root cache/hash/upload/package contract.
</verification>

<success_criteria>
- `ci-rust.yml`, `ci-cpp.yml`, and `benchmarks.yml` contain repo-root cache/hash/upload paths only.
- The Phase 9 audit and planning test both treat `classic-gui/build_gui.ps1 -Package` as required proof.
- No workflow in this plan still depends on `ClassicLib-rs/target` or `ClassicLib-rs/**/*.rs`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clean-validation-and-ci-refresh/09-02-SUMMARY.md`
</output>
