# Milestone v9.1.0-root: Move Crates to Project Root

**Status:** ✅ SHIPPED 2026-04-15
**Phases:** 6-12
**Total Plans:** 33

## Overview

Moved the Rust workspace out of `ClassicLib-rs/` into repo-root layer directories, rewired wrapper/parity/CI/doc flows to the new layout, and closed the remaining relocation and integration verification gaps with current verification evidence.

## Phases

### Phase 6: Repo-Root Workspace Cutover

**Goal**: Contributors can treat the repository root as the single authoritative Cargo workspace root.
**Depends on**: Phase 5
**Requirements**: ROOT-01, ROOT-02
**Plans**: 4 plans

Plans:

- [x] `06-00-PLAN.md` -- Bootstrap the Phase 6 validation scaffold and clean-run helper.
- [x] `06-01-PLAN.md` -- Promote the repo-root workspace shell and root-aware helper scripts.
- [x] `06-02-PLAN.md` -- Move benchmark-owned support files to repo root and remove old copies.
- [x] `06-03-PLAN.md` -- Rewire cargo-based CI/workflow paths, sync active workflow docs, and prove the clean repo-root Cargo contract.

**Details:**

- Contributor can run Cargo from the repository root without relying on `ClassicLib-rs/Cargo.toml` as the live workspace manifest.
- Canonical repo-root workflows including `cargo fmt --all`, `cargo clippy --workspace`, and `cargo test --workspace` are verified.
- Clean proof and active workflow/doc audits guard against stale `ClassicLib-rs/target` reuse and stale workspace guidance.

### Phase 7: Crate Relocation and Path Rewire

**Goal**: Every Rust crate currently under `ClassicLib-rs/` exists at its new repo-root-relative location with working local path relationships.
**Depends on**: Phase 6
**Requirements**: MOVE-01, MOVE-02
**Plans**: 3 plans

Plans:

- [x] `07-01-PLAN.md` -- Bootstrap the relocation audit artifact and Phase 7 planning validation scaffold.
- [x] `07-02-PLAN.md` -- Move the six Rust layer directories to repo root and rewire only broken member/path edges.
- [x] `07-03-PLAN.md` -- Complete the relocation audit and prove `ClassicLib-rs` is no longer a live Rust workspace home.

**Details:**

- Every crate previously under `ClassicLib-rs/` now exists at its repo-root-relative location with preserved internal layout.
- Workspace members and local path dependencies resolve from the repo-root `Cargo.toml`.
- `ClassicLib-rs/` remains only as explicit non-authoritative residue, not a live workspace home.

### Phase 8: Wrapper and Parity Rewire

**Goal**: Existing Rust-consuming wrappers, frontends, and parity gates continue to operate against the relocated workspace.
**Depends on**: Phase 7
**Requirements**: INTG-01, INTG-02
**Plans**: 6 plans

Plans:

- [x] `08-01-PLAN.md` -- Rewire repo-root rebuild wrappers and collapse `rebuild_node.ps1` into the canonical Node flow.
- [x] `08-02-PLAN.md` -- Rewire native CLI/GUI bridge includes and add a lightweight repo-root TUI smoke path.
- [x] `08-03-PLAN.md` -- Cut Python stub/parity tooling over to root-level binding paths with legacy-path rejection.
- [x] `08-04-PLAN.md` -- Rewire `classic-node` package scripts plus Node parity and d.ts freshness defaults.
- [x] `08-05-PLAN.md` -- Cut CXX parity tooling over to repo-root bridge paths and refresh stale CXX baseline metadata.
- [x] `08-06-PLAN.md` -- Refresh Python/Node checked-in parity artifacts and add the final Phase 8 planning audit.

**Details:**

- Existing rebuild wrappers and native CLI/GUI/TUI flows run against the relocated workspace.
- Python, Node, and CXX parity gates stay green without parity-contract drift caused by the move.
- Phase-level validation and refreshed parity artifacts preserve wrapper/native/parity traceability.

### Phase 9: Clean Validation and CI Refresh

**Goal**: Clean-state verification and CI prove the repo-root workspace works without stale caches or legacy path artifacts.
**Depends on**: Phase 8
**Requirements**: INTG-03, INTG-04
**Plans**: 4 plans

Plans:

- [x] `09-01-PLAN.md` -- Bootstrap the Phase 9 clean-validation audit and stronger targeted-clean harness.
- [x] `09-02-PLAN.md` -- Refresh Rust, native C++, and benchmark workflow path contracts plus GUI package proof invariants.
- [x] `09-03-PLAN.md` -- Refresh Python and Node PR workflow path contracts and lock the allowed artifact scope.
- [x] `09-04-PLAN.md` -- Regenerate only the owned path-bearing artifacts and finish the clean-state + GUI package proof.

**Details:**

- CI and path-sensitive build/package jobs work against the repo-root layout.
- Clean-state proof regenerates path-bearing artifacts without relying on stale caches or leftover outputs.
- GUI packaging and parity reruns were recorded with allowed tracked diffs limited to owned Python parity artifacts.

### Phase 10: Docs, Guidance, and Tripwires

**Goal**: Active documentation and agent guidance teach the new workspace layout and help prevent `ClassicLib-rs` workspace-root regressions.
**Depends on**: Phase 9
**Requirements**: DOCS-01, DOCS-02, DOCS-03
**Plans**: 10 plans

Plans:

- [x] `10-00-PLAN.md` -- Bootstrap the Phase 10 planning audit and wrapper-script tripwires before doc rewrites begin.
- [x] `10-01-PLAN.md` -- Create the shared workspace migration matrix and repoint top-level contributor entry docs to repo-root guidance.
- [x] `10-02-PLAN.md` -- Refresh the active API hubs and binding workflow docs to the repo-root path and artifact contract.
- [x] `10-03-PLAN.md` -- Update the foundation and settings API reference pages to root-level source links.
- [x] `10-04-PLAN.md` -- Update the version/config/web/update API reference pages to root-level source links.
- [x] `10-05-PLAN.md` -- Update the path/setup/file/resource API workflow docs to the moved repo-root tree.
- [x] `10-06-PLAN.md` -- Update the scan/database/bridge/gui API docs to the moved repo-root tree.
- [x] `10-07-PLAN.md` -- Update `AGENTS.md`, `CLAUDE.md`, and all classic-project-guide entrypoint mirrors to the repo-root contract.
- [x] `10-08-PLAN.md` -- Refresh the mirrored classic-project-guide repo-guide references with repo-root architecture, command, and parity-path guidance.
- [x] `10-09-PLAN.md` -- Update all active `.planning/codebase/*.md` maps to repo-root examples, commands, and live-tree paths.

**Details:**

- Active docs, skills, and agent context files teach repo-root paths instead of `ClassicLib-rs` workspace-root workflows.
- `docs/workspace-migration-matrix.md` provides one canonical translation layer from old paths and commands to the repo-root layout.
- Validation-critical docs, scripts, and tests now have tripwires against active `ClassicLib-rs` workspace-root regressions.

### Phase 11: Relocation Proof and Verification Closure

**Goal**: The moved-crate proof surfaces match the current repo state and Phase 7 has a rerunnable verification record.
**Depends on**: Phase 10
**Requirements**: MOVE-01, MOVE-02
**Plans**: 3 plans

Plans:

- [x] `11-01-PLAN.md` -- Replace the obsolete Phase 11 audit with a current closure scaffold for `07-VERIFICATION.md` and moved-crate proof.
- [x] `11-02-PLAN.md` -- Refresh the stale Phase 7 relocation audit and planning test to the live `ClassicLib-rs` residue inventory.
- [x] `11-03-PLAN.md` -- Create `07-VERIFICATION.md` with direct `MOVE-01`/`MOVE-02` evidence and sync planning status files.

**Details:**

- `07-VERIFICATION.md` provides direct `MOVE-01` and `MOVE-02` evidence against the live repo state.
- The relocation audit and planning tests now match the current `ClassicLib-rs` residue inventory.
- Milestone metadata no longer reports moved-crate requirements as orphaned.

### Phase 12: Integration Replay and Verification Closure

**Goal**: The remaining integration proof surfaces are replayable and the orphaned wrapper, parity, CI, and clean-state requirements are closed with current verification evidence.
**Depends on**: Phase 11
**Requirements**: INTG-01, INTG-02, INTG-03, INTG-04
**Plans**: 3 plans

Plans:

- [x] `12-01-PLAN.md` -- Repair the Phase 9 clean replay harness so `.venv` is recreated and the Python rebuild wrapper remains usable after proof.
- [x] `12-02-PLAN.md` -- Write `08-VERIFICATION.md` and backfill Phase 8 summary requirement metadata for wrapper/parity closure.
- [x] `12-03-PLAN.md` -- Write `09-VERIFICATION.md`, backfill Phase 9 summary requirement metadata, and sync milestone closure metadata.

**Details:**

- Clean-state replay no longer leaves Python rebuild wrappers unusable afterward.
- `08-VERIFICATION.md` and `09-VERIFICATION.md` carry current wrapper, parity, CI, and clean-state evidence.
- All four integration requirements are closed in planning metadata and milestone audit coverage.

---

## Milestone Summary

**Decimal Phases:**

- None.

**Key Decisions:**

- Treat `v9.1.0-root` as a workspace-location migration only, not a crate-graph or API redesign.
- Keep the repository root as the single live Cargo workspace entrypoint with no compatibility shim.
- Preserve the moved layer directories intact and only rebase repo-relative paths proven broken by the move.
- Use canonical `07-VERIFICATION.md`, `08-VERIFICATION.md`, and `09-VERIFICATION.md` artifacts plus synchronized planning metadata to close relocated and integration requirements.

**Issues Resolved:**

- Root Cargo, CI, wrapper, parity, package, and active-doc flows now all point to repo-root paths.
- The stale moved-crate proof gap was closed with a replayable Phase 7 verification artifact.
- The remaining integration requirement orphaning was closed with direct Phase 8/9 verification evidence and replayable clean-state proof.

**Issues Deferred:**

- Non-active and historical docs outside the Phase 10 audited surface still contain some legacy `ClassicLib-rs` references.
- Phase 12 Nyquist coverage was closed after archive by `12-VALIDATION.md` and `tests/planning/test_phase12_validation.py`.

**Technical Debt Incurred:**

- Milestone shipped with accepted audit status `tech_debt` rather than a fully clean `passed`.
- Future doc cleanup should remove the remaining non-active legacy-path residue outside the actively audited surfaces.

---

_For current project status, see .planning/ROADMAP.md_
