---
phase: 01-yaml-settings-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs
  - ClassicLib-rs/business-logic/classic-settings-core/src/yaml_merge.rs
  - ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs
  - ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs
  - ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs
  - ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs
  - ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs
  - ClassicLib-rs/business-logic/classic-yaml-core/src/merge.rs
  - ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs
  - ClassicLib-rs/business-logic/classic-yaml-core/tests/phase2_yaml_dead_code_audit.rs
  - ClassicLib-rs/business-logic/classic-yaml-core/benches/yaml_benchmarks.rs
  - ClassicLib-rs/business-logic/classic-config-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-config-core/src/lib.rs
  - ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs
  - ClassicLib-rs/business-logic/classic-config-core/src/config.rs
  - ClassicLib-rs/business-logic/classic-version-registry-core/Cargo.toml
  - ClassicLib-rs/business-logic/classic-version-registry-core/src/registry.rs
  - ClassicLib-rs/business-logic/classic-version-registry-core/src/error.rs
  - ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml
  - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/Cargo.toml
  - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/yaml.rs
  - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs
  - ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/config.rs
  - ClassicLib-rs/node-bindings/classic-node/Cargo.toml
  - ClassicLib-rs/node-bindings/classic-node/src/yaml.rs
  - ClassicLib-rs/Cargo.toml
  - ClassicLib-rs/.idea/ClassicLib-rs.iml
  - .idea/CLASSIC-Fallout4.iml
  - ClassicLib-rs/.idea/workspace.xml
autonomous: true
requirements: [YAML-01, YAML-02, YAML-03]

must_haves:
  truths:
    - "All public types/functions from classic-yaml-core are importable from classic_settings_core"
    - "No crate in the workspace depends on classic-yaml-core"
    - "classic-yaml-core directory no longer exists"
    - "classic-settings-core/tests/ and benches/ contain the former yaml-core tests/benches with preserved blame history"
    - "classic-yaml-py is removed from workspace members in the same commit as yaml-core removal (reviews-mode BLOCKER 1 fix — Wave 1 must end buildable; the yaml-py crate directory remains on disk and is deleted in Plan 01-02 Task 3)"
    - "cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml exits 0 at end of Wave 1 (reviews-mode BLOCKER 1 acceptance gate)"
    - "cargo test -p classic-settings-core passes (including migrated integration tests)"
  artifacts:
    - path: "ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs"
      provides: "YamlOperations, YamlError, YamlCacheStats, YAML_CACHE, yaml_cache_stats, reset_yaml_cache_stats, clear_global_yaml_cache"
      min_lines: 200
    - path: "ClassicLib-rs/business-logic/classic-settings-core/src/yaml_merge.rs"
      provides: "merge_keys function"
      min_lines: 30
    - path: "ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs"
      provides: "Migrated yaml-core integration tests (via git mv for blame)"
      contains: "use classic_settings_core"
    - path: "ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs"
      provides: "Migrated dead code audit test (via git mv for blame)"
      contains: "include_str!"
    - path: "ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs"
      provides: "Migrated yaml benchmarks (via git mv for blame)"
      contains: "use classic_settings_core"
    - path: "ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs"
      provides: "mod yaml_ops; mod yaml_merge; pub use re-exports"
      contains: "pub use yaml_ops"
    - path: "ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml"
      provides: "Absorbed dependencies (indexmap, serde_json), criterion dev-dep, [[bench]] entry"
      contains: "indexmap"
  key_links:
    - from: "ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs"
      to: "classic_settings_core::YamlOperations"
      via: "use statement"
      pattern: "use classic_settings_core::YamlOperations"
    - from: "ClassicLib-rs/business-logic/classic-version-registry-core/src/registry.rs"
      to: "classic_settings_core::YamlOperations"
      via: "use statement"
      pattern: "use classic_settings_core::YamlOperations"
    - from: "ClassicLib-rs/business-logic/classic-version-registry-core/src/error.rs"
      to: "classic_settings_core::YamlError"
      via: "#[from] variant"
      pattern: "classic_settings_core::YamlError"
---

<objective>
Absorb classic-yaml-core source (incl. tests and benches) into classic-settings-core and migrate all workspace consumers.

Purpose: Eliminate the classic-yaml-core crate as a standalone compilation unit by moving its source modules, tests, and benchmarks into classic-settings-core, renaming colliding types per D-03, and updating every consumer crate's Cargo.toml and import paths. After this plan, `cargo build --workspace` and `cargo test -p classic-settings-core` pass with zero failures. Tests and benchmarks retain full git blame history via `git mv`.

Output: yaml_ops.rs, yaml_merge.rs, yaml_integration_tests.rs, yaml_dead_code_audit.rs, and yaml_benchmarks.rs all living inside classic-settings-core with history preserved, all consumer imports updated, classic-yaml-core removed from workspace.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-yaml-settings-merge/01-CONTEXT.md
@.planning/phases/01-yaml-settings-merge/01-RESEARCH.md

@ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs
@ClassicLib-rs/business-logic/classic-yaml-core/src/merge.rs
@ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml
@ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs
@ClassicLib-rs/business-logic/classic-yaml-core/tests/phase2_yaml_dead_code_audit.rs
@ClassicLib-rs/business-logic/classic-yaml-core/benches/yaml_benchmarks.rs
@ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs
@ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml

<interfaces>
<!-- Key types and contracts the executor needs. -->

From classic-yaml-core lib.rs (public API to preserve after merge):
- struct YamlOperations (with methods: new, parse_yaml, dump_yaml, load_yaml_file, save_yaml_file, get_setting, set_setting, get_string_value, get_vec_value, get_hashmap_value, get_indexmap_value, get_hashmap_vec_value, get_indexmap_vec_value, get_settings_batch, set_settings_batch, load_yaml_files_batch, get_cache_stats, set_cache_enabled, is_cache_enabled, clear_cache)
- enum YamlError (ParseError, SerializeError, IoError, EmptyDocument, InvalidValue, UnresolvedAlias, InvalidKeyPath, TypeConversionError)
- struct CacheStats -> rename to YamlCacheStats (fields: hits, misses, hit_rate, size, capacity)
- fn cache_stats() -> rename to yaml_cache_stats()
- fn reset_cache_stats() -> rename to reset_yaml_cache_stats()
- fn clear_global_yaml_cache()
- static YAML_CACHE: LazyLock<Cache<PathBuf, CachedYaml>> (128 entries)
- static CACHE_HITS: AtomicU64
- static CACHE_MISSES: AtomicU64

From classic-yaml-core merge.rs:
- pub fn merge_keys(yaml: Yaml) -> Result<Yaml, YamlError>

From classic-yaml-core tests/phase2_yaml_dead_code_audit.rs (CRITICAL — uses include_str! pointing at sibling files):
- `const LIB_RS: &str = include_str!("../src/lib.rs");` — must become `include_str!("../src/yaml_ops.rs")` after rename
- `const INTEGRATION_TESTS_RS: &str = include_str!("integration_tests.rs");` — must become `include_str!("yaml_integration_tests.rs")` after rename
- `const YAML_BENCHMARKS_RS: &str = include_str!("../benches/yaml_benchmarks.rs");` — path stays (benchmark file is still named yaml_benchmarks.rs, just in settings-core)
- assertion strings referencing "classic-yaml-core" should become "classic-settings-core" (cosmetic)

From classic-settings-core lib.rs (existing re-exports to keep):
- CacheStats, cache_keys, cache_size, cache_stats, clear_cache, get_cached, invalidate, is_cached, load_batch_async, load_batch_sync, load_settings_async, load_settings_sync, reset_cache_stats
- Result, SettingsError, SettingsSource
- load_yaml_async, load_yaml_batch_async, load_yaml_batch_sync, load_yaml_merged_async, load_yaml_merged_sync, load_yaml_sync, merge_yaml_documents, parse_yaml_content
- Yaml (re-export from yaml_rust2)

Consumer import sites (from RESEARCH.md):
- classic-config-core/src/lib.rs:27 -- pub use classic_yaml_core::clear_global_yaml_cache
- classic-config-core/src/yamldata.rs:18 -- use classic_yaml_core::YamlOperations
- classic-config-core/src/config.rs:8 -- use classic_yaml_core::YamlOperations
- classic-version-registry-core/src/registry.rs:11 -- use classic_yaml_core::YamlOperations
- classic-version-registry-core/src/error.rs:24 -- classic_yaml_core::YamlError in #[from]
- classic-cpp-bridge/src/yaml.rs:10 -- use classic_yaml_core::{CacheStats as YamlCacheStats, ...}
- classic-cpp-bridge/src/scanner.rs:15 -- use classic_yaml_core::YamlOperations
- classic-cpp-bridge/src/config.rs:682 -- classic_yaml_core::YamlOperations::new()
- classic-node/src/yaml.rs:11 -- use classic_yaml_core::{YamlError, YamlOperations, ...}

IntelliJ module config (for .iml cleanup):
- `ClassicLib-rs/.idea/ClassicLib-rs.iml` lines 33-35 currently declare:
  ```
  <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-yaml-core/benches" isTestSource="true" />
  <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-yaml-core/src" isTestSource="false" />
  <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-yaml-core/tests" isTestSource="true" />
  ```
  These three lines must be removed. Additionally, add new entries for settings-core tests and benches (lines 27 already declares settings-core/src; we need /tests and /benches added).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv yaml-core source/tests/benches files into settings-core (D-15 rename commit)</name>
  <files>
    ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs,
    ClassicLib-rs/business-logic/classic-settings-core/src/yaml_merge.rs,
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs,
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs,
    ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs
  </files>
  <read_first>
    ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs,
    ClassicLib-rs/business-logic/classic-yaml-core/src/merge.rs,
    ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs,
    ClassicLib-rs/business-logic/classic-yaml-core/tests/phase2_yaml_dead_code_audit.rs,
    ClassicLib-rs/business-logic/classic-yaml-core/benches/yaml_benchmarks.rs,
    ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs
  </read_first>
  <action>
    Per D-15, this task creates a git-mv-only commit to preserve blame history for EVERY file moving out of yaml-core (source, tests, AND benchmarks). Per checker BLOCKER 1 fix, tests and benchmarks must be git-mv'd here — NOT recreated from `git show` in Plan 01-03 (which would lose `git log --follow` history).

    Precondition: `git status` is clean before starting. Work in a clean working tree so the rename commit contains only the moves.

    1. Run `git mv` for the five files. `git mv` creates the destination parent directories automatically, so `tests/` and `benches/` under settings-core will come into existence as a side effect of these commands — no manual mkdir required.

       ```
       git mv ClassicLib-rs/business-logic/classic-yaml-core/src/lib.rs \
              ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs

       git mv ClassicLib-rs/business-logic/classic-yaml-core/src/merge.rs \
              ClassicLib-rs/business-logic/classic-settings-core/src/yaml_merge.rs

       git mv ClassicLib-rs/business-logic/classic-yaml-core/tests/integration_tests.rs \
              ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs

       git mv ClassicLib-rs/business-logic/classic-yaml-core/tests/phase2_yaml_dead_code_audit.rs \
              ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs

       git mv ClassicLib-rs/business-logic/classic-yaml-core/benches/yaml_benchmarks.rs \
              ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs
       ```

    2. Verify the rename via `git status` — all five should show as `renamed:` (not `deleted`/`new file` pairs). If any show as delete+add, abort and investigate (this usually means too much content drift for git's similarity detector, which should not happen since no content changes yet).

    3. Commit with message: `Refactor: git mv yaml-core source, tests, and benches into settings-core`

    IMPORTANT: Do NOT make any content changes in this commit. The files must be moved as-is so git tracks them as renames and `git log --follow` preserves blame. All content edits (CacheStats rename, module path fixes, include_str! fixes, import rewrites, etc.) happen in Task 2.

    IMPORTANT: The yaml-core crate directory still exists after this task (it has Cargo.toml and now-empty src/, tests/, benches/ directories — or possibly empty directories pruned by git). Do NOT delete it yet. Do NOT update any Cargo.toml files yet.
  </action>
  <verify>
    <automated>cd J:/CLASSIC-Fallout4 && git log --diff-filter=R --name-status -1 HEAD | grep -c "yaml_ops\|yaml_merge\|yaml_integration_tests\|yaml_dead_code_audit\|yaml_benchmarks"</automated>
  </verify>
  <acceptance_criteria>
    - `ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs` exists and is the former yaml-core lib.rs (byte-identical)
    - `ClassicLib-rs/business-logic/classic-settings-core/src/yaml_merge.rs` exists and is the former yaml-core merge.rs (byte-identical)
    - `ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs` exists and is the former yaml-core integration_tests.rs (byte-identical)
    - `ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs` exists and is the former phase2_yaml_dead_code_audit.rs (byte-identical)
    - `ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs` exists and is the former yaml-core yaml_benchmarks.rs (byte-identical)
    - All five originals at old paths no longer exist
    - `git log --follow ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs` shows commits from original yaml-core lib.rs history
    - `git log --follow ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs` shows commits from original phase2_yaml_dead_code_audit.rs history
    - `git show --stat HEAD` shows all five files as renames (not deletions+additions)
    - Commit message starts with `Refactor:`
    - No content changes in the commit (pure rename across all five files)
  </acceptance_criteria>
  <done>Five yaml-core files (2 src + 2 tests + 1 bench) are git-mv'd into settings-core with blame history preserved. A dedicated rename-only commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: Content edits, dependency absorption, consumer migration, test/bench compile fixes, and yaml-core removal (D-01..D-04, D-10, YAML-01, YAML-02, YAML-03)</name>
  <files>
    ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs,
    ClassicLib-rs/business-logic/classic-settings-core/src/yaml_merge.rs,
    ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs,
    ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml,
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs,
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs,
    ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs,
    ClassicLib-rs/business-logic/classic-config-core/Cargo.toml,
    ClassicLib-rs/business-logic/classic-config-core/src/lib.rs,
    ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs,
    ClassicLib-rs/business-logic/classic-config-core/src/config.rs,
    ClassicLib-rs/business-logic/classic-version-registry-core/Cargo.toml,
    ClassicLib-rs/business-logic/classic-version-registry-core/src/registry.rs,
    ClassicLib-rs/business-logic/classic-version-registry-core/src/error.rs,
    ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/Cargo.toml,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/yaml.rs,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/config.rs,
    ClassicLib-rs/node-bindings/classic-node/Cargo.toml,
    ClassicLib-rs/node-bindings/classic-node/src/yaml.rs,
    ClassicLib-rs/Cargo.toml,
    ClassicLib-rs/.idea/ClassicLib-rs.iml
  </files>
  <read_first>
    ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs,
    ClassicLib-rs/business-logic/classic-settings-core/src/yaml_merge.rs,
    ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs,
    ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml,
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs,
    ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs,
    ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs,
    ClassicLib-rs/business-logic/classic-yaml-core/Cargo.toml,
    ClassicLib-rs/business-logic/classic-config-core/Cargo.toml,
    ClassicLib-rs/business-logic/classic-config-core/src/lib.rs,
    ClassicLib-rs/business-logic/classic-config-core/src/yamldata.rs,
    ClassicLib-rs/business-logic/classic-config-core/src/config.rs,
    ClassicLib-rs/business-logic/classic-version-registry-core/Cargo.toml,
    ClassicLib-rs/business-logic/classic-version-registry-core/src/registry.rs,
    ClassicLib-rs/business-logic/classic-version-registry-core/src/error.rs,
    ClassicLib-rs/business-logic/classic-scanlog-core/Cargo.toml,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/Cargo.toml,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/yaml.rs,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scanner.rs,
    ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/config.rs,
    ClassicLib-rs/node-bindings/classic-node/Cargo.toml,
    ClassicLib-rs/node-bindings/classic-node/src/yaml.rs,
    ClassicLib-rs/Cargo.toml,
    ClassicLib-rs/.idea/ClassicLib-rs.iml
  </read_first>
  <action>
    This is the big coordinated edit that makes the workspace compile after the file moves. All changes described below go in a single commit.

    **A. yaml_ops.rs content edits (D-01, D-03):**

    1. Change the module-level doc comment from "CLASSIC YAML Core - Pure Rust YAML business logic" to "YAML operations absorbed from classic-yaml-core (D-01)."
    2. Replace every `use crate::` that referenced yaml-core internals to reference `crate::` (now settings-core). The `mod merge` import at the bottom of the old lib.rs must be REMOVED (it was a module declaration, not needed in yaml_ops.rs since yaml_merge.rs is declared in lib.rs).
    3. Apply the D-03 CacheStats rename:
       - Rename `pub struct CacheStats` to `pub struct YamlCacheStats`
       - Rename `pub fn cache_stats() -> CacheStats` to `pub fn yaml_cache_stats() -> YamlCacheStats`
       - Rename `pub fn reset_cache_stats()` to `pub fn reset_yaml_cache_stats()`
       - Update all internal references within the file to use the new names
    4. Remove the `pub mod merge;` declaration at the bottom of the file (it was the module mount in the old lib.rs -- no longer applicable since lib.rs now declares the module)
    5. Remove the `pub use merge::merge_keys;` re-export line (this is now handled by settings-core lib.rs)
    6. Keep all inline `#[cfg(test)] mod tests` blocks as-is (they use `super::*` which will resolve correctly)

    **B. yaml_merge.rs content edits:**

    1. Change `use crate::YamlError;` -- this continues to work because `YamlError` is still defined in yaml_ops.rs and re-exported from the crate root
    2. Update doc comment examples: change `use classic_yaml_core::{YamlOperations, merge_keys};` to `use classic_settings_core::{YamlOperations, merge_keys};`
    3. In `#[cfg(test)] mod tests`, change `use crate::YamlOperations;` -- this works via the re-export in lib.rs

    **C. settings-core lib.rs updates (D-01, D-04):**

    Add the following lines after the existing module declarations (`mod cache; mod error; mod loader; pub mod validators;`):

    ```rust
    // YAML operations (absorbed from classic-yaml-core per D-01)
    mod yaml_ops;
    mod yaml_merge;
    ```

    Add the following re-exports after the existing `pub use` blocks:

    ```rust
    // YAML operations re-exports (D-04 flat re-exports)
    pub use yaml_ops::{
        YamlCacheStats, YamlError, YamlOperations,
        clear_global_yaml_cache, reset_yaml_cache_stats, yaml_cache_stats,
    };
    pub use yaml_merge::merge_keys;
    ```

    Keep the existing `pub use yaml_rust2::Yaml;` re-export (already present -- no duplication).

    **D. settings-core Cargo.toml dependency and bench-entry updates (Wave 0 items from VALIDATION.md):**

    Add to `[dependencies]`:
    ```toml
    indexmap = { workspace = true }
    serde_json = { workspace = true }
    ```

    Add to `[dev-dependencies]`:
    ```toml
    criterion = { version = "0.6.0", features = ["html_reports"] }
    ```

    Add at the end of the file (new section — tests auto-discover but custom-harness benchmarks need explicit registration):
    ```toml
    [[bench]]
    name = "yaml_benchmarks"
    harness = false
    ```

    The `dhat-heap` feature already exists in both crates -- no change needed (both use `dhat = { version = "0.3", optional = true }`).

    **E. Migrated test file content edits (D-10, checker BLOCKER 2 fix):**

    1. `tests/yaml_integration_tests.rs` — rewrite imports to use settings-core:
       - Replace `use classic_yaml_core::` with `use classic_settings_core::` (may appear multiple times)
       - Replace bare `CacheStats` with `YamlCacheStats`
       - Replace bare `cache_stats()` with `yaml_cache_stats()` (be careful: settings-core ALSO exports a `cache_stats()` for settings cache — the yaml tests want the yaml-specific one)
       - Replace bare `reset_cache_stats()` with `reset_yaml_cache_stats()` (same caveat)
       - `clear_global_yaml_cache()` keeps its name — no change needed

    2. `tests/yaml_dead_code_audit.rs` — fix the `include_str!` macros that now point at wrong paths (BLOCKER 2):
       - Change `const LIB_RS: &str = include_str!("../src/lib.rs");` to `const LIB_RS: &str = include_str!("../src/yaml_ops.rs");`
       - Change `const INTEGRATION_TESTS_RS: &str = include_str!("integration_tests.rs");` to `const INTEGRATION_TESTS_RS: &str = include_str!("yaml_integration_tests.rs");`
       - `const YAML_BENCHMARKS_RS: &str = include_str!("../benches/yaml_benchmarks.rs");` — path string is unchanged (the file is still named yaml_benchmarks.rs, just in settings-core now), but verify it still resolves from the new audit file location
       - Update assertion messages: change any strings containing `classic-yaml-core` or `classic_yaml_core` to `classic-settings-core` / `classic_settings_core` (e.g., `"classic-yaml-core should continue exposing YamlOperations"` → `"classic-settings-core should continue exposing YamlOperations"`). This is cosmetic but matches documentary intent.

    3. `benches/yaml_benchmarks.rs` — rewrite imports:
       - Change `use classic_yaml_core::YamlOperations` to `use classic_settings_core::YamlOperations` (and any other yaml-core imports)
       - If the file has `#[path = "../../../benches/common/mod.rs"]` or similar, verify the relative path still resolves from `ClassicLib-rs/business-logic/classic-settings-core/benches/` (three levels up lands at `ClassicLib-rs/`). If the path is wrong, compute the correct relative path from the new location. Most likely the original yaml-core bench used the same relative depth so the path stays identical.

    **F. Consumer Cargo.toml updates (YAML-02):**

    For each consumer, remove the `classic-yaml-core` dependency line. Add `classic-settings-core` dependency if not already present:

    - `classic-config-core/Cargo.toml`: Remove `classic-yaml-core = { path = "../classic-yaml-core" }`. Already has `classic-settings-core` dep -- no addition needed.
    - `classic-version-registry-core/Cargo.toml`: Remove `classic-yaml-core = { path = "../classic-yaml-core" }`. Add `classic-settings-core = { path = "../classic-settings-core" }` (does not currently depend on settings-core).
    - `classic-scanlog-core/Cargo.toml`: Remove `classic-yaml-core = { path = "../classic-yaml-core" }`. Verify no direct `use classic_yaml_core` in scanlog-core source (research says there are none).
    - `classic-cpp-bridge/Cargo.toml`: Remove `classic-yaml-core = { path = "../../business-logic/classic-yaml-core" }`. Already has `classic-settings-core` dep.
    - `classic-node/Cargo.toml` (at `ClassicLib-rs/node-bindings/classic-node/Cargo.toml`): Remove `classic-yaml-core = { path = "../../business-logic/classic-yaml-core" }`. Already has `classic-settings-core` dep.

    **F.5. scanlog-core direct-usage grep verification (reviews-mode concern 11):**

    Before removing the `classic-yaml-core` line from `classic-scanlog-core/Cargo.toml`, run:

    ```
    grep -rn "classic_yaml_core\|classic-yaml-core" ClassicLib-rs/business-logic/classic-scanlog-core/src/
    ```

    Expected result: **zero hits in `src/`**. A hit in `Cargo.toml` is expected (that is the line being removed). A hit inside `target/` or a stale `build_output.txt` artifact is fine. If there IS a hit in `src/*.rs`, STOP — the research claim that scanlog-core has no direct yaml-core `use` statements is wrong and additional source edits are needed before the dep line can be removed safely.

    **G. Consumer source import updates (YAML-02):**

    - `classic-config-core/src/lib.rs` line ~27: Change `pub use classic_yaml_core::clear_global_yaml_cache` to `pub use classic_settings_core::clear_global_yaml_cache`
    - `classic-config-core/src/yamldata.rs` line ~18: Change `use classic_yaml_core::YamlOperations` to `use classic_settings_core::YamlOperations`
    - `classic-config-core/src/config.rs` line ~8: Change `use classic_yaml_core::YamlOperations` to `use classic_settings_core::YamlOperations`
    - `classic-version-registry-core/src/registry.rs` line ~11: Change `use classic_yaml_core::YamlOperations` to `use classic_settings_core::YamlOperations`
    - `classic-version-registry-core/src/error.rs` line ~24: Change `classic_yaml_core::YamlError` to `classic_settings_core::YamlError` in the `#[from]` variant
    - `classic-cpp-bridge/src/yaml.rs` line ~10: Change `use classic_yaml_core::{CacheStats as YamlCacheStats, YamlOperations, cache_stats as yaml_cache_stats}` to `use classic_settings_core::{YamlCacheStats, YamlOperations, yaml_cache_stats}` (no more alias needed per D-03)
    - `classic-cpp-bridge/src/scanner.rs` line ~15: Change `use classic_yaml_core::YamlOperations` to `use classic_settings_core::YamlOperations`
    - `classic-cpp-bridge/src/config.rs`: Find `classic_yaml_core::YamlOperations::new()` and change to `classic_settings_core::YamlOperations::new()`. Also check for any `use classic_yaml_core` at the top and change accordingly.
    - `classic-node/src/yaml.rs` line ~11: Change `use classic_yaml_core::{YamlError, YamlOperations, cache_stats as yaml_cache_stats}` to `use classic_settings_core::{YamlError, YamlOperations, yaml_cache_stats}` (no more alias needed per D-03)

    **H. Workspace Cargo.toml edits — yaml-core removal AND yaml-py workspace-member removal (YAML-03 + reviews-mode BLOCKER 1 fix):**

    This section implements the reviews-mode BLOCKER 1 consensus fix. Both reviewers independently caught that deleting `classic-yaml-core` from disk while `classic-yaml-py` still lists it as a path dependency AND is still a workspace member will break `cargo build --workspace` at the end of Wave 1. The fix (Option A from the consensus summary) is to also drop `classic-yaml-py` from `members` in the same commit. The `classic-yaml-py` crate directory itself stays on disk until Plan 01-02 Task 3 — only its workspace membership is revoked here.

    1. In `ClassicLib-rs/Cargo.toml`, open the `members = [ ... ]` array.

    2. Remove the line: `"business-logic/classic-yaml-core",`

    3. Remove the line: `"python-bindings/classic-yaml-py",` (reviews-mode BLOCKER 1)

       Rationale: after step H.1, `classic-yaml-py/Cargo.toml` still declares `classic-yaml-core = { path = "../../business-logic/classic-yaml-core" }`. Leaving `classic-yaml-py` as a workspace member would make cargo try to resolve that dep during `cargo build --workspace`, which fails because the path no longer exists. Dropping workspace membership removes the crate from the build graph so the workspace still builds. The crate remains on disk (its source is untouched) so Plan 01-02 Task 3 can still read `classic-yaml-py/src/lib.rs` and copy content into `classic-settings-py`.

    4. Delete the entire `ClassicLib-rs/business-logic/classic-yaml-core/` directory. After Task 1's git-mv'd files, yaml-core contains only Cargo.toml and (possibly git-pruned) empty src/tests/benches directories. Remove everything including the Cargo.toml.

    5. Do **NOT** delete `ClassicLib-rs/python-bindings/classic-yaml-py/` here. That deletion happens in Plan 01-02 Task 3 after its content is folded into settings-py.

    **I. IntelliJ metadata cleanup — describe edits by XML element content, not line number (reviews-mode LOW concern fix):**

    Line numbers in `.iml` files drift and are fragile across IDE refreshes. Describe each edit by the exact XML element whose content contains `classic-yaml-core`. Use grep to locate, then remove the matching elements.

    **I.1. `ClassicLib-rs/.idea/ClassicLib-rs.iml`**

    Remove every `<sourceFolder>` element whose `url` attribute contains `business-logic/classic-yaml-core`. At revision time there are three such elements:

    ```xml
    <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-yaml-core/benches" isTestSource="true" />
    <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-yaml-core/src" isTestSource="false" />
    <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-yaml-core/tests" isTestSource="true" />
    ```

    Add two new `<sourceFolder>` elements adjacent to the existing `business-logic/classic-settings-core/src` element so the IDE picks up the newly-migrated tests and benches:

    ```xml
    <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-settings-core/tests" isTestSource="true" />
    <sourceFolder url="file://$MODULE_DIR$/business-logic/classic-settings-core/benches" isTestSource="true" />
    ```

    Keep XML well-formed and preserve surrounding indentation.

    **I.2. `.idea/CLASSIC-Fallout4.iml` (repo root — reviews-mode LOW concern)**

    The root `.iml` also contains stale yaml-core entries. Grep first, then remove each matched element by content:

    ```
    grep -n "classic-yaml-core" .idea/CLASSIC-Fallout4.iml
    ```

    At revision time the known yaml-core references in this file are at lines 28 and 59-61 (a short-path `<sourceFolder url=".../classic-yaml-core/src" .../>` plus the fully-qualified `ClassicLib-rs/business-logic/classic-yaml-core/benches|src|tests` entries). Remove every `<sourceFolder>` element whose `url` attribute contains `classic-yaml-core`. NOTE: This file is already listed as modified in the current git status at revision time; the executor must reconcile any pre-existing in-flight edits before committing.

    **I.3. `ClassicLib-rs/.idea/workspace.xml` (reviews-mode LOW concern)**

    Grep for yaml-core package references:

    ```
    grep -n "classic-yaml-core" ClassicLib-rs/.idea/workspace.xml
    ```

    Remove every `<package file="$PROJECT_DIR$/business-logic/classic-yaml-core">` element (and any matching closing tag / child payload nodes) found. At revision time this is at approximately line 128. Do NOT alter other workspace.xml state (breakpoints, run configs, recent files, etc.).

    **I.4. yaml-py IDE cleanup — deferred**

    `classic-yaml-py` source folder entries in `ClassicLib-rs/.idea/ClassicLib-rs.iml` (two entries matching `python-bindings/classic-yaml-py`) and the root `.idea/CLASSIC-Fallout4.iml` (entries matching `classic-yaml-py/src`) and `workspace.xml` (the `<package file=".../classic-yaml-py">` element) are deferred to Plan 01-02 Task 3 — that is where the yaml-py crate is deleted from disk, so co-locating the IDE cleanup there keeps responsibility single-sourced.

    **J. Commit:**

    Commit with message: `Refactor: Absorb classic-yaml-core into classic-settings-core and migrate all consumers`
  </action>
  <verify>
    <automated>cd J:/CLASSIC-Fallout4 && source tools/use_msvc_from_git_bash.sh && cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml && cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0
    - `cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml` exits 0 (all migrated integration tests + dead code audit test pass)
    - `cargo bench -p classic-settings-core --no-run --manifest-path ClassicLib-rs/Cargo.toml` exits 0 (benchmarks compile)
    - `grep -r "classic.yaml.core" ClassicLib-rs/*/Cargo.toml ClassicLib-rs/*/*/Cargo.toml ClassicLib-rs/*/*/*/Cargo.toml 2>/dev/null | grep -v target | grep -v ".git"` returns no results
    - `grep -r "classic_yaml_core" ClassicLib-rs/business-logic/*/src/*.rs ClassicLib-rs/cpp-bindings/*/src/*.rs ClassicLib-rs/node-bindings/*/src/*.rs 2>/dev/null | grep -v target` returns no results
    - File `ClassicLib-rs/business-logic/classic-yaml-core/` directory does not exist
    - `ClassicLib-rs/Cargo.toml` does not contain `classic-yaml-core` in members list
    - `ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs` contains `pub struct YamlCacheStats` (not `pub struct CacheStats`)
    - `ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs` contains `pub fn yaml_cache_stats()`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/yaml_ops.rs` contains `pub fn reset_yaml_cache_stats()`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs` contains `mod yaml_ops;`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs` contains `mod yaml_merge;`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs` contains `pub use yaml_ops::`
    - `ClassicLib-rs/business-logic/classic-settings-core/src/lib.rs` contains `pub use yaml_merge::merge_keys`
    - `ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml` contains `indexmap`
    - `ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml` contains `serde_json`
    - `ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml` contains `criterion`
    - `ClassicLib-rs/business-logic/classic-settings-core/Cargo.toml` contains `name = "yaml_benchmarks"` under a `[[bench]]` section
    - `ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs` contains `include_str!("../src/yaml_ops.rs")` (NOT `../src/lib.rs`)
    - `ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_dead_code_audit.rs` contains `include_str!("yaml_integration_tests.rs")` (NOT `integration_tests.rs`)
    - `ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs` contains `use classic_settings_core` (NOT `classic_yaml_core`)
    - `ClassicLib-rs/business-logic/classic-settings-core/benches/yaml_benchmarks.rs` contains `use classic_settings_core::YamlOperations`
    - `ClassicLib-rs/business-logic/classic-version-registry-core/Cargo.toml` contains `classic-settings-core`
    - `ClassicLib-rs/.idea/ClassicLib-rs.iml` does NOT contain `classic-yaml-core/src` or `classic-yaml-core/tests` or `classic-yaml-core/benches`
    - `ClassicLib-rs/.idea/ClassicLib-rs.iml` contains `classic-settings-core/tests` and `classic-settings-core/benches` sourceFolder entries
    - `ClassicLib-rs/Cargo.toml` does not contain `classic-yaml-py` in the `members` list (reviews-mode BLOCKER 1 fix — the directory itself still exists on disk until Plan 01-02 Task 3)
    - `ClassicLib-rs/python-bindings/classic-yaml-py/` directory DOES still exist on disk (will be deleted in Plan 01-02 Task 3)
    - `grep -rn "classic_yaml_core\|classic-yaml-core" ClassicLib-rs/business-logic/classic-scanlog-core/src/` returns 0 matches (reviews-mode concern 11 — no source-level dependency)
    - `.idea/CLASSIC-Fallout4.iml` does NOT contain any `<sourceFolder>` element whose url attribute contains `classic-yaml-core` (reviews-mode LOW concern)
    - `ClassicLib-rs/.idea/workspace.xml` does NOT contain `<package file="$PROJECT_DIR$/business-logic/classic-yaml-core">` (reviews-mode LOW concern)
    - **Wave 1 acceptance gate:** `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` exits 0 at the end of this task, leaving the workspace in a buildable state before Wave 2 starts (reviews-mode BLOCKER 1)
  </acceptance_criteria>
  <done>classic-yaml-core is fully absorbed into classic-settings-core. All workspace crates compile. All consumer imports point to classic_settings_core. Migrated tests and benchmarks compile and pass from settings-core. The classic-yaml-core crate directory is deleted and removed from workspace members. `classic-yaml-py` is removed from workspace members (but its on-disk directory persists for Plan 01-02 Task 3 to fold into settings-py). IntelliJ metadata files (.iml + workspace.xml, both ClassicLib-rs and repo root) no longer reference yaml-core source folders. **Wave 1 ends with a buildable workspace.**</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `cargo build --workspace --manifest-path ClassicLib-rs/Cargo.toml` -- MUST exit 0
2. `cargo test -p classic-settings-core --manifest-path ClassicLib-rs/Cargo.toml` -- MUST exit 0 (includes migrated tests + audit)
3. `cargo bench -p classic-settings-core --no-run --manifest-path ClassicLib-rs/Cargo.toml` -- MUST exit 0 (benchmarks compile)
4. `cargo clippy --workspace --all-targets --all-features --manifest-path ClassicLib-rs/Cargo.toml -- -D warnings` -- SHOULD exit 0 (may have pre-existing warnings)
5. No file in the workspace references `classic_yaml_core` or `classic-yaml-core` (except git history, archived docs, parity contracts, and target/)
6. `git log --follow ClassicLib-rs/business-logic/classic-settings-core/tests/yaml_integration_tests.rs` spans commits from the original yaml-core integration_tests.rs era
</verification>

<success_criteria>
- classic-yaml-core source, tests, and benchmarks live in classic-settings-core with full public API preserved and blame history intact (YAML-01, D-10, D-15)
- All consumers import from classic_settings_core (YAML-02)
- classic-yaml-core crate removed from workspace and directory deleted (YAML-03)
- Workspace compiles, settings-core tests pass, settings-core benchmarks compile
- IntelliJ module config cleaned up (no dangling yaml-core source folders)
</success_criteria>

<output>
After completion, create `.planning/phases/01-yaml-settings-merge/01-01-SUMMARY.md`
</output>
</content>
</invoke>