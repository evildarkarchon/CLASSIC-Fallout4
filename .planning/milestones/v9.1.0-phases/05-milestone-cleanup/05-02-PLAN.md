---
phase: 05-milestone-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - ClassicLib-rs/business-logic/classic-constants-core/
  - tests/planning/test_phase05_validation.py
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - The live workspace no longer contradicts the passed Phase 3 verification claim that `classic-constants-core` is gone.
    - Phase 5's planning audit fails if the retired `classic-constants-core` directory reappears or if Phase 3 bookkeeping drifts from live-tree reality again.
  artifacts:
    - path: ClassicLib-rs/business-logic/classic-constants-core/
      provides: Retired Phase 3 crate directory removed from the live tree.
    - path: tests/planning/test_phase05_validation.py
      provides: Automated audit coverage for the live-tree condition behind the refreshed Phase 3 verification artifact.
  key_links:
    - from: tests/planning/test_phase05_validation.py
      to: ClassicLib-rs/business-logic/classic-constants-core/
      via: pathlib live-tree absence assertion
      pattern: classic-constants-core
    - from: tests/planning/test_phase05_validation.py
      to: .planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md
      via: verification artifact fragment assertions
      pattern: "status: passed|stale `gaps_found` bookkeeping"
---

<objective>
Close the last verified Phase 5 gap by making the live tree match the canonical Phase 3 closure artifact and locking that condition into the planning audit.

Purpose: The current Phase 5 verifier found one real mismatch: `03-VERIFICATION.md` says the retired `classic-constants-core` directory is gone, but an empty leftover directory still exists and the audit does not check it.
Output: The stray retired crate directory is removed and `tests/planning/test_phase05_validation.py` proves that absence in future runs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-cleanup/05-VERIFICATION.md
@.planning/phases/05-milestone-cleanup/05-VALIDATION.md
@.planning/phases/05-milestone-cleanup/05-01-SUMMARY.md
@.planning/phases/03-constants-version-registry-merge/03-01-SUMMARY.md
@.planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md
@.planning/phases/03-constants-version-registry-merge/03-VALIDATION.md
@tests/planning/test_phase05_validation.py

<interfaces>
From tests/planning/test_phase05_validation.py:
```python
REPO_ROOT = Path(__file__).resolve().parents[2]
PHASE3_VERIFICATION = (
    REPO_ROOT
    / ".planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md"
)

class Phase05ValidationAuditTests(unittest.TestCase):
    def test_phase3_verification_reports_the_live_passed_state(self) -> None:
        ...
```

From .planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md:
```markdown
| 5 | `classic-constants-core` is removed from workspace membership and disk. | ✓ VERIFIED | The current tree still has no `ClassicLib-rs/business-logic/classic-constants-core/` directory and no workspace member entry. |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the leftover retired constants-core directory</name>
  <files>ClassicLib-rs/business-logic/classic-constants-core/</files>
  <action>Delete the stray `ClassicLib-rs/business-logic/classic-constants-core/` directory and its empty `src/` child so the live workspace matches the already-passed Phase 3 closure artifact. Preserve the prior redistribution outcome from Phase 3 plan 03-01 and the cleanup intent from Phase 05-01: do not recreate stubs, do not add compatibility files, and do not touch workspace manifests unless you find a real surviving reference. This is the minimal fix because the canonical verification artifact already describes the intended post-merge truth.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "if (Test-Path 'ClassicLib-rs/business-logic/classic-constants-core' -PathType Container) { Write-Error 'classic-constants-core still exists'; exit 1 }"</automated>
  </verify>
  <done>The retired `classic-constants-core` directory no longer exists anywhere under `ClassicLib-rs/business-logic/`, so the live tree no longer contradicts `03-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the live-tree absence into the Phase 5 audit</name>
  <files>tests/planning/test_phase05_validation.py</files>
  <action>Strengthen `tests/planning/test_phase05_validation.py` so the Phase 3 verification audit proves the live-tree condition behind the artifact text, not just selected fragments. Keep the existing repo-native `unittest.TestCase` style, add a path constant for `ClassicLib-rs/business-logic/classic-constants-core/`, and extend the Phase 3 verification test to assert the directory is absent on disk while the refreshed `03-VERIFICATION.md` still contains the passed-state fragments. Do not broaden scope into new parity assertions or artifact rewrites; this task exists only to prevent the same bookkeeping drift from escaping again.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase05_validation.py -q -k phase3_verification</automated>
  </verify>
  <done>The Phase 5 planning audit fails if the retired constants-core directory reappears or if the Phase 3 passed-state bookkeeping drifts from live-tree reality.</done>
</task>

</tasks>

<verification>
- Run `python -m pytest tests/planning/test_phase05_validation.py -q` after the audit change.
- Re-run `python -m pytest tests/planning/test_phase05_validation.py tools/node_api_parity/tests/test_check_parity_gate.py -q` before handing back for re-verification so the full Phase 5 audit surface remains green.
</verification>

<success_criteria>
- `ClassicLib-rs/business-logic/classic-constants-core/` is gone from the live repo tree.
- `tests/planning/test_phase05_validation.py` asserts both the passed-state fragments and the real on-disk absence behind the Phase 3 verification claim.
- The targeted and full Phase 5 pytest audits pass without further Phase 3 bookkeeping edits.
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cleanup/05-02-SUMMARY.md`
</output>
