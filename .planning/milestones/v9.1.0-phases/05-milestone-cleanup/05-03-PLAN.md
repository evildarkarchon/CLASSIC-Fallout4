---
phase: 05-milestone-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - docs/implementation/node_api_parity/baseline/parity_contract.md
  - tests/planning/test_phase05_validation.py
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - The committed human-readable Node parity contract describes the live 705-row one-tier baseline instead of the retired hybrid Tier-1/Tier-2 model.
    - Phase 5's planning audit fails if `parity_contract.md` drifts away from the live one-tier contract tracked by the JSON contract and diff report.
  artifacts:
    - path: docs/implementation/node_api_parity/baseline/parity_contract.md
      provides: Human-readable Node parity contract narrative aligned to the live one-tier 705-row baseline.
    - path: tests/planning/test_phase05_validation.py
      provides: Automated audit coverage that keeps the markdown contract synchronized with the live contract artifacts.
  key_links:
    - from: tests/planning/test_phase05_validation.py
      to: docs/implementation/node_api_parity/baseline/parity_contract.md
      via: file-backed contract narrative assertions
      pattern: one-tier|705|Tier 2
    - from: docs/implementation/node_api_parity/baseline/parity_contract.md
      to: docs/implementation/node_api_parity/baseline/parity_contract.json
      via: synchronized contract narrative
      pattern: tierDefinitions|tier1Mappings
---

<objective>
Close the remaining Phase 5 verification gap by making the human-readable Node parity contract agree with the live 705-row one-tier baseline and by locking that agreement into the Phase 5 audit.

Purpose: Phase 5 still fails because `parity_contract.md` preserves hybrid Tier-1/Tier-2 governance language even though the executable tripwire, deferred note, JSON contract, and diff report already agree on a one-tier 705-row contract.
Output: A refreshed `parity_contract.md` plus a stronger `tests/planning/test_phase05_validation.py` audit that catches future markdown-contract drift.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-cleanup/05-VERIFICATION.md
@.planning/phases/05-milestone-cleanup/05-RESEARCH.md
@.planning/phases/05-milestone-cleanup/05-01-SUMMARY.md
@.planning/phases/05-milestone-cleanup/05-02-SUMMARY.md
@.planning/phases/02-crashgen-config-merge/deferred-items.md
@docs/implementation/node_api_parity/baseline/parity_contract.md
@docs/implementation/node_api_parity/baseline/parity_contract.json
@docs/implementation/node_api_parity/baseline/parity_diff_report.md
@tools/node_api_parity/tests/test_check_parity_gate.py
@tests/planning/test_phase05_validation.py

<interfaces>
From docs/implementation/node_api_parity/baseline/parity_contract.md (stale narrative to replace):
```markdown
This contract establishes a hybrid-tiered baseline: Tier-1 is enforced now, Tier-2 is intentionally triaged.

- **Tier 2 (defer-capable)**
  - Lower-level internals and advanced APIs not required for immediate workflow parity.
```

From docs/implementation/node_api_parity/baseline/parity_diff_report.md (live source of truth):
```markdown
- Tier-1 contract rows: **705**
- Tier-1 matched: **705**
- Total gaps: **0**
```

From tools/node_api_parity/tests/test_check_parity_gate.py:
```python
assert len(tier1) >= 705
assert "tier2" not in tier_defs
```

From tests/planning/test_phase05_validation.py:
```python
def test_node_floor_reconciliation_matches_the_live_one_tier_contract(self) -> None:
    gate_test = read_text(NODE_PARITY_GATE_TEST)
    deferred_items = read_text(PHASE2_DEFERRED_ITEMS)
    diff_report = read_text(NODE_PARITY_DIFF_REPORT)
    contract = json.loads(read_text(NODE_PARITY_CONTRACT))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the markdown Node parity contract to the live one-tier baseline</name>
  <files>docs/implementation/node_api_parity/baseline/parity_contract.md</files>
  <action>Rewrite `docs/implementation/node_api_parity/baseline/parity_contract.md` so its prose matches the live source-backed contract already enforced elsewhere: document a one-tier Tier-1-only baseline, remove the stale hybrid/Tier-2 governance language, and make the acceptance criteria and source-of-truth section explicitly consistent with `parity_contract.json`, `parity_diff_report.md`, `.planning/phases/02-crashgen-config-merge/deferred-items.md`, and `tools/node_api_parity/tests/test_check_parity_gate.py`. Keep scope cleanup-only per `05-RESEARCH.md`: do not refresh baselines, do not redesign the Node API policy, and do not touch the machine-readable contract unless a real contradiction is discovered.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "$text = Get-Content 'docs/implementation/node_api_parity/baseline/parity_contract.md' -Raw; if ($text -match 'hybrid-tiered' -or $text -match 'Tier 2 \(defer-capable\)') { throw 'stale tiered narrative remains' }; if ($text -notmatch 'one-tier' -or $text -notmatch '705' -or $text -notmatch 'parity_contract.json') { throw 'live one-tier contract narrative incomplete' }"</automated>
  </verify>
  <done>`parity_contract.md` no longer claims a hybrid Tier-1/Tier-2 baseline and instead explains the live one-tier 705-row contract in the same terms used by the committed JSON contract, diff report, and tripwire test.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the Phase 5 audit to guard the markdown contract narrative</name>
  <files>tests/planning/test_phase05_validation.py</files>
  <action>Extend `tests/planning/test_phase05_validation.py` so the existing Node floor reconciliation audit also reads `docs/implementation/node_api_parity/baseline/parity_contract.md` and proves the human-readable contract stays synchronized with the live one-tier artifacts. Keep the existing `unittest.TestCase` style and file-backed assertions. The audit must fail if the markdown contract reintroduces `hybrid-tiered` or Tier 2 wording, omits the 705-row one-tier floor, or stops pointing readers at the machine-readable `parity_contract.json` source of truth. Do not add new scripts or manual steps; this guard belongs in the existing Phase 5 audit surface.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase05_validation.py -q -k node_floor_reconciliation</automated>
  </verify>
  <done>The Phase 5 audit now fails whenever `parity_contract.md` drifts away from the live one-tier contract documented in the JSON contract, diff report, deferred note, and tripwire test.</done>
</task>

</tasks>

<verification>
- Run `python -m pytest tests/planning/test_phase05_validation.py -q` after the audit change.
- Run `python -m pytest tools/node_api_parity/tests/test_check_parity_gate.py -q` to keep the executable tripwire green.
- Re-run `pwsh -NoProfile -Command "Set-Location 'ClassicLib-rs/node-bindings/classic-node'; bun run parity:gate"` before handoff so the plain Node gate still agrees with the refreshed narrative.
</verification>

<success_criteria>
- `docs/implementation/node_api_parity/baseline/parity_contract.md` describes the live one-tier Node parity contract and no longer documents active Tier 2 scope.
- `tests/planning/test_phase05_validation.py` asserts the markdown contract narrative stays synchronized with the 705-row one-tier JSON contract and diff report.
- The touched pytest audit, Node tripwire test, and plain Node parity gate all pass without refreshing the checked-in baselines.
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cleanup/05-03-SUMMARY.md`
</output>
