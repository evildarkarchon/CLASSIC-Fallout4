---
phase: 05-milestone-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - docs/implementation/node_api_parity/baseline/parity_contract.json
  - tests/planning/test_phase05_validation.py
  - tools/node_api_parity/tests/test_check_parity_gate.py
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - The machine-readable Node parity contract now describes the live 705-row one-tier baseline instead of the retired hybrid-tiered contract.
    - The Phase 5 audit and Node parity tripwire both fail if the committed JSON contract narrative drifts back to hybrid-tier wording.
  artifacts:
    - path: docs/implementation/node_api_parity/baseline/parity_contract.json
      provides: Machine-readable Node parity contract metadata aligned to the live one-tier 705-row baseline.
    - path: tests/planning/test_phase05_validation.py
      provides: Planning audit coverage that checks the JSON contract description for one-tier wording.
    - path: tools/node_api_parity/tests/test_check_parity_gate.py
      provides: Executable tripwire coverage that rejects stale hybrid-tier wording in the committed JSON contract.
  key_links:
    - from: tests/planning/test_phase05_validation.py
      to: docs/implementation/node_api_parity/baseline/parity_contract.json
      via: JSON description assertion
      pattern: one-tier|Hybrid-tiered|705
    - from: tools/node_api_parity/tests/test_check_parity_gate.py
      to: docs/implementation/node_api_parity/baseline/parity_contract.json
      via: direct committed-contract tripwire
      pattern: description|tier1Mappings|tierDefinitions
---

<objective>
Close the final Phase 5 verification gap by reconciling the machine-readable Node parity contract narrative with the live 705-row one-tier baseline and locking that wording into both audit surfaces.

Purpose: Phase 5 still fails because `parity_contract.json` keeps a stale `Hybrid-tiered` description even though the markdown contract, diff reports, deferred note, and executable floor checks already agree on the one-tier contract.
Output: A refreshed `parity_contract.json` description plus stronger audit/tripwire coverage that fails on stale hybrid-tier wording.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-cleanup/05-VERIFICATION.md
@.planning/phases/05-milestone-cleanup/05-RESEARCH.md
@.planning/phases/05-milestone-cleanup/05-03-SUMMARY.md
@docs/implementation/node_api_parity/baseline/parity_contract.json
@docs/implementation/node_api_parity/baseline/parity_contract.md
@tests/planning/test_phase05_validation.py
@tools/node_api_parity/tests/test_check_parity_gate.py

<interfaces>
From docs/implementation/node_api_parity/baseline/parity_contract.json:
```json
{
  "description": "Hybrid-tiered Node vs Rust parity contract for high-level APIs.",
  "tierDefinitions": {
    "tier1": "Must-have high-level APIs required by app workflows and cross-language integration."
  }
}
```

From tests/planning/test_phase05_validation.py:
```python
self.assertEqual(len(contract.get("tier1Mappings", [])), 705)
self.assertNotIn("tier2", contract.get("tierDefinitions", {}))
```

From tools/node_api_parity/tests/test_check_parity_gate.py:
```python
assert len(tier1) >= 705
assert "tier2" not in tier_defs
```

From .planning/phases/05-milestone-cleanup/05-VERIFICATION.md:
```markdown
missing:
  - Refresh `docs/implementation/node_api_parity/baseline/parity_contract.json` so its `description` matches the live one-tier 705-row contract.
  - Extend the Phase 5 audit and/or the Node tripwire to fail if the machine-readable contract narrative reintroduces `Hybrid-tiered` wording.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the JSON Node parity contract description to the live one-tier baseline</name>
  <files>docs/implementation/node_api_parity/baseline/parity_contract.json</files>
  <action>Update the top-level `description` in `docs/implementation/node_api_parity/baseline/parity_contract.json` so it explicitly names the live one-tier 705-row Node vs Rust parity contract, per the remaining verifier gap and the locked cleanup scope in `05-RESEARCH.md`. Keep this edit narrative-only: do not regenerate baselines, do not change `tier1Mappings`, and do not reintroduce any Tier 2 language. The JSON description must agree with `parity_contract.md`, the 705/705 diff report, and the current tripwire expectations.</action>
  <verify>
    <automated>python -c "import json, pathlib; data=json.loads(pathlib.Path(r'docs/implementation/node_api_parity/baseline/parity_contract.json').read_text(encoding='utf-8')); desc=data.get('description',''); assert 'one-tier' in desc and '705' in desc and 'Hybrid-tiered' not in desc, desc"</automated>
  </verify>
  <done>`parity_contract.json` describes the live one-tier 705-row contract and no longer contradicts the other committed Node parity artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Extend both audit surfaces to fail on stale JSON contract wording</name>
  <files>tests/planning/test_phase05_validation.py, tools/node_api_parity/tests/test_check_parity_gate.py</files>
  <action>Extend the existing Phase 5 audit and the committed Node parity tripwire so both directly read `parity_contract.json` and fail if its description drifts back to hybrid-tier wording. Preserve the repo-native testing styles already in use: `unittest.TestCase` under pytest for `tests/planning/test_phase05_validation.py`, and plain pytest assertions for `tools/node_api_parity/tests/test_check_parity_gate.py`. Keep the current 705-row floor and `tier2`-absence checks intact; add narrative checks instead of replacing them. This closes the remaining verifier gap by making stale machine-readable wording a durable automated failure.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase05_validation.py tools/node_api_parity/tests/test_check_parity_gate.py -q</automated>
  </verify>
  <done>Both the planning audit and the parity tripwire reject stale hybrid-tier wording in the committed JSON contract while preserving the live 705-row one-tier invariants.</done>
</task>

</tasks>

<verification>
- Run `python -m pytest tests/planning/test_phase05_validation.py -q` after the audit update.
- Run `python -m pytest tools/node_api_parity/tests/test_check_parity_gate.py -q` after the tripwire update.
- Re-run `pwsh -NoProfile -Command "Set-Location 'ClassicLib-rs/node-bindings/classic-node'; bun run parity:gate"` so the plain Node gate still agrees with the refreshed contract metadata.
</verification>

<success_criteria>
- `docs/implementation/node_api_parity/baseline/parity_contract.json` describes the live one-tier 705-row contract and contains no stale hybrid-tier wording.
- `tests/planning/test_phase05_validation.py` and `tools/node_api_parity/tests/test_check_parity_gate.py` both fail if the JSON contract narrative drifts from the live one-tier contract.
- The touched pytest suites and the plain Node parity gate all pass without refreshing any committed parity baseline files.
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cleanup/05-04-SUMMARY.md`
</output>
