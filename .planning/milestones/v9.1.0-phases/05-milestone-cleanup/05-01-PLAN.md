---
phase: 05-milestone-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/planning/test_phase05_validation.py
  - docs/RUST_DOCUMENTATION_INDEX.md
  - .planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md
  - tools/node_api_parity/tests/test_check_parity_gate.py
  - .planning/phases/02-crashgen-config-merge/deferred-items.md
autonomous: true
requirements: []
must_haves:
  truths:
    - Contributor-facing Rust docs no longer link to deleted absorbed-crate pages and instead point readers to surviving owner docs.
    - Phase 3 verification bookkeeping reports the current live tree truth instead of stale `gaps_found` debt.
    - The Node parity floor tripwire and the Phase 2 deferred note both describe the live one-tier contract floor of 705 rows.
  artifacts:
    - path: tests/planning/test_phase05_validation.py
      provides: Automated audit coverage for all three cleanup truths.
    - path: docs/RUST_DOCUMENTATION_INDEX.md
      provides: Top-level contributor routing to surviving API owner docs.
    - path: .planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md
      provides: Refreshed passed verification artifact for Phase 3.
    - path: tools/node_api_parity/tests/test_check_parity_gate.py
      provides: Live floor tripwire locked to the current Node contract.
    - path: .planning/phases/02-crashgen-config-merge/deferred-items.md
      provides: Historical note reconciled to the resolved 705-row floor.
  key_links:
    - from: tests/planning/test_phase05_validation.py
      to: docs/RUST_DOCUMENTATION_INDEX.md
      via: planning audit assertions
      pattern: classic-settings-core|classic-version-registry-core|classic-shared-core
    - from: tests/planning/test_phase05_validation.py
      to: .planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md
      via: verification artifact assertions
      pattern: "Status:\*\* passed|Score:\*\* 9/9"
    - from: tools/node_api_parity/tests/test_check_parity_gate.py
      to: docs/implementation/node_api_parity/baseline/parity_contract.json
      via: tier1Mappings floor assertion
      pattern: "len\(tier1\) >= 705"
---

<objective>
Close the remaining consolidation audit debt without reopening finished implementation work.

Purpose: Reconcile stale docs, stale verification bookkeeping, and stale Node parity-floor tracking so the milestone's planning and contributor surfaces match the live repo.
Output: A new Phase 5 planning audit test plus refreshed `docs/RUST_DOCUMENTATION_INDEX.md`, `03-VERIFICATION.md`, and Node floor tripwire/deferred-note files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v9.1.0-MILESTONE-AUDIT.md
@.planning/phases/05-milestone-cleanup/05-RESEARCH.md
@.planning/phases/05-milestone-cleanup/05-VALIDATION.md
@docs/api/README.md
@docs/RUST_DOCUMENTATION_INDEX.md
@.planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md
@.planning/phases/03-constants-version-registry-merge/03-VALIDATION.md
@.planning/phases/04-gate-validation-documentation/04-VERIFICATION.md
@tools/node_api_parity/tests/test_check_parity_gate.py
@.planning/phases/02-crashgen-config-merge/deferred-items.md
@docs/implementation/node_api_parity/baseline/parity_contract.json
@docs/implementation/node_api_parity/baseline/parity_diff_report.md
@tests/planning/test_phase03_validation.py
@tests/planning/test_phase04_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 5 audit guard and repair top-level doc routing</name>
  <files>tests/planning/test_phase05_validation.py, docs/RUST_DOCUMENTATION_INDEX.md</files>
  <read_first>
    - tests/planning/test_phase03_validation.py
    - tests/planning/test_phase04_validation.py
    - docs/RUST_DOCUMENTATION_INDEX.md
    - docs/api/README.md
    - .planning/phases/05-milestone-cleanup/05-RESEARCH.md
    - .planning/phases/05-milestone-cleanup/05-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `tests/planning/test_phase05_validation.py` exists and defines `class Phase05ValidationAuditTests(unittest.TestCase):`.
    - `tests/planning/test_phase05_validation.py` contains a documentation-index test method that asserts `docs/RUST_DOCUMENTATION_INDEX.md` does not contain `classic-yaml-core.md` or `classic-constants-core.md`.
    - `docs/RUST_DOCUMENTATION_INDEX.md` no longer links to `docs/api/classic-yaml-core.md` or `docs/api/classic-constants-core.md`.
    - `docs/RUST_DOCUMENTATION_INDEX.md` explicitly mentions `classic-settings-core`, `classic-version-registry-core`, and `classic-shared-core` as the surviving owner routing for the absorbed YAML/constants surfaces.
  </acceptance_criteria>
  <action>Create `tests/planning/test_phase05_validation.py` using the same repo pattern as `test_phase03_validation.py` and `test_phase04_validation.py`: `from __future__ import annotations`, `unittest.TestCase`, repo-root `Path` constants, and deterministic file-backed assertions. Include three test methods now, even though only the documentation-index method must pass in this task: one for the doc index, one for refreshed Phase 3 verification, and one for Node floor reconciliation. In `docs/RUST_DOCUMENTATION_INDEX.md`, delete the dead bullets for `docs/api/classic-yaml-core.md` and `docs/api/classic-constants-core.md`. Then update the existing surviving-owner bullets so the index explicitly routes contributors to the live owners: `classic-settings-core` for YAML parsing/cache helpers and `YamlFile`/settings constants, `classic-version-registry-core` for `Fallout4Version` and `NULL_VERSION`, and `classic-shared-core` for `GameId`. Keep the change minimal and repo-native; do not add new pages or duplicate the full `docs/api/README.md` list.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase05_validation.py -q -k documentation_index</automated>
  </verify>
  <done>The top-level Rust docs index has no broken absorbed-crate links, and Phase 5 has a durable audit test scaffold for all three cleanup truths.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh Phase 3 verification bookkeeping to the current live state</name>
  <files>.planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md
    - .planning/phases/03-constants-version-registry-merge/03-VALIDATION.md
    - .planning/phases/04-gate-validation-documentation/04-VERIFICATION.md
    - docs/api/classic-version-registry-core.md
    - docs/implementation/python_api_parity/baseline/python_api_surface.json
    - docs/implementation/node_api_parity/baseline/rust_api_surface.json
    - tests/planning/test_phase05_validation.py
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md` frontmatter contains `status: passed` and `score: 9/9 must-haves verified`.
    - The body contains `**Status:** passed` and `**Re-verification:** Yes`.
    - The Observable Truths table marks the contributor-doc, parity-artifact, and retired-directory closure rows as verified rather than failed/partial.
    - The Gaps Summary states there are no blocking gaps and that the stale `gaps_found` bookkeeping was refreshed during Phase 05 cleanup.
  </acceptance_criteria>
  <action>Rewrite `.planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md` in place so it reports the live, post-cleanup truth recorded in `03-VALIDATION.md` and the current repo tree. Preserve the structured verifier format used by `04-VERIFICATION.md`: frontmatter, Goal Achievement, Observable Truths, Required Artifacts, Key Link Verification, Data-Flow Trace, Behavioral Spot-Checks, Requirements Coverage, Anti-Patterns Found, Human Verification Required, and Gaps Summary. Set the artifact to a passed re-verification, not a new checklist file. Update the previously stale closure rows so they cite current evidence: no active contributor docs point to retired constants bindings, the committed Python/Node parity surface artifacts are refreshed with 2026-04-12-era state, and `ClassicLib-rs/python-bindings/classic-constants-py/` is absent. End the summary by explicitly stating that the earlier `gaps_found` result was stale bookkeeping superseded by this Phase 05 refresh.</action>
  <verify>
    <automated>python -m pytest tests/planning/test_phase05_validation.py -q -k phase3_verification</automated>
  </verify>
  <done>`03-VERIFICATION.md` becomes the canonical passed verifier artifact for Phase 3 and no longer contradicts the live tree or `03-VALIDATION.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Recalibrate the Node parity floor tripwire to the live 705-row contract</name>
  <files>tools/node_api_parity/tests/test_check_parity_gate.py, .planning/phases/02-crashgen-config-merge/deferred-items.md</files>
  <read_first>
    - tools/node_api_parity/tests/test_check_parity_gate.py
    - .planning/phases/02-crashgen-config-merge/deferred-items.md
    - docs/implementation/node_api_parity/baseline/parity_contract.json
    - docs/implementation/node_api_parity/baseline/parity_diff_report.md
    - ClassicLib-rs/node-bindings/classic-node/package.json
    - tests/planning/test_phase05_validation.py
  </read_first>
  <acceptance_criteria>
    - `tools/node_api_parity/tests/test_check_parity_gate.py` contains `assert len(tier1) >= 705` and no longer contains `711` in the floor assertion or failure message.
    - `tools/node_api_parity/tests/test_check_parity_gate.py` describes the current one-tier contract rather than a pending Tier-2 cleanup/xfail story.
    - `.planning/phases/02-crashgen-config-merge/deferred-items.md` records the floor issue as resolved/reconciled to the live 705-row contract instead of an unresolved regression.
    - `python -m pytest tests/planning/test_phase05_validation.py tools/node_api_parity/tests/test_check_parity_gate.py -q` exits 0.
    - `pwsh -Command "Set-Location 'ClassicLib-rs/node-bindings/classic-node'; bun run parity:gate"` exits 0.
  </acceptance_criteria>
  <action>Update `tools/node_api_parity/tests/test_check_parity_gate.py` so the floor tripwire protects the live one-tier contract shown by `docs/implementation/node_api_parity/baseline/parity_contract.json` and `parity_diff_report.md`: the minimum Tier-1 row count is 705, not 711. Replace the stale historical comment and failure wording so they explain that the current committed contract/report already show 705/705 matched rows and `tierDefinitions.tier2` is absent. Keep `test_tier2_definition_removed_after_plan_6` as a positive assertion that `tier2` is not present, but remove any wording that implies it is still awaiting a future Plan 6 cleanup. In `.planning/phases/02-crashgen-config-merge/deferred-items.md`, keep the history of the old mismatch but mark it resolved during Phase 05 cleanup, stating that the live one-tier contract floor is 705 and the tripwire was recalibrated to that source-backed truth. After the edits, run the touched pytest suite and the plain Node parity gate; do not refresh baselines because the research established there is no live drift.</action>
  <verify>
    <automated>pwsh -NoProfile -Command "python -m pytest tests/planning/test_phase05_validation.py tools/node_api_parity/tests/test_check_parity_gate.py -q; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Set-Location 'ClassicLib-rs/node-bindings/classic-node'; bun run parity:gate"</automated>
  </verify>
  <done>The Node floor test and the historical deferred note both align to the live 705-row one-tier contract, and the audit + plain Node gate pass without baseline refresh.</done>
</task>

</tasks>

<verification>
- Run the targeted audit method after Task 1 and Task 2, then the full Phase 5 audit plus plain Node parity gate after Task 3.
- Treat this as artifact reconciliation only; do not expand scope into parity baseline regeneration or new implementation work.
</verification>

<success_criteria>
- `docs/RUST_DOCUMENTATION_INDEX.md` routes contributors only to surviving owner docs for absorbed YAML/constants surfaces.
- `.planning/phases/03-constants-version-registry-merge/03-VERIFICATION.md` records a passed re-verification consistent with `03-VALIDATION.md` and the live tree.
- `tools/node_api_parity/tests/test_check_parity_gate.py`, `.planning/phases/02-crashgen-config-merge/deferred-items.md`, and the committed Node parity artifacts all agree on the live 705-row one-tier floor.
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cleanup/05-01-SUMMARY.md`
</output>
