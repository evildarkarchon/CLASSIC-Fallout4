---
phase: 260331-4d1
plan: 01
subsystem: build
tags: [code-signing, signtool, powershell, self-signed-certificate, windows-sdk]

requires:
  - phase: none
    provides: standalone quick task
provides:
  - Shared code signing script with certificate lifecycle management
  - Opt-in signing integration in CLI and GUI build scripts
affects: [classic-cli, classic-gui, build-tooling]

tech-stack:
  added: [signtool.exe, New-SelfSignedCertificate, X509Certificate2]
  patterns: [opt-in post-install signing, shared tooling via dot-source]

key-files:
  created: [tools/sign-binaries.ps1]
  modified: [classic-cli/build_cli.ps1, classic-gui/build_gui.ps1, .gitignore]

key-decisions:
  - "Self-signed PFX on disk with CLASSIC_SIGNING_PFX env var override"
  - "Sign after install (Step 5.5), before package -- clean install directory has only project binaries"
  - "try/catch wrapper in Invoke-CodeSigning ensures signing failures never block builds"
  - "CLASSIC_SIGNING_PASSWORD env var override added alongside default password"

patterns-established:
  - "Opt-in signing: builds succeed without SDK or certificate"
  - "Shared tooling in tools/ directory, dot-sourced by build scripts"

requirements-completed: [SIGN-01]

duration: 3min
completed: 2026-03-31
---

# Quick Task 260331-4d1: Self-signing of Compiled Code Summary

**Self-signed code signing via signtool.exe with auto-generated PFX certificate, integrated as opt-in Step 5.5 in both CLI and GUI build scripts**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-31T10:24:29Z
- **Completed:** 2026-03-31T10:27:12Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Created `tools/sign-binaries.ps1` with four functions: `Find-SignTool`, `Ensure-SigningCert`, `Sign-Binary`, `Invoke-CodeSigning`
- Integrated opt-in signing into both `build_cli.ps1` and `build_gui.ps1` as Step 5.5 (after install, before package)
- Certificate auto-generates on first use (1-year validity, RSA 2048, SHA256)
- Certificate auto-renews when within 30 days of expiration
- Only project EXEs signed: `classic-cli.exe` and `CLASSIC.exe` (not third-party DLLs or static .lib)
- PFX path configurable via `CLASSIC_SIGNING_PFX` env var; password via `CLASSIC_SIGNING_PASSWORD`
- `tools/certs/` excluded from git via `.gitignore`

## Task Commits

Each task was committed atomically:

1. **Task 1: Create shared signing script and update .gitignore** - `811fea95` (feat)
2. **Task 2: Integrate signing into both build scripts** - `72b0e81f` (feat)

**Plan metadata:** pending (docs: complete plan)

## Files Created/Modified
- `tools/sign-binaries.ps1` - Shared signing functions: Find-SignTool, Ensure-SigningCert, Sign-Binary, Invoke-CodeSigning (237 lines)
- `classic-cli/build_cli.ps1` - Added Step 5.5 signing block inside if ($Install) block, signs classic-cli.exe
- `classic-gui/build_gui.ps1` - Added Step 5.5 signing block inside if ($Install) block, signs CLASSIC.exe
- `.gitignore` - Added tools/certs/ exclusion for auto-generated PFX certificates

## Decisions Made
- Used `tools/certs/classic-signing.pfx` as default PFX location with env var override (`CLASSIC_SIGNING_PFX`)
- Added `CLASSIC_SIGNING_PASSWORD` env var override alongside the hardcoded default password
- Certificate subject: `CN=CLASSIC Dev Signing, O=CLASSIC Project` -- clearly identifies as dev/self-signed
- Sign after install (Step 5.5) rather than after build -- install directory contains clean set of project binaries
- Wrapped entire `Invoke-CodeSigning` body in try/catch so signing errors never block builds
- Used `Out-Null` everywhere instead of redirecting to `nul` (per project CLAUDE.md constraint)
- Certificate is removed from Windows cert store immediately after PFX export to avoid store accumulation

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## Known Stubs
None - all functions are fully implemented with no placeholder logic.

## User Setup Required
None - signing is fully opt-in and auto-generates certificates on first use. Windows SDK with signtool.exe is the only external dependency, and builds proceed normally without it.

## Next Phase Readiness
- Signing infrastructure is complete and ready for use
- Run `build_cli.ps1 -Install` or `build_gui.ps1 -Install` to trigger signing
- For CI/CD: set `CLASSIC_SIGNING_PFX` and `CLASSIC_SIGNING_PASSWORD` environment variables to use a shared certificate

## Self-Check: PASSED

All created files verified present. All task commits verified in git log.

---
*Quick Task: 260331-4d1*
*Completed: 2026-03-31*
