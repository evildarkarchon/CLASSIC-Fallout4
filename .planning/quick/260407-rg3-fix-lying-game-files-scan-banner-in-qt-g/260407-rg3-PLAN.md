---
phase: quick-260407-rg3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - classic-gui/src/workers/gamefilesworker.cpp
autonomous: true
requirements:
  - QGUI-BANNER-01
must_haves:
  truths:
    - "When the Game Files scan finds an ENB Partial-binaries condition, the GUI status banner shows '(errors found)' and the report header shows 'Issues Found'."
    - "When the Game Files scan finds an ENB Unreadable-config condition, the GUI status banner shows '(errors found)' and the report header shows 'Issues Found'."
    - "When the Game Files scan finds Crashgen issue_count > 0, the GUI status banner shows '(errors found)' and the report header shows 'Issues Found'."
    - "On every successful Game Files scan, the banner's check count is exactly orchestrator total_checks + 3 (2 ENB + 1 Crashgen)."
    - "ENB NotInstalled / NotFound / Present / Valid states never cause the banner to escalate to '(errors found)' on their own."
    - "The textual report body emitted to the Results tab is unchanged — it still appends the existing '[ENB] Binaries: ... | Config: ...' line and '[Crashgen] Buffout4 plugins detected: N, config issues: M' line."
  artifacts:
    - path: "classic-gui/src/workers/gamefilesworker.cpp"
      provides: "Worker that emits combined-truth has_errors + total_checks instead of orchestrator-only fields."
      contains: "combinedHasErrors"
  key_links:
    - from: "classic-gui/src/workers/gamefilesworker.cpp"
      to: "classic-gui/src/app/mainwindow.cpp::onGameFilesScanFinished"
      via: "emit finished(combinedText, combinedHasErrors, combinedTotalChecks)"
      pattern: "emit finished\\(\\s*combinedText,\\s*combinedHasErrors,\\s*combinedTotalChecks"
---

<objective>
Fix the lying "Game Files Scan" status banner in the Qt GUI. The worker currently
forwards `result.has_errors` and `result.total_checks` straight from
`classic::scangame::run_setup_checks` (which only knows about integrity + xse + docs
checks), so the banner can announce "All Clear / N checks" while the report body
clearly shows ENB partial binaries, ENB unreadable config, or Crashgen config issues.

Purpose: Make the visible banner truth-consistent with the report body. Compute
`combinedHasErrors` and `combinedTotalChecks` at the worker boundary (the only place
where the three scan results — orchestrator setup, ENB validation, Crashgen orchestrator
summary — are stitched together) and emit those instead of the raw bridge fields.

Output: A single-file edit to `classic-gui/src/workers/gamefilesworker.cpp` plus a
GUI rebuild verifying the change links and runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md

# Files the executor MUST read in full before editing
@classic-gui/src/workers/gamefilesworker.cpp
@classic-gui/src/app/mainwindow.cpp

# Ground-truth references (read targeted ranges, do NOT modify)
@ClassicLib-rs/business-logic/classic-scangame-core/src/setup.rs
@ClassicLib-rs/cpp-bindings/classic-cpp-bridge/src/scangame.rs

<rules_in_force>
From CLAUDE.md (project):
- GSD Workflow Enforcement: this edit is being done under `/gsd:quick`, which is the
  required entry point for small fixes. Do NOT make freeform edits outside this plan.
- Commit Conventions: prefix commit messages with `Fix:` (capitalized first word after
  the prefix) per the project commit-convention rule.
- Never write to `nul` on Windows — it creates an undeletable file on a system drive.

From AGENTS.md (project, always-on rule #8):
- "Never run C++ tests by invoking test binaries or raw `ctest` directly; use
  `classic-cli/build_cli.ps1 -Test` or `classic-gui/build_gui.ps1 -Test`, with
  `-CTestName` or `-IntegrationTestName` when you need a subset."
- Rule #2: business logic stays in Rust. This fix is intentionally a UI-layer
  composition fix (the worker stitches three already-computed Rust results together
  into a UI-facing aggregate), so it stays in C++ and does NOT touch any -core crate
  or the CXX bridge. That stays out of scope.

From global user CLAUDE.md:
- Prefer PowerShell over Bash for build/test invocation. Use the PowerShell tool to
  run `pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test`.
- No emojis anywhere (code, plan, commit message).
</rules_in_force>

<interfaces>
<!-- Confirmed from classic-cpp-bridge/src/scangame.rs lines 604-620, 730-740. -->
<!-- Confirmed from classic-scangame-core/src/setup.rs lines 95-128. -->
<!-- Executor uses these directly — no further codebase exploration needed. -->

CXX shared enums (already in scope in gamefilesworker.cpp via classic_cxx_bridge/scangame.h):

  enum class classic::scangame::EnbResult : uint8_t {
      Present       = 0,
      Partial       = 1,
      NotInstalled  = 2,
  };

  enum class classic::scangame::EnbConfigResult : uint8_t {
      Valid       = 0,
      NotFound    = 1,
      Unreadable  = 2,
  };

CXX shared structs (already returned by the calls already in scope):

  struct EnbValidationResultDto {
      EnbResult       binaries;
      EnbConfigResult config;
      // NO `errors` Vec — Codex HIGH correction.
  };

  struct CrashgenReportSummaryDto {
      ::rust::String message;
      ::rust::String crashgen_name;
      ::rust::String config_path_or_empty;
      bool           has_config_path;
      uint32_t       issue_count;
      uint32_t       installed_plugin_count;
  };

ROOT-CAUSE confirmation from setup.rs lines 110-128 — `SetupCheckResults::has_errors`
and `total_checks` literally only count integrity_results + xse_results + docs_results
(plus the dedicated `errors` Vec for has_errors). They have ZERO knowledge of ENB or
Crashgen, which is exactly why the banner can lie. The fix MUST happen in the C++
worker, where ENB and Crashgen results are already in scope.

Existing locals in `GameFilesWorker::doScan` at the relevant point (lines 31, 71-74):
  - `auto result   = classic::scangame::run_setup_checks(...);`     // line 22-27
  - `auto enb      = classic::scangame::enb_checker_validate(...);` // line 31-33
  - `auto crashgen = classic::scangame::crashgen_orchestrator_check_summary(...);` // line 71-74

Consumer signature already matches — `mainwindow.cpp:1770`:
  void MainWindow::onGameFilesScanFinished(const QString& output, bool hasErrors, uint32_t totalChecks);

The semantics of the bool/uint32_t flowing through that slot will become "combined
truth across orchestrator + ENB + Crashgen", which is exactly what `mainwindow.cpp`
already uses to render the banner (line 1777-1781) and the report header
(line 1805 — `statusLabel = hasErrors ? "Issues Found" : "All Clear"`). No consumer
edit is needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute combined ENB+Crashgen severity in GameFilesWorker and emit combined truth</name>
  <files>classic-gui/src/workers/gamefilesworker.cpp</files>
  <action>
Edit ONLY `classic-gui/src/workers/gamefilesworker.cpp`. Do not touch any other file.

Step 1 — Read the file in full first (it is ~96 lines). Confirm the three locals
`result`, `enb`, and `crashgen` exist in scope at the point just before
`emit progress(100.0f, ...)` on line 84. (They do; lines 22-27, 31-33, 71-74.)

Step 2 — Insert a new code block immediately AFTER line 82 (the closing of the
`combinedText` assignment) and immediately BEFORE line 84 (`emit progress(100.0f,
QStringLiteral("Complete"));`). The block MUST read exactly:

    // Compute combined-truth has_errors / total_checks across the three scan
    // sources stitched into combinedText. SetupCheckResults only counts
    // integrity + xse + docs (see classic-scangame-core/src/setup.rs::total_checks),
    // so forwarding result.has_errors / result.total_checks alone makes the banner
    // lie when ENB or Crashgen turn up issues.
    bool     combinedHasErrors   = result.has_errors;
    uint32_t combinedTotalChecks = result.total_checks;

    // ENB contributes 2 checks: binaries + config.
    combinedTotalChecks += 2;
    // Locked severity model: only Partial binaries OR Unreadable config escalate
    // to error. NotInstalled / NotFound / Present / Valid do NOT escalate
    // (the user has opted out of ENB or it is healthy).
    if (enb.binaries == classic::scangame::EnbResult::Partial) {
        combinedHasErrors = true;
    }
    if (enb.config == classic::scangame::EnbConfigResult::Unreadable) {
        combinedHasErrors = true;
    }

    // Crashgen contributes 1 check (the config audit). Any reported issue
    // is treated as an error.
    combinedTotalChecks += 1;
    if (crashgen.issue_count > 0) {
        combinedHasErrors = true;
    }

Step 3 — Change the existing `emit finished(...)` call (currently lines 85-89) so it
passes the combined values instead of the raw bridge fields. The final call MUST be:

    emit finished(
        combinedText,
        combinedHasErrors,
        combinedTotalChecks
    );

Step 4 — Do NOT modify any other line in the file. In particular:
- Leave `combinedText` unchanged. It still equals
  `classic::toQString(result.combined_output) + enbSummary + crashgenLine`.
- Leave the `enbSummary` and `crashgenLine` switch/`arg` blocks unchanged.
- Leave the `try` / `catch (const rust::Error& e)` / `catch (const std::exception& e)`
  structure unchanged.
- Leave the indeterminate-progress emit on line 18 unchanged.

Step 5 — Constraints checklist:
- No edits outside `classic-gui/src/workers/gamefilesworker.cpp`.
- No emojis in code, comments, or commit message.
- Per project clang-format (`classic-gui/.clang-format`): 4-space indent, no tabs,
  120-column limit, K&R braces (already the file's existing style — match it exactly).
- The new block uses `uint32_t`, which is already implicitly available because
  `result.total_checks` is already used as `uint32_t` by the existing
  `emit finished(...)` call and `mainwindow.cpp:1770` types it that way. If the
  build complains about an unknown identifier, add `#include &lt;cstdint&gt;` at the
  top (after the existing `#include &lt;QDir&gt;`); the existing file does not have it
  but the type is already in scope transitively via Qt headers, so this is a
  defensive fallback only.

Why this is the right fix point (rationale recorded for future maintainers in the
inline comment above): `SetupCheckResults` is intentionally narrow in the Rust core
(integrity + xse + docs) — pushing ENB and Crashgen severity into it would change
the contract of a shared `-core` crate that other binding surfaces (Python, Node, TUI,
CLI) consume. The Qt GUI is the only surface that currently stitches the three
results into a single user-facing banner, so the composition fix lives in the Qt
worker.
  </action>
  <verify>
    <automated>
Static check (run from repo root, PowerShell):

  pwsh -Command "Select-String -Path classic-gui/src/workers/gamefilesworker.cpp -Pattern 'combinedHasErrors' | Measure-Object | Select-Object -ExpandProperty Count"

Expected: at least 4 matches (1 declaration, 2 assignments inside the if-blocks,
1 in the emit call). Anything less means the edit did not land correctly.

Then:

  pwsh -Command "Select-String -Path classic-gui/src/workers/gamefilesworker.cpp -Pattern 'combinedTotalChecks' | Measure-Object | Select-Object -ExpandProperty Count"

Expected: at least 4 matches (1 declaration, 2 `+= 2` / `+= 1` lines, 1 in the emit
call).

Then assert the raw fields are NOT in the emit call any more:

  pwsh -Command "Select-String -Path classic-gui/src/workers/gamefilesworker.cpp -Pattern 'emit finished\(' -Context 0,4"

Expected: the emit-call body must show `combinedText`, `combinedHasErrors`,
`combinedTotalChecks` — NOT `result.has_errors` or `result.total_checks`.

Dynamic build/test (the canonical project rule per AGENTS.md #8 — never raw ctest):

  pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test

Expected: build succeeds, all GUI tests green.

Worktree Qt fallback (recorded in project memory, see
project_build_paths_and_quirks.md): This worktree may not have a pre-built Qt Release
preset available. If `build_gui.ps1 -Test` fails with a Qt-Release-not-found error,
the executor MUST retry with `-Debug`:

  pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test -Debug

Capture the FULL build/test command output (success or failure path, including
which preset was used) into the SUMMARY. Do not skip the build verification.
    </automated>
  </verify>
  <done>
- `classic-gui/src/workers/gamefilesworker.cpp` declares `combinedHasErrors` and
  `combinedTotalChecks`, escalates them per the locked severity model (ENB Partial
  binaries → error, ENB Unreadable config → error, Crashgen issue_count > 0 → error),
  always increments `combinedTotalChecks` by exactly 3, and emits those locals
  through `emit finished(...)`.
- The `emit finished(...)` call no longer references `result.has_errors` or
  `result.total_checks`.
- `combinedText` body assignment is byte-identical to the previous version.
- `pwsh ... build_gui.ps1 -Test` (with `-Debug` fallback if needed) succeeds and the
  output is captured in the SUMMARY.
- No edits exist outside `classic-gui/src/workers/gamefilesworker.cpp`.
- Commit (single commit) created with message prefix `Fix:` per project commit
  conventions, e.g.
    `Fix: stop GameFilesWorker banner lying about ENB and Crashgen results`
  No emojis. No `Co-Authored-By` line unless the user explicitly asks for one.
  </done>
</task>

</tasks>

<verification>
- Visual sanity: open `classic-gui/src/workers/gamefilesworker.cpp` and confirm the
  new block sits between the `combinedText` assignment and the
  `emit progress(100.0f, ...)` line.
- Static greps from the verify block all return the expected counts.
- `pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test` (or `-Debug`
  fallback) is green; full output captured in SUMMARY.
- Confirm `classic-gui/src/app/mainwindow.cpp::onGameFilesScanFinished` was NOT
  modified — its signature `(const QString&, bool, uint32_t)` already matches and
  no consumer change was needed.
- Confirm no files outside `classic-gui/src/workers/gamefilesworker.cpp` are
  staged for commit (`git status` should show exactly one modified file).
</verification>

<success_criteria>
- The Qt GUI banner is no longer capable of saying "All Clear / N checks" while the
  report body shows an ENB Partial-binaries, ENB Unreadable-config, or
  Crashgen-with-issues condition. With the new code path, any of those three
  conditions sets `combinedHasErrors = true`, which `mainwindow.cpp:1778-1781` then
  renders as `" (errors found)"` on the status bar and `mainwindow.cpp:1805` renders
  as `"Issues Found"` in the report header.
- The check count in the banner becomes
  `orchestrator.total_checks + 2 (ENB) + 1 (Crashgen)` on EVERY scan, regardless of
  outcome — counts reflect what was inspected, not what passed.
- The textual report body emitted to the Results tab is byte-identical to the
  pre-fix version (the existing ENB summary and Crashgen line are still appended
  unchanged).
- `pwsh -ExecutionPolicy Bypass -File classic-gui/build_gui.ps1 -Test` returns
  success (Release path, or Debug fallback if the worktree-specific Qt-Release
  quirk hits).
- No files outside `classic-gui/src/workers/gamefilesworker.cpp` are touched.
- No -core, no CXX bridge, no Python/Node/TUI/CLI edits — confirming this stayed a
  pure UI-composition fix as scoped.
- The commit message uses the `Fix:` prefix and contains no emojis.
</success_criteria>

<output>
After completion, create:
`.planning/quick/260407-rg3-fix-lying-game-files-scan-banner-in-qt-g/260407-rg3-01-SUMMARY.md`

Summary MUST include:
- The exact diff (or a tight description of the inserted block and the changed
  `emit finished(...)` arguments).
- The output of the static greps from the verify block.
- The full output of `build_gui.ps1 -Test` (Release if it worked, Debug fallback
  output if Release was not available in the worktree). If Debug fallback was used,
  call that out explicitly so the user knows which path was taken.
- The commit hash and commit message.
- An explicit "no files outside `classic-gui/src/workers/gamefilesworker.cpp` were
  modified" assertion backed by `git show --stat HEAD` output.
</output>
