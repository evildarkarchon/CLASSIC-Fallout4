---
phase: quick-260411-m7y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CNST-01, CNST-02, CNST-03]

must_haves:
  truths:
    - "ROADMAP.md Phase 3 title reads 'Constants Redistribution' (not 'Constants -> Version Registry Merge')"
    - "ROADMAP.md Phase 3 Goal paragraph describes three-target redistribution across version-registry-core, settings-core, and shared-core"
    - "ROADMAP.md Phase 3 Success Criteria no longer claim all APIs are accessible from classic-version-registry-core"
    - "ROADMAP.md milestone target-features bullet describes three-target redistribution"
    - "REQUIREMENTS.md CNST-01 describes semantic-domain redistribution with three target crates"
    - "REQUIREMENTS.md CNST-02 describes importing from the semantic-domain-appropriate target crate"
    - "REQUIREMENTS.md CNST-03 is unchanged (classic-constants-core crate removed and directory deleted)"
    - "REQUIREMENTS.md section header reads '### Constants Redistribution'"
    - "PROJECT.md Current Milestone target-features bullet reflects three-target redistribution"
    - "PROJECT.md Validated section and Key Decisions table are unchanged"
    - "Phase 3 checkbox status stays '- [ ]' and CNST-0{1,2,3} traceability status stays 'Pending'"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 3 scope"
      contains: "Phase 3: Constants Redistribution"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended CNST-01 and CNST-02"
      contains: "### Constants Redistribution"
    - path: ".planning/PROJECT.md"
      provides: "Amended target-features bullet"
      contains: "classic-shared-core"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/03-constants-version-registry-merge/03-CONTEXT.md"
      via: "Phase 3 scope matches D-01 decision"
      pattern: "Constants Redistribution"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "CNST-01..03 section title matches Phase 3 title"
      pattern: "Constants Redistribution"
---

<objective>
Amend ROADMAP.md, REQUIREMENTS.md, and PROJECT.md so they reflect the Phase 3 three-target redistribution decision (D-01 in 03-CONTEXT.md) instead of the stale flat-merge scope.

Purpose: `/gsd:plan-phase 3` reads these three files for scope anchoring. If they still describe a flat merge of classic-constants-core into classic-version-registry-core, the plan agent will generate an incorrect plan that contradicts the locked user decision. This quick-task is a prerequisite for phase planning.

Output: Single atomic commit touching all three docs with scope text aligned to the three-target redistribution (Fallout4Version + NULL_VERSION -> version-registry-core, YamlFile + SETTINGS_IGNORE_NONE + must_not_be_none -> settings-core, GameId -> shared-core).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-constants-version-registry-merge/03-CONTEXT.md

<!--
Source of truth for amendments: 03-CONTEXT.md Decision D-01.
Do NOT touch 03-CONTEXT.md — it already reflects the new scope.
Do NOT rename the phase directory slug (03-constants-version-registry-merge/).
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP + REQUIREMENTS + PROJECT for three-target redistribution</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <action>
Make a SINGLE atomic commit that amends all three docs per D-01 in 03-CONTEXT.md. All edits are pure text — no code, no tests, no builds.

### 1. .planning/ROADMAP.md

(a) In the "v9.1.0-consolidation Crate Consolidation" phase list (around line 21), rename:
  FROM: `- [ ] **Phase 3: Constants -> Version Registry Merge** - Absorb classic-constants-core into classic-version-registry-core, update all consumers across the workspace`
  TO:   `- [ ] **Phase 3: Constants Redistribution** - Redistribute classic-constants-core across three target crates by semantic domain (version-registry-core, settings-core, shared-core), update all consumers across the workspace`

(b) In "Phase Details" section (around line 88), rename the Phase 3 subsection heading:
  FROM: `### Phase 3: Constants -> Version Registry Merge`
  TO:   `### Phase 3: Constants Redistribution`

(c) Replace the Phase 3 Goal line (around line 89):
  FROM: `**Goal**: classic-constants-core no longer exists as a separate crate; all game/version identity constants live in classic-version-registry-core with no consumer-visible behavior change`
  TO:   `**Goal**: classic-constants-core no longer exists as a separate crate; its contents are redistributed by semantic domain: Fallout4Version and NULL_VERSION live in classic-version-registry-core, YamlFile and settings constants live in classic-settings-core, and GameId lives in classic-shared-core (foundation). Zero consumer-visible behavior change.`

(d) Replace the Phase 3 Success Criteria block (around lines 92-96) with three-target wording:
  ```
  **Success Criteria** (what must be TRUE):
    1. Fallout4Version, NULL_VERSION, and related version identity APIs are accessible from classic-version-registry-core at the same public names
    2. YamlFile, SETTINGS_IGNORE_NONE, and must_not_be_none() are accessible from classic-settings-core at the same public names
    3. GameId is accessible from classic-shared-core (foundation layer) at the same public name
    4. No crate in the workspace has a `classic-constants-core` dependency in its Cargo.toml -- all former consumers import from the semantic-domain-appropriate target crate (version-registry-core, settings-core, or shared-core)
    5. The classic-constants-core directory is deleted and removed from workspace members in the root Cargo.toml
    6. `cargo build --workspace` and `cargo test --workspace` succeed with zero failures
  ```

(e) In the "Progress" table at the bottom (around line 120), rename the Phase 3 row label:
  FROM: `| 3. Constants -> Version Registry Merge | 0/TBD | Not started | - |`
  TO:   `| 3. Constants Redistribution | 0/TBD | Not started | - |`

(f) Phase 3 checkbox remains `- [ ]` (not complete). Do NOT touch Phase 1, Phase 2, Phase 4 content. Do NOT touch any Milestones bullet at the top — the milestone name `v9.1.0-consolidation Crate Consolidation` is unchanged.

### 2. .planning/REQUIREMENTS.md

(a) Rename the section header (line 23):
  FROM: `### Constants -> Version Registry Merge`
  TO:   `### Constants Redistribution`

(b) Rewrite CNST-01 (line 25):
  FROM: `- [ ] **CNST-01**: classic-constants-core source modules are relocated into classic-version-registry-core with the same public API surface preserved`
  TO:   `- [ ] **CNST-01**: classic-constants-core source modules are redistributed by semantic domain: Fallout4Version + NULL_VERSION to classic-version-registry-core, YamlFile + SETTINGS_IGNORE_NONE + must_not_be_none to classic-settings-core, and GameId to classic-shared-core, with the same public API names preserved at their new locations`

(c) Rewrite CNST-02 (line 26):
  FROM: `- [ ] **CNST-02**: All workspace crates that imported from classic-constants-core import from classic-version-registry-core instead`
  TO:   `- [ ] **CNST-02**: All workspace crates that imported from classic-constants-core now import from the semantic-domain-appropriate target crate (classic-version-registry-core, classic-settings-core, or classic-shared-core) depending on which symbol they use`

(d) CNST-03 (line 27) stays UNCHANGED verbatim.

(e) Traceability table rows (lines 65-67) stay UNCHANGED — CNST-01/02/03 status stays "Pending", phase stays "Phase 3".

(f) Do NOT touch YAML-01..04, CGEN-01..03, GATE-01..06, Out of Scope table, or Future Requirements.

### 3. .planning/PROJECT.md

(a) In "Current Milestone: v9.1.0-consolidation Crate Consolidation" -> "Target features:" bullet list (around line 10), update the constants bullet:
  FROM: `- Merge `classic-constants-core` into `classic-version-registry-core` (unify game/version identity metadata)`
  TO:   `- Redistribute `classic-constants-core` across `classic-version-registry-core` (Fallout4Version, NULL_VERSION), `classic-settings-core` (YamlFile, settings constants), and `classic-shared-core` (GameId) by semantic domain`

(b) In the "Active" requirements section (around line 70), update the bullet:
  FROM: `- [ ] Merge classic-constants-core into classic-version-registry-core`
  TO:   `- [ ] Redistribute classic-constants-core across version-registry-core, settings-core, and shared-core`

(c) Do NOT touch the "Validated" section, "Out of Scope" section, "Context", "Constraints", "Key Decisions" table, or "Evolution" section — those reflect historical state.

(d) The `*Last updated: ...*` footer at the bottom MAY be updated to `*Last updated: 2026-04-11 after Phase 3 scope amendment to three-target redistribution*` but this is optional cosmetic polish.

### Commit

Stage the three files and commit with message (per CLAUDE.md Commit Conventions — `Docs:` prefix, capitalized first word after prefix):

```
Docs(quick-260411-m7y): Amend Phase 3 scope to three-target redistribution

Align ROADMAP.md, REQUIREMENTS.md, and PROJECT.md with D-01 in
.planning/phases/03-constants-version-registry-merge/03-CONTEXT.md.
Phase 3 now redistributes classic-constants-core by semantic domain:
Fallout4Version + NULL_VERSION to classic-version-registry-core,
YamlFile + SETTINGS_IGNORE_NONE + must_not_be_none to classic-settings-core,
and GameId to classic-shared-core. Zero consumer-visible behavior change.
Prerequisite for /gsd:plan-phase 3.
```

Use `git commit` via the gsd-tools commit helper if available, else plain `git commit -m`. Do NOT use `--no-verify` flags.
  </action>
  <verify>
    <automated>grep -q "Phase 3: Constants Redistribution" .planning/ROADMAP.md && grep -q "### Constants Redistribution" .planning/REQUIREMENTS.md && grep -q "classic-shared-core" .planning/PROJECT.md && ! grep -q "Merge classic-constants-core into classic-version-registry-core" .planning/PROJECT.md && ! grep -q "Phase 3: Constants -> Version Registry Merge" .planning/ROADMAP.md && grep -q "semantic-domain-appropriate target crate" .planning/REQUIREMENTS.md && grep -q "Fallout4Version and NULL_VERSION live in classic-version-registry-core" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - ROADMAP.md: Phase 3 title, Goal, Success Criteria, phase-list bullet, and Progress row all reflect three-target redistribution
    - REQUIREMENTS.md: Section header "Constants Redistribution", CNST-01 and CNST-02 rewritten, CNST-03 unchanged, traceability table unchanged
    - PROJECT.md: Target features bullet and Active bullet reflect three-target redistribution; Validated / Key Decisions / Out of Scope untouched
    - Single atomic commit staged and committed with `Docs(quick-260411-m7y):` prefix
    - Stale text ("Merge classic-constants-core into classic-version-registry-core", "Phase 3: Constants -> Version Registry Merge") is gone
    - 03-CONTEXT.md is NOT modified; phase directory slug is NOT renamed
  </done>
</task>

</tasks>

<verification>
After the task completes, run the automated grep chain in <verify> to confirm:
1. New strings are present in all three files
2. Stale strings are gone
3. Git log shows the new commit with the `Docs(quick-260411-m7y):` prefix

Then manually spot-check that:
- Phase 3 checkbox is still `- [ ]`
- CNST-01/02/03 traceability status is still "Pending"
- 03-CONTEXT.md is untouched (`git status` clean for that file)
</verification>

<success_criteria>
- All three files committed in a single atomic commit
- ROADMAP.md, REQUIREMENTS.md, and PROJECT.md all describe three-target redistribution consistently
- `/gsd:plan-phase 3` can now read these files without encountering stale flat-merge scope
- No code files touched; no builds or tests needed
</success_criteria>

<output>
After completion, create `.planning/quick/260411-m7y-amend-roadmap-md-requirements-md-and-pro/260411-m7y-SUMMARY.md` summarizing:
- Which stale strings were replaced
- Which files were touched (exactly three)
- The commit hash and message
- Confirmation that 03-CONTEXT.md was NOT modified
</output>
