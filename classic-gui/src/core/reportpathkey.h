#pragma once

#include <QString>

namespace classic::gui {

/// Canonical key for matching report file paths across controller and widget.
QString reportPathKey(const QString& path);

} // namespace classic::gui
