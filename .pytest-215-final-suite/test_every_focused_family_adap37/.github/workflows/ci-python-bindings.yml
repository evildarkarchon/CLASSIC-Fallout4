name: CI - Python Bindings

on:
  push:
    branches: [classic-next]
  pull_request:
    branches: [classic-next]

env:
  RUST_BACKTRACE: 1
  CARGO_TERM_COLOR: always

jobs:
  parity-gates:
    name: Python Parity Gates
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: python-bindings/uv.lock

      - name: Install User Settings conformance YAML parser
        run: python -m pip install --disable-pip-version-check "ruamel.yaml>=0.18,<0.19"

      - name: Run Python binding compliance profile
        run: python tools/binding_compliance/check_compliance.py --repo-root . --profile python-ci

      - name: Upload parity and runtime coverage diagnostics
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: python-parity-diagnostics
          path: |
            tools/binding_compliance/artifacts/
            python-bindings/parity-artifacts/
          if-no-files-found: warn
          retention-days: 7

  build-and-test:
    name: Python Bindings Build + Smoke Tests
    needs: [parity-gates]
    if: ${{ !cancelled() }}
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: python-bindings/uv.lock

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache cargo index
        uses: actions/cache@v5
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-index-

      - name: Cache cargo build
        uses: actions/cache@v5
        with:
          path: target
          key: ${{ runner.os }}-cargo-python-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('foundation/**/*.rs', 'business-logic/**/*.rs', 'cpp-bindings/**/*.rs', 'node-bindings/**/*.rs', 'python-bindings/**/*.rs', 'ui-applications/**/*.rs') }}
          restore-keys: |
            ${{ runner.os }}-cargo-python-${{ hashFiles('**/Cargo.lock') }}-
            ${{ runner.os }}-cargo-python-

      # `uv sync` creates `python-bindings/.venv` and installs the locked
      # tooling set (`maturin`, `pytest`). `--inexact` guards against the
      # maturin-built `classic-*-py` wheels being pruned on any re-sync.
      # `--locked` fails the job if the committed uv.lock drifts from
      # pyproject.toml — catches silent lock/manifest divergence.
      - name: Install Python bindings tooling
        run: uv sync --project python-bindings --inexact --locked --group drift-guards

      # `maturin` is intentionally installed into `python-bindings/.venv` by
      # the step above. `rebuild_rust.ps1` targets that interpreter explicitly
      # for Python binding builds. Smoke-test collection imports promoted
      # modules at import time, so CI must install the full Python binding
      # set instead of a hand-maintained subset.
      - name: Build and install Python bindings
        shell: pwsh
        run: pwsh -ExecutionPolicy Bypass -File rebuild_rust.ps1 -Target python

      # Use the repository-standard module invocation so local and CI commands match.
      - name: Run Python bindings smoke tests
        run: uv run --project python-bindings python -m pytest python-bindings/tests -q

      - name: Run Python Crash Log Scan Run conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_scan_run_conformance.py --participant python

      # Existing runtime and registry-backed parity gates remain blocking.
      - name: Run Python User Settings conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_user_settings_conformance.py --participant python --artifact-root tools/binding_compliance/artifacts/user-settings

      - name: Upload Python User Settings conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-user-settings-conformance
          path: tools/binding_compliance/artifacts/user-settings/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python crash-suspect conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family crash-suspect --participant python --artifact-root tools/binding_compliance/artifacts/crash-suspect

      - name: Upload Python crash-suspect conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-crash-suspect-conformance
          path: tools/binding_compliance/artifacts/crash-suspect/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python crashgen-settings conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family crashgen-settings --participant python --artifact-root tools/binding_compliance/artifacts/crashgen-settings

      - name: Upload Python crashgen-settings conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-crashgen-settings-conformance
          path: tools/binding_compliance/artifacts/crashgen-settings/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python mod-guidance conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family mod-guidance --participant python --artifact-root tools/binding_compliance/artifacts/mod-guidance

      - name: Upload Python mod-guidance conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-mod-guidance-conformance
          path: tools/binding_compliance/artifacts/mod-guidance/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python formid-lookup conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/removed-family-launcher --artifact-root tools/binding_compliance/artifacts/formid-lookup

      - name: Upload Python formid-lookup conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-formid-lookup-conformance
          path: tools/binding_compliance/artifacts/formid-lookup/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python named-record conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family named-record --participant python --artifact-root tools/binding_compliance/artifacts/named-record

      - name: Upload Python named-record conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-named-record-conformance
          path: tools/binding_compliance/artifacts/named-record/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python plugin-evidence conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family plugin-evidence --participant python --artifact-root tools/binding_compliance/artifacts/plugin-evidence

      - name: Upload Python plugin-evidence conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-plugin-evidence-conformance
          path: tools/binding_compliance/artifacts/plugin-evidence/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python installed-yaml-data conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family installed-yaml-data --participant python --artifact-root tools/binding_compliance/artifacts/installed-yaml-data

      - name: Upload Python installed-yaml-data conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-installed-yaml-data-conformance
          path: tools/binding_compliance/artifacts/installed-yaml-data/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python config-vocabulary conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family config-vocabulary --participant python --artifact-root tools/binding_compliance/artifacts/config-vocabulary

      - name: Upload Python config-vocabulary conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-config-vocabulary-conformance
          path: tools/binding_compliance/artifacts/config-vocabulary/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python config-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family config-operations --participant python --artifact-root tools/binding_compliance/artifacts/config-operations

      - name: Upload Python config-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-config-operations-conformance
          path: tools/binding_compliance/artifacts/config-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python file-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family file-operations --participant python --artifact-root tools/binding_compliance/artifacts/file-operations

      - name: Upload Python file-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-file-operations-conformance
          path: tools/binding_compliance/artifacts/file-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python database-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family database-operations --participant python --artifact-root tools/binding_compliance/artifacts/database-operations

      - name: Upload Python database-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-database-operations-conformance
          path: tools/binding_compliance/artifacts/database-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-registry conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-registry --participant python --artifact-root tools/binding_compliance/artifacts/version-registry

      - name: Upload Python version-registry conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-registry-conformance
          path: tools/binding_compliance/artifacts/version-registry/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python scan-game conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family scan-game --participant python --artifact-root tools/binding_compliance/artifacts/scan-game

      - name: Upload Python scan-game conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-scan-game-conformance
          path: tools/binding_compliance/artifacts/scan-game/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python path-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family path-operations --participant python --artifact-root tools/binding_compliance/artifacts/path-operations

      - name: Upload Python path-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-path-operations-conformance
          path: tools/binding_compliance/artifacts/path-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python path-normalization conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family path-normalization --participant python --artifact-root tools/binding_compliance/artifacts/path-normalization

      - name: Upload Python path-normalization conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-path-normalization-conformance
          path: tools/binding_compliance/artifacts/path-normalization/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python message-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family message-operations --participant python --artifact-root tools/binding_compliance/artifacts/message-operations

      - name: Upload Python message-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-message-operations-conformance
          path: tools/binding_compliance/artifacts/message-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python file-fingerprint conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family file-fingerprint --participant python --artifact-root tools/binding_compliance/artifacts/file-fingerprint

      - name: Upload Python file-fingerprint conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-file-fingerprint-conformance
          path: tools/binding_compliance/artifacts/file-fingerprint/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python performance conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family performance --participant python --artifact-root tools/binding_compliance/artifacts/performance

      - name: Upload Python performance conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-performance-conformance
          path: tools/binding_compliance/artifacts/performance/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python update-decisions conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family update-decisions --participant python --artifact-root tools/binding_compliance/artifacts/update-decisions

      - name: Run Python update-services conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family update-services --participant python --artifact-root tools/binding_compliance/artifacts/update-services

      - name: Upload Python update-decisions conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-update-decisions-conformance
          path: tools/binding_compliance/artifacts/update-decisions/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Python update-services conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-update-services-conformance
          path: tools/binding_compliance/artifacts/update-services/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python string-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family string-operations --participant python --artifact-root tools/binding_compliance/artifacts/string-operations

      - name: Upload Python string-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-string-operations-conformance
          path: tools/binding_compliance/artifacts/string-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python registry-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family registry-operations --participant python --artifact-root tools/binding_compliance/artifacts/registry-operations

      - name: Upload Python registry-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-registry-operations-conformance
          path: tools/binding_compliance/artifacts/registry-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-registry-values conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-registry-values --participant python --artifact-root tools/binding_compliance/artifacts/version-registry-values

      - name: Upload Python version-registry-values conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-registry-values-conformance
          path: tools/binding_compliance/artifacts/version-registry-values/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python game-version-parse conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family game-version-parse --participant python --artifact-root tools/binding_compliance/artifacts/game-version-parse

      - name: Upload Python game-version-parse conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-game-version-parse-conformance
          path: tools/binding_compliance/artifacts/game-version-parse/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python game-version-distance conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family game-version-distance --participant python --artifact-root tools/binding_compliance/artifacts/game-version-distance

      - name: Upload Python game-version-distance conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-game-version-distance-conformance
          path: tools/binding_compliance/artifacts/game-version-distance/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python game-version-order conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family game-version-order --participant python --artifact-root tools/binding_compliance/artifacts/game-version-order

      - name: Upload Python game-version-order conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-game-version-order-conformance
          path: tools/binding_compliance/artifacts/game-version-order/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python fallout4-identity conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family fallout4-identity --participant python --artifact-root tools/binding_compliance/artifacts/fallout4-identity

      - name: Upload Python fallout4-identity conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-fallout4-identity-conformance
          path: tools/binding_compliance/artifacts/fallout4-identity/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python fallout4-paths conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family fallout4-paths --participant python --artifact-root tools/binding_compliance/artifacts/fallout4-paths

      - name: Upload Python fallout4-paths conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-fallout4-paths-conformance
          path: tools/binding_compliance/artifacts/fallout4-paths/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python fallout4-metadata conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family fallout4-metadata --participant python --artifact-root tools/binding_compliance/artifacts/fallout4-metadata

      - name: Upload Python fallout4-metadata conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-fallout4-metadata-conformance
          path: tools/binding_compliance/artifacts/fallout4-metadata/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python registry-keys conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family registry-keys --participant python --artifact-root tools/binding_compliance/artifacts/registry-keys

      - name: Upload Python registry-keys conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-registry-keys-conformance
          path: tools/binding_compliance/artifacts/registry-keys/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python registry-game conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family registry-game --participant python --artifact-root tools/binding_compliance/artifacts/registry-game

      - name: Upload Python registry-game conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-registry-game-conformance
          path: tools/binding_compliance/artifacts/registry-game/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python registry-gui conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family registry-gui --participant python --artifact-root tools/binding_compliance/artifacts/registry-gui

      - name: Upload Python registry-gui conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-registry-gui-conformance
          path: tools/binding_compliance/artifacts/registry-gui/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python registry-context conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family registry-context --participant python --artifact-root tools/binding_compliance/artifacts/registry-context

      - name: Upload Python registry-context conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-registry-context-conformance
          path: tools/binding_compliance/artifacts/registry-context/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python registry-paths conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family registry-paths --participant python --artifact-root tools/binding_compliance/artifacts/registry-paths

      - name: Upload Python registry-paths conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-registry-paths-conformance
          path: tools/binding_compliance/artifacts/registry-paths/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-extraction conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-extraction --participant python --artifact-root tools/binding_compliance/artifacts/version-extraction

      - name: Upload Python version-extraction conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-extraction-conformance
          path: tools/binding_compliance/artifacts/version-extraction/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-f4se conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-f4se --participant python --artifact-root tools/binding_compliance/artifacts/version-f4se

      - name: Upload Python version-f4se conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-f4se-conformance
          path: tools/binding_compliance/artifacts/version-f4se/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-pe conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-pe --participant python --artifact-root tools/binding_compliance/artifacts/version-pe

      - name: Upload Python version-pe conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-pe-conformance
          path: tools/binding_compliance/artifacts/version-pe/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-pe-path conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-pe-path --participant python --artifact-root tools/binding_compliance/artifacts/version-pe-path

      - name: Upload Python version-pe-path conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-pe-path-conformance
          path: tools/binding_compliance/artifacts/version-pe-path/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python settings-yaml conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family settings-yaml --participant python --artifact-root tools/binding_compliance/artifacts/settings-yaml

      - name: Upload Python settings-yaml conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-settings-yaml-conformance
          path: tools/binding_compliance/artifacts/settings-yaml/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python settings-validation conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family settings-validation --participant python --artifact-root tools/binding_compliance/artifacts/settings-validation

      - name: Upload Python settings-validation conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-settings-validation-conformance
          path: tools/binding_compliance/artifacts/settings-validation/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python settings-cached-docs conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family settings-cached-docs --participant python --artifact-root tools/binding_compliance/artifacts/settings-cached-docs

      - name: Upload Python settings-cached-docs conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-settings-cached-docs-conformance
          path: tools/binding_compliance/artifacts/settings-cached-docs/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-registry-details conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-registry-details --participant python --artifact-root tools/binding_compliance/artifacts/version-registry-details

      - name: Upload Python version-registry-details conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-registry-details-conformance
          path: tools/binding_compliance/artifacts/version-registry-details/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python web-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family web-operations --participant python --artifact-root tools/binding_compliance/artifacts/web-operations

      - name: Upload Python web-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-web-operations-conformance
          path: tools/binding_compliance/artifacts/web-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python resource-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family resource-operations --participant python --artifact-root tools/binding_compliance/artifacts/resource-operations

      - name: Upload Python resource-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-resource-operations-conformance
          path: tools/binding_compliance/artifacts/resource-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python version-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family version-operations --participant python --artifact-root tools/binding_compliance/artifacts/version-operations

      - name: Upload Python version-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-version-operations-conformance
          path: tools/binding_compliance/artifacts/version-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python xse-operations conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family xse-operations --participant python --artifact-root tools/binding_compliance/artifacts/xse-operations

      - name: Upload Python xse-operations conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-xse-operations-conformance
          path: tools/binding_compliance/artifacts/xse-operations/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python installation-paths conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family installation-paths --participant python --artifact-root tools/binding_compliance/artifacts/installation-paths

      - name: Upload Python installation-paths conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-installation-paths-conformance
          path: tools/binding_compliance/artifacts/installation-paths/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python game-identity conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family game-identity --participant python --artifact-root tools/binding_compliance/artifacts/game-identity

      - name: Upload Python game-identity conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-game-identity-conformance
          path: tools/binding_compliance/artifacts/game-identity/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python settings-load conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family settings-load --participant python --artifact-root tools/binding_compliance/artifacts/settings-load

      - name: Upload Python settings-load conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-settings-load-conformance
          path: tools/binding_compliance/artifacts/settings-load/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python scan-run-vocabulary conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_semantic_conformance.py --family scan-run-vocabulary --participant python --artifact-root tools/binding_compliance/artifacts/scan-run-vocabulary

      - name: Upload Python scan-run-vocabulary conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-scan-run-vocabulary-conformance
          path: tools/binding_compliance/artifacts/scan-run-vocabulary/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python autoscan-report conformance
        if: ${{ !cancelled() }}
        run: uv run --project python-bindings python tools/binding_compliance/run_scan_run_conformance.py --family autoscan-report --participant python --artifact-root tools/binding_compliance/artifacts/autoscan-report

      - name: Upload Python autoscan-report conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-autoscan-report-conformance
          path: tools/binding_compliance/artifacts/autoscan-report/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Python Crash Log Scan Run conformance diagnostics
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: python-scan-run-conformance
          path: tools/binding_compliance/artifacts/python/python/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Python CLI diagnostics
        run: |
          uv run --project python-bindings classic-py --fixture-root python-bindings/tests/fixtures doctor
          uv run --project python-bindings classic-py --json bindings list
          uv run --project python-bindings classic-py compliance run --profile smoke
