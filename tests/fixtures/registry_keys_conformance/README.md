# Registry key constants

The empty input request reads the 16 common public Rust/Python `Keys` constants.
Expected values belong only to the pack, and adapters read the exported constants.
`APP_DIR` is not publicly exposed by Python and is outside this common carrier pack.
CXX exposes only two key operations, covered in the game and GUI accessor packs.
