# Registry operation fixtures

Input-only typed values exercise insert, get, overwrite, unregister and clear in
a dedicated process. Empty string, false and -1 distinguish values from missing
keys using explicit presence observations. Expectations live in the pack.
