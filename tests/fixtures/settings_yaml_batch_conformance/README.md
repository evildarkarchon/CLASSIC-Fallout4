# YAML batch fixtures

Fixtures supply YAML, query paths and string updates. Ordered maps deliberately
use reverse alphabetical insertion order, absent queries remain omitted, and
string-vector projection must discard the numeric member.
