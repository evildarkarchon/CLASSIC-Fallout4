# Version Registry value methods

The owned registry has a game compatibility range and a narrower crash-generator range. Requests cover both inclusive boundaries and outside values. Rust invokes the core models; Python invokes every selected class method. Full YAML byte inventories forbid unrequested writes. Granular core method mappings and exact Python operation paths cover model methods without enrolling unrelated DTO consumers.
