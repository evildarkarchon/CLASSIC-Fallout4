# Runtime access fixture

The empty request performs initial and repeated shared runtime access. The
authored oracle compares availability without host-dependent worker counts.
This does not create an independent runtime or claim scheduler equivalence.
