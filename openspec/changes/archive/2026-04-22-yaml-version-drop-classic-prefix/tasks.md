## 1. YAML value and schema bump

- [x] 1.1 Update `CLASSIC Data/databases/CLASSIC Main.yaml`: change `version: CLASSIC v9.1.0` → `version: v9.1.0` (exact current release string).
- [x] 1.2 In the same file, bump `schema_version: "1.0"` → `schema_version: "2.0"`.
- [x] 1.3 Confirm no other key under `CLASSIC_Info` carries the old decoration — `version_date` and `is_prerelease` stay untouched.

## 2. Bump `MAIN_YAML` `SchemaCompat` to match

- [x] 2.1 In `business-logic/classic-config-core/src/client_schemas.rs:28`, change `pub const MAIN_YAML: SchemaCompat = SchemaCompat::new(1, 0);` → `SchemaCompat::new(2, 0);` so the client accepts the newly bumped file.
- [x] 2.2 Re-read the module doc comment above the constant and add a short migration note referencing this change (e.g., "MAJOR bumped from 1 → 2 in 2026-04 when `CLASSIC_Info.version` dropped the `CLASSIC v` display prefix").

## 3. Rust consumer: scanlog report prepends decoration at format time

- [x] 3.1 In `business-logic/classic-scanlog-core/src/report.rs`, update `generate_header` so the header line becomes `format!("**AUTOSCAN REPORT GENERATED BY CLASSIC {}**\n\n", self.classic_version)` — the literal `CLASSIC ` (with trailing space) is prepended at format time.
- [x] 3.2 In the same file, update the "Generated by" trailing line at `report.rs:640` the same way: prepend `"Generated by CLASSIC "` and interpolate `self.classic_version`.
- [x] 3.3 Update the doc comment on `ReportGenerator::with_config` (`report.rs:378`) so the example changes from `"CLASSIC v8.0.0"` to `"v8.0.0"` (reflecting the new contract: the caller passes the bare SemVer, the generator prepends the product name).
- [x] 3.4 Update the `ReportGenerator::new()` default at `report.rs:369` from `"CLASSIC"` to an empty-or-sensible default consistent with the new "caller provides bare SemVer, generator provides decoration" contract. Document whether the default renders as `"AUTOSCAN REPORT GENERATED BY CLASSIC"` or `"CLASSIC Crashgen"` (same visible-output result with empty classic_version).

## 4. C++ consumer: GUI drops the prefix-strip

- [x] 4.1 In `classic-gui/src/main.cpp`, remove the block at lines 178–182 (`if (qVersion.startsWith("CLASSIC v")) qVersion = qVersion.mid(9).trimmed();`). The value from `yaml_ops_get_string("CLASSIC_Info.version", "")` is already bare SemVer.
- [x] 4.2 Update the example string on line 187 from `"CLASSIC v9.0.0"` → `"v9.0.0"` so the error message still matches the new on-disk format.
- [x] 4.3 Update the comment on line 179 that currently says `// Strip "CLASSIC v" prefix (YAML commonly stores "CLASSIC vX.Y.Z")` — either delete it entirely or replace with `// CLASSIC_Info.version is bare SemVer per schema_version 2.0; pass through directly.`

## 5. C++ consumer: CMake build-time guard regex

- [x] 5.1 In `classic-cli/CMakeLists.txt` line 86, change the `file(STRINGS ... REGEX ...)` pattern from `^[ \t]+version:[ \t]+CLASSIC[ \t]+v[0-9]+\\.[0-9]+\\.[0-9]+` → `^[ \t]+version:[ \t]+v?[0-9]+\\.[0-9]+\\.[0-9]+`.
- [x] 5.2 On lines 95–99, update the capture-group regex to match: `^[ \t]+version:[ \t]+v?([0-9]+\\.[0-9]+\\.[0-9]+).*$` so `CLASSIC_APP_VERSION` captures the bare `<MAJOR>.<MINOR>.<PATCH>`.
- [x] 5.3 Update the error messages on lines 88–92 and 101–104 so the "Expected" literal now reads `  version: v<MAJOR>.<MINOR>.<PATCH>` (drop the `CLASSIC` token).
- [x] 5.4 Run `classic-cli/build_cli.ps1` (configure + build) to confirm the regex matches and `PROJECT_VERSION STREQUAL CLASSIC_APP_VERSION` still passes.

## 6. Tooling: `set_version.ps1`

- [x] 6.1 In `set_version.ps1:314`, change the `$VersionPattern` from `(?m)^(\s*version:\s*CLASSIC\s+v)[^\n\r]*` → `(?m)^(\s*version:\s*v?)[0-9][^\n\r]*`.
- [x] 6.2 Confirm `$VersionReplacement` (line 315) still produces the correct output. With the new pattern, Group 1 captures `  version: v` (or `  version: ` if a future YAML drops the `v`) and the replacement appends `$NewVersion` — but because we want to guarantee the output carries the leading `v`, change the replacement to `'${1}' + $NewVersion` where Group 1 is normalized to end in `v` (simplest: pattern `(?m)^(\s*version:\s*)(?:CLASSIC\s+v|v)?[^\n\r]*` capturing only `  version: ` and making the replacement `'${1}v' + $NewVersion`).
- [x] 6.3 Update the script's parameter-help comment on line 21–22 from "For the YAML file, this will be prefixed with 'CLASSIC v'" → "For the YAML file, this will be prefixed with a literal 'v'."
- [x] 6.4 Update the `.EXAMPLE` and `NOTES` sections in the `Show-Help` function that reference the old decorated YAML form.
- [x] 6.5 Dry-run `./set_version.ps1 -Version "9.1.0" -WhatIf` and confirm the "would write" output reports a YAML update that matches the new `version: v9.1.0` shape.

## 7. Update test fixtures — Rust

- [x] 7.1 In `business-logic/classic-config-core/tests/integration_tests.rs`, update every inline YAML fragment that declares `version: CLASSIC v<X>` to `version: v<X>`, and bump any accompanying `schema_version: "1.<X>"` to `"2.0"` if the fixture is meant to represent a post-change shippable file. Tests that exist specifically to validate the `schema_version 1.x` gate behavior keep the old schema and the old value (that is the point of those tests) — disambiguate explicitly in the test name or inline comment.
- [x] 7.2 In `business-logic/classic-config-core/src/yamldata_tests.rs`, do the same review for every inline YAML string fixture and every `assert_eq!(config.classic_version, ...)` — if a fixture previously expected the fully decorated form, the expected string changes to the bare form.
- [x] 7.3 In `business-logic/classic-config-core/src/shippable_tests.rs` (lines 48, 52, 56 show `version: CLASSIC v9.0.0`/`v9.1.0`/`vFuture` alongside `schema_version: "1.0"`/`"1.2"`/`"2.0"`), update so post-change fixtures (`"2.0"`) carry the bare form and legacy `"1.0"`/`"1.2"` fixtures keep the old decorated form (they are testing that the old shape is still recognizable under the legacy schema).
- [x] 7.4 In `business-logic/classic-config-core/src/config_tests.rs`, scan every occurrence of `CLASSIC_Info:` and `version: CLASSIC v` and apply the same old-vs-new disambiguation.
- [x] 7.5 In `business-logic/classic-scanlog-core/src/report_tests.rs` and `orchestrator_tests.rs`, update any fixture that constructs a `ReportGenerator::with_config("CLASSIC v9.0.0", ...)` to `ReportGenerator::with_config("v9.0.0", ...)` and — if the test asserts on rendered header text — the assertion text should STAY the same (`AUTOSCAN REPORT GENERATED BY CLASSIC v9.0.0`) because the `CLASSIC ` decoration now comes from the format string, not from the input argument.
- [x] 7.6 In `business-logic/classic-settings-core/tests/yaml_integration_tests.rs`, apply the same pattern as 7.1 for any shipped-file-shape fixtures.

## 8. Update test fixtures — Bindings

- [x] 8.1 In `cpp-bindings/classic-cpp-bridge/src/config_tests.rs`, update the inline `CLASSIC Main.yaml` content strings (around lines 60, 88) to the new `version: v9.1.0` + `schema_version: "2.0"` shape. Assertions on `yaml_data_classic_version(&data)` now expect the bare SemVer.
- [x] 8.2 In `cpp-bindings/classic-cpp-bridge/src/markdown_tests.rs:232,258`, the literal `**AUTOSCAN REPORT GENERATED BY CLASSIC v9.0.0**` in the expected-output snapshot STAYS (the decoration is now provided by the report generator); but any fixture *input* that seeds `classic_version` with the old decorated form must update to the bare form.
- [x] 8.3 In `cpp-bindings/classic-cpp-bridge/src/update_tests.rs`, the `CLASSIC Main.yaml` references are update-delivery artifact names and do not embed the `version:` field shape — confirm no change needed.
- [x] 8.4 In `node-bindings/classic-node/__test__/` and `node-bindings/classic-node/__test__/fixtures/`, scan for `CLASSIC_Info:` + `version:` occurrences and update post-change fixtures to the new shape. Run `cd node-bindings/classic-node && bun run parity:gate` afterward and confirm it stays green (no API drift, only value-shape drift).
- [x] 8.5 In `python-bindings/tests/fixtures/tier1_parity_fixtures.py`, `python-bindings/tests/test_promoted_scanlog_report_smoke.py`, and any `.pyi` examples in `python-bindings/classic-config-py/classic_config.pyi` that show the old decorated value, update to the new shape.

## 9. Docs

- [x] 9.1 In `docs/api/classic-config-core-yaml-schema.md`, add a short paragraph under the `CLASSIC_Info.version` entry (or create one if it does not exist) documenting the new bare-SemVer contract and cross-linking `openspec/specs/yaml-app-version-field/spec.md`.
- [x] 9.2 In `docs/api/yaml-update-delivery.md`, add the `CLASSIC_Info.version` prefix-drop as a concrete "MAJOR bump" example alongside any existing examples.
- [x] 9.3 In `docs/examples/CODE_EXAMPLES.md`, `docs/testing/rust_testing_guide.md`, and `docs/migration/async-yaml-migration-examples.md`, review every `CLASSIC_Info:` + `version:` occurrence and update those intended to illustrate a *current* shippable file to the new bare-SemVer shape. Leave any illustrating a *historical* pre-2.0 file intact, with a short note.
- [x] 9.4 Confirm `AGENTS.md` / `CLAUDE.md` do not embed the old value-shape anywhere. (Grep result at proposal-time: they do not. Re-verify.)

## 10. Validation

- [x] 10.1 From repo root, run `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- [x] 10.2 Run `cargo test --workspace` and confirm every test passes. Pay particular attention to `classic-config-core` shippable and yaml-integration tests (schema-gate coverage) and `classic-scanlog-core` report tests (header/trailer text coverage).
- [x] 10.3 Run `classic-cli/build_cli.ps1 -Test` and `classic-gui/build_gui.ps1 -Test`. The CLI configure step specifically exercises the new CMake regex and the YAML/CMake `PROJECT_VERSION STREQUAL` guard.
- [x] 10.4 Run the Python binding pipeline per `CLAUDE.md` §Python Binding Testing Quickstart: set `PYO3_PYTHON`, `./rebuild_rust.ps1 -Target python`, then `uv run --python python-bindings/.venv/Scripts/python.exe python -m pytest python-bindings/tests -q`.
- [x] 10.5 Run the CXX parity gate: `python tools/cxx_api_parity/check_parity_gate.py --repo-root .`. Expected: green (no bridge-surface drift).
- [x] 10.6 Run the Node parity gate: `cd node-bindings/classic-node && bun run parity:gate`. Expected: green.
- [x] 10.7 Run the Python parity gate: `python tools/python_api_parity/check_parity_gate.py --repo-root .`. Expected: green.
- [x] 10.8 Run `./set_version.ps1 -Version "9.1.0" -WhatIf` as a final confidence check that the script's YAML-write pattern still matches the new canonical shape.
- [x] 10.9 Run `openspec verify yaml-version-drop-classic-prefix` (or `/opsx:verify`) so the spec/implementation coherence check passes before archival.
