name: CI — TypeScript

on:
  push:
    branches: [classic-next]  
  pull_request:
    branches: [classic-next]

env:
  RUST_BACKTRACE: 1
  CARGO_TERM_COLOR: always

jobs:
  parity-gates:
    name: Node Parity Gates
    runs-on: windows-latest
    timeout-minutes: 60
    env:
      RUST_BACKTRACE: full
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache cargo index
        uses: actions/cache@v5
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-index-

      - name: Cache cargo build
        uses: actions/cache@v5
        with:
          path: target
          key: ${{ runner.os }}-cargo-node-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('foundation/**/*.rs', 'business-logic/**/*.rs', 'cpp-bindings/**/*.rs', 'node-bindings/**/*.rs', 'python-bindings/**/*.rs', 'ui-applications/**/*.rs') }}
          restore-keys: |
            ${{ runner.os }}-cargo-node-${{ hashFiles('**/Cargo.lock') }}-
            ${{ runner.os }}-cargo-node-

      - name: Install npm dependencies
        working-directory: node-bindings/classic-node
        run: bun install

      - name: Build NAPI-RS binary
        working-directory: node-bindings/classic-node
        run: bun run build

      - name: Verify NAPI binary exists
        working-directory: node-bindings/classic-node
        shell: pwsh
        run: |
          $binary = Get-ChildItem -Filter "*.node" -Recurse
          if ($binary) {
            Write-Host "NAPI binary found: $($binary.FullName) ($([math]::Round($binary.Length / 1MB, 2)) MB)"
          } else {
            Write-Error "NAPI binary not found after build"
            exit 1
          }

      - name: Install User Settings conformance YAML parser
        run: python -m pip install --disable-pip-version-check "ruamel.yaml>=0.18,<0.19"

      - name: Run Node binding compliance profile
        run: python tools/binding_compliance/check_compliance.py --repo-root . --profile node-ci

      - name: Upload parity and runtime coverage diagnostics
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: node-parity-diagnostics
          path: |
            tools/binding_compliance/artifacts/
            node-bindings/classic-node/parity-artifacts/
          if-no-files-found: warn
          retention-days: 7

  build-and-test:
    name: NAPI-RS Build + Runtime Tests (${{ matrix.runtime }})
    needs: [parity-gates]
    if: ${{ !cancelled() }}
    runs-on: windows-latest
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        runtime: [bun, node]
    env:
      RUST_BACKTRACE: full
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"

      - name: Set up Python for scan conformance
        if: matrix.runtime == 'node'
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Bun
        uses: oven-sh/setup-bun@v2

      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache cargo index
        uses: actions/cache@v5
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-index-

      - name: Cache cargo build
        uses: actions/cache@v5
        with:
          path: target
          key: ${{ runner.os }}-cargo-node-${{ hashFiles('**/Cargo.lock') }}-${{ hashFiles('foundation/**/*.rs', 'business-logic/**/*.rs', 'cpp-bindings/**/*.rs', 'node-bindings/**/*.rs', 'python-bindings/**/*.rs', 'ui-applications/**/*.rs') }}
          restore-keys: |
            ${{ runner.os }}-cargo-node-${{ hashFiles('**/Cargo.lock') }}-
            ${{ runner.os }}-cargo-node-

      - name: Install npm dependencies
        working-directory: node-bindings/classic-node
        run: bun install

      - name: Build NAPI-RS binary
        working-directory: node-bindings/classic-node
        run: bun run build

      - name: Verify NAPI binary exists
        working-directory: node-bindings/classic-node
        shell: pwsh
        run: |
          $binary = Get-ChildItem -Filter "*.node" -Recurse
          if ($binary) {
            Write-Host "NAPI binary found: $($binary.FullName) ($([math]::Round($binary.Length / 1MB, 2)) MB)"
          } else {
            Write-Error "NAPI binary not found after build"
            exit 1
          }

      - name: Run Bun tests
        if: matrix.runtime == 'bun'
        working-directory: node-bindings/classic-node
        run: bun run test:bun

      - name: Run Node runtime smoke tests
        if: matrix.runtime == 'node'
        working-directory: node-bindings/classic-node
        run: bun run test:node

      - name: Install User Settings conformance YAML parser
        if: matrix.runtime == 'node'
        run: python -m pip install --disable-pip-version-check "ruamel.yaml>=0.18,<0.19"

      - name: Run Node Crash Log Scan Run conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_scan_run_conformance.py --participant node

      # Existing runtime and registry-backed parity gates remain blocking.
      - name: Run Node User Settings conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_user_settings_conformance.py --participant node --artifact-root tools/binding_compliance/artifacts/user-settings

      - name: Upload Node User Settings conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-user-settings-conformance
          path: tools/binding_compliance/artifacts/user-settings/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node crash-suspect conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family crash-suspect --participant node --artifact-root tools/binding_compliance/artifacts/crash-suspect

      - name: Upload Node crash-suspect conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-crash-suspect-conformance
          path: tools/binding_compliance/artifacts/crash-suspect/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node crashgen-settings conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family crashgen-settings --participant node --artifact-root tools/binding_compliance/artifacts/crashgen-settings

      - name: Upload Node crashgen-settings conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-crashgen-settings-conformance
          path: tools/binding_compliance/artifacts/crashgen-settings/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node mod-guidance conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family mod-guidance --participant node --artifact-root tools/binding_compliance/artifacts/mod-guidance

      - name: Upload Node mod-guidance conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-mod-guidance-conformance
          path: tools/binding_compliance/artifacts/mod-guidance/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node formid-lookup conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family formid-lookup --participant node --artifact-root tools/binding_compliance/artifacts/formid-lookup

      - name: Upload Node formid-lookup conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-formid-lookup-conformance
          path: tools/binding_compliance/artifacts/formid-lookup/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node named-record conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family named-record --participant node --artifact-root tools/binding_compliance/artifacts/named-record

      - name: Upload Node named-record conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-named-record-conformance
          path: tools/binding_compliance/artifacts/named-record/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node plugin-evidence conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family plugin-evidence --participant node --artifact-root tools/binding_compliance/artifacts/plugin-evidence

      - name: Upload Node plugin-evidence conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-plugin-evidence-conformance
          path: tools/binding_compliance/artifacts/plugin-evidence/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node installed-yaml-data conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family installed-yaml-data --participant node --artifact-root tools/binding_compliance/artifacts/installed-yaml-data

      - name: Upload Node installed-yaml-data conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-installed-yaml-data-conformance
          path: tools/binding_compliance/artifacts/installed-yaml-data/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node config-vocabulary conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family config-vocabulary --participant node --artifact-root tools/binding_compliance/artifacts/config-vocabulary

      - name: Upload Node config-vocabulary conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-config-vocabulary-conformance
          path: tools/binding_compliance/artifacts/config-vocabulary/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node config-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family config-operations --participant node --artifact-root tools/binding_compliance/artifacts/config-operations

      - name: Upload Node config-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-config-operations-conformance
          path: tools/binding_compliance/artifacts/config-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node file-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family file-operations --participant node --artifact-root tools/binding_compliance/artifacts/file-operations

      - name: Upload Node file-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-file-operations-conformance
          path: tools/binding_compliance/artifacts/file-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node database-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family database-operations --participant node --artifact-root tools/binding_compliance/artifacts/database-operations

      - name: Upload Node database-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-database-operations-conformance
          path: tools/binding_compliance/artifacts/database-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node version-registry conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family version-registry --participant node --artifact-root tools/binding_compliance/artifacts/version-registry

      - name: Upload Node version-registry conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-version-registry-conformance
          path: tools/binding_compliance/artifacts/version-registry/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node scan-game conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family scan-game --participant node --artifact-root tools/binding_compliance/artifacts/scan-game

      - name: Upload Node scan-game conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-scan-game-conformance
          path: tools/binding_compliance/artifacts/scan-game/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node path-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family path-operations --participant node --artifact-root tools/binding_compliance/artifacts/path-operations

      - name: Upload Node path-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-path-operations-conformance
          path: tools/binding_compliance/artifacts/path-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node path-normalization conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/removed-family-launcher --artifact-root tools/binding_compliance/artifacts/path-normalization

      - name: Upload Node path-normalization conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-path-normalization-conformance
          path: tools/binding_compliance/artifacts/path-normalization/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node message-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family message-operations --participant node --artifact-root tools/binding_compliance/artifacts/message-operations

      - name: Upload Node message-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-message-operations-conformance
          path: tools/binding_compliance/artifacts/message-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node file-fingerprint conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family file-fingerprint --participant node --artifact-root tools/binding_compliance/artifacts/file-fingerprint

      - name: Upload Node file-fingerprint conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-file-fingerprint-conformance
          path: tools/binding_compliance/artifacts/file-fingerprint/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node performance conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family performance --participant node --artifact-root tools/binding_compliance/artifacts/performance

      - name: Upload Node performance conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-performance-conformance
          path: tools/binding_compliance/artifacts/performance/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node update-decisions conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family update-decisions --participant node --artifact-root tools/binding_compliance/artifacts/update-decisions

      - name: Run Node update-services conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family update-services --participant node --artifact-root tools/binding_compliance/artifacts/update-services

      - name: Upload Node update-decisions conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-update-decisions-conformance
          path: tools/binding_compliance/artifacts/update-decisions/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Node update-services conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-update-services-conformance
          path: tools/binding_compliance/artifacts/update-services/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node string-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family string-operations --participant node --artifact-root tools/binding_compliance/artifacts/string-operations

      - name: Upload Node string-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-string-operations-conformance
          path: tools/binding_compliance/artifacts/string-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node registry-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family registry-operations --participant node --artifact-root tools/binding_compliance/artifacts/registry-operations

      - name: Upload Node registry-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-registry-operations-conformance
          path: tools/binding_compliance/artifacts/registry-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node game-version-parse conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family game-version-parse --participant node --artifact-root tools/binding_compliance/artifacts/game-version-parse

      - name: Upload Node game-version-parse conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-game-version-parse-conformance
          path: tools/binding_compliance/artifacts/game-version-parse/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node game-version-distance conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family game-version-distance --participant node --artifact-root tools/binding_compliance/artifacts/game-version-distance

      - name: Upload Node game-version-distance conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-game-version-distance-conformance
          path: tools/binding_compliance/artifacts/game-version-distance/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node fallout4-identity conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family fallout4-identity --participant node --artifact-root tools/binding_compliance/artifacts/fallout4-identity

      - name: Upload Node fallout4-identity conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-fallout4-identity-conformance
          path: tools/binding_compliance/artifacts/fallout4-identity/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node settings-yaml-batch conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family settings-yaml-batch --participant node --artifact-root tools/binding_compliance/artifacts/settings-yaml-batch

      - name: Upload Node settings-yaml-batch conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-settings-yaml-batch-conformance
          path: tools/binding_compliance/artifacts/settings-yaml-batch/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node registry-game conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family registry-game --participant node --artifact-root tools/binding_compliance/artifacts/registry-game

      - name: Upload Node registry-game conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-registry-game-conformance
          path: tools/binding_compliance/artifacts/registry-game/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node registry-paths conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family registry-paths --participant node --artifact-root tools/binding_compliance/artifacts/registry-paths

      - name: Upload Node registry-paths conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-registry-paths-conformance
          path: tools/binding_compliance/artifacts/registry-paths/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node version-extraction conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family version-extraction --participant node --artifact-root tools/binding_compliance/artifacts/version-extraction

      - name: Upload Node version-extraction conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-version-extraction-conformance
          path: tools/binding_compliance/artifacts/version-extraction/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node version-pe conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family version-pe --participant node --artifact-root tools/binding_compliance/artifacts/version-pe

      - name: Upload Node version-pe conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-version-pe-conformance
          path: tools/binding_compliance/artifacts/version-pe/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node version-pe-path conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family version-pe-path --participant node --artifact-root tools/binding_compliance/artifacts/version-pe-path

      - name: Upload Node version-pe-path conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-version-pe-path-conformance
          path: tools/binding_compliance/artifacts/version-pe-path/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node settings-yaml conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family settings-yaml --participant node --artifact-root tools/binding_compliance/artifacts/settings-yaml

      - name: Upload Node settings-yaml conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-settings-yaml-conformance
          path: tools/binding_compliance/artifacts/settings-yaml/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node settings-cached-docs conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family settings-cached-docs --participant node --artifact-root tools/binding_compliance/artifacts/settings-cached-docs

      - name: Upload Node settings-cached-docs conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-settings-cached-docs-conformance
          path: tools/binding_compliance/artifacts/settings-cached-docs/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node version-registry-details conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family version-registry-details --participant node --artifact-root tools/binding_compliance/artifacts/version-registry-details

      - name: Upload Node version-registry-details conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-version-registry-details-conformance
          path: tools/binding_compliance/artifacts/version-registry-details/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node web-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family web-operations --participant node --artifact-root tools/binding_compliance/artifacts/web-operations

      - name: Upload Node web-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-web-operations-conformance
          path: tools/binding_compliance/artifacts/web-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node resource-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family resource-operations --participant node --artifact-root tools/binding_compliance/artifacts/resource-operations

      - name: Upload Node resource-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-resource-operations-conformance
          path: tools/binding_compliance/artifacts/resource-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node version-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family version-operations --participant node --artifact-root tools/binding_compliance/artifacts/version-operations

      - name: Upload Node version-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-version-operations-conformance
          path: tools/binding_compliance/artifacts/version-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node xse-operations conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family xse-operations --participant node --artifact-root tools/binding_compliance/artifacts/xse-operations

      - name: Upload Node xse-operations conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-xse-operations-conformance
          path: tools/binding_compliance/artifacts/xse-operations/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node game-identity conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family game-identity --participant node --artifact-root tools/binding_compliance/artifacts/game-identity

      - name: Upload Node game-identity conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-game-identity-conformance
          path: tools/binding_compliance/artifacts/game-identity/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node runtime-access conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family runtime-access --participant node --artifact-root tools/binding_compliance/artifacts/runtime-access

      - name: Upload Node runtime-access conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-runtime-access-conformance
          path: tools/binding_compliance/artifacts/runtime-access/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node settings-load conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family settings-load --participant node --artifact-root tools/binding_compliance/artifacts/settings-load

      - name: Upload Node settings-load conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-settings-load-conformance
          path: tools/binding_compliance/artifacts/settings-load/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node scan-run-vocabulary conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_semantic_conformance.py --family scan-run-vocabulary --participant node --artifact-root tools/binding_compliance/artifacts/scan-run-vocabulary

      - name: Upload Node scan-run-vocabulary conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-scan-run-vocabulary-conformance
          path: tools/binding_compliance/artifacts/scan-run-vocabulary/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Run Node autoscan-report conformance
        if: matrix.runtime == 'node' && !cancelled()
        run: python tools/binding_compliance/run_scan_run_conformance.py --family autoscan-report --participant node --artifact-root tools/binding_compliance/artifacts/autoscan-report

      - name: Upload Node autoscan-report conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-autoscan-report-conformance
          path: tools/binding_compliance/artifacts/autoscan-report/node/node/
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Node Crash Log Scan Run conformance diagnostics
        if: matrix.runtime == 'node' && always()
        uses: actions/upload-artifact@v6
        with:
          name: node-scan-run-conformance
          path: tools/binding_compliance/artifacts/node/node/
          if-no-files-found: warn
          retention-days: 7
